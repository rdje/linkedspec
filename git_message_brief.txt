Author spec.spec structural/syntactic rule paragraphs (PHASE7-SELF-HOSTED-SPEC.2)

3 rules: spec_file::AND+ (top-level edge delegation), rule_paragraph:AND
(own header regex + body delegation), body_element:* (5 regex-anchored
alternatives + 2 subdefs). Compiles with language_agnostic_ready_ratio
1.0000. Full suite: 1007 PASS.
