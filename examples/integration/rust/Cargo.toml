[package]
name = "linkedspec-integration-example"
version = "0.1.0"
edition = "2024"
rust-version = "1.95"
publish = false
default-run = "linkedspec-integration-example"

# Keep this runnable example independent of an enclosing application's workspace.
[workspace]

[dependencies]
linkedspec-runtime = { path = "../../../rust/linkedspec-runtime" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
