---
id: lua-cursor-completion-package-prefix-reading
title: Lua cursor completion executes fresh generated children and preserves bounded package proof
answers:
  - "what exact Lua source did startup reading child 37 cover"
  - "does the Lua cursor generated source consumer execute a fresh child process"
  - "does stale Lua generated version rejection precede malformed payload decoding"
  - "what does Lua cursor option removal test before corpus or input access"
  - "which first five Lua package groups were executed during startup reading"
  - "what do Lua normalization tests establish about bare edge physical lines"
  - "what focused evidence passed for Lua reading child 37"
date: 2026-09-13
status: exact source reading and focused proof complete; package suffix and all prior repairs remain open
tags: [lua, cursor, generated-source, normalization, trace, package, startup, evidence]
evidence: "LUA-STARTUP-READING.1.37 activates from 968ebb99a14cbdedde87137d2a91988e95bdc400. Eleven complete windows read 1500 fragments /59736 bytes. Both installed hosts pass execution108, generated106, normalization258 and removal96: 1136 assertions, plus five complete package groups each. Fresh generated child processes succeed; the generated ledger passes and prior cursor-neutral inputs remain exact. No production change, new repair node or guidance extension."
reverify: "Run LUA_CURSOR_PACKAGE_READING_37 and the managed commands below; the package selection contains only the first five complete groups through line450."
---

# Exact reading

| Repository source | Inclusive lines | Fragments / bytes | Raw SHA-256 |
| --- | --- | --- | --- |
| lua/test/rule_local_cursor_execution_test.lua | 328–424 | 97 /3142 | 88ac5085bff83d779b7f558df97c32479ed99aad3b6ab68ea025796fa10059e1 |
| lua/test/rule_local_cursor_generated_source_test.lua | 1–439 | 439 /16523 | 963b856ecf69888f3bb9e4662494a0f0753a999505c639a96b905a3bbabe32f7 |
| lua/test/rule_local_cursor_normalization_test.lua | 1–225 | 225 /10012 | 65ba8c204996a2dcf5673374c17877b2240661febbef12cc3611f6efa7e19a21 |
| lua/test/rule_local_cursor_option_removal_test.lua | 1–276 | 276 /10886 | 63eaacfb3d4c5186a9ee2c2284169885f50533460b4644e574225a54fddd71f0 |
| lua/test/run.lua | 1–463 | 463 /19173 | 7fd3aeccac799fb9435b421dcce04f26c1eb8d60c92b0a63d97d7a82ecc6da29 |

Execution328–424 is one window; generated uses1–170,171–325 and326–439;
normalization uses1–135 and136–225; option removal uses1–155 and156–276;
package uses1–165,166–320 and321–463. All eleven credited source outputs are
complete. Ordered range SHA-256 is
`5201d5604cd714055186fb76ae7c3b8f32576b3e0b6c08bcc96d8bed25c87a60`.
All 99 Lua sources remain baseline-identical to
`baeb984e36a94a15951cd23d4c52def5064cdaca`. Reading reaches37/51 groups,
51,151 fragments /1,912,415 bytes and77 complete files. The package runner
remains partial, ending inside the named/file/inline primary test.

# Cursor completion and fresh generated execution

The execution suffix adds a file-loaded parent/child fixture, debug trace proof
that parent AND/consume and child OR/seek each retain their own policy, and native/
generated-v2 agreement for anchored AND and compact-Pipe choice. Together with
the prefix read in child36, the full consumer passes108 assertions per host.
Earlier 110-assertion migration evidence remains a dated milestone.

The generated-source consumer checks current v2 exports and retirement of public
v1 functions. Its plan contains only the neutral minimal fields. Ten selected
family rows check classification, derived seek/consume policy and native/generated
outcomes. For those family comparisons, failures are deliberately reduced to
ok=false: they do not assert identical error detail for every family. Separate
version and payload cases below own exact typed failure fields.

Explicit v1 validation rejects with stage/code, source identity, expected/actual
contract and regeneration detail. Compact Pipe uses the OR blind family and
returns the selected child. Two structural fixtures compare generated and native
values. Emission is deterministic and contains no serialized cursor override.
Current-host loading executes direct and traced entry points and checks generated
entry/decision/exit events plus source identity.

The fresh-process role writes valid, malformed-current and malformed-legacy
modules plus a small runner under routed TMPDIR. It launches the selected host
with the existing project LUA_PATH/LUA_CPATH and captures stdout/stderr in that
same owned directory. The child loads all three modules. Valid v2 returns exact
metadata and direct/traced x results; the parent checks exit success, empty stderr,
event markers and identity. The child also projects exact typed failures:
legacy contract rejection occurs before malformed effective-spec decoding,
whereas malformed v2 reaches compile_or_load_generated_source with
generated_source_compile_failed. The temporary root is removed after the run.
This consumer passes106 assertions per host and launches a separate child on each.

# Normalization and retired options

The normalization consumer imports AST/parser/validator/compiler modules directly.
It checks36 family rows,18 edge rows and six ownership sets. Compact Pipe is
OR/default and compact ampersand is AND. Invalid rows preserve exact portable
code, stage, field names/values and diagnostic JSON roundtrips. Valid bare rows
retain typed targets, code/fluent metadata, no raw fallback and AST reconstruction.
Compiled ownership selects action versus blind tables, preserving target order
and resolving omitted action indices to zero. Mixed ownership keeps its exact
typed rejection. Complete-line/header-rest bare forms are admitted; a bare-looking
suffix after a regex on the same physical line is not classified as a bare edge.
The complete consumer passes258 assertions on each host. The managed wrapper
still prepares the ABI modules, though this consumer does not import the matcher.

Option removal checks source-level absence of outer override state and retention
of the CLI removal branch plus low-level matcher functions. Native AND remains
consume. Engine snake/camel keys, parse and traced parse, loaded create_engine,
both corpus spellings, generated direct and current-host emitted traced calls
produce the exact runtime diagnostic. Available logical or loaded-path identity
is preserved. Corpus rejection precedes opening a missing corpus path.

The CLI rejects retired flag and inline-flag forms with usage status2 and the
targeted first-line message, even when source is invalid or an input file is
missing. Help omits the retired flag. Explicit top-rule selection still overrides
authored markers; medium request traces omit outer cursor state. Direct low-level
seek and consume retain their independent algorithms. The full consumer passes
96 assertions per host. No per-rule cursor policy is changed here.

# Five complete package groups

The package preamble defines the test harness, repository-routed temporary-file
helpers, exact cleanup checks, corpus fixture construction and byte decoding.
Only its first five complete groups are selected through line450; the sixth
starts at451. The selected prefix verifies:

- Exact backend and CLI/corpus entrypoint identities, plus fresh detached backend
  status with runtime identity and the admitted runtime-corpus-primary-cli marker.
- Trace aliases/numeric thresholds, ordered levels, immutable configs, environment
  configuration, typed events, exact rendering and detached line snapshots.
- File reset and append, mirrored output/emoji, quiet result neutrality, direct
  runtime result neutrality and event order, failed selection before parse scope,
  caller-option preservation, routed output and invalid-emitter rejection.
- Exact primary help bytes, accumulated argument errors, usage status and parser
  export identity.

The first bullet contains two groups; the remaining bullets contain one each.
Both hosts pass all five. Inner assertions are not added to the separately counted
cursor assertions. Trace proof uses healthy writers; previously owned error-masking,
indentation and disabled-reset limitations are not reclassified or closed.
The partially read sixth group and all later package groups are not executed by
this prefix. No complete-package or full primary-matrix result is claimed.

# Knowledge and proof reconciliation

Retrieved [[lua-generated-source-v2-rule-local-cursor]],
[[lua-global-cursor-option-removal]], [[lua-trace-controls-sinks]] and
[[lua-runtime-diagnostics-trace-split]]. Cursor execution, normalization,
admission and neutral owners were read in child36. Existing trace qualifications
remain under .2.1/.2.30; no old card, repair node or historical task changes here.

Fresh proof is108+106+258+96=568 assertions per installed host,1,136 total, plus
five package groups per host. All ten managed native jobs exit0, including the
generated child processes, and the neutral generated checker exits0. That checker
reports v1 in its ledger status,10 families,one behavior case, shared8/105
and strict-Rust105/105 accepted inventories with census100/0/0. This is current
ledger validation, not fresh execution of those cross-backend matrices. Actual
new Lua emitted modules retain their independently checked v2 identity.

The cursor checker is not repeated. Its contract remains38,051 bytes,
SHA37cf5b997a805465a6aab4f42e27d0764ab19c294793758fbff22105699b7472;
its checker remains58,129 bytes,
SHA27576c7d7e3ea32667f70e7a2849984befd3de755f28bbb219b3b42cab9ee42c.
Both equal child36's committed inputs; that cursor8/0/60/public30/28 proof stays
dated. The exercised hosts are installed PUC5.5 and LuaJIT, not certification of
unavailable supported PUC5.4. All33 repair roots, previous nil-error and public
selector failures, startup gates and parked named arguments remain open. No
full CI, dependency change, PGEN/RGX build, recurring all-runtime gate or push occurs.

# Exact replay

```bash
bash tools/project_data_run.sh python3 - <<'LUA_CURSOR_PACKAGE_READING_37'
from pathlib import Path
p = Path('.linkedspec-data/scratch/lua137')
p.mkdir(parents=True, exist_ok=True)
(p / 'select-tests.py').write_text('from pathlib import Path\n\np=Path(\'.linkedspec-data/scratch/lua137\')\nsource=Path(\'lua/test/run.lua\').read_text().splitlines(keepends=True)\nstarts=[i for i,line in enumerate(source) if line.startswith(\'test(\')]\nassert starts[:6]==[199,209,225,296,431,450]\n(p/\'package-prefix.lua\').write_text(\'\'.join(source[:450])+\'\\nassert(total == 5, "package prefix count drift: " .. total)\\nassert(failed == 0, "package prefix failures: " .. failed)\\nprint("package prefix: " .. total .. " test groups passed")\\n\')\nprint(\'Selected package lines 1-450: first five complete test groups\')\n')
(p / 'verify.py').write_text("from pathlib import Path\nimport hashlib,json\n\np=Path('.linkedspec-data/scratch/lua137')\nfor row in json.loads((p/'scope.json').read_text()):\n    data=b''.join(Path(row['path']).read_bytes().splitlines(keepends=True)[row['start']-1:row['end']])\n    assert len(data)==row['bytes'] and hashlib.sha256(data).hexdigest()==row['sha256']\nfor row in json.loads((p/'reused-cursor-inputs.json').read_text()):\n    data=Path(row['path']).read_bytes()\n    assert len(data)==row['bytes'] and hashlib.sha256(data).hexdigest()==row['sha256']\npackage='''ok 1 - native module identity is exact\nok 2 - backend status is a fresh structured value\nok 3 - trace controls expose ordered levels immutable config and structured primitives\nok 4 - trace sinks and direct runtime entrypoints stay caller-owned and result-neutral\nok 5 - primary CLI help and strict argument schema are exact\npackage prefix: 5 test groups passed\n'''\nfor host in ['puc','luajit']:\n    for name,label,count in [('execution','execution',108),('generated','generated source',106),('normalization','normalization',258),('removal','option removal',96)]:\n        assert (p/f'{name}-{host}.log').read_text()==f'rule-local cursor {label}: {count} assertions passed\\n'\n    assert (p/f'package-{host}.log').read_text()==package\nassert (p/'neutral-generated.log').read_text()=='generated-source-contract: OK (v1; 10 families; 1 behavior case; Dart/Julia/Lua 8/105 + strict Rust 105/105; census states 100/0/0)\\n'\nprint('PASS: exact source ranges; execution108 + generated106 + normalization258 + removal96 per host, 1136 assertions plus ten package-prefix groups; current generated ledger and prior cursor-neutral input identity')\n")
(p / 'scope.json').write_text('[\n  {\n    "path": "lua/test/rule_local_cursor_execution_test.lua",\n    "start": 328,\n    "end": 424,\n    "bytes": 3142,\n    "sha256": "88ac5085bff83d779b7f558df97c32479ed99aad3b6ab68ea025796fa10059e1"\n  },\n  {\n    "path": "lua/test/rule_local_cursor_generated_source_test.lua",\n    "start": 1,\n    "end": 439,\n    "bytes": 16523,\n    "sha256": "963b856ecf69888f3bb9e4662494a0f0753a999505c639a96b905a3bbabe32f7"\n  },\n  {\n    "path": "lua/test/rule_local_cursor_normalization_test.lua",\n    "start": 1,\n    "end": 225,\n    "bytes": 10012,\n    "sha256": "65ba8c204996a2dcf5673374c17877b2240661febbef12cc3611f6efa7e19a21"\n  },\n  {\n    "path": "lua/test/rule_local_cursor_option_removal_test.lua",\n    "start": 1,\n    "end": 276,\n    "bytes": 10886,\n    "sha256": "63eaacfb3d4c5186a9ee2c2284169885f50533460b4644e574225a54fddd71f0"\n  },\n  {\n    "path": "lua/test/run.lua",\n    "start": 1,\n    "end": 463,\n    "bytes": 19173,\n    "sha256": "7fd3aeccac799fb9435b421dcce04f26c1eb8d60c92b0a63d97d7a82ecc6da29"\n  }\n]\n')
(p / 'reused-cursor-inputs.json').write_text('[\n  {\n    "path": "capability_conformance/rule_local_cursor_contract.json",\n    "bytes": 38051,\n    "sha256": "37cf5b997a805465a6aab4f42e27d0764ab19c294793758fbff22105699b7472"\n  },\n  {\n    "path": "tools/check_rule_local_cursor_contract.py",\n    "bytes": 58129,\n    "sha256": "27576c7d7e3ea32667f70e7a2849984befd3de755f28bbb219b3b42cab9ee42c"\n  }\n]\n')
LUA_CURSOR_PACKAGE_READING_37
bash tools/run_python_project_data.sh .linkedspec-data/scratch/lua137/select-tests.py
for host in puc luajit; do
  bash tools/run_lua_project_data.sh "$host" lua/test/rule_local_cursor_execution_test.lua > ".linkedspec-data/scratch/lua137/execution-$host.log" 2>&1 || exit
  bash tools/run_lua_project_data.sh "$host" lua/test/rule_local_cursor_generated_source_test.lua > ".linkedspec-data/scratch/lua137/generated-$host.log" 2>&1 || exit
  bash tools/run_lua_project_data.sh "$host" lua/test/rule_local_cursor_normalization_test.lua > ".linkedspec-data/scratch/lua137/normalization-$host.log" 2>&1 || exit
  bash tools/run_lua_project_data.sh "$host" lua/test/rule_local_cursor_option_removal_test.lua > ".linkedspec-data/scratch/lua137/removal-$host.log" 2>&1 || exit
  bash tools/run_lua_project_data.sh "$host" .linkedspec-data/scratch/lua137/package-prefix.lua > ".linkedspec-data/scratch/lua137/package-$host.log" 2>&1 || exit
done
bash tools/project_data_run.sh perl tools/check_generated_source_contract.pl > .linkedspec-data/scratch/lua137/neutral-generated.log 2>&1 || exit
bash tools/run_python_project_data.sh .linkedspec-data/scratch/lua137/verify.py
```

Related: [[lua-root-cursor-consumer-reading]], [[lua-startup-reading-coverage]],
[[lua-trace-controls-sinks]].

# Preservation and documentation checks

The independent Git audit preserves 1,413 source/old-Knowledge/decision/history/
policy files exactly, including the prior rollover manifest and segment. Of 2,582
prior task nodes, 2,580 stay exact; only this leaf and startup .3.6 change. There
are no new task nodes. All 90 Known headings, parked authoring state, historical
chronology and original history prefixes remain intact. All four embedded payloads
match their executed scratch bytes; exact source coverage reconstructs all 99 files,
51 groups and 149 ranges without changing any baseline digest.

Memory passes at 60 lines; Knowledge passes with 1,123 facts /8,980 questions.
Shared history passes 34 mutation controls across three surfaces and 67 segments.
Current Changes is 256 lines /22,508 bytes; Notes is 403 lines /34,088 bytes.
The book builds successfully; its 10,140,727-byte search-index warning remains
owned by startup .41.9. The normal nine-doctrine hooks remain the commit boundary.
