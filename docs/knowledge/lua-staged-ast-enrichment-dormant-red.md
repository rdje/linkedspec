---
id: lua-staged-ast-enrichment-dormant-red
title: Lua general staged-AST enrichment has one shared dual-ABI dormant GREEN carrier
answers:
  - "what is the Lua staged AST enrichment dormant RED"
  - "where is the Lua staged AST enrichment contract test"
  - "how do I run the Lua staged AST enrichment dormant consumer"
  - "does ordinary Lua discovery run staged AST enrichment"
  - "does canonical CI run the Lua staged AST enrichment consumer"
  - "how does Lua compile parse_job before its dedicated marker"
  - "what error does Lua parse_job produce"
  - "which Lua carriers preserve the staged AST enrichment RED"
  - "does Lua function body staged parsing still work"
  - "does the same staged AST test run on PUC Lua and LuaJIT"
  - "what does FUTURE-PARITY-BACKLOG 14.7.7.0 own"
date: 2026-08-27
status: current shared dual-ABI dormant GREEN; ordinary and canonical admission remain pending
tags: [lua, PUC-Lua, LuaJIT, staged-parsing, parse-job, red-test, generated-source, governance]
evidence: "FUTURE-PARITY-BACKLOG.14.7.7.0 adds one Lua-5.1-compatible consumer at lua/test/staged_ast_enrichment_contract_test.lua and executes the unchanged bytes independently on PUC Lua and LuaJIT. Each run passes 153 assertions over the complete 98-mutation neutral inventory, unchanged function-body-v1 resolve/load/compile/execute/cache/stitch behavior and wrong-top context, generic assign_scalar/call parse_job structure, narrow expr-v1 registry denial, and native/SpecFile-JSON reconstructed/validated generated-plan/independently loaded emitted-module unsupported-helper observations. The only failure is the labeled missing STAGED_PARSE_JOB_MARKER/staged_parse_job_v2 assertion. tools/run_lua_local.sh, lua/test/run.lua, and canonical CI omit the consumer; both Lua rollout legs remain pending; no Lua production source, generated format, or public/outward surface moves."
evidence_update_2026_08_27_marker_provenance: "FUTURE-PARITY-BACKLOG.14.7.7.1 supersedes only the generic-call/missing-marker boundary. Exact scalar assignment lowers to staged_parse_job_marker with strict literal options and a detached inert staged_parse_job_v2 record. Private PCRE2 capture byte ranges feed the existing typed source authority directly, so Unicode-scalar direct/ordered-derived provenance requires no copied-text search or regex replay. Native, reconstructed, generated-plan, and independently loaded emitted routes agree on PUC Lua and LuaJIT. Each run is 392 GREEN/one exact .14.7.7.2 caller-frozen resolution/cache/result/failure authority RED; ordinary/canonical discovery, both rollout rows, function-body v1, generated format, public/outward behavior, neutral lifecycle, and 98-mutation governance stay unchanged."
evidence_update_2026_08_27_current_depth: "FUTURE-PARITY-BACKLOG.14.7.7.2 adds the separate private frozen registry/cache and one-depth result/failure engine. Both ABIs reach 597 GREEN assertions and one exact .14.7.7.3 recurrence/bounds/rebasing/fresh-carrier RED. Discovery, rollout, v1, formats, public/outward behavior, lifecycle, and 98-mutation governance remain unchanged."
evidence_update_2026_08_27_recursive_carriers: "FUTURE-PARITY-BACKLOG.14.7.7.3 adds bounded breadth-first recurrence, strict lineage, shared resources, expiring safe points, original-source rebasing, and a fresh host-only seed across native, reconstructed, generated-plan, and independently loaded emitted execution. Both ABIs pass all 888 assertions. The stable path remains absent from ordinary/canonical discovery; rollout, neutral 98-mutation lifecycle, v1, formats, public/outward behavior, and other backends remain unchanged."
reverify:
  - "bash tools/run_python_project_data.sh tools/check_staged_ast_enrichment_contract.py"
  - "bash tools/run_lua_project_data.sh puc lua/test/staged_ast_enrichment_contract_test.lua"
  - "bash tools/run_lua_project_data.sh luajit lua/test/staged_ast_enrichment_contract_test.lua"
  - "rg -n 'LINKEDSPEC_STAGED_AST_ENRICHMENT_LUA_RED|STAGED_PARSE_JOB_MARKER|staged_parse_job_v2|staged_parse_job_marker' lua/test/staged_ast_enrichment_contract_test.lua lua/src/linkedspec"
  - "rg -n 'staged_ast_enrichment_contract_test.lua' tools/run_lua_local.sh lua/test/run.lua tools/run_ci_local.sh"
---

# Shared Lua staged-AST enrichment dormant boundary

The stable consumer path is `lua/test/staged_ast_enrichment_contract_test.lua`. It is ordinary Lua source with no
ABI-specific branch: the repository-local wrapper runs the same file independently on PUC Lua and LuaJIT. The
file deliberately stays absent from `tools/run_lua_local.sh`, `lua/test/run.lua`, and canonical CI until admission
owner `.14.7.7.4` moves the unchanged final path.

Assignment-form `parse_job(...)` now compiles exclusively as one dedicated `staged_parse_job_marker`. Strict
literal/static validation rejects malformed and residual generic forms; recognition transactions cannot reach the
declaration effect. The private runtime constructs a detached `STAGED_PARSE_JOB_MARKER` / `staged_parse_job_v2`
record from live PCRE2 whole-match or compact capture byte ranges through the existing Unicode-scalar source
authority. The narrow function-body-v1 registry remains unchanged and still rejects general `expr-v1` at resolve.

Native, normalized `SpecFile`-JSON reconstructed, validated generated-plan, and independently loaded emitted Lua
execution now return the same inert logical marker. Repeated equal captures retain distinct source ranges and
UTF-8 byte offsets become Unicode-scalar spans without copied-text search. Match JSON and marker data retain no
live capture/source/parser authority.

Each ABI now reports 888/888. Leaf `.14.7.7.1` owns the completed dedicated inert annotation/provenance carrier;
`.2` owns caller-frozen resolution/cache and all policies; `.3` owns breadth-first recurrence, shared bounds,
rebasing, and four fresh dormant production carriers. `.4` alone owns ordinary/canonical dual-ABI admission and
rollout; `.5` independently recomposes the two admitted ABI routes and closes the shared parent.

Related: [[general-staged-ast-enrichment-neutral-contract]], [[general-staged-ast-current-boundary]],
[[lua-staged-function-body-registry]], [[lua-progressive-span-dispatch-dormant-red]], and
[[function-body-staged-registry-dispatch]], [[lua-staged-ast-enrichment-marker-provenance]], and
[[lua-staged-ast-enrichment-current-depth-authority]] plus
[[lua-staged-ast-enrichment-recursive-carriers]].
