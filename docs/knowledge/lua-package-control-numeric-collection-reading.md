---
id: lua-package-control-numeric-collection-reading
title: Lua package controls numeric contracts and copied collections pass with prior limitations retained
answers:
  - "what exact Lua source did startup reading child 43 cover"
  - "which Lua control numeric and collection package groups pass during startup reading"
  - "what do Lua package controls prove about branch laziness and while limits"
  - "does the Lua package numeric fixture close earlier integer overflow defects"
  - "how does Lua package proof distinguish direct nested writes from statement set key errors"
  - "what does Lua package proof establish about selectors child push and copied collections"
  - "what focused evidence passed for Lua reading child 43"
date: 2026-09-13
status: exact source reading and selected proof complete; prior runtime defects and exclusions stay open
tags: [lua, package, control-flow, numeric, bindings, collections, startup, evidence]
evidence: "LUA-STARTUP-READING.1.43 activates from f58aaef09045348a90f2c3f12e8fdfff38409216. Eight complete windows read 1500 fragments /52604 bytes. Both installed hosts pass 28 complete package groups each, covering controls, numeric contracts, selectors, bindings and copied collections. Existing .2.1 gains precise direct-write/numeric and historical-stage qualifications. No production change or new repair node."
reverify: "Run LUA_PACKAGE_COLLECTION_READING_43 and the managed commands below; only complete groups 7805-9319 are selected, preserving all prior native-error exclusions and omitting partial array transforms."
---

# Exact reading and execution boundaries

| Repository source | Inclusive lines | Fragments / bytes | Raw SHA-256 |
| --- | --- | --- | --- |
| lua/test/run.lua | 7853–9352 | 1500 /52604 | 713cf5ca84eaf5a0da86c5bd0cdcba4d9b5ecd4dc721ed855941d8bc4793fe40 |

Complete credited windows are 7853–8040, 8041–8225, 8226–8410, 8411–8595,
8596–8780, 8781–8965, 8966–9150 and 9151–9352. Ordered range SHA-256 is
`f8161e6ed1d028a6c066977ad29db3091cadc1002ad05c7831a71a7deb03a703`.
All 99 Lua sources remain baseline-identical to
`baeb984e36a94a15951cd23d4c52def5064cdaca`. Reading reaches 43/51 groups,
60,040 fragments /2,283,819 bytes and 77 complete files. The runner ends inside
the array-transform group beginning at 9320. No earlier excluded native-error
group is selected, and no malformed native compile is induced.

# Lazy controls and eager logical values

The inline-control group begins at 7805, whose prefix was read in child 42.
Complete execution preserves selected false/null payloads, lazy alternatives,
empty-aggregate truth, nonempty string zero, one-time switch subjects, literal
bare labels and dynamic compound labels. Null shares the governed empty scalar
switch spelling, false shares numeric zero, and aggregates do not collapse into
empty scalar text. Inline control composes inside fluent return. Malformed arity
retains typed helper fields; structural i/elif/when/otherwise aliases stay outside
the value surface, and unless retains an unknown-helper contract result.

Statement if selects one attached or marker body in ActionIR order, including
portable alias shapes, empty branches and nested marker ranges. Expression blocks
catch local return; an ordinary action return exits its enclosing action. Thirteen
malformed structures lock orphaned, missing, duplicate, misordered and mixed-shape
diagnostics with exact code, keyword, reason and rule attribution.

Statement switch validates complete structure before evaluating its subject,
spends that subject once and selects only the first matching/default body. Nested
markers retain their boundaries; statements outside all marker branch ranges are
skipped under the already-recorded cross-backend caveat. Local returns and the
same null/boolean/aggregate comparisons remain exact. Thirteen malformed cases
lock orphaned markers, missing close, duplicate/default order, endcase boundaries
and attached-versus-marker shape. This does not close the earlier incomplete
attached-switch diagnostic source-span limitation under .2.7.

Attached while observes each prior condition state, allows body mutation to reach
the next condition, treats next as inner-loop continue and preserves action/local
return boundaries. At max_iterations 3, a condition becoming false after three
bodies succeeds; a still-true condition raises while_iteration_limit_exceeded
before body four with exact keyword, ActionIR kind, threshold, rule and message.
A missing body rejects before its condition executes. This finite option proof
does not close false-default or private infinite-cursor limitations.

Valid and/or/not calls return booleans and evaluate all operands once in order,
even after the logical outcome is determined. Empty aggregates are false;
nonempty aggregates and string zero are true. A boolean result can enter with.
The separate logical consumer's earlier arity/route proof remains recorded;
this package group is specifically the valid eager-value test.

# Numeric fixture, aliases and reducers

The package reads the unchanged scalar-numeric-v1 source and compares its full
55-case expected JSON value. Eighteen word aliases and eleven symbol callees share
the scalar evaluator; integer, float and bare-variable numeric receivers compose.
Comparisons return numeric truth and terminate their receiver chains. Grouped
comma and character-class regex ASTs remain regexes beside division-symbol calls;
these two literals are parsed only, not malformed native probes.

Array reducers accept the governed typed numeric inputs, preserve their source
and return exact sum/average/median/range/min/max results. Empty sum is zero;
other empty reducers, invalid elements, wrong kinds and wrong arity yield null.
Array reducer receivers are terminal. Finite fixture success does not close the
previously measured PUC integer arithmetic overflow under .2.13, certify every
integer, or represent a fresh six-runtime matrix.

# Selectors, typed bindings and child push

All neutral invalid-selector cases reject compilation with exact code, surface,
identifier and replacement fields. Dead branches, deferred unused function bodies
and fluent actions reject too. Mutating a compiled action payload before engine
construction cannot restore the retired form. Empty/quoted/computed/multi-argument
constructors and direct literals retain their distinct values. The unchanged
uniform-binding fixture executes despite its historical test title saying future.
The already-failing public selector scan is not rerun or reclassified here.

Absent push creates a typed array; saved push/array-end results remain independent
after later updates and support result chaining. A registered rule wins ambiguous
push(name,target) dispatch while its same-named working array remains unchanged.
Whole/indexed child pushes reuse the selected edge value, support zero-based
selection and implicit/explicit accumulators, and retain empty absent-rule reads.
Fluent push fills both accumulator forms; a scalar accumulator fails with exact
binding_kind_mismatch fields. These non-false child fixtures do not close the
earlier whole-child false-value loss under .2.10.

Mutable three-argument split updates its target; pure split remains a value.
Direct indexed writes return copied post-operation snapshots. Statement set_key
creates absent harrays and mutates existing ones; assigned/nested/receiver set_key
forms stay pure. Numeric array-index assignment remains available. Wrong statement
targets use binding_kind_mismatch; the direct integer-root write uses
nested_write_kind_conflict with binding and actual_kind=integer. Saved nested
snapshots remain unchanged after later source writes. Dropped values do not leak,
while standalone collection transforms rebind the typed array.

# Construction, copy, deterministic views and transforms

Array constructors/literals evaluate once left-to-right and retain ordinary
arrays as nested values. Only explicit flat/flat_array/flat_hash syntax marks
list-context insertion. One-level flat/concat results and saved copies isolate
later mutation; flat() is [null], whereas empty array/flat_array/concat are empty.
The earlier cross-backend zero/variadic caveat remains recorded.

Harray construction preserves ordered effects, nested values, explicit hash/array
pair splices, terminal receiver intent and null for a missing odd trailing value.
Later explicit keys override earlier splices. Harray-to-array insertion uses sorted
keys. Empty explicit splices remain empty, while ordinary arrays remain nested
values. Nested source and pair-token mutation does not change saved outputs.
Cross-backend odd-arity and ordering caveats remain with their prior owners.

Receiver copy uses the evaluated value exactly once, isolates nested harray/array
updates and preserves harray, array, string and scalar continuation kinds.
Missing copy remains null; subsequent helper behavior follows its own contract.
Sorted harray keys and copied values share lexical order; present-null fields count
as members. Invalid/missing views retain exact empty/zero outcomes, and count/key
membership results stay terminal.

Copied merge applies later overrides. Set/rename/drop/pick preserve inputs and
nested saved values; rename collision replaces the destination, while same-name
rename preserves the result. Invalid/missing transforms retain their distinct
copied-source/null/empty outcomes. Array selection uses zero-based slices/indexes,
default take/drop counts, stable first-occurrence uniqueness and copied sort/reverse
results; invalid boundaries preserve the current fallback contract. The following
array-transform/regex-filter group is partial and unexecuted in this leaf.

# Knowledge reconciliation and proof limits

Retrieved [[lua-runtime-lazy-inline-controls]], [[lua-runtime-if-statement-controls]],
[[lua-runtime-switch-statement-controls]], [[lua-runtime-attached-while-controls]],
[[lua-scalar-numeric-runtime]], [[lua-numeric-call-receiver-runtime]],
[[lua-numeric-aggregate-reducers]], [[lua-logical-helper-execution]],
[[lua-uniform-binding-runtime]], [[lua-aggregate-selector-compile-rejection]],
[[lua-runtime-named-harray-mutation]], [[lua-runtime-array-construction]],
[[lua-runtime-harray-construction]], [[lua-receiver-copy-value-preservation]],
[[lua-runtime-harray-views]], [[lua-runtime-harray-transforms]] and
[[lua-runtime-array-selection]]. Prior exact defect facts remain authoritative.

Existing .2.1 now owns direct indexed-write wording that still uses the earlier
named-harray seam and generic wrong-kind fields, numeric portability wording that
needs the existing integer-overflow qualification, and historical dating of
if/switch/while next-leaf links. No old Knowledge card, historical task node or
production source changes. No repair node is added.

Original helpers are 1–199, 3029–3121 and 7012–7026; all 28 complete groups from
7805–9319 execute unchanged. Both native jobs exit 0 with exact logs, 56 groups
total. No individual assertion total is invented. Neutral fixtures are consumed;
separate neutral checkers, generated/primary routes and cross-backend matrices
are not freshly executed. Installed PUC 5.5 and LuaJIT are exercised, without
certifying unavailable supported PUC 5.4. All 33 repair roots, prior native-error
exclusions, nil-error/public-selector failures, startup prerequisites and parked
named arguments remain. No full CI, PGEN/RGX build or push occurs.

# Exact replay

```bash
bash tools/project_data_run.sh python3 - <<'LUA_PACKAGE_COLLECTION_READING_43'
from pathlib import Path
p=Path('.linkedspec-data/scratch/lua143')
p.mkdir(parents=True, exist_ok=True)
(p / 'select-tests.py').write_text('from pathlib import Path\nimport re\n\np=Path(\'.linkedspec-data/scratch/lua143\')\nlines=Path(\'lua/test/run.lua\').read_text().splitlines(keepends=True)\nranges=[(1,199),(3029,3121),(7012,7026),(7805,9319)]\nselected=\'\'.join(\'\'.join(lines[a-1:b]) for a,b in ranges)\nnames=re.findall(r\'^test\\("([^"\\n]+)"\',selected,re.M)\nassert len(names)==28,len(names)\nassert lines[7804].startswith(\'test("runtime inline value controls\')\nassert lines[9319].startswith(\'test("runtime copied array transforms\')\n(p/\'package-selection.lua\').write_text(selected+\'\\nassert(total == 28, "package selection count drift: " .. total)\\nassert(failed == 0, "package selection failures: " .. failed)\\nprint("package selection: " .. total .. " test groups passed")\\n\')\nprint(\'Selected original helpers and 28 complete groups, 7805-9319; partial array-transform group and all earlier native-error groups excluded\')\n')
(p / 'verify.py').write_text('from pathlib import Path\nimport hashlib,json,re\n\np=Path(\'.linkedspec-data/scratch/lua143\')\nfor row in json.loads((p/\'scope.json\').read_text()):\n    data=b\'\'.join(Path(row[\'path\']).read_bytes().splitlines(keepends=True)[row[\'start\']-1:row[\'end\']])\n    assert len(data)==row[\'bytes\'] and hashlib.sha256(data).hexdigest()==row[\'sha256\']\nlines=Path(\'lua/test/run.lua\').read_text().splitlines(keepends=True)\nnames=re.findall(r\'^test\\("([^"\\n]+)"\',\'\'.join(lines[7804:9319]),re.M)\nassert len(names)==28\nexpected=\'\'.join(f\'ok {i} - {name}\\n\' for i,name in enumerate(names,1))+\'package selection: 28 test groups passed\\n\'\nfor host in [\'puc\',\'luajit\']:\n    assert (p/f\'package-{host}.log\').read_text()==expected,host\nprint(\'PASS: exact source range and 28 complete package groups per host, 56 total; control/numeric/binding/selector/copied-collection proof; prior runtime and native-error limitations remain open\')\n')
(p / 'scope.json').write_text('[\n  {\n    "path": "lua/test/run.lua",\n    "start": 7853,\n    "end": 9352,\n    "bytes": 52604,\n    "sha256": "713cf5ca84eaf5a0da86c5bd0cdcba4d9b5ecd4dc721ed855941d8bc4793fe40"\n  }\n]\n')
LUA_PACKAGE_COLLECTION_READING_43
bash tools/run_python_project_data.sh .linkedspec-data/scratch/lua143/select-tests.py
for host in puc luajit; do
  bash tools/run_lua_project_data.sh "$host" .linkedspec-data/scratch/lua143/package-selection.lua > ".linkedspec-data/scratch/lua143/package-$host.log" 2>&1 || exit
done
bash tools/run_python_project_data.sh .linkedspec-data/scratch/lua143/verify.py
```

Related: [[lua-package-named-capture-scoped-callback-reading]],
[[lua-startup-reading-coverage]], [[lua-recognition-numeric-semantic-reading-and-copy-gaps]].

# Preservation audit

All three embedded replay payloads match their saved files exactly. The final
preservation audit passes: 1,419 baseline source/Knowledge/decision/history/policy
files remain byte-identical; 2,579 of 2,582 prior task nodes are unchanged. Only
this reading leaf, its existing .2.1 guidance owner and startup .3.6 change; no
node is added. All 90 prior book limitation headings and parked authoring bytes
remain intact. Full coverage replay retains all 99 files and 51 groups; completed
totals are 60,040 fragments /2,283,819 bytes.

Document history passes all 34 mutation controls and exact reconstruction across
three surfaces and 67 segments. Explicit memory validation passes at 60 lines.
The book builds successfully; its existing search-index pressure warning reports
10,163,332 bytes and remains owned by startup .41.9. Production code, historical
Knowledge and prior immutable history stay unchanged.
