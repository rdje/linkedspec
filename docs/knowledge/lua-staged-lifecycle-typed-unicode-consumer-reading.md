---
id: lua-staged-lifecycle-typed-unicode-consumer-reading
title: Lua staged completion, lifecycle, typed-source and Unicode identity consumer boundaries
answers:
  - "what exact Lua source ranges does reading child 49 cover"
  - "does the staged Lua consumer run production carriers in a fresh process"
  - "does passing staged diagnostic truncation prove the byte ceiling"
  - "does the Lua standalone lifecycle consumer execute emitted source"
  - "what does the complete 240 assertion Lua typed source consumer verify"
  - "what does the Lua Unicode identity prefix through line 357 prove"
  - "which typed source admission guidance needs dating after reading child 49"
date: 2026-09-13
status: exact source reading and focused evidence complete; earlier repairs remain pending
tags: [lua, startup, reading, staged-parsing, lifecycle, source-location, unicode, tests]
evidence: "LUA-STARTUP-READING.1.49 reads five exact ranges, 1500 fragments/57785 bytes, in eleven complete windows; all ten native runs exit0, with 890+109+240+1706+341 assertions per host, 6572 total. Three neutral checkers pass. Existing .2.1 owns guidance qualification; no new repair node or production change."
reverify: "Run the exact repository-local replay below; preserve its bounded identity prefix and earlier native-error exclusions."
---

# Exact coverage and reading

Activation is `0900d3451cf2a4f860bb416d4facffc7c7241412`.
The unchanged Lua baseline is `baeb984e36a94a15951cd23d4c52def5064cdaca`.
The ordered group digest is
`625df2d24b896f1788c4dffc59193536acfece01368b90ff8a8cf670bf854b7d`.

| Path under lua/test/ | Inclusive lines | Fragments | Bytes | SHA-256 |
| --- | --- | ---: | ---: | --- |
| staged_ast_enrichment_contract_test.lua | 1741-2094 | 354 | 15741 | `f500129d8f3e375e8409dd930585d49089ebfcc616e4c8d95d586b2eb42d9468` |
| standalone_lifecycle_block_contract_test.lua | 1-228 | 228 | 7663 | `51afa9e2a77d4196dcf2e8b2edac7fcc676309aec3b9d24bfbdaedf612ebb990` |
| typed_source_location_contract_test.lua | 1-435 | 435 | 14020 | `8943b4fd6e78f0ea9b71ee3e3458aa044301e51e520652ef2a9ca905e693aebc` |
| unicode_rule_label_classifier_test.lua | 1-98 | 98 | 4632 | `63307a70b8fcb2e93f00585b137dcef9aba4c213cd5a22fc5fc76d9e66522d20` |
| unicode_rule_label_identity_routes_test.lua | 1-385 | 385 | 15729 | `7082c881306bf0515693c8935f7e667f52bf2a7bdd3347f7818b22f0a4fad7f3` |

Complete, untruncated windows are staged 1741-1920/1921-2094;
lifecycle 1-125/126-228; typed source 1-150/151-300/301-435;
classifier 1-98; identity 1-135/136-270/271-385. The first four files
reach EOF; identity remains partial. Cumulative reading is 49/51 groups,
69,040 fragments/2,642,497 bytes and 94 complete files. The codebase as a
whole remains unread until the remaining Lua and supporting-code owners close.

# Staged suffix: rebasing, settlement and production state

Direct child positions/spans/end offsets rebase to the original source. A
concatenated ASCII/Unicode provenance fixture crosses two source segments and
returns derived-text spans. An invalid local diagnostic range becomes the exact
`source_projection=invalid_local_range` record. The 64-byte diagnostic fixture
checks `staged_diagnostic_truncated` and remaining bytes zero; it does not measure
serialized output length. Existing .2.26 already owns the measured output-cap
violation, so passing this assertion does not close that defect.

Callback safe points actively recheck cancellation and deadlines: the second
cancellation check becomes true and the clock changes from 1 to 11 against limit
10. Both return complete typed diagnostics. The earlier recursive limits retain
the shared resource proof from child 48.

Without a seed, the production parser returns a raw staged marker. The opaque
seed rejects a string impostor. Native, SpecFile-JSON reconstructed, generated-plan
and current-host emitted modules each execute twice. All eight results have the
same expression AST, no diagnostics, one successful sidecar, one cache miss/entry,
zero hits and one call. Eight distinct starts and callbacks carry matching run ids
and fresh tokens, including repeated calls through the same native engine.

This is fresh execution state in the current host, not a separately spawned
production-parser process. Parent failure starts no staged authority. A live
recognition transaction rejects with `staged_transaction_forbidden`, and the same
execution token then rejects reuse as expired. Mutating one output leaves the
other seven ASTs unchanged. AST JSON, the explicitly reduced label/family plan and
emitted source omit six named runtime-authority tokens; this is an exact token
scan of those three artifacts, not a structural scan of every possible field.
Native and generated-plan failure routes preserve `staged_child_failed` and
complete context.

The full source also checks exact ordinary/canonical registration multiplicity,
absence from the inline suite, private token absence in the umbrella module and
presence of the recursive implementation. These are topology checks, not execution
of canonical CI. Full staged proof is 890 per installed host; the earlier 888
current-guidance discrepancy remains owned by .2.1.

# Lifecycle and typed source consumers

Nine placement twins compare normalized lifecycle-I kind, trimmed code and opening
line and exclude new plain blocks. Provenance twins compare exact authored source
and source-stripped compiled ActionIR. Four duplicate combinations preserve authored
order through native, reconstructed, generated-plan and executed emitted Lua in
the current host. Six brace-ownership cases retain their prior node kinds. Three
malformed twins compare rejection, error type and nonempty messages, not exact
portable diagnostic fields. Reconstructed legacy plain payloads remain readable
and inert under native and generated-plan execution with source validation disabled.
The exact total is 109 per host; no other backend is executed here.

The complete typed-source consumer remains 240 per host. It checks three sources,
seven positions, six direct spans, three ordered derived-text cases and four
private errors with exact required context and six forbidden fields. Scalar
coordinates retain the expected UTF-8 byte conversion. Mutating a detached position
record leaves the token intact; mutating caller source text leaves authority-owned
text unchanged. Cross-source, out-of-range, reversed and foreign-authority inputs
reject. This finite source fixture does not close the separate private cursor
infinity .2.12 boundary.

All 92 projection names are unique and catalogs equal the neutral contract. The
seven compatibility aliases and projection rows are detached across calls. Three
fixtures (complete named marks, save/rewind/restore cursor and compatibility aliases)
execute through native, reconstructed and generated-plan routes. The Unicode alias
fixture retains scalar span length four before named `ab`, and cursor restoration
returns to two. This file does not emit a host module; child 47 separately preserves
the alias consumer's emitted proof.

# Unicode classifier and bounded identity execution

The classifier checks metadata, every endpoint of all 806 neutral ranges, invalid
numeric scalars, all 9/8/2 positive/negative/distinct fixtures, one-based byte prefix
boundaries and eight malformed UTF-8 classes. It executes 1,706 assertions per
host without regex compilation of malformed patterns.

Identity reading reaches line 385, inside a later conditional. Execution copies
unchanged lines 1-357 and closes the existing temporary-directory callback after
its complete primary-route loop. This yields 341 assertions per host; it does not
claim the full 359 or execute the later native selector/diagnostic fixtures.

Ten unique exact label byte strings retain declaration/compiled/descriptor/plan
order and map keys, reconstructed compiled state and runtime selection. Both
normalization-sensitive rules remain distinct. Compiled/descriptor JSON omit table
and userdata identity tokens. The hex emitted payload reconstructs the same labels;
the loaded emitted module retains metadata and plan and executes every label.

A fresh process on the same installed runtime reads the generated module and labels,
validates the plan and returns all exact values. Its selected trace preserves
requested rule, effective top rule and source identity; child status is successful
and stderr empty. Strict loading preserves source bytes, order and values while
portable JSON omits the temporary source path. Each of ten labels executes through
both inline-spec and spec-file primary APIs with exit zero, exact stdout and empty
stderr. The temporary directory cleanup command must succeed; the original helper
does not separately assert post-removal absence. Managed wrappers keep these data
on the repository volume.

# Knowledge reconciliation and proof limits

Read canonical staged carrier admission, standalone lifecycle audit, typed-source
admission and Unicode implementation-plan cards before testing. One combined
Knowledge display clipped four words in the lifecycle metadata; the complete
first 48 lines were reread untruncated. No source viewing window was truncated.
The already-owned staged 888/890 and Unicode next-stage qualifications remain.
Existing .2.1 now also owns the typed-source card's present-tense 8/6/42 conclusion:
retain its dated admission evidence while reflecting current neutral 14/0/231.

Neutral lifecycle proof reports 9 placements/4 duplicates/6 ownership/3 malformed,
6 runtime topology rows, 15 public documents/7 stale denials/14 mutations. Typed
source remains 14 complete/0 pending/231 mutations, with 92+7+2 catalogs and
observation/program-wide public guards. Unicode remains 17.0.0, 806 ranges and
9/8/2 fixtures. These checkers do not rerun the six-runtime matrix.

All 34 repair roots remain open, including .2.25 malformed arrays, .2.26 diagnostic
bytes, .2.27 marker cycles, .2.28 private captures, .2.29 provenance, .2.2 installed
PUC nil-error failures and .2.34 fixture coverage. The earlier malformed native
regex exclusions remain untouched. No new defect is found; no full component gate,
canonical CI, dependency build, push or supported-PUC admission is claimed.

# Exact replay

```bash
bash tools/project_data_run.sh python3 - <<'LUA_STAGED_SOURCE_IDENTITY_READING_49'
from pathlib import Path
p=Path('.linkedspec-data/scratch/lua149')
p.mkdir(parents=True, exist_ok=True)
(p / 'select-tests.py').write_text('from pathlib import Path\np=Path(\'.linkedspec-data/scratch/lua149\')\nlines=Path(\'lua/test/unicode_rule_label_identity_routes_test.lua\').read_text().splitlines(keepends=True)\nassert lines[246].startswith(\'with_temp_directory(function(root)\')\nassert lines[356].strip()==\'end\'\nassert \'selectors, diagnostics\' in lines[358]\nfooter=\'\\nend)\\nif #failures > 0 then error(table.concat(failures, "\\\\n"), 0) end\\nio.stdout:write("Lua Unicode identity read prefix: ", assertions, " assertions passed\\\\n")\\n\'\n(p/\'identity-read-prefix.lua\').write_text(\'\'.join(lines[:357])+footer)\nprint(\'Selected unchanged identity lines 1-357; close the existing temporary-directory callback after both primary source forms. Later selector diagnostics remain excluded.\')\n')
(p / 'verify.py').write_text("from pathlib import Path\nimport hashlib,json\np=Path('.linkedspec-data/scratch/lua149')\nrows=json.loads((p/'scope.json').read_text())\nassert len(rows)==5 and sum(r['end']-r['start']+1 for r in rows)==1500\nassert sum(r['bytes'] for r in rows)==57785\nfor r in rows:\n data=b''.join(Path(r['path']).read_bytes().splitlines(keepends=True)[r['start']-1:r['end']])\n assert len(data)==r['bytes'] and hashlib.sha256(data).hexdigest()==r['sha256'],r['path']\nlines=Path(rows[-1]['path']).read_text().splitlines(keepends=True)\nprefix=(p/'identity-read-prefix.lua').read_text()\nassert prefix.startswith(''.join(lines[:357])+'\\nend)\\n')\nassert 'selectors, diagnostics, and traces' not in prefix\nfor host,runtime in [('puc','puc-lua'),('luajit','luajit')]:\n expected={\n  'staged':f'Lua staged-AST enrichment contract: 890 assertions passed on {runtime}\\n',\n  'lifecycle':f'standalone lifecycle block {runtime} admission: 109 assertions passed\\n',\n  'typed':'Lua typed source-location contract: 240 assertions passed\\n',\n  'classifier':'Lua Unicode rule-label classifier: 1706 assertions passed\\n',\n  'identity':'Lua Unicode identity read prefix: 341 assertions passed\\n'}\n for item,value in expected.items():\n  assert (p/f'{item}-{host}.log').read_text()==value,(item,host)\n  assert (p/f'{item}-{host}.exit').read_text()=='0\\n',(item,host)\nneutral={\n 'standalone_lifecycle_block':'standalone lifecycle-block contract: 9 placements, 4 duplicate forms, 6 ownership cases, 3 malformed twins, 6 runtime routes, 15 public documents, 7 stale-claim denials, 14 drift mutations\\n',\n 'typed_source_location':'typed-source-location-contract: OK (3 sources; 7 positions; 6 direct spans; 3 derived texts; 8+8+33 state transitions; 6 recursive observations; 4 structural cases; 92 helper projections + 7 aliases + 2 internal ids; 33 diagnostics; recursive-observation public 6 documents/6 forbidden/10 surface guards; program-wide public 8 documents/8 forbidden/6 safety/10 surface guards; 14 complete / 0 pending rollout; 231 drift mutations)\\n',\n 'unicode_rule_label':'unicode-rule-label-contract: OK (Unicode 17.0.0; 806 XID_Continue ranges; 9 positive; 8 negative; 2 distinct pairs)\\n'}\nfor item,value in neutral.items():assert (p/f'neutral-{item}.log').read_text()==value,item\nprint('PASS exact five ranges/1500 fragments/57785 bytes; ten native exits0 and 6572 assertions (890+109+240+1706+341 per host); unchanged identity1-357; three exact neutral/public outputs. No full359 identity, later selector proof, fresh-process staged claim or prior repair closure.')\n")
(p / 'scope.json').write_text('[\n  {\n    "path": "lua/test/staged_ast_enrichment_contract_test.lua",\n    "start": 1741,\n    "end": 2094,\n    "bytes": 15741,\n    "sha256": "f500129d8f3e375e8409dd930585d49089ebfcc616e4c8d95d586b2eb42d9468"\n  },\n  {\n    "path": "lua/test/standalone_lifecycle_block_contract_test.lua",\n    "start": 1,\n    "end": 228,\n    "bytes": 7663,\n    "sha256": "51afa9e2a77d4196dcf2e8b2edac7fcc676309aec3b9d24bfbdaedf612ebb990"\n  },\n  {\n    "path": "lua/test/typed_source_location_contract_test.lua",\n    "start": 1,\n    "end": 435,\n    "bytes": 14020,\n    "sha256": "8943b4fd6e78f0ea9b71ee3e3458aa044301e51e520652ef2a9ca905e693aebc"\n  },\n  {\n    "path": "lua/test/unicode_rule_label_classifier_test.lua",\n    "start": 1,\n    "end": 98,\n    "bytes": 4632,\n    "sha256": "63307a70b8fcb2e93f00585b137dcef9aba4c213cd5a22fc5fc76d9e66522d20"\n  },\n  {\n    "path": "lua/test/unicode_rule_label_identity_routes_test.lua",\n    "start": 1,\n    "end": 385,\n    "bytes": 15729,\n    "sha256": "7082c881306bf0515693c8935f7e667f52bf2a7bdd3347f7818b22f0a4fad7f3"\n  }\n]\n')
LUA_STAGED_SOURCE_IDENTITY_READING_49
bash tools/run_python_project_data.sh .linkedspec-data/scratch/lua149/select-tests.py
for host in puc luajit; do
  for item in staged lifecycle typed classifier identity; do
    case "$item" in
      staged) script=lua/test/staged_ast_enrichment_contract_test.lua ;;
      lifecycle) script=lua/test/standalone_lifecycle_block_contract_test.lua ;;
      typed) script=lua/test/typed_source_location_contract_test.lua ;;
      classifier) script=lua/test/unicode_rule_label_classifier_test.lua ;;
      identity) script=.linkedspec-data/scratch/lua149/identity-read-prefix.lua ;;
    esac
    status=0
    bash tools/run_lua_project_data.sh "$host" "$script" > ".linkedspec-data/scratch/lua149/$item-$host.log" 2>&1 || status=$?
    printf '%s\n' "$status" > ".linkedspec-data/scratch/lua149/$item-$host.exit"
  done
 done
for item in standalone_lifecycle_block typed_source_location unicode_rule_label; do
  bash tools/run_python_project_data.sh "tools/check_${item}_contract.py" > ".linkedspec-data/scratch/lua149/neutral-$item.log" 2>&1 || exit
done
bash tools/run_python_project_data.sh .linkedspec-data/scratch/lua149/verify.py
```

Related: [[lua-staged-declaration-depth-recursion-consumer-reading]],
[[lua-semantic-admission-source-alias-consumer-reading]],
[[lua-startup-reading-coverage]], [[standalone-lifecycle-block-audit]].

# Preservation and focused completion

Independent preservation retains 1,426 prior source/Knowledge/decision/history/
policy files and 2,582 of 2,585 prior task nodes byte-exactly. Only this reading
leaf, existing .2.1 guidance and startup .3.6 change; no node is added. All ninety-one
Known book headings and the parked authoring tree remain exact. Three embedded
payloads match scratch; independent coverage verifies all 49 completed groups and
unchanged inventory/range/group digests.

History validation passes 34 mutation controls, three surfaces and 68 segments;
both pressure checks pass at Changes 340 and Notes 270 lines. Memory remains 60 lines
with correct activation and handoff semantics. Knowledge regeneration reports
1,135 cards /9,065 question keys. The book renders with its existing large-search-
index warning at 10,184,873 bytes, still owned by startup .41.9. Whitespace checks
pass. Normal doctrine and activation hooks remain required at landing; no full CI
or push is claimed here.
