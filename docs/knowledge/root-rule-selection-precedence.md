---
id: root-rule-selection-precedence
title: "Directed root-selection contract: explicit selector > first authored `::` > first authored `:` when no marker exists"
answers:
  - "what is the root rule selection precedence"
  - "does --top-rule have priority over Rule::"
  - "can --top-rule select an ordinary single colon rule"
  - "what rule is top when a spec has no double colon rule"
  - "is Rule:: required in every spec"
  - "what task owns first rule as top"
  - "which backends currently reject a spec without Rule::"
  - "does selecting the entry rule count as a strict unused reference"
  - "does root selection rewrite descriptor is_top metadata"
  - "why did pre-implementation Perl select an ordinary rule before a later Rule:: marker"
date: 2026-07-18
status: neutral contract, all five backends, and recurring/public no-drift admitted; rollout 7 complete / 0 pending
tags: [dsl, root-rule, top-rule, entry-selection, cli, descriptor, generated-source, strict-syntax, backend-parity, ADR-0046, FUTURE-PARITY-BACKLOG]
evidence: "Director clarification on 2026-07-18 fixes exact precedence: (1) an explicit entry selector, specifically CLI `--top-rule NAME`, has highest priority and may designate any declared rule, including an ordinary `Rule:` even when a `Rule::` exists; (2) without an explicit selector, the first authored `Rule::` is the default; (3) when no rule ends in `::`, the first authored ordinary `Rule:` is the default. Full five-backend audit under `.9.1.1.2.0` found: Perl validation requires a marker but `Compiler::run_get_pipeline` finally selects requested label or parsed row zero, so an earlier ordinary rule defeats a later marker; Rust validation and native/generated defaults require the first marker, with explicit selection confined to `execute_value`/primary; Dart, Julia, and Lua validation require a marker while their ordered runtime helpers already implement marker-then-first fallback. The existing shared CLI case proves an explicit ordinary rule beats two markers. Strict-unused remains defined-minus-statically-referenced with no selected/marked-rule exemption. ADR 0046 plus `capability_conformance/root_rule_selection_contract.json` now lock eight selections, three failures, three strict cases, source identity, route projections, exact current inventory, seven rollout legs, and 24 mutations without changing backend behavior."
evidence_update_2026_07_18_perl_admission: "Leaves `.9.1.1.2.1.1-.3` implement and admit the Perl reference across native, loaded, reconstructed, generated direct/traced/Get, descriptor, structured diagnostic, runtime/request trace, strict, and primary routes. The shared manifest is now 65 cases with exact first-marker and markerless-default successes beside explicit and unknown selection. The checker topology-locks the core/routes consumers and canonical registration; rollout is 2 complete / 5 pending. Rust, Dart, Julia, and Lua retain their audited staged differences until `.2-.5`, followed by final admission `.6`."
evidence_update_2026_07_18_rust_admission: "Leaves `.9.1.1.2.2.1-.3` implement and admit Rust across native, loaded/reconstructed, generated/emitted direct/traced, descriptor, structured diagnostic, effective runtime trace, strict, and primary/request-trace routes. One topology-checked 15-role consumer composes every declared projection exactly once. Rust passes the shared 65-case primary manifest with `POSIXLY_CORRECT` unset and set. The checker rejects 29 mutations and advances only Rust, so rollout is 3 complete / 4 pending; Dart, Julia, Lua, and final admission remain `.3-.6`."
evidence_update_2026_07_18_dart_preflight: "Behavior-free Dart preflight `.9.1.1.2.3.0` proves exact primary 64/65 with `POSIXLY_CORRECT` unset and set; markerless default alone fails compile validation. The ordered runtime, normalized-state, and generated-direct fallbacks already work when validation is bypassed, while loaded and emitted source re-enter the marker-required validator. Explicit unknown and zero-rule compiled-state failures use old untyped stages, and root descriptor metadata lacks the entry-rule contract. Implementation remains safely split across Dart core `.1`, composed routes `.2`, and admission `.3`; rollout stays 3/7."
evidence_update_2026_07_18_dart_core: "Leaf `.9.1.1.2.3.1` implements Dart marker-optional one-or-more-rule validation and one `CompiledSpec.resolveEntryRule` owner. Explicit selection wins, otherwise the first authored marker wins, otherwise the first authored rule wins; zero and unknown selection fail portably before user code. Empty/comment-only sources now remain parser envelopes so validation owns `no_rules_defined`; malformed non-rule text remains a parse error. Descriptor metadata publishes the root contract while definition order and authored `is_top` remain immutable, and strict-unused remains authored-edge-only. Focused 107, package 266, corpus 105, analyzer/format, and primary 65x2 pass. Composed routes/admission remain `.3.2-.3`, so rollout stays 3/7."
evidence_update_2026_07_18_dart_routes: "Leaf `.9.1.1.2.3.2` proves loaded/normalized-JSON and generated/emitted direct/traced Dart execution reuse the compiled resolver. Low runtime trace records requested/effective/basis; loader and generated failures preserve portable zero/unknown identities; stale generated contract/plan validation remains before selection; descriptors and generated v2/format 2 minimal label/family plans remain unchanged. Focused 86+14 and complete Dart format/analyzer/package 269/primary 65x2/corpus 105 pass. Canonical Perl root 7+5, cursor 288, reference primary 65x2, and Phase 0 1,031/1,031 pass. Admission `.3.3` remains pending, so rollout stays 3/7."
evidence_update_2026_07_18_dart_admission: "Leaf `.9.1.1.2.3.3` admits Dart through one exact 15-role consumer, package-wide/canonical driver registration, six shared primary ids, and five additional omission mutations. Complete Dart proof passes package 270, primary 65x2, and corpus 105. The checker rejects 34 mutations and advances only Dart, so rollout is 4 complete / 3 pending; Julia, Lua/LuaJIT, and final no-drift remain `.4-.6`."
evidence_update_2026_07_18_julia_preflight: "Behavior-free Julia preflight `.9.1.1.2.4.0` measures exact shared primary at 31/65 in both environments. One failure is root-owned markerless validation; 22 help/usage and 11 request-trace failures are owned by pending cursor migration `.9.1.6`. Ordered runtime fallback and explicit override already work under bypass, while loaded/normalized/emitted routes validate first; failures, descriptor, and trace identity remain legacy. Root core `.4.1` and routes `.4.2` now precede cursor `.9.1.6`; final exact 65x2 admission `.4.3` depends on it. Rollout stays 4/7."
evidence_update_2026_07_18_julia_core: "Leaf `.9.1.1.2.4.1` implements Julia marker-optional validation and one compiled resolver with structure-before-selector failures. Native execution applies explicit > first marker > first rule before context/user code; descriptors publish immutable root identity and strict-unused remains authored-edge-only. Focused neutral proof passes 79, shared primary is exactly 32/65 twice with only 33 cursor failures, and corpus is 105/105. Composed routes/admission remain pending, so rollout stays 4/7."
evidence_update_2026_07_18_julia_routes: "Leaf `.9.1.1.2.4.2` proves Julia loaded/normalized and generated/emitted direct/traced execution reuse the compiled resolver. Low trace records requested/effective/basis; loader and generated failures preserve portable zero/unknown identities; generated plan validation remains first; descriptors and generated v1/format 1 minimal label/family plans remain unchanged. Focused routes 57, core 79, loader 82, emitter 59, primary 32/65x2, corpus 105, and governance 34 pass. Cursor/admission remain pending, so rollout stays 4/7."
evidence_update_2026_07_18_julia_cursor_option: "Cursor leaves `.9.1.6.1-.5` now make Julia rule-local through normalization, runtime, descriptor v1, generated v2, and public/CLI removal. `--top-rule` remains independent and highest-priority; shared primary is exact 65/65 twice. Cursor admission `.6` and root topology admission `.4.3` remain pending, so root rollout stays 4/7."
evidence_update_2026_07_18_julia_admission: "Leaf `.9.1.1.2.4.3` admits Julia through one exact 15-role consumer, package/canonical registration, six shared case ids, and five additional omission mutations. Hand-authored selection fixtures use distinct entry-lifecycle `I` returns; only the fixed request-trace source retains canonical `E` bytes. Package 3,428, primary 65x2, corpus 105, and canonical Phase 0 1,031/1,031 pass. The checker rejects 39 mutations and advances only Julia, so rollout is 5 complete / 2 pending."
evidence_update_2026_07_18_lua_preflight: "Behavior-free Lua preflight `.9.1.1.2.5.0` proves PUC Lua and LuaJIT are identical. All four default/POSIX shared-primary legs are exactly 31/65: markerless default is the sole root-owned failure, while 22 help/usage and 11 request-trace failures belong to cursor `.9.1.7`. Bypassed native/reconstructed/generated-v1 execution already preserves ordered first-rule fallback, but parser/validator, portable failure, descriptor, trace, loaded, and emitted seams need `.5.1-.2`. Complete package is 176 pass plus one cursor help failure and corpus is 105/105 per ABI. Frozen order is root core `.5.1`, routes `.5.2`, cursor `.9.1.7`, then 15-role dual-ABI admission `.5.3`; authored selection fixtures use `I`, while fixed request-trace bytes retain `E`."
evidence_update_2026_07_18_lua_core: "Leaf `.9.1.1.2.5.1` implements Lua marker-optional one-or-more-rule validation and one compiled-state resolver. PUC Lua and LuaJIT apply explicit > first marker > first rule before runtime context/user code, return portable zero/unknown failures, preserve strict authored-edge analysis and immutable descriptor identity, and pass 99 focused assertions each. Package is 176/177 with only the cursor-help mismatch, every default/POSIX primary leg is exactly 32/65 with the same 33 cursor-owned failures, and corpus is 105/105 per ABI. Routes/cursor/admission remain pending, so rollout stays 5/7."
evidence_update_2026_07_19_lua_routes: "Leaf `.9.1.1.2.5.2` proves loaded/normalized and generated/emitted direct/traced routes reuse Lua's one compiled resolver, preserve low trace and portable wrapper failures, and retain plan validation before selection. Focused routes pass 101 assertions per ABI. Cursor `.9.1.7` then closes every staged primary mismatch without changing root precedence."
evidence_update_2026_07_19_lua_admission: "Leaf `.9.1.1.2.5.3` admits Lua through one exact 15-role source executed on PUC Lua and LuaJIT. Exact topology RED 3/3x2 becomes 139/139x2; complete package is 177/177x2, primary 65/65x4, and corpus 105/105x2. The checker locks both ABI driver legs, six primary ids, canonical input, exact role order/completion, Lua rollout, and five new omission mutations. Only Lua advances: root governance is 6 complete / 1 pending with 44 rejected mutations. Hand-authored selection fixtures use `I`; fixed request-trace bytes retain `E`. Final public no-drift `.6` remains."
evidence_update_2026_07_19_final_admission: "Leaf `.9.1.1.2.6` closes rollout at 7 complete / 0 pending. `tools/check_root_rule_selection_five_backend.sh` composes Perl core/routes, exact Rust/Dart/Julia admissions, the same Lua admission source on PUC Lua and LuaJIT, six selected primary cases across five commands and two environments, and generated/capability/coverage ledgers. The checker now requires 25 public/KM/task documents, forbids 19 stale current claims, makes ROADMAP.md and ROADMAP_V2.md required current-state inputs, registers the optional canonical switch, and rejects 54 mutations."
reverify: "bash tools/check_root_rule_selection_five_backend.sh"
---

# Root-rule selection precedence

The durable target contract is ordered, not ambiguous:

1. An explicit entry selector wins. On the shared primary command this is `--top-rule NAME`; it may select any
   declared rule and has priority even when the source contains one or more `Rule::` markers.
2. With no explicit selector, the first authored `Rule::` in definition order is the default.
3. With no explicit selector and no `::` marker, the first authored ordinary `Rule:` is the default.

Zero-rule input remains invalid and an explicit unknown name remains an error. A rule selected by any route remains
an ordinary rule at execution time; authored `is_top` metadata records source syntax and is not rewritten to mimic
the dynamic selector. Generated and reconstructed paths preserve definition order plus authored markers, while
request trace keeps the requested selector/`<default>` distinction and runtime attribution records the resolved
label.

Strict-unused is deliberately orthogonal. Selecting or marking a rule does not add an authored edge and does not
exempt the selected rule from the existing defined-minus-referenced check.

ADR [0046](../decisions/0046-root-rule-selection-precedence.md), the executable neutral contract, and composed
Perl, Rust, Dart, Julia, and dual-ABI Lua are complete through separate omission-sensitive 15-role admission
consumers. Their
hand-authored selection fixtures return from entry lifecycle `I`, proving the chosen rule was entered; the fixed
shared request-trace fixture retains canonical `E` bytes. Final recurring/public admission composes those
consumers, the exact selected primary projection, and public no-drift at 7 complete / 0 pending. Run
`bash tools/check_root_rule_selection_five_backend.sh` to reverify the complete root-selection contract.
