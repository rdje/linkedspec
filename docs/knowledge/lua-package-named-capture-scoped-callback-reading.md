---
id: lua-package-named-capture-scoped-callback-reading
title: Lua package named captures and scoped callbacks pass while native-error groups remain excluded
answers:
  - "what exact Lua source did startup reading child 42 cover"
  - "which Lua named capture and scoped callback package groups pass during startup reading"
  - "which Lua regex helper package groups remain unexecuted under the native formatter repair"
  - "what does Lua package proof establish about named marks and slot marker timing"
  - "does Lua package call name census prove every helper result"
  - "what do Lua scoped callback and root kind traversal package tests establish"
  - "what focused evidence passed for Lua reading child 42"
date: 2026-09-13
status: exact source reading and safe selected proof complete; three native-error groups and other repairs stay open
tags: [lua, package, captures, marks, callbacks, traversal, startup, evidence]
evidence: "LUA-STARTUP-READING.1.42 activates from d4c9f938d30e5f49161f0854113066ebcfb4133a. Eight complete windows read 1500 fragments /61135 bytes. Both installed hosts pass 17 safe complete package groups each, covering named captures, marker timing, child reuse and scoped/tree callbacks. Three native-error groups 6820-6977 stay unexecuted under .2.3; .2.1 guidance extends. No production change or new repair node."
reverify: "Run LUA_PACKAGE_CALLBACK_READING_42 and the managed commands below; the selector omits all three known native-error groups 6820-6977 and the partial inline-control group 7805 onward."
---

# Exact reading and execution boundaries

| Repository source | Inclusive lines | Fragments / bytes | Raw SHA-256 |
| --- | --- | --- | --- |
| lua/test/run.lua | 6353–7852 | 1500 /61135 | 7c2a7598a55dc8e2c08e9c6e05fba19db5601174cdde4c506f1517fdfe472e92 |

Complete credited windows are 6353–6540, 6541–6725, 6726–6910, 6911–7095,
7096–7280, 7281–7465, 7466–7650 and 7651–7852. Ordered range SHA-256 is
`109237226136012ab79b71665b6ea225a776eb20b151e1a549c26f9e6c06267f`.
All 99 Lua sources remain baseline-identical to
`baeb984e36a94a15951cd23d4c52def5064cdaca`. Reading reaches 42/51 groups,
58,540 fragments /2,231,215 bytes and 77 complete files. The package runner
ends inside the inline-value-control group beginning at 7805.

Three complete groups include malformed patterns on the known native compile
path: helper matches at 6820–6860, pure split at 6861–6907 and statement regex
substitution at 6908–6977. All three are excluded on both installed hosts under
existing .2.3. Their otherwise-valid flags, regex values, Unicode progress and
mutation cases are not counted as fresh passes. No malformed native compile or
new crash is induced. Re-admission belongs to the same repair after its native
fix and runtime prerequisites. Reading nevertheless covers every owned line.

# Boundary capture, named marks and slot timing

The boundary group starts at 6312, whose prefix was read in child 41. The full
selected group proves earliest usable-rule search without consuming the boundary.
A usable rule with no later match captures to input end; unresolved or regex-free
rules are neutral. Zero arguments remain a neutral no-op pending the existing
cross-backend arity owner. Unicode cursor and remainder values stay exact.

The unchanged complete-named-mark neutral fixture executes natively and after
public SpecFile JSON reconstruction. Governed named spans likewise execute their
exact fixture through both routes. The supplemental Unicode bridge distinguishes
stable reads from takes, copies anonymous boundaries into named marks and back,
deletes a target when its copied source is absent, and preserves state after a
missing bridge or reversed span. Mark writers return null; named span arity
failures retain typed helper name, expected arity and actual count.

Five compiled slot events preserve their type, kind, preceding regex index, mark
name and compatibility spelling. Same-slot actions see the prior mark/boundary;
later slots see the update. Preferred @capture_slice, @capture_from_here,
@move_pos and two @mark names execute with exact Unicode positions natively and
after public AST reconstruction. The checked-in EBNF logging_annotation line
compiles to its one @move_pos event at slot 1. Authored malformed markers reject
through validation; a manually malformed AST with validation disabled produces
malformed_rule_slot_marker with rule and line attribution. This test compiles
the shipped line; it does not execute the whole shipped grammar.

# Scalar text and dispatch coverage

Deterministic scalar/string tests preserve false and zero, stable negative-zero
text, null/non-text propagation, lazy coalesce, Unicode whitespace and scalar
slicing, literal replacement and lexical comparisons. Terminal receiver results
do not continue as strings. The exact six-variant scalar-text neutral fixture
executes with every expected field, including non-text codeblocks and the retired
concat spelling. Statement split replaces a bare target with a typed array,
preserves empty fields and its source scalar, and keeps pure value split distinct.
These safe tests do not close the earlier false-value split/join or wide-integer
coercion repairs.

The admitted-call census parses, compiles and invokes every current name with
zero authored arguments. It counts only typed unsupported-helper dispatch and
locks exactly 13 documented non-function names. Other runtime outcomes are
deliberately not asserted by that census. It proves that dispatch-owner inventory,
not correct successful execution or exact diagnostics for every helper.

Direct call(Child) returns the child value and refreshes retv/cursor. Trace-backed
action-edge cases show one current-child entry, zero passive-child re-search with
one passive dispatch, exact self-recursive entry count, and separate unrelated-rule
execution before the edge fallback. This preserves existing child-reuse behavior;
it does not close the different false whole-child binding limitation under .2.10.

# Punctuation aliases, local returns and copied callback scope

The neutral punctuation contract compares bare and parenthesized semantic ASTs
after omitting provenance fields. It preserves statement-only next, ordinary
identifier reads, final-only receiver aliases and excluded syntax. Missing-needle
contains aliases share the existing result rather than normalizing backend arity.
The exact future_fixture field executes through public AST reconstruction; that
field name is not evidence that the implemented native route is still future.

Ordinary eager blocks yield their final expression or block-local return, skip
later statements after return, execute earlier mutations and allow compatible
receiver continuation. Empty/keyed braces remain harrays; trailing contextual
blocks remain structural until invocation.

Five built-in final-block contract entries retain exact min/max counts and copied
callback metadata. Excess/missing arguments reject before callbacks. A direct
scoped-binding test hides all prior stores, copies input/output and restores a
false scalar plus existing array/harray stores after success. An error marker
passes unchanged through a five-name frame; all originally absent stores remain
absent after unwind. The marker is a table, so this does not re-test or close the
previous PUC nil-error identity failure.

Attached and parenthesized helper/receiver with forms execute in the same copied
scope. Values evaluate before entry, missing bindings restore absence, aggregates
remain isolated, nested scopes restore correctly and local return skips later
effects. Invalid final kinds/counts retain exact typed fields; callback exit_now
retains status 86. Broader malformed tagged-array copy limitations remain open.

# Root-kind tree traversal

Harray traversal visits lexical keys depth-first, recursing only into harrays and
passing arrays whole as leaves. Array traversal uses zero-based indexes/paths and
recurses only into arrays, passing harrays whole as leaves. Root leaf depth is 1;
path/depth/index-or-key/value/acc are copied into temporary callback bindings.
Exact output proves deterministic order, source isolation and restoration of all
outer bindings after callbacks mutate their local frame.

Map rebuilds the root kind; walk returns a copied source and retains compatible
continuation; reduce threads its accumulator and stays terminal. Empty roots skip
callbacks and isolate the copied seed. Invalid receivers skip callback and reduce
seed evaluation. Both tests compare unchanged checked-in Perl oracle values; no
fresh Perl runtime or cross-backend matrix is claimed here.

The following inline-control group is only partially read. Its already visible
source covers lazy selection, one-time switch subject evaluation, literal bare
labels and scalar spelling distinctions; it is not executed in this leaf.

# Knowledge reconciliation and proof limits

Retrieved [[lua-capture-cursor-runtime-audit]], [[lua-complete-named-mark-parity]],
[[lua-governed-named-span-parity]], [[lua-rule-slot-marker-execution]],
[[lua-string-helper-parity-closeout]], [[lua-helper-regex-split-mutation-preflight]],
[[lua-action-edge-child-call-reuse]], [[punctuation-light-zero-argument-calls]],
[[lua-runtime-eager-block-values]], [[lua-runtime-builtin-final-codeblocks-with]],
[[lua-runtime-harray-tree-callbacks]] and [[lua-runtime-array-tree-callbacks]].

Existing .2.1 gains exact capture/punctuation future-generated guidance, capture
diagnostics/string numeric next-owner wording, stale missing regex mechanisms,
and the harray card's categorical non-harray rejection sentence despite its
generalized array-root dispatcher. Existing explicit-callable and malformed-copy
qualifications remain. Date historical stages and retain their evidence; .2.3
owns all three execution exclusions. No old Knowledge card or production source
changes, and no repair node is added.

Selected original preamble/helpers are 1–199, 1718–1753 and 3999–4019; complete
groups are 6312–6819 and 6978–7804, including their local uniform-binding helpers.
Both managed native jobs exit 0 with exact logs: 17 groups per host, 34 total.
No individual assertion count is invented. These tests execute native and public
AST reconstruction routes; no fresh emitted module or child process is claimed.
Neutral fixtures are consumed but their separate checkers are not rerun.
Installed PUC 5.5 and LuaJIT are exercised; unavailable supported PUC 5.4 is not
certified. All 33 repair roots, earlier nil-error/public-selector failures, startup
gates and parked named arguments remain. No full CI, primary matrix, PGEN/RGX
build or push occurs.

# Exact replay

```bash
bash tools/project_data_run.sh python3 - <<'LUA_PACKAGE_CALLBACK_READING_42'
from pathlib import Path
p=Path('.linkedspec-data/scratch/lua142')
p.mkdir(parents=True, exist_ok=True)
(p / 'select-tests.py').write_text('from pathlib import Path\nimport hashlib,json,re\n\np=Path(\'.linkedspec-data/scratch/lua142\')\nlines=Path(\'lua/test/run.lua\').read_text().splitlines(keepends=True)\nranges=[(1,199),(1718,1753),(3999,4019),(6312,6819),(6978,7804)]\nselected=\'\'.join(\'\'.join(lines[a-1:b]) for a,b in ranges)\nnames=re.findall(r\'^test\\("([^"\\n]+)"\',selected,re.M)\nassert len(names)==17,len(names)\nfor name in [\'helper regex flags and matches are strict in function and receiver form\',\n             \'pure split preserves typed literal regex Unicode and receiver boundaries\',\n             \'statement regex substitution mutates bare scalars and preserves pure substr\']:\n    assert name not in names,name\n(p/\'package-selection.lua\').write_text(selected+\'\\nassert(total == 17, "package selection count drift: " .. total)\\nassert(failed == 0, "package selection failures: " .. failed)\\nprint("package selection: " .. total .. " test groups passed")\\n\')\nprint(\'Selected original helpers and 17 complete groups; three known native-error groups 6820-6977 and partial inline-control group 7805 onward excluded\')\n')
(p / 'verify.py').write_text('from pathlib import Path\nimport hashlib,json,re\n\np=Path(\'.linkedspec-data/scratch/lua142\')\nfor row in json.loads((p/\'scope.json\').read_text()):\n    data=b\'\'.join(Path(row[\'path\']).read_bytes().splitlines(keepends=True)[row[\'start\']-1:row[\'end\']])\n    assert len(data)==row[\'bytes\'] and hashlib.sha256(data).hexdigest()==row[\'sha256\']\nlines=Path(\'lua/test/run.lua\').read_text().splitlines(keepends=True)\nbody=\'\'.join(lines[6311:6819]+lines[6977:7804])\nnames=re.findall(r\'^test\\("([^"\\n]+)"\',body,re.M)\nassert len(names)==17\nexpected=\'\'.join(f\'ok {i} - {name}\\n\' for i,name in enumerate(names,1))+\'package selection: 17 test groups passed\\n\'\nfor host in [\'puc\',\'luajit\']:\n    assert (p/f\'package-{host}.log\').read_text()==expected,host\nprint(\'PASS: exact source range and 17 safe complete package groups per host, 34 total; boundary/marks/scalar text/child reuse/scoped callbacks/tree traversal proof; three known native-error groups remain excluded\')\n')
(p / 'scope.json').write_text('[\n  {\n    "path": "lua/test/run.lua",\n    "start": 6353,\n    "end": 7852,\n    "bytes": 61135,\n    "sha256": "7c2a7598a55dc8e2c08e9c6e05fba19db5601174cdde4c506f1517fdfe472e92"\n  }\n]\n')
LUA_PACKAGE_CALLBACK_READING_42
bash tools/run_python_project_data.sh .linkedspec-data/scratch/lua142/select-tests.py
for host in puc luajit; do
  bash tools/run_lua_project_data.sh "$host" .linkedspec-data/scratch/lua142/package-selection.lua > ".linkedspec-data/scratch/lua142/package-$host.log" 2>&1 || exit
done
bash tools/run_python_project_data.sh .linkedspec-data/scratch/lua142/verify.py
```

Related: [[lua-package-generated-runtime-cursor-reading]],
[[lua-startup-reading-coverage]], [[lua-native-readme-and-action-ast-reading]].

# Preservation audit

All three embedded replay payloads match their saved executed files exactly.
The final preservation audit passes: 1,418 baseline source/Knowledge/decision/
history/policy files remain byte-identical; 2,578 of 2,582 prior task nodes are
unchanged. Only this reading leaf, its existing .2.1/.2.3 repair owners and startup
.3.6 change; no node is added. All 90 prior book limitation headings and parked
authoring bytes remain intact. Full coverage replay retains all 99 files and
51 groups; completed totals are 58,540 fragments /2,231,215 bytes.

Document history passes all 34 mutation controls and exact reconstruction across
three surfaces and 67 segments. Explicit memory validation passes at 60 lines.
The book builds successfully; its existing search-index pressure warning reports
10,159,559 bytes and remains owned by startup .41.9. Production code, historical
Knowledge and prior immutable history stay unchanged.
