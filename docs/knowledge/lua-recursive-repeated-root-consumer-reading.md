---
id: lua-recursive-repeated-root-consumer-reading
title: Lua recursive observation repeated actions and root selection consumers preserve exact proof boundaries
answers:
  - "what exact Lua source did startup reading child 35 cover"
  - "does the Lua recursive observation consumer bind rejected records before errors"
  - "do Lua repeated action and root admission tests launch emitted code child processes"
  - "which fifteen roles does the Lua repeated action consumer verify"
  - "what does the Lua root core prefix through line 134 prove"
  - "which Lua observation and root public closeout statements still need qualification"
  - "what focused evidence passed for Lua reading child 35"
date: 2026-09-13
status: scoped source reading and focused proof complete; root core suffix and previous repair owners remain open
tags: [lua, recursion, observation, repetition, root-selection, startup, evidence]
evidence: "LUA-STARTUP-READING.1.35 activates from a9aecff4891255be5a565ffc939242a6cbb96aaa. Eight complete windows read 1500 fragments /55021 bytes. Both installed hosts pass recursive43, repeated175, root-admission139 and root-core-prefix57: 828 assertions. Neutral typed14/0/231, repeated8/0/54 and root7/0/54 pass. Existing .2.1 owns precise current public-closeout guidance qualification. No production change."
reverify: "Run LUA_RECURSIVE_REPEATED_ROOT_READING_35 and the managed commands below; root-core proof is limited to source lines 1-134."
---

# Exact reading

| Repository source | Inclusive lines | Fragments / bytes | Raw SHA-256 |
| --- | --- | --- | --- |
| lua/test/recursive_observation_contract_test.lua | 143–454 | 312 /11328 | 2bc05f057ab4406672b4931c3d4f5f426f8837e3c5f260a809b28ce5497d6c94 |
| lua/test/repeated_action_result_contract_test.lua | 1–416 | 416 /14987 | 67849506673bed5800ff56025999ea4a99199dc375bb4e3acd32befaf2cb9bd2 |
| lua/test/root_rule_selection_admission_test.lua | 1–568 | 568 /20754 | 25f83c481159e16f2f77b45d6a082f8e479b0c16775f4e2a4e3a99fed3a972a1 |
| lua/test/root_rule_selection_core_test.lua | 1–204 | 204 /7952 | 5bf42e8f1390fbbd250e9fcd89237be09e5220d99bdfef356e172ff36af85429 |

Recursive windows are143–300 and301–454; repeated windows are1–205 and206–416;
root-admission windows are1–190,191–385 and386–568; root-core1–204 is one window.
The first recursive window's original combined tool result was unavailable after
context truncation, so143–300 was reread in full before receiving reading credit.
All eight credited source windows are complete. Ordered range SHA-256 is
`eba36de062d16494d3302a19f1c53aa03481e8a55d287ef9c69b52f704cc47e8`.
All99 Lua sources remain baseline-identical to
`baeb984e36a94a15951cd23d4c52def5064cdaca`. Reading reaches35/51 groups,
48,151 fragments /1,797,379 bytes and69 complete files. Root core remains partial.

# Recursive observations

The completed consumer retains the dedicated lowering proof from child34 and
rejects non-bare targets, dynamic/missing rules and observation binding writes
reachable within recognition transactions through rules or user functions.
Native execution returns false separately from an accepted observation. Mutating
the returned entry and selected-match records cannot alter later execution.
Reconstructed state gives the same detached Unicode-scalar record for multibyte
input. Failed recognition has null payload and failed outcome; a zero-regex child
can accept without a selected match. The action-edge fixture distinguishes the
parent entry match from the child's final selected match and accepted exit.

Ordinary nested self-recursion retains its existing cutoff and the observed child
can still return an accepted value. Explicit direct/mutual observed recursion
rejects with its exact typed progress diagnostic; the rejected attempted-child
record is captured before propagation. An abort caused by a missing recognition
token similarly binds an aborted record before preserving the original typed
diagnostic. Generated-plan execution and freshly emitted/loadstring-or-load code
preserve the false payload, record and source identity. This emitted code executes
inside the current host process. The complete source passes43 assertions per host.

# Repeated-action admission

The exact declared map has15 unique roles and each runs once: neutral contract,
AST metadata, native modes, native special cases, loaded, reconstructed, descriptor,
emitted source, generated direct, native trace, generated trace, primary command,
corpus bundle, lifecycle authority, and bounds/progress. Metadata checks all eight
mode rows and a separate blind-OR row. The general native special loop excludes
blind rows; this is not every special case on every carrier.

Explicit repetition collects per-hit action values, including returned arrays and
nulls as single outer elements. Pipe returns a selected scalar. Lifecycle overrides
remain whole-rule control, outside that collection. Exact optional/bounded/minimum
results retain the contract; bounded zero-width repetition retains one Z and stops.
Generated direct executes all mode rows. Loaded/reconstructed, descriptor, primary,
corpus and trace roles use their declared narrower examples. Selected-slot traces
retain both ordered identities. The corpus input removes one terminal LF only.

The emitted role loads freshly emitted source with loadstring/load in the current
host and executes both direct and traced functions. It checks two selected hits
and rejects a stale or_acode plan with the exact typed family-mismatch code. A
repository-routed temporary directory is used only by the loaded-spec role and is
removed after success or failure. The full admission passes175 assertions per host.

# Root admission and core prefix

The exact15-role map composes neutral selection/failures/strict, native, loaded,
reconstructed, generated direct/traced, emitted direct/traced, descriptor,
diagnostic, runtime trace, primary CLI and primary request trace. The neutral
resolver loop checks selected label, basis and unchanged authored identity. Explicit
selection creates no strict-unused reference; authored cycles retain their policy.
Native/default/explicit routes preserve first-marker and markerless fallback.
Entry-lifecycle I values prove which authored rule is entered before matching.

Loaded source, normalized reconstruction and generated-v2 execution reuse that
selection. Emitted direct/traced roles persist fresh modules under routed TMPDIR,
loadfile and execute them in the current host, then remove each exact owned root.
Descriptors remain byte-equivalent before/after selection, preserve authored order
and marker bits, publish the contract id and omit selected-entry invocation state.
Unknown selection and zero-rule failures keep their typed code/stage/identity;
zero rules outrank unknown selection. A stale generated-v1 contract rejects at
plan validation. Selected runtime failure retains the selected rule and family in
the generated error plus requested/effective/basis trace evidence.

Primary default, markerless, explicit and unknown routes check exact stdout/stderr
and exits. The frozen request-trace fixture retains its original bytes. Routed
trace reset with an unknown selector containing a newline checks the existing
escaped failure trace exactly, and cleans only its temporary directory. The full
admission passes139 assertions per host.

Root-core source1–204 additionally reads native selection and most typed runtime
failure checks, ending inside an open block. Focused selection executes only the
complete source prefix1–134: contract identity1, eight selection cases times3,
three failure cases times5, and17 parser/validator assertions, totaling57 per host.
Empty/comment-only text produces an empty parser envelope and typed no-rules
validation; malformed non-rule text remains a parse error; markerless text validates.
No later runtime/descriptor assertion is counted in this prefix proof. The source
suffix205–278 and full core execution remain owned by child36.

# Knowledge and repair reconciliation

Retrieved [[explicit-or-action-result-shape-parity-gap]],
[[recursive-observation-public-no-drift]], [[lua-root-rule-selection-routes]],
[[lua-root-rule-selection-preflight]], [[lua-root-rule-selection-core]],
[[lua-root-rule-selection-admission]] and [[root-rule-selection-five-backend-admission]].
The full observation admission card was already read in child34; its exact pending
guidance was reread here. A combined typed-rollout card read was truncated; the
required dated observation transitions and final program-wide closeout at its
lines153–163 and current closeout body were retrieved separately before use.
This is relevant Knowledge retrieval, not full-card or additional source credit.

Existing `.2.1` now owns the observation admission's current pending-public metadata
and final sentence, superseded by completed observation-specific .14.4.8 and
program-wide .14.8; it also owns the root-routes card's current pending final .6
sentence, superseded by the complete five-backend closeout. Explicit historical
preflight and dated admission counts stay intact. No old card is changed here.

Fresh proof is43+175+139+57=414 per installed host,828 total. Typed governance
passes14 complete/0 pending/231 mutations, observation public6/6/10 and combined
public8/8/6/10. Repetition passes8 modes/10 specials/8 complete/0 pending/54 mutations.
Root governance passes8 selection/3 failure/3 strict cases, five backends,
7 complete/0 pending/24 public documents/18 current denials/54 mutations.
All eleven selected native/neutral jobs finish with exit0. PUC5.5 and LuaJIT are
the installed hosts; this does not certify unavailable supported PUC5.4. Earlier
nil-error and public-selector failures, all33 repair roots, startup gates and
parked named arguments remain open. No production edit, full CI, six-runtime
recurring driver, PGEN/RGX build, dependency change or push occurs.

# Exact replay

```bash
bash tools/project_data_run.sh python3 - <<'LUA_RECURSIVE_REPEATED_ROOT_READING_35'
from pathlib import Path
p = Path('.linkedspec-data/scratch/lua135')
p.mkdir(parents=True, exist_ok=True)
(p / 'select-tests.py').write_text('from pathlib import Path\nimport json\n\np = Path(\'.linkedspec-data/scratch/lua135\')\nsource = Path(\'lua/test/root_rule_selection_core_test.lua\').read_text().splitlines(keepends=True)\nassert source[133] == \'end\\n\'\nassert source[135] == \'do\\n\'\ncontract = json.loads(Path(\'capability_conformance/root_rule_selection_contract.json\').read_text())\nexpected = 1 + 3 * len(contract[\'selection_cases\']) + 5 * len(contract[\'failure_cases\']) + 17\n(p / \'root-core-prefix.lua\').write_text(\'\'.join(source[:134]) + \'\\nassert(#failures == 0, table.concat(failures, " | "))\\nassert(assertions == \' + str(expected) + \', "root core prefix count drift: " .. assertions)\\nprint("root core prefix: " .. assertions .. " assertions passed")\\n\')\nprint(\'Selected root core lines 1-134:\', expected, \'assertions\')\n')
(p / 'verify.py').write_text("from pathlib import Path\nimport hashlib, json\n\np = Path('.linkedspec-data/scratch/lua135')\nfor row in json.loads((p / 'scope.json').read_text()):\n    data = b''.join(Path(row['path']).read_bytes().splitlines(keepends=True)[row['start'] - 1:row['end']])\n    assert len(data) == row['bytes']\n    assert hashlib.sha256(data).hexdigest() == row['sha256']\nfor host, runtime in [('puc', 'puc-lua'), ('luajit', 'luajit')]:\n    assert (p / f'recursive-{host}.log').read_text() == f'Lua recursive-observation contract: 43 assertions passed on {runtime}\\n'\n    assert (p / f'repeated-{host}.log').read_text() == 'repeated action-result dual-ABI admission: 175 assertions passed\\n'\n    assert (p / f'root-admission-{host}.log').read_text() == 'root-rule selection dual-ABI admission: 139 assertions passed\\n'\n    assert (p / f'root-core-{host}.log').read_text() == 'root core prefix: 57 assertions passed\\n'\nfor name, markers in {\n    'typed': ['14 complete / 0 pending rollout', '231 drift mutations', 'recursive-observation public 6 documents/6 forbidden/10 surface guards', 'program-wide public 8 documents/8 forbidden/6 safety/10 surface guards'],\n    'repeated': ['8 mode cases, 10 special cases, 8 complete + 0 pending, 54 drift mutations'],\n    'root': ['8 selection cases; 3 failures; 3 strict cases; 5 backends; 7 complete / 0 pending; 24 public documents; 18 forbidden current claims; 54 drift mutations'],\n}.items():\n    log = (p / f'neutral-{name}.log').read_text()\n    for marker in markers:\n        assert marker in log, marker\nprint('PASS: exact source ranges; recursive43 + repeated175 + root-admission139 + root-core-prefix57 per host, 828 assertions; typed14/0/231, repeated8/0/54 and root7/0/54 neutral proof')\n")
(p / 'scope.json').write_text('[\n  {\n    "path": "lua/test/recursive_observation_contract_test.lua",\n    "start": 143,\n    "end": 454,\n    "bytes": 11328,\n    "sha256": "2bc05f057ab4406672b4931c3d4f5f426f8837e3c5f260a809b28ce5497d6c94"\n  },\n  {\n    "path": "lua/test/repeated_action_result_contract_test.lua",\n    "start": 1,\n    "end": 416,\n    "bytes": 14987,\n    "sha256": "67849506673bed5800ff56025999ea4a99199dc375bb4e3acd32befaf2cb9bd2"\n  },\n  {\n    "path": "lua/test/root_rule_selection_admission_test.lua",\n    "start": 1,\n    "end": 568,\n    "bytes": 20754,\n    "sha256": "25f83c481159e16f2f77b45d6a082f8e479b0c16775f4e2a4e3a99fed3a972a1"\n  },\n  {\n    "path": "lua/test/root_rule_selection_core_test.lua",\n    "start": 1,\n    "end": 204,\n    "bytes": 7952,\n    "sha256": "5bf42e8f1390fbbd250e9fcd89237be09e5220d99bdfef356e172ff36af85429"\n  }\n]\n')
LUA_RECURSIVE_REPEATED_ROOT_READING_35
bash tools/run_python_project_data.sh .linkedspec-data/scratch/lua135/select-tests.py
for host in puc luajit; do
  bash tools/run_lua_project_data.sh "$host" lua/test/recursive_observation_contract_test.lua > ".linkedspec-data/scratch/lua135/recursive-$host.log" 2>&1 || exit
  bash tools/run_lua_project_data.sh "$host" lua/test/repeated_action_result_contract_test.lua > ".linkedspec-data/scratch/lua135/repeated-$host.log" 2>&1 || exit
  bash tools/run_lua_project_data.sh "$host" lua/test/root_rule_selection_admission_test.lua > ".linkedspec-data/scratch/lua135/root-admission-$host.log" 2>&1 || exit
  bash tools/run_lua_project_data.sh "$host" .linkedspec-data/scratch/lua135/root-core-prefix.lua > ".linkedspec-data/scratch/lua135/root-core-$host.log" 2>&1 || exit
done
bash tools/run_python_project_data.sh tools/check_typed_source_location_contract.py > .linkedspec-data/scratch/lua135/neutral-typed.log 2>&1 || exit
bash tools/run_python_project_data.sh tools/check_repeated_action_result_contract.py > .linkedspec-data/scratch/lua135/neutral-repeated.log 2>&1 || exit
bash tools/run_python_project_data.sh tools/check_root_rule_selection_contract.py > .linkedspec-data/scratch/lua135/neutral-root.log 2>&1 || exit
bash tools/run_python_project_data.sh .linkedspec-data/scratch/lua135/verify.py
```

Related: [[lua-stdio-progressive-recognition-consumer-reading]],
[[lua-recursive-observation-admission]], [[typed-source-location-runtime-rollout-plan]],
[[lua-startup-reading-coverage]].

# Preservation and documentation checks

The independent Git audit preserves1,410 source/old-Knowledge/decision/history/policy
files exactly. Of2,582 prior task nodes,2,579 remain exact; only this leaf, existing
guidance owner .2.1 and startup .3.6 change. No task is added. All90 Known headings,
the parked authoring tree, historical chronology and original history prefixes
remain intact. All three embedded replay payloads match their executed scratch
bytes, and independent coverage reconstructs99 files/51 groups/149 ranges exactly.

Memory passes at60 lines. Knowledge passes with1,121 facts/8,966 questions. Shared
history passes34 mutation controls across three surfaces/66 segments; current
Changes is459 lines/34,259 bytes and Notes389/32,490. The book builds successfully;
its10,135,972-byte search-index warning remains owned by startup .41.9. The normal
nine-doctrine hooks remain the commit admission boundary.
