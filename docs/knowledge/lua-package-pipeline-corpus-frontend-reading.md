---
id: lua-package-pipeline-corpus-frontend-reading
title: Lua package pipeline corpus and frontend proof preserves exact selected scope
answers:
  - "what exact Lua source did startup reading child 38 cover"
  - "which Lua package pipeline and frontend test groups pass during startup reading"
  - "does Lua full corpus execution pass through both library and developer runner"
  - "why does current Lua core corpus proof use endpoint six instead of one"
  - "does Lua native pipeline tracing preserve source identity and typed failures"
  - "which stale Lua CLI and corpus claims remain owned after reading child 38"
  - "what focused evidence passed for Lua reading child 38"
date: 2026-09-13
status: exact source reading and selected proof complete; remaining package source and prior repairs stay open
tags: [lua, package, corpus, frontend, native-loading, trace, startup, evidence]
evidence: "LUA-STARTUP-READING.1.38 activates from 55f61211631bf713af833087c0fccd0fd8fa3dfa. Eight complete windows read 1492 fragments /65483 bytes. Both installed hosts pass 36 complete package groups each, including full105 library and developer-runner execution. Existing .2.1 guidance extends; no production change or new repair node."
reverify: "Run LUA_PACKAGE_PIPELINE_READING_38 and the managed commands below; exact selected groups451-1939 use original preamble1-199."
---

# Exact reading

| Repository source | Inclusive lines | Fragments / bytes | Raw SHA-256 |
| --- | --- | --- | --- |
| lua/test/run.lua | 464–1955 | 1492 /65483 | 0973f3e6a677529c3c1a87da8c5cb70f90caac33afd213d3785e1daead6f0129 |

Complete credited windows are464–635,636–815,816–1000,1001–1175,
1176–1350,1351–1530,1531–1720 and1721–1955. Two lost outputs were
reread in full before credit. Ordered range SHA-256 is
`53217793de0f5e01e545e422435ded9aa1f395fd8c3cda097625283c92b324c7`.
All99 Lua sources remain baseline-identical to
`baeb984e36a94a15951cd23d4c52def5064cdaca`. Reading reaches38/51 groups,
52,643 fragments /1,977,898 bytes and77 complete files. The package runner
remains partial, ending inside the validation_function helper.

# Native CLI, strict IO and pipeline trace

The named/file/inline CLI group starts at451, whose prefix was read in child37.
It finishes here and checks native delegation, explicit root selection and
recursively sorted canonical JSON. The next group verifies strict UTF-8 failures,
compile-before-input ordering and exact routed/reset portable phase trace,
including Unicode source/input/result byte counts and clean JSON stdout.

Two JSON groups preserve tagged null/array/harray identity and round trips,
decode a surrogate pair, and reject invalid UTF-8, duplicate keys, unpaired
surrogates and ambiguous plain arrays. These examples do not close earlier
wide-integer or malformed-host-container limitations.

Native loading consumes all14 neutral name cases, nine resolution cases and
four strict text cases. Supplemental inputs include Unicode, NUL and malformed
UTF-8 paths. Real repository-routed files test declared roots, exact paths,
file-kind identities and typed missing/resolved fields. Text bytes are preserved.

Automatic function-aware loading composes parse, validation and compilation;
caller options remain unchanged. Loaded logical name/path identity survives
engine creation and attributed unknown selection. Exact-path and inline routes
converge on equivalent behavior. Separate negative sources check parse, validation,
compile and missing-name stage ownership; the compile-negative source uses the
retired aggregate selector. These are parser/compiler contract fixtures.

The complete native trace test uses one explicit caller-owned emitter and
temporarily counts hidden factory calls, restoring the factory after pcall.
It verifies zero hidden creation, ordered IO/frontend/staged/compiler/engine/
runtime topics, absence from retained descriptor state and traced/untraced
descriptor/result JSON identity. Quiet and disabled cases emit nothing; low
filters construction detail and medium retains decisions. A typed validation
failure keeps baseline JSON, closes its phase and balances scope depth with a
healthy writer. Invalid emitters reject. Earlier .2.30 writer masking/indentation
and .2.1 reset qualifications remain open; these healthy fixtures do not disprove them.

# Strict corpus execution and exact full-manifest proof

The validation group loads all105 fixtures without executing them. Runner
default use validates only. Manifest format, names, duplicate/count drift,
missing/stale directories, required files, malformed expected JSON and invalid
UTF-8 fail their owning boundary. Controlled runner mismatch exits1; invalid
manifest exits2; all-pass execution exits0.

Six controlled success fixtures exercise scalar and nested aggregate values,
rule/blind dispatch, lifecycle output, automatic functions and earliest usable
anonymous boundary. Results retain expected and actual values, names, match,
byte/scalar endpoints and optional trace lines. Missing result lookup rejects.
Six failure/continuation fixtures retain parse, validation, execution, comparison
and no-match records and allow the final passing fixture to run. The selection
group validates the whole corpus first, preserves caller name order, supports
zero-based offset and capped positive limit, and rejects missing/duplicate names,
mixed selectors and invalid bounds.

Permanent windows independently pass all40 core, six capability and59 advanced/
shipped cases with their literal names, exact wrapped outputs and endpoints.
Core byte/scalar endpoints are6/6; capability endpoints are2,1,2,1,5,5.
The advanced window uses its complete literal endpoint ledger. A separate
unselected library invocation executes all105 in manifest order, and a spawned
developer-runner process also executes all105, printing the exact passing summary
with empty stderr. Both installed hosts pass all these groups. The full105 library
and runner results are fresh execution, not merely accepted inventory checks.

Historical core1/1 wording was reconciled with Git rather than guessed away:
blame identifies `7ed47a0a1104d984967808c98f52a3e1d5031520`, the September4
write-vivification commit. Its message explicitly records correction of stale
child-regex cursor evidence; its test diff changes core endpoints and the first28
advanced endpoints from1 to6. This reading does not claim that write vivification
caused the underlying cursor movement. Existing .2.1 now owns dating the old
permanent-endpoint prose while preserving July admission evidence.

# AST and rule parser consumer boundaries

The AST roundtrip group constructs function payloads, staged provenance, rules,
bounded modes, regex/action/lifecycle elements and optional fields. Mutating the
input payload does not change the constructed node. JSON reconstruction keeps
node kinds, lookup/mode queries and exact projected bytes. A second group covers
all ten body variants. Unknown modes/variants, wrong or sparse rule collections
and ambiguous payload tables reject. Malformed tagged-container gaps established
in earlier reading are not covered or closed by these valid-node tests.

Parser groups cover eight header mode examples, inline regex/lifecycle bodies,
header-line regex slots, compact/grouped/indexed action and blind edges, nested
fluent arguments, multiline continuations, attached when/otherwise blocks,
physical newline versus same-line semicolon normalization and quoted braces.
Raw fallback remains typed. Direct rule parsing rejects a top-level function
shell and invalid UTF-8 source with typed parse errors. All shipped sources and
all rule-only corpus sources parse; the corpus test skips function shells and
asserts more than80 rule-only files and at least one shell, rather than an exact
current count. The automatic pipeline above separately handles functions.
Validation helper definitions are read through1955, but the partial helper and
subsequent validation tests remain outside this execution selection.

# Knowledge reconciliation and limits

Retrieved [[lua-native-spec-resolution]], [[lua-primary-cli-adapter]],
[[lua-primary-cli-recurring-admission]], [[lua-primary-cli-no-drift-closeout]],
[[lua-controlled-corpus-execution]], [[lua-full-corpus-gate]],
[[lua-corpus-manifest-io]], [[lua-native-full-pipeline-trace]],
[[lua-frontend-ast-json-contract]], [[lua-core-spec-parser]],
[[lua-controlled-corpus-admission-split]] and [[lua-advanced-corpus-residual-split]].
Earlier facade, frontend, trace and cursor fact owners remain authoritative
subject to their already tracked precise limitations.

Existing .2.1 gains exact unqualified169/61 and pending-generated guidance in the
three CLI cards, two-module checkout wording, core1/1 permanent wording, and
core-parser final later-owner prose. Dated177/66 admission and current three-module
facts were established earlier; this leaf does not rerun those full gates.
The corpus scaffold claim, frontend current60 claims, native-resolution frontier
and trace-writer qualifications were already owned. No old Knowledge card,
historical task node, production source or supported-runtime declaration changes.

The two managed jobs exit0 with36 selected groups each,72 total. The original
preamble is reused byte-for-byte, and complete groups451–1939 exclude earlier
groups already proved in child37 and all later package groups. Inner assertion
counts are not invented. The selected hosts are installed PUC5.5 and LuaJIT,
not certification of unavailable supported PUC5.4. All33 repair roots, prior
PUC nil-error and public-selector failures, startup gates and parked named
arguments remain open. No full CI, primary matrix, PGEN/RGX build or push occurs.

# Exact replay

```bash
bash tools/project_data_run.sh python3 - <<'LUA_PACKAGE_PIPELINE_READING_38'
from pathlib import Path
p = Path('.linkedspec-data/scratch/lua138')
p.mkdir(parents=True, exist_ok=True)
(p / 'select-tests.py').write_text('from pathlib import Path\nimport re\n\np=Path(\'.linkedspec-data/scratch/lua138\')\nsource=Path(\'lua/test/run.lua\').read_text().splitlines(keepends=True)\nassert source[199].startswith(\'test(\') and source[450].startswith(\'test(\')\nend=next(i for i,line in enumerate(source) if line.startswith(\'local function assert_validation_error(\'))\nassert end==1939\nbody=\'\'.join(source[450:end])\nnames=re.findall(r\'^test\\("([^"\\n]+)"\',body,re.M)\nassert len(names)==36\n(p/\'package-selection.lua\').write_text(\'\'.join(source[:199])+body+\'\\nassert(total == 36, "package selection count drift: " .. total)\\nassert(failed == 0, "package selection failures: " .. failed)\\nprint("package selection: " .. total .. " test groups passed")\\n\')\nprint(\'Selected preamble 1-199 and complete package groups 451-1939: 36 groups\')\n')
(p / 'verify.py').write_text('from pathlib import Path\nimport hashlib,json,re\n\np=Path(\'.linkedspec-data/scratch/lua138\')\nfor row in json.loads((p/\'scope.json\').read_text()):\n    data=b\'\'.join(Path(row[\'path\']).read_bytes().splitlines(keepends=True)[row[\'start\']-1:row[\'end\']])\n    assert len(data)==row[\'bytes\'] and hashlib.sha256(data).hexdigest()==row[\'sha256\']\nsource=Path(\'lua/test/run.lua\').read_text().splitlines(keepends=True)\nbody=\'\'.join(source[450:1939])\nnames=re.findall(r\'^test\\("([^"\\n]+)"\',body,re.M)\nassert len(names)==36\nexpected=\'\'.join(f\'ok {i} - {name}\\n\' for i,name in enumerate(names,1))+\'package selection: 36 test groups passed\\n\'\nfor host in [\'puc\',\'luajit\']:\n    assert (p/f\'package-{host}.log\').read_text()==expected,host\nprint(\'PASS: exact source range and 36 complete package groups per host, 72 total; full corpus library and developer runner pass 105/105 on both installed hosts\')\n')
(p / 'scope.json').write_text('[\n  {\n    "path": "lua/test/run.lua",\n    "start": 464,\n    "end": 1955,\n    "bytes": 65483,\n    "sha256": "0973f3e6a677529c3c1a87da8c5cb70f90caac33afd213d3785e1daead6f0129"\n  }\n]\n')
LUA_PACKAGE_PIPELINE_READING_38
bash tools/run_python_project_data.sh .linkedspec-data/scratch/lua138/select-tests.py
for host in puc luajit; do
  bash tools/run_lua_project_data.sh "$host" .linkedspec-data/scratch/lua138/package-selection.lua > ".linkedspec-data/scratch/lua138/package-$host.log" 2>&1 || exit
done
bash tools/run_python_project_data.sh .linkedspec-data/scratch/lua138/verify.py
```

Related: [[lua-cursor-completion-package-prefix-reading]],
[[lua-startup-reading-coverage]], [[lua-compiled-corpus-facade-reading]].

# Preservation and final focused checks

Independent audit preserves1,414 baseline source/Knowledge/decision/history/policy
files and2,579 of2,582 existing task nodes byte-for-byte. Only this reading leaf,
existing guidance owner .2.1 and startup .3.6 change; no node is added. The parked
authoring tree, all90 existing Known headings, both prior live-document suffixes
and the indexed live-status History section remain exact. All three embedded
replay payloads match their executed scratch files. Independent full-inventory
reconstruction confirms all99 source files, all51 groups and exact38-child credit.

Document-history checks pass34 mutation controls across three surfaces and67
segments; no rollover is needed. Explicit memory architecture and diff whitespace
checks pass. The book builds successfully; its10,144,818-byte search-index warning
remains under startup .41.9. The generated map has1,124 facts and8,987 question
keys. These are focused pre-commit observations; normal commit hooks remain required.
