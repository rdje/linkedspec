---
id: lua-final-source-consumer-reading
title: Final Lua source ranges, complete write proof and dormant progressive authority
answers:
  - "are all 99 Lua source files physically read after child 51"
  - "what remains after the final Lua source reading child"
  - "do the nineteen write failure-state mutations survive the full Lua suite"
  - "what does the final Lua write consumer suffix execute"
  - "does ordinary or canonical discovery execute the private Lua progressive authority test"
  - "does the Lua progressive authority parent_state fixture exercise shared state"
  - "does the healthy Lua nested authority budget test close parent ceiling inheritance"
date: 2026-09-13
status: all physical Lua reading complete; independent audit, startup closeout and repairs pending
tags: [lua, startup, reading, assignment, progressive-parsing, tests, coverage]
evidence: "LUA-STARTUP-READING.1.51 reads final 729 fragments/28118 bytes in five complete windows. All 51 groups/99 baseline-identical files are physically read. Complete write 438 and dormant authority 273 pass per installed host,1422 ordinary assertions. Nineteen single-field expected_binding mutations survive the full 438 consumer on each host,38 observations extending .2.35. Neutral write/progressive/public and exact discovery checks pass. Parent .1/startup .3.6 do not close here."
reverify: "Run the exact repository-local replay below; preserve all prior native-error exclusions and the separate independent/canonical closeout requirements."
---

# Exact reading and boundary

Activation is `91ff8a6424c5959baff4c88437febd5467789c05`.
Source baseline remains `baeb984e36a94a15951cd23d4c52def5064cdaca`.
Ordered group digest:
`e94181f3ffd688eed7f1cbf12b1bca08b3b412ecbcfa110fb12fcf6b8439a8f2`.

| Path | Inclusive lines | Fragments | Bytes | SHA-256 |
| --- | --- | ---: | ---: | --- |
| lua/test/write_vivification_contract_test.lua | 400-542 | 143 | 5988 | `6533310a4326574e70fab6938f618c36b02c6bb96a1c31f41ebff04684642623` |
| lua/test_dormant/progressive_span_dispatch_authority_test.lua | 1-586 | 586 | 22130 | `1c623f19e1b4503eaebe8c6e41ce80e13ec575cfb37b1a0943c5d3f55e703686` |

Five complete untruncated windows: write 400-542; dormant authority 1-150/
151-300/301-450/451-586. Both files reach EOF. Every generated and non-generated
Lua source byte is physically read: 51 groups, 99 files, 71,268 physical lines,
71,269 owned fragments and 2,732,450 bytes. The extra fragment comes only from the
previously split oversized MCP line. No new reading is credited to a digest alone.

This ordinary final child does not close reading parent .1 or startup .3.6.
Independent .3 must audit range coverage, comprehension, findings, unique commits
and first-parent activations, then satisfy its canonical milestone prerequisite
or obtain a newly explicit scoped exception. The existing PGEN/RGX no-build
directive remains binding. Supporting code, formal book .4 and policy .5 still
prevent claiming full codebase reading or starting production repairs.

# Complete write suffix and existing coverage defect

The full unchanged write consumer passes 438 per installed host. The suffix completes
fresh user-function locals: two calls independently create an items array. Passing
null as an existing function parameter rejects with exact kind-conflict/binding/
actual-kind fields. One dynamic string/integer path creates sections[0].title and
preserves typed node, path segment and authored selector tokens in the descriptor.
Its reconstructed SpecFile, generated-plan helper, executed emitted module in the
current host and inline primary API return the same structure. Primary exit and
stderr are exact. This consumer does not spawn an independent emitted process.

An empty compiled segments list rejects through serialized-state validation,
runtime-engine construction, source emission and generated-plan validation with
their expected typed error categories and nested-write-invalid details. A reserved
`retv` base rejects at the serialized-state validator. Those exact controls do not
claim exhaustive malformed-carrier coverage.

After reading the suffix, child 50's nineteen independent fixture-input mutations
were repeated against this full source, including every suffix assertion. Each
changes only one structural/expression failure row's expected_binding to a wrong
present object; restoring that one field exactly reconstructs the original JSON
semantics. A scratch io.open adapter redirects only the canonical fixture read,
executes the original full file and restores itself. All 19 mutants preserve 438
passing assertions per host,38 full-suite observations. Repeated mutation-run
assertions are not counted again in the ordinary 1422 total.

The suffix adds no state oracle for those nineteen failed executions. This extends
existing .2.35 without adding a new defect or inferring wrong runtime state. The
prior 408-prefix evidence remains exact. Repair .2.35.1 must observe presence and
value after the same failed execution; .2.35.2 must reject wrong expected-state and
partial-publication mutations while retaining completed expression effects.

# Dormant progressive authority: exact coverage and limits

The complete separate authority source passes 273/273 on installed PUC Lua 5.5 and
LuaJIT's Lua 5.1 API. Its neutral fixtures have two registry entries, two sources,
eight views, six authority cases, six cancellation cases, eight chains and four
execution rows. Views return bounded text and every scalar local-to-global boundary.
Effective capabilities/policies and ceilings match the neutral intersections;
accepted callbacks preserve false. Cancellation identity, deadlines, costs and
remaining budgets match exact expected values. Active-chain cases exercise cycle,
span decrease, depth and total-call admission.

Execution rows return detached child values and check remaining steps. Their
parent_state comparison uses a JSON copy of parent_before that is never supplied
to the callback or authority; it proves consistency of that fixture pair, not
adversarial shared-parent mutation isolation. This precise proof limit is owned
under existing guidance .2.1, without inferring a runtime bug from an unreachable
reference. Eight invalid seam requests plus registry mutation/load attempts collect
all 26 governed diagnostic codes and require every declared context field; the
suite also rejects any observed code absent from the neutral catalog.

The healthy nested fixture dispatches a five-scalar outer span and then unicode
span 1-4, returning `é🙂B`. Child position 1 rebases to 2; span 0-2 rebases to 1-3;
diagnostic offsets/spans agree. Starting 20 steps, outer cost 2 and child cost 3 leave
15, with two calls. Retained source views and nested requests expire after return.
This fixture does not apply a smaller exhausted parent ceiling, so the previously
measured startup .37.1 inheritance defect remains open.

Registry entry top-rule/capability arrays are copied before caller mutation.
Returned seed objects remain detached after nested caller changes. Overlarge node
results, cycles, infinity, live-looking source_text fields and a tagged array with
an extra authority member reject. The string error `éé` under a one-byte child-
diagnostic limit becomes `?`; this is the child_diagnostic field's UTF-8 bound,
not a claim that the entire surrounding diagnostic JSON is one byte. It is also
distinct from the staged-enrichment .2.26 output-budget failure.

The private module is absent from the package facade. The source checks exact
module/dedicated-node tokens in five owners. Ordinary and canonical drivers contain
no authority-test reference; the separate carrier source appears exactly twice
in ordinary discovery and once per host plus one tracked-file requirement in
canonical CI. These are static topology checks and this direct authority run,
not execution of the full driver or the separately admitted carrier in this leaf.

# Knowledge reconciliation and focused proof

Read the private-authority, historical dormant-RED and admission cards completely;
the Lua write/neutral cards were already read in child 50. Existing .2.1 owns their
stage-specific 7/9/112 or pending-recurring/public pointers. Current progressive
neutral proof is 9/9/116 plus public 6/12/10/60, preserving 26 diagnostics and 9 Lua
carrier paths. Intentional authority dormancy stays current; historical 273 and 178
milestones must remain dated evidence. Child 34's carrier proof remains separate.

Neutral write checking retains 5/7/11/16/3/3 fixtures,8 composed writes and 105
mutations. All four ordinary native runs and both mutation probes exit 0; the
mutation result is a confirmed coverage failure, not a passing state oracle.
All 35 repair roots, installed-PUC nil-error failures, the public-selector baseline
failure, malformed native regex exclusions and parked named arguments remain.
No new source defect, full component run, canonical CI, PGEN/RGX build, push or
supported-PUC admission is claimed.

# Exact replay

```bash
bash tools/project_data_run.sh python3 - <<'LUA_FINAL_SOURCE_READING_51'
from pathlib import Path
p=Path('.linkedspec-data/scratch/lua151')
p.mkdir(parents=True, exist_ok=True)
(p / 'verify.py').write_text('from pathlib import Path\nimport hashlib,json\np=Path(\'.linkedspec-data/scratch/lua151\')\nrows=json.loads((p/\'scope.json\').read_text())\nassert len(rows)==2 and sum(r[\'end\']-r[\'start\']+1 for r in rows)==729\nassert sum(r[\'bytes\'] for r in rows)==28118\nfor r in rows:\n data=b\'\'.join(Path(r[\'path\']).read_bytes().splitlines(keepends=True)[r[\'start\']-1:r[\'end\']])\n assert len(data)==r[\'bytes\'] and hashlib.sha256(data).hexdigest()==r[\'sha256\'],r[\'path\']\noriginal=json.loads(Path(\'capability_conformance/write_vivification_contract.json\').read_text())\nmutants=json.loads((p/\'failure-state-mutants.json\').read_text());assert len(mutants)==19\nassert [r[\'id\'] for r in mutants]==[r[\'id\'] for family in [\'failure_cases\',\'evaluation_failure_cases\'] for r in original[family]]\nfor m in mutants:\n data=json.loads(Path(m[\'path\']).read_text());row=original[m[\'family\']][m[\'index\']]\n assert data[m[\'family\']][m[\'index\']][\'expected_binding\']!=row[\'expected_binding\']\n data[m[\'family\']][m[\'index\']][\'expected_binding\']=row[\'expected_binding\'];assert data==original\nprobe=(p/\'failure-state-probe.lua\').read_text()\nassert \'loadfile("lua/test/write_vivification_contract_test.lua")\' in probe and \'write-read-prefix\' not in probe\nexpected_probe=\'\'.join(\'write-vivification Lua dual-ABI admission: 438 assertions passed\\nSURVIVED expected_binding mutation: \'+m[\'id\']+\'\\n\' for m in mutants)+\'OBSERVED failure-state coverage gap: 19 of 19 individual input mutations survive\\n\'\nfor host,version in [(\'puc\',\'Lua 5.5\'),(\'luajit\',\'Lua 5.1\')]:\n expected={\n  \'write\':\'write-vivification Lua dual-ABI admission: 438 assertions passed\\n\',\n  \'authority\':f\'Lua progressive span-dispatch authority: 273/273 assertions passed on {version}\\n\',\n  \'probe\':expected_probe}\n for item,value in expected.items():\n  assert (p/f\'{item}-{host}.log\').read_text()==value,(item,host)\n  assert (p/f\'{item}-{host}.exit\').read_text()==\'0\\n\',(item,host)\nneutral={\n \'write_vivification\':\'write-vivification contract: 5 valid syntax, 7 invalid syntax, 11 success, 16 structural failures, 3 evaluation failures, 3 read exclusions, 8 composed writes, 105 rejected mutations; neutral authority remains frozen; capability admission is external\\n\',\n \'progressive_span_dispatch\':\'progressive-span-dispatch-contract: OK (2 registry entries; 2 sources/8 views; 6 authority + 6 cancellation + 8 chain + 4 execution cases; 9 Rust + 8 Dart carrier paths; 9 Julia carrier paths; 9 Lua carrier paths; 0 backend guards/0 paths; 10 outward guards; 26 diagnostics; rollout 9/9 complete, 0 pending; 116 contract mutations; public 6 documents/12 forbidden/10 outward/60 mutations)\\n\'}\nfor item,value in neutral.items():assert (p/f\'neutral-{item}.log\').read_text()==value,item\nfor path in [\'tools/run_lua_local.sh\',\'tools/run_ci_local.sh\']:\n text=Path(path).read_text();assert \'progressive_span_dispatch_authority_test\' not in text\nassert Path(\'tools/run_lua_local.sh\').read_text().count(\'lua/test/progressive_span_dispatch_contract_test.lua\')==2\nassert Path(\'tools/run_ci_local.sh\').read_text().count(\'require_tracked_file lua/test/progressive_span_dispatch_contract_test.lua\')==1\nfor host in [\'puc\',\'luajit\']:\n assert Path(\'tools/run_ci_local.sh\').read_text().count(\'bash tools/run_lua_project_data.sh \'+host+\' lua/test/progressive_span_dispatch_contract_test.lua\')==1\nprint(\'PASS exact final729 fragments/28118 bytes; four ordinary runs0 and1422 assertions (438+273 per host); intentional authority non-discovery and carrier topology; two exact neutral outputs. OBSERVED19 individual expected_binding mutations survive the FULL438 consumer per host (38 total), extending .2.35 coverage gap. Source reading complete; independent audit and canonical closeout remain pending.\')\n')
(p / 'scope.json').write_text('[\n  {\n    "path": "lua/test/write_vivification_contract_test.lua",\n    "start": 400,\n    "end": 542,\n    "bytes": 5988,\n    "sha256": "6533310a4326574e70fab6938f618c36b02c6bb96a1c31f41ebff04684642623"\n  },\n  {\n    "path": "lua/test_dormant/progressive_span_dispatch_authority_test.lua",\n    "start": 1,\n    "end": 586,\n    "bytes": 22130,\n    "sha256": "1c623f19e1b4503eaebe8c6e41ce80e13ec575cfb37b1a0943c5d3f55e703686"\n  }\n]\n')
(p / 'prepare-probe.py').write_text('from pathlib import Path\nimport json,copy\np=Path(\'.linkedspec-data/scratch/lua151\')\noriginal=json.loads(Path(\'capability_conformance/write_vivification_contract.json\').read_text())\nrows=[]\nfor family in [\'failure_cases\',\'evaluation_failure_cases\']:\n for index,row in enumerate(original[family]):\n  changed=copy.deepcopy(original)\n  wrong={\'present\':True,\'value\':{\'deliberately_wrong_failure_state\':row[\'id\']}}\n  assert wrong!=row[\'expected_binding\']\n  changed[family][index][\'expected_binding\']=wrong\n  check=copy.deepcopy(changed);check[family][index][\'expected_binding\']=row[\'expected_binding\'];assert check==original\n  target=p/f\'failure-state-mutant-{len(rows)+1:02}.json\'\n  target.write_text(json.dumps(changed,ensure_ascii=False,indent=2)+\'\\n\')\n  rows.append({\'family\':family,\'index\':index,\'id\':row[\'id\'],\'path\':str(target)})\nassert len(rows)==19\n(p/\'failure-state-mutants.json\').write_text(json.dumps(rows,indent=2)+\'\\n\')\nrunner=\'\'\'local json = require("linkedspec.json")\nlocal original_open = io.open\nlocal handle = assert(original_open(".linkedspec-data/scratch/lua151/failure-state-mutants.json", "rb"))\nlocal rows = json.decode(assert(handle:read("*a"))); assert(handle:close())\nlocal survived = 0\nfor _, row in ipairs(rows) do\n  io.open = function(path, mode)\n    if path == "capability_conformance/write_vivification_contract.json" then path = row.path end\n    return original_open(path, mode)\n  end\n  local ok, failure = pcall(function()\n    assert(loadfile("lua/test/write_vivification_contract_test.lua"))()\n  end)\n  io.open = original_open\n  if not ok then error("mutant did not survive " .. row.id .. ": " .. tostring(failure), 0) end\n  survived = survived + 1\n  io.stdout:write("SURVIVED expected_binding mutation: ", row.id, "\\\\n")\nend\nassert(survived == 19)\nio.stdout:write("OBSERVED failure-state coverage gap: 19 of 19 individual input mutations survive\\\\n")\n\'\'\'\n(p/\'failure-state-probe.lua\').write_text(runner)\nprint(\'Prepared19 individual fixture-input mutations, each changing only one expected_binding; production and original assertion bytes untouched.\')\n')
LUA_FINAL_SOURCE_READING_51
bash tools/run_python_project_data.sh .linkedspec-data/scratch/lua151/prepare-probe.py
for host in puc luajit; do
  for item in write authority probe; do
    case "$item" in
      write) script=lua/test/write_vivification_contract_test.lua ;;
      authority) script=lua/test_dormant/progressive_span_dispatch_authority_test.lua ;;
      probe) script=.linkedspec-data/scratch/lua151/failure-state-probe.lua ;;
    esac
    status=0
    bash tools/run_lua_project_data.sh "$host" "$script" > ".linkedspec-data/scratch/lua151/$item-$host.log" 2>&1 || status=$?
    printf '%s\n' "$status" > ".linkedspec-data/scratch/lua151/$item-$host.exit"
  done
done
for item in write_vivification progressive_span_dispatch; do
  bash tools/run_python_project_data.sh "tools/check_${item}_contract.py" > ".linkedspec-data/scratch/lua151/neutral-$item.log" 2>&1 || exit
done
bash tools/run_python_project_data.sh .linkedspec-data/scratch/lua151/verify.py
```

Related: [[lua-unicode-write-failure-state-consumer-reading]],
[[lua-startup-reading-coverage]], [[lua-progressive-span-dispatch-private-authority]],
[[lua-authority-compiled-reading-and-nested-step-gap]].

# Preservation and focused completion

Independent preservation retains 1,428 prior source/Knowledge/decision/history/
policy files and 2,584 of 2,588 prior task nodes byte-exactly. Only this reading
leaf, existing .2.1 guidance, .2.35 coverage evidence and startup .3.6 change;
no node is added and no parent closes. All ninety-two Known book headings and
the parked authoring tree remain exact. Three embedded payloads match scratch;
independent coverage reconstructs all 51 groups and unchanged baseline digests.

History validation passes 34 mutation controls, three surfaces and 68 segments;
both pressure checks pass at Changes 354 and Notes 284 lines. Memory remains 60 lines
with correct activation and handoff semantics. Knowledge regeneration reports
1,137 cards /9,079 question keys. The book renders with its existing large-search-
index warning at 10,193,271 bytes, still owned by startup .41.9. Whitespace checks
pass. Normal doctrine and activation hooks remain required at landing; independent
reading audit, canonical closeout and all repair prerequisites remain pending.
