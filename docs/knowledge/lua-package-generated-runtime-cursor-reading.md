---
id: lua-package-generated-runtime-cursor-reading
title: Lua package generated runtime and cursor proof preserves the known native-error exclusion
answers:
  - "what exact Lua source did startup reading child 41 cover"
  - "which Lua generated host runtime and cursor package groups pass during startup reading"
  - "which Lua package group is excluded because of the known native regex formatter defect"
  - "does Lua startup reading execute all generated families and the accepted eight modules in fresh children"
  - "why is the Lua history object name null while ordinary indexed reads still work"
  - "what do Lua package cursor and anonymous capture tests establish"
  - "what focused evidence passed for Lua reading child 41"
date: 2026-09-13
status: exact source reading and safe selected proof complete; native-error group and other repairs stay open
tags: [lua, package, generated-source, matching, runtime, cursor, capture, startup, evidence]
evidence: "LUA-STARTUP-READING.1.41 activates from 10acd96d54db66240d23c19a3ca73dd928423a75. Eight complete windows read 1500 fragments /64241 bytes. Both installed hosts pass 25 safe complete package groups each, including fresh all-family/accepted-eight generated children and runtime/cursor/capture behavior. Native-error group 5193-5215 stays unexecuted under .2.3; .2.1 guidance extends. No production change or new repair node."
reverify: "Run LUA_PACKAGE_RUNTIME_READING_41 and the managed commands below; the selector omits the entire known native-error group 5193-5215 and the partial boundary group 6312 onward."
---

# Exact reading and excluded execution

| Repository source | Inclusive lines | Fragments / bytes | Raw SHA-256 |
| --- | --- | --- | --- |
| lua/test/run.lua | 4853–6352 | 1500 /64241 | baa275cbbb97cb0c997666a77ef1c0214f9a4b4df40058d1b6490f27b930e27a |

Complete credited windows are 4853–5040, 5041–5225, 5226–5410, 5411–5595,
5596–5780, 5781–5965, 5966–6150 and 6151–6352. Ordered range SHA-256 is
`b334de83df1b27f93651825d32d96be8962d8a49f5a5c2209c6b1633cc07de20`.
All 99 Lua sources remain baseline-identical to
`baeb984e36a94a15951cd23d4c52def5064cdaca`. Reading reaches 41/51 groups,
57,040 fragments /2,170,080 bytes and 77 complete files. The package runner
remains partial, ending inside the boundary-capture group that starts at 6312.

The complete matching-failure group at 5193–5215 begins with a direct malformed
native regex compile through the formatter path already located under .2.3.
That entire group is excluded on both installed hosts. Its remaining input,
boundary, mode and foreign-match checks are not silently counted as passed.
Existing .2.3 now explicitly owns re-admission after the native repair and runtime
prerequisites. No fresh malformed-pattern failure or crash is induced. Source
reading still covers every line; selected execution is explicitly narrower.

# Fresh generated family and accepted-subset execution

The all-family host runner begins at 4819, whose prefix was read in child 40.
Its completed source loads a persisted module and matrix, validates the plan,
checks each root family's value and observes root/nested generated trace identity.
The parent constructs expected values through native execution before emitting
the module. A selected-runtime child runs all ten families, emits one exact success
line with empty stderr, and its temporary root is removed and checked absent.

The accepted-subset group validates all 105 corpus fixtures before looking up
the eight names directly from the neutral generated contract. For each name,
the native interpreter must equal the unchanged fixture expected JSON before
emission. The test writes eight source-identified modules plus expected metadata,
typed plans and a runner. A fresh child loads every module, validates each plan,
executes each input and returns exact ordered names/values/metadata/plans.
The first module additionally executes traced mode with exact identity and all
three portable generated event roles. Parent output comparison and empty-stderr
checks pass; the owned root is absent after cleanup. Both ABI parents run both
child roles. This is fresh execution of the accepted eight modules, not a full
generated 105 claim or a static admission-only check.

# Compilation and matching

Two compiler groups retain dependency errors when source validation is explicitly
disabled: an out-of-range child slot identifies parent/target, and an undefined
target fails distinctly. Public parsed AST input keeps parent-regex ownership,
typed ActionIR and resolved action contracts.

Four matching groups verify the native PCRE identity and supported valid inline
flags, POSIX/possessive classes, named captures, recursion, match-start reset and
fixed-width lookbehind. Alternation selects earliest byte position and then source
order; consume stays anchored. A Unicode fixture preserves byte/scalar positions,
full group slots, compact participating captures, named maps and line/column data.
Registers distinguish absent local state from a present zero-width match, seed
child entry state from caller-local state and retain immutable cursor/capture
updates. The excluded fifth matching group is not executed.

# Runtime, diagnostics and the history boundary

Default repetition preserves direct null value plus its one-value wrapper.
Per-rule seek can advance to a later match while AND/consume stays anchored.
The public initial cursor skips complete leading blank/comment lines, including
comment EOF, while ordinary leading content remains intact and coordinates
remain absolute. The history fixture's null object name initially warranted a
Knowledge lookup: [[ds-vhistory-leading-newline-oracle-boundary]] already records
the exact Perl public-wrapper versus direct-handler cause and cross-backend
admission. This is the expected public boundary, not a new indexed-read defect.
The same group proves ordinary payload[1] still returns its indexed value.

Action edges publish child retv and cursor progress; blind AND/OR retain ordered
and first-success behavior. Lifecycle events preserve the exact guarded-loop
order. Bounded minima fail, zero-width matches remain present without progress,
ordinary structural recursion cuts off, and unsupported helpers remain typed.
Trace tests preserve full result JSON on success, no-match and recursion-cutoff
paths. Healthy writers observe rule policies, regex/dispatch/lifecycle decisions,
cursor controls, source boundaries and mark events with balanced scopes.

Structured diagnostics retain exact missing-entry select_entry_rule JSON,
source name/path, code and handler identity. Deep child failures survive unwind;
richer nested lookup attribution is preserved. Empty compiled state uses
validate_spec/no_rules_defined and invalid UTF-8 input retains the effective rule.
Successful result fields remain unchanged. Child-local stores do not leak; a
false child result remains false on this ordinary dispatch route. This does not
close the different whole-child binding-helper limitation under .2.10.

Diagnostic print/say/print_each arguments evaluate once in order, with ten exact
Unicode/text events in the fixture. Missing sinks suppress delivery while retaining
side effects and parse values. Wrong sink type and print_each arity reject.
Typed exit_now preserves status and stops later output after delivering prior
events. These package cases use healthy sinks; earlier arbitrary nil-error/writer
limitations retain their owners and are not reclassified by this result.

# Stores, Unicode cursor views and rolling capture

Typed stores preserve false scalars/hash members, arrays/harrays, explicit named
bindings, copied snapshots, checked missing reads and eager ordinary block values.
Nested writes select kinds from evaluated segments and create missing structures.
The exact offset20 core fixture passes with its unchanged wrapped output and
current byte/scalar endpoint 6/6. Earlier malformed-host-container gaps remain open.

Entry/match helper tests preserve compact and named captures, text, length,
scalar positions and 1-based line/column aliases. Absent text/group/length/positions
are null; collections retain empty array/harray identity, presence is zero and
line/column remain origin 1. Public cursor views and input slicing retain Unicode
coordinates, remainder and complete input. Invalid slice values return null;
negative starts/widths clamp, and past-end slices are empty. Nested save/restore,
entry/match rewinds, empty stacks and absent anchors retain exact behavior.
Wrong arities produce attributed helper_arity_mismatch diagnostics. These finite
cases do not close earlier oversized-integer or infinite private-cursor limits.

Anonymous capture uses one rolling rule-local boundary. Stable forms expose
local-match-start, live-cursor and input-end spans with exact scalar positions,
lengths and receiver continuation. The setter returns void/null. Six advancing
forms return their text/length and move only the capture boundary to the proper
cursor or input endpoint. Reversed spans return null and preserve the boundary;
later valid capture still succeeds. Wrong arity remains typed. The next
earliest-boundary group is only partially read and is excluded until child 42.

# Knowledge reconciliation and proof limits

Retrieved [[ds-vhistory-leading-newline-oracle-boundary]],
[[lua-generated-source-accepted-subset]], [[lua-runtime-matching-state]],
[[lua-diagnostic-output-events]], [[lua-runtime-structured-diagnostics]],
[[lua-runtime-trace-events]] and [[lua-runtime-anonymous-capture-helpers]].
Earlier generated-family, cursor, interpreter and typed-output owners remain
authoritative with their already tracked exact qualifications.

Existing .2.1 gains module-pair/blanket invalid-pattern wording in matching,
obsolete top_rule_selection/current126/status wording in structured diagnostics,
and pending full-pipeline/current trace-only status in trace events. Historical
proof stays intact; .2.3 owns the native group exclusion and its re-admission.
No old Knowledge card, historical task node or production source changes.

Original preamble/helpers are selected from 1–199,1718–1753,3999–4019,
4285–4370 and 4726–4746, followed by 4819–5192 and 5216–6311. These contain
25 complete groups and omit all of 5193–5215 plus the partial boundary group.
Both managed native jobs exit 0 with exact logs, 50 groups total, including fresh
children and cleanup. No individual assertion total is invented. Installed
PUC 5.5 and LuaJIT are exercised; unavailable supported PUC 5.4 is not certified.
All 33 repair roots, prior nil-error/public-selector failures, startup gates and
parked named arguments remain open. No full CI, primary matrix, PGEN/RGX build
or push occurs.

# Exact replay

```bash
bash tools/project_data_run.sh python3 - <<'LUA_PACKAGE_RUNTIME_READING_41'
from pathlib import Path
p = Path('.linkedspec-data/scratch/lua141')
p.mkdir(parents=True, exist_ok=True)
(p / 'select-tests.py').write_text('from pathlib import Path\nimport re\n\np=Path(\'.linkedspec-data/scratch/lua141\')\nsource=Path(\'lua/test/run.lua\').read_text().splitlines(keepends=True)\nassert source[4818].startswith(\'local function generated_family_host_runner(\')\nassert source[5192].startswith(\'test("runtime matching returns typed dialect input and boundary failures"\')\nassert source[5215].startswith(\'test("runtime interpreter repeats\')\nassert source[6311].startswith(\'test("runtime boundary capture\')\nranges=[(1,199),(1718,1753),(3999,4019),(4285,4370),(4726,4746),(4819,5192),(5216,6311)]\nselected=\'\'.join(\'\'.join(source[start-1:end]) for start,end in ranges)\nnames=re.findall(r\'^test\\("([^"\\n]+)"\',selected,re.M)\nassert len(names)==25,len(names)\nassert \'runtime matching returns typed dialect input and boundary failures\' not in names\n(p/\'package-selection.lua\').write_text(selected+\'\\nassert(total == 25, "package selection count drift: " .. total)\\nassert(failed == 0, "package selection failures: " .. failed)\\nprint("package selection: " .. total .. " test groups passed")\\n\')\nprint(\'Selected original helpers and 25 complete groups; entire known native-error group5193-5215 and partial boundary group6312 onward excluded\')\n')
(p / 'verify.py').write_text('from pathlib import Path\nimport hashlib,json,re\n\np=Path(\'.linkedspec-data/scratch/lua141\')\nfor row in json.loads((p/\'scope.json\').read_text()):\n    data=b\'\'.join(Path(row[\'path\']).read_bytes().splitlines(keepends=True)[row[\'start\']-1:row[\'end\']])\n    assert len(data)==row[\'bytes\'] and hashlib.sha256(data).hexdigest()==row[\'sha256\']\nsource=Path(\'lua/test/run.lua\').read_text().splitlines(keepends=True)\nbody=\'\'.join(source[4818:5192]+source[5215:6311])\nnames=re.findall(r\'^test\\("([^"\\n]+)"\',body,re.M)\nassert len(names)==25\nexpected=\'\'.join(f\'ok {i} - {name}\\n\' for i,name in enumerate(names,1))+\'package selection: 25 test groups passed\\n\'\nfor host in [\'puc\',\'luajit\']:\n    assert (p/f\'package-{host}.log\').read_text()==expected,host\nprint(\'PASS: exact source range and 25 safe complete package groups per host, 50 total; fresh all-family and accepted8 generated children, runtime/matching/diagnostic/cursor proof; known native-error group remains excluded\')\n')
(p / 'scope.json').write_text('[\n  {\n    "path": "lua/test/run.lua",\n    "start": 4853,\n    "end": 6352,\n    "bytes": 64241,\n    "sha256": "baa275cbbb97cb0c997666a77ef1c0214f9a4b4df40058d1b6490f27b930e27a"\n  }\n]\n')
LUA_PACKAGE_RUNTIME_READING_41
bash tools/run_python_project_data.sh .linkedspec-data/scratch/lua141/select-tests.py
for host in puc luajit; do
  bash tools/run_lua_project_data.sh "$host" .linkedspec-data/scratch/lua141/package-selection.lua > ".linkedspec-data/scratch/lua141/package-$host.log" 2>&1 || exit
done
bash tools/run_python_project_data.sh .linkedspec-data/scratch/lua141/verify.py
```

Related: [[lua-package-function-generated-execution-reading]],
[[lua-startup-reading-coverage]], [[lua-native-readme-and-action-ast-reading]].
# Preservation audit

The initial payload check caught a prose spacing replacement inside the embedded
selector's printed status message. Restoring that literal from the saved executed
script fixes the evidence mismatch; selected ranges and executed results were
unchanged. All three embedded payloads now match their saved files exactly.

The final preservation audit passes: 1,417 baseline source/Knowledge/decision/
history/policy files remain byte-identical; 2,578 of 2,582 prior task nodes are
unchanged. Only this reading leaf, its existing .2.1/.2.3 repair owners and startup
.3.6 change; no node is added. All 90 prior book limitation headings and parked
authoring bytes remain intact. Full coverage replay retains all 99 files and
51 groups; completed totals are 57,040 fragments /2,170,080 bytes.

Document history passes all 34 mutation controls and exact reconstruction across
three surfaces and 67 segments. Explicit memory validation passes at 60 lines.
The book builds successfully; its existing search-index pressure warning reports
10,156,192 bytes and remains owned by startup .41.9. Production code, historical
Knowledge and prior immutable history stay unchanged.
