---
id: lua-staged-declaration-depth-recursion-consumer-reading
title: Lua staged test prefix verifies declarations, complete-depth policies and bounded recursion before rebasing
answers:
  - "what exact Lua source did startup reading child 48 cover"
  - "which staged enrichment prefix runs during Lua reading child 48"
  - "what verifies Lua staged marker provenance and recognition exclusion"
  - "what tests prove Lua complete-depth reservation and plan-only caching"
  - "what shared recursive resource counts does the Lua staged prefix verify"
  - "does Lua reading child 48 execute source rebasing or fresh production carriers"
  - "which prior staged defects remain after the 704 assertion Lua prefix"
date: 2026-09-13
status: exact source reading and bounded dual-host proof complete; no new defect, all prior owners remain open
tags: [lua, staged-parsing, provenance, registry, recursion, tests, startup]
evidence: "LUA-STARTUP-READING.1.48 activates from a4ccda13e00a701eab22a8540158d7c1922e98fe. Eight complete windows read staged-test 241-1740, 1500 fragments /62275 bytes. Exact selected 1-1728 plus closure of its existing IIFE passes 704 assertions per host, 1408 total. Neutral 9/9/123 and public 6/17/10/129 pass. Rebasing and fresh production-seed carrier checks stay outside this proof."
reverify: "Run LUA_STAGED_PREFIX_READING_48 and the managed commands below. The selector retains exact lines 1-1728 and appends only the original anonymous-function closure and a failure/count summary. Do not report full 890 or later carrier/rebasing proof from this prefix."
---

# Exact source coverage

| Repository source | Inclusive lines | Fragments / bytes | Raw SHA-256 |
| --- | --- | --- | --- |
| lua/test/staged_ast_enrichment_contract_test.lua | 241–1740 | 1500 /62275 | 8d9f0445d6412a7f472e313534cb7f0c0149a52d36347b80e346033cd954a775 |

Credited windows are 241–425,426–610,611–795,796–980,981–1165,1166–1350,
1351–1535 and 1536–1740, each returned completely. Ordered range SHA-256 is
`e246f1a148b9bc27c539469ffa83a8afa48b7f782caa2934e908bc528c6b03c8`.
All 99 Lua files remain baseline-identical to
`baeb984e36a94a15951cd23d4c52def5064cdaca`. Reading reaches 48/51 groups,
67,540 fragments /2,584,712 bytes, with 90 complete files. Staged reading remains
partial after the opening direct-rebasing fixture; no file completes in this leaf.

# Declaration and source-provenance tests

The prefix locks the current neutral lifecycle, all expected counts and ordered
case ids: four registry entries, eight provenance cases, eight resolutions, six
authority cases, ten cache identities, four queues, three isolation cases, four
result and three failure policies, ten chains and five detachment cases. It keeps
all nine rollout rows, five backend consumers/six routes, 37 diagnostics and 35
owners exact. These metadata assertions do not execute every backend.

The narrow v1 function-body adapter retains resolve/load/compile/execute phases,
exact builtin identity/digest, action_block result and replace_field policy.
Wrong top keeps attributed typed context. A general-v2 record remains rejected by
that narrow registry; general staging has its own caller-frozen authority.

Exact assignment-form parse_job lowers to one dedicated logical marker, not a
generic call or preconstructed runtime sidecar. Its typed source plan and literal
options remain normalized; capabilities sort uniquely. Native, public-AST
reconstructed, generated-plan and freshly loaded current-host emitted routes
construct identical detached markers. Emitted text keeps sidecar data hex-encoded
and contains no serialized runtime marker or staged contract id.

Direct entry/match whole-text and capture variants retain exact text and spans.
Neutral provenance accepts and rejects the exact eight cases. Two equal a captures
plus é and a supplementary scalar retain distinct ordered scalar spans 0–1,1–2,
2–3,3–4 when concatenated. Runtime match values, outward matching exports and JSON
omit private capture ranges. This admitted fixture does not close malformed typed
provenance .2.29 or private capture-range .2.28 limitations.

Fourteen malformed marker inputs reject with their specific typed codes: missing/
dynamic/duplicate/unknown options, duplicate capabilities, bad identities/policies/
targets, copied/transformed text, dynamic capture index and empty concatenation.
Five residual direct/nested/receiver/append/indexed forms reject. A forged generic
call in compiled state is caught at engine construction. Recognition-reachable
declarations reject through both direct child actions and a transitive function.
Every native regex exercised here is a valid fixture; earlier native-error
exclusions remain unchanged.

# Frozen current-depth authority

Registry identity is opaque and its cache starts empty. The exact resolution and
authority tables retain precedence, intersection/minima and attributed failures.
Forbidden top, malformed snapshot flags/digest/default/order, missing/extra callback
bindings, registration and implicit loading reject. Mutating caller snapshot and
callback maps cannot change frozen resolution. Three job-id rows and ten cache-key
rows preserve exact normalized identities; ambient cache fields reject.

Four ordering fixtures retain typed path/provenance/job-id order. All four stitch
policies yield exact parent values, no diagnostics and succeeded sidecars. Fail
publishes nothing; keep_text and diagnostic_node retain their exact text, target,
diagnostic and sidecar state. Default top selection precedes job identity.

Sibling callbacks get fresh cursor/marks/captures/variables, even after the previous
callback writes each store. Repeating the same two-job input reuses only plans:
first cache state is 1 entry/1 miss/1 hit, then 1/1/3, while callbacks execute four
times. A fresh registry has no cached entry. Caller AST remains unchanged.

Four invalid target shapes and unresolved lookup reject before callback one and
leave the cache empty. A later child failure publishes no earlier sibling change.
Cross-plan replacement conflicts and duplicate sibling targets also reject before
callbacks; multiple appends to one array remain ordered. Returned valid markers
stay inert in one-depth execution, while a marker containing a live callback key
rejects.

Five neutral detachment rows lock acceptance and visited-node counts directly and
through child settlement. A real array cycle rejects. Repeated failed callbacks
still execute twice while sharing only their plan; a subsequent success after a
failure produces its fresh value. A thrown string becomes the exact contained
child-exception diagnostic under keep_text. These controls do not close nested
tagged-array loss .2.25, marker extra-cycle .2.27 or retained diagnostic-byte .2.26.

# Recursive prefix and exact resources

All ten neutral chain rows retain acceptance and diagnostics. The executable
recursive fixture orders abcdef,ghijkl,bc,hi breadth first at depths 1,1,2,2, with
fresh contexts, one shared cancellation token and deadline 100. Remaining work
before callbacks is 19,17,15,13; each dispatch plus callback consumes work. Final
resources are 12 steps, 4 calls and 116 result nodes. Sidecar/request lineage lengths
remain distinct, and retaining a settled context does not keep safe_point valid.

Recursive replace_field, sibling_field and append_child results reach their exact
new paths, with three jobs at each of two depths. Exact tuple cycles reject before
a second callback; same-extent recurrence rejects as non-decreasing. Depth/call
limits retain typed diagnostics. Exhausted work, cancellation and expired deadline
reject before callback one. A caller step ceiling narrows supplied 500 to 200 when
required work is zero. A five-node result ceiling spans siblings and rejects after
the second callback rather than resetting its count.

These are staged-scheduler resource fixtures. They do not repair the separately
owned progressive child-budget reset or the staged false-option boundaries.
The later direct/derived rebasing, callback safe-point detail, diagnostic-byte
and fresh production-seed carrier checks remain outside execution in this leaf.
The production_seed helper is read but its factory is not invoked by this prefix.

# Selection, Knowledge and accounting

Original lines 1–240 were fully read in child 47. The scratch selector copies exact
lines 1–1728, ending after the complete cumulative-node assertion, then appends
`end)()` to close the anonymous function opened at 1473. It appends only a failure
and assertion summary after that. No retained fixture or assertion is changed.
The partial direct_rebased call begins at 1730 and is excluded. Both installed
hosts exit 0 with 704 assertions, 1,408 total. No full 890 claim follows.

Read [[lua-staged-ast-enrichment-recursive-carriers]],
[[lua-staged-ast-enrichment-marker-provenance]] and
[[general-staged-ast-enrichment-neutral-contract]]; reuse the fully read current-
depth card and earlier staged source/defect cards. Existing .2.1 already owns
historical 888 versus current 890 qualification, so no duplicate repair is opened.
Source/card reading adds no new defect and changes no earlier repair node.

The neutral checker passes all nine rollout legs and 123 mutations, plus the public
six-document/17-forbidden/10-outward/129-mutation boundary. It preserves five backend
sources and six routes; it does not execute all six runtimes. No full CI, primary
matrix, all-toolchain recurrence, PGEN/RGX build or push runs. Unavailable declared
PUC5.4 remains uncertified, and known PUC5.5 nil-callback failures stay failed.
All 34 repair roots, native-error exclusions and parked named arguments remain.

# Exact replay

```bash
bash tools/project_data_run.sh python3 - <<'LUA_STAGED_PREFIX_READING_48'
from pathlib import Path
p=Path('.linkedspec-data/scratch/lua148')
p.mkdir(parents=True, exist_ok=True)
(p / 'select-tests.py').write_text('from pathlib import Path\n\np=Path(\'.linkedspec-data/scratch/lua148\')\nlines=Path(\'lua/test/staged_ast_enrichment_contract_test.lua\').read_text().splitlines(keepends=True)\nassert lines[1472].strip()==\';(function()\'\nassert lines[1727].strip()==\'check_equal(node_calls, 2, "cumulative result-node limit spans siblings")\'\nassert lines[1729].startswith(\'local direct_rebased =\')\nfooter=\'\'\'\nend)()\nif #failures > 0 then\n  io.stderr:write("Lua staged enrichment read prefix: ", #failures, " of ", assertions, " assertions failed\\\\n")\n  for _, message in ipairs(failures) do io.stderr:write("- ", message, "\\\\n") end\n  os.exit(1)\nend\nio.stdout:write("Lua staged enrichment read prefix: ", assertions, " assertions passed\\\\n")\n\'\'\'\n(p/\'staged-read-prefix.lua\').write_text(\'\'.join(lines[:1728])+footer)\nprint(\'Selected exact staged-test lines1-1728, closing the existing IIFE after its complete node-limit test; rebasing and production-carrier checks excluded\')\n')
(p / 'verify.py').write_text("from pathlib import Path\nimport hashlib,json\n\np=Path('.linkedspec-data/scratch/lua148')\nrow=json.loads((p/'scope.json').read_text())[0]\ndata=b''.join(Path(row['path']).read_bytes().splitlines(keepends=True)[row['start']-1:row['end']])\nassert len(data)==62275==row['bytes'] and hashlib.sha256(data).hexdigest()==row['sha256']\nprefix=(p/'staged-read-prefix.lua').read_text()\nlines=Path(row['path']).read_text().splitlines(keepends=True)\nassert prefix.startswith(''.join(lines[:1728])+'\\nend)()\\n')\nassert 'local direct_rebased =' not in prefix\nfor host in ['puc','luajit']:\n    assert (p/f'prefix-{host}.log').read_text()=='Lua staged enrichment read prefix: 704 assertions passed\\n',host\n    assert (p/f'prefix-{host}.exit').read_text()=='0\\n',host\nassert (p/'neutral-staged.log').read_text()=='staged-AST enrichment contract: 4 registry entries, 8 provenance, 8 resolution/6 authority, 10 cache, 4 queue/3 isolation, 4 result policies, 3 failure policies, 10 chain, 5 detachment, 5 backend consumers/6 runtime routes, 37 diagnostics, 9 rollout legs, 35 owners, 123 mutations; public 6 documents/17 forbidden/10 outward/129 mutations\\n'\nprint('PASS: exact staged source241-1740 and unchanged selected1-1728; 704 assertions per host (1408 total), closing only the existing IIFE; rebasing and fresh production-carrier checks excluded. Neutral9/9/123 plus public6/17/10/129 remains exact; prior repairs stay open.')\n")
(p / 'scope.json').write_text('[\n  {\n    "path": "lua/test/staged_ast_enrichment_contract_test.lua",\n    "start": 241,\n    "end": 1740,\n    "bytes": 62275,\n    "sha256": "8d9f0445d6412a7f472e313534cb7f0c0149a52d36347b80e346033cd954a775"\n  }\n]\n')
LUA_STAGED_PREFIX_READING_48
bash tools/run_python_project_data.sh .linkedspec-data/scratch/lua148/select-tests.py
for host in puc luajit; do
  status=0
  bash tools/run_lua_project_data.sh "$host" .linkedspec-data/scratch/lua148/staged-read-prefix.lua > ".linkedspec-data/scratch/lua148/prefix-$host.log" 2>&1 || status=$?
  printf '%s\n' "$status" > ".linkedspec-data/scratch/lua148/prefix-$host.exit"
done
bash tools/run_python_project_data.sh tools/check_staged_ast_enrichment_contract.py > .linkedspec-data/scratch/lua148/neutral-staged.log 2>&1 || exit
bash tools/run_python_project_data.sh .linkedspec-data/scratch/lua148/verify.py
```

Related: [[lua-semantic-admission-source-alias-consumer-reading]],
[[lua-startup-reading-coverage]], [[lua-staged-completion-reading-and-boundary-gaps]].

# Preservation and focused completion

Independent preservation retains 1,425 prior source/Knowledge/decision/history/
policy files and 2,583 of 2,585 prior task nodes byte-exactly. Only this reading
leaf and startup .3.6 change; no node is added. All ninety-one Known book headings
and the parked authoring tree remain exact. Three embedded payloads match scratch;
independent coverage verifies all 48 completed groups and unchanged baseline digests.
The current task index also names all thirty-four existing Lua repair roots.

History validation passes 34 mutation controls, three surfaces and 68 segments;
both pressure checks pass at Changes 333 and Notes 263 lines. Memory remains 60 lines
with correct activation and handoff semantics. Knowledge regeneration reports
1,134 cards /9,058 question keys. The book renders with its existing large-search-
index warning at 10,182,060 bytes, still owned by startup .41.9. Normal doctrine and
activation hooks remain required at landing; no full CI or push is claimed here.
