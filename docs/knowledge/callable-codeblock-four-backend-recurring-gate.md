---
id: callable-codeblock-four-backend-recurring-gate
title: One routed gate composes current callable-codeblock proof on four backends
answers:
  - "how does recurring four-backend callable-codeblock proof run"
  - "which command composes callable-codeblock proof across Perl Rust Dart and Julia"
  - "what does LINKEDSPEC_RUN_CALLABLE_CODEBLOCK_MATRIX run"
  - "which backends are current in the callable-codeblock recurring gate"
  - "does the recurring callable-codeblock gate include Lua"
  - "how many callable-codeblock governance mutations are rejected"
  - "where does the generic final-codeblock future exclusion point now"
date: 2026-08-01
status: superseded by callable-codeblock-five-backend-admission
tags: [callable, codeblock, recurring-gate, perl, rust, dart, julia, lua, conformance, local-ci]
evidence: "FUTURE-PARITY-BACKLOG.11.7.1 adds tools/check_callable_codeblock_four_backend.sh. One repository-routed invocation runs the unchanged neutral 7/11/9/7/4/8 model followed by the exact Perl 10, Rust 18, Dart 21, and Julia 125+118+239 consumers. The checker locks ten roles per backend, exact paths/order/project-data routes, one canonical LINKEDSPEC_RUN_CALLABLE_CODEBLOCK_MATRIX branch, and the future.generic_final_codeblock record through 17 recurring mutations. Public closeout .11.7.2 adds 23 documents, nine stale-claim denials, and three omission mutations. The capability census remains 80/0/0; Lua contextual forms are current while explicit literals/general bound calls stay FUTURE-PARITY-BACKLOG.11.8. The driver is the 42nd governed project-data workflow entrypoint and adds no temporary allocator."
evidence_update_2026_08_01_lua_invocation: "FUTURE-PARITY-BACKLOG.11.8.1-.2 subsequently make Lua construction and dynamic invocation focused-current on both ABIs. The driver intentionally remains four-backend until .11.8.3 route proof and .11.8.4 replacement admission."
evidence_update_2026_08_01_lua_emitted_identity: "FUTURE-PARITY-BACKLOG.11.8.3 makes Lua native/reconstructed/generated/fresh-emitted identity focused-current on both ABIs. The existing driver intentionally remains four-backend until replacement admission .11.8.4."
evidence_update_2026_08_01_five_backend_admission: "FUTURE-PARITY-BACKLOG.11.8.4 replaces this driver with tools/check_callable_codeblock_five_backend.sh, adding the same Lua focused file on both ABIs and removing the satisfied callable exclusion."
reverify: "bash tools/check_callable_codeblock_five_backend.sh && bash tools/run_python_project_data.sh tools/check_callable_codeblock_contract.py && bash tools/test_project_data_workflow_routing.sh && perl tools/check_capability_conformance.pl"
---

Five-backend callable recurring/public admission is complete. This card preserves the superseded `.11.7.1`
four-backend boundary; [[callable-codeblock-five-backend-admission]] owns the current driver and status.

The recurring layer composes established authorities; it does not introduce a fifth implementation of the
callable contract. In order, the driver runs the neutral model, Perl, Rust, Dart, and Julia. Each focused consumer
already owns inert construction, dynamic caller-context invocation, static callable precedence, contextual final
blocks, portable failures, and native/reconstructed/generated/emitted identity.

The driver sources the repository project-data initializer and enters one managed run before launching a runtime.
Python, Cargo, Dart, and Julia use their supported repository-rooted wrappers; Perl runs inside the managed driver
with ambient `PERL5LIB` cleared. The workflow-routing oracle lists this command explicitly as entrypoint 42.

Canonical CI always requires and syntax-checks the driver. Setting
`LINKEDSPEC_RUN_CALLABLE_CODEBLOCK_MATRIX=1` opts into the all-toolchain composition; the execution line occurs
exactly once. The ordinary canonical path continues to run the neutral checker and Perl consumer unconditionally.

Seventeen mutations deny missing/reordered backend roles, stale consumer paths, project-data bypass, absent or
duplicated canonical execution, lost switch registration, and premature Lua/five-backend claims. The capability
census remains 80 pass / 0 partial / 0 gap because this leaf corrects an exclusion narrative rather than adding a
capability row. Lua explicit construction, general bound calls, and generated/emitted identity are focused-current.
The later `FUTURE-PARITY-BACKLOG.11.8.4` leaf completes final admission and supersedes this four-backend boundary.

Public no-drift closes parent `.11.7` by checking 23 backend/API/book/roadmap/task/Knowledge documents and nine
forbidden stale claims. Three independent omissions for a document, required marker, or denylist entry raise the
complete governance total to 20 without broadening the recurring runtime driver.

Related facts: [[callable-codeblock-literal-contract]], [[lua-explicit-callable-codeblock-gap]],
[[lua-callable-codeblock-dynamic-invocation]], [[lua-callable-codeblock-emitted-route-identity]],
[[project-data-ssd-storage-locality]], and
[[repository-root-relocation-process-proof]].
