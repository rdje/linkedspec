---
id: lua-root-cursor-consumer-reading
title: Lua root routes and cursor consumers distinguish execution source identity and emitted text proof
answers:
  - "what exact Lua source did startup reading child 36 cover"
  - "does Lua cursor admission execute its emitted source role"
  - "are Lua loaded and inline descriptors byte identical before source normalization"
  - "what does the Lua cursor execution prefix through line 322 prove"
  - "does Lua root route validation precede unknown entry selection"
  - "which cursor guidance counts and identity claims still need qualification"
  - "what focused evidence passed for Lua reading child 36"
date: 2026-09-13
status: scoped source reading and focused proof complete; cursor execution suffix and prior repair owners remain open
tags: [lua, cursor, descriptor, root-selection, generated-source, startup, evidence]
evidence: "LUA-STARTUP-READING.1.36 activates from 5e716ab1efe0dae02d2d121dd1c227cc8395a61f. Ten complete windows read 1500 fragments /55300 bytes. Both installed hosts pass root99, routes106, cursor119, descriptor912 and execution-prefix95: 2662 assertions. Fresh cursor8/0/60/public30/28 passes; prior root-neutral inputs are byte-identical. Existing .2.1 owns exact guidance qualification. No production change."
reverify: "Run LUA_ROOT_CURSOR_READING_36 and the managed commands below; cursor execution proof is limited to source lines 1-322."
---

# Exact reading

| Repository source | Inclusive lines | Fragments / bytes | Raw SHA-256 |
| --- | --- | --- | --- |
| lua/test/root_rule_selection_core_test.lua | 205–278 | 74 /3499 | 6397ee3b7f666d23a322a3d9af5d6c52a2227ee5c37b04df889a4a76a0ec01f8 |
| lua/test/root_rule_selection_routes_test.lua | 1–353 | 353 /15031 | 3988b1da2e9433000c9d8e1ed77a0c8ddb96d6be7b5294f8c6af1cab0ce2ef7c |
| lua/test/rule_local_cursor_contract_test.lua | 1–486 | 486 /17331 | 9915dbb149a130a80fd9cb48704755052b84e946b8136e1fcab4f41fd8b18696 |
| lua/test/rule_local_cursor_descriptor_test.lua | 1–260 | 260 /11583 | 4fb29b2446ae146639116615d2d09f26f8539f6aca7c63ed11ec0e20d62aee2e |
| lua/test/rule_local_cursor_execution_test.lua | 1–327 | 327 /7856 | de13b7f5826ab56f3d7fee7fc028d5700f4ccf35f548a7aef9242116efe70f38 |

Root-core 205–278 is one window; routes use 1–180 and 181–353; cursor admission
uses 1–175, 176–350 and 351–486; descriptor uses 1–150 and 151–260; execution
uses 1–180 and 181–327. All ten credited outputs are complete. Ordered range SHA-256:
`c19db68ddab9476cd7c6ef7f976ead22388e5a89daceeadc37bc4ffcb20ff763`.
All 99 Lua sources remain baseline-identical to
`baeb984e36a94a15951cd23d4c52def5064cdaca`. Reading reaches 36/51 groups,
49,651 fragments /1,852,679 bytes and 73 complete files. Cursor execution remains
partial, ending inside the loaded-fixture block.

# Root core and route convergence

The core suffix finishes typed zero-rule runtime diagnostics, verifies descriptor
identity before/after explicit selection, preserves authored order and marker bits,
checks strict-unused policy through the neutral cases and runs the markerless
primary command with exact exit/stdout/stderr. Combined with the prefix read in
child35, the complete core passes 99 assertions per host. Unknown selection fails
before the source's entry action can execute; a zero-rule spec takes precedence.

The route consumer checks portable generated stage/code names and two loaded
fixtures, marked and markerless. Loaded execution leaves its descriptor unchanged.
For comparison with inline reconstruction, every loaded slot source ID is first
checked against the logical file basename and then changed in the detached
comparison descriptor to inline. Normalized state retains authored identity and
default/explicit selection. Comment-only loaded input keeps no_rules_defined at
validate_spec; duplicate rules retain the unrelated spec_validation_failed code.

Generated plans omit entry selection and authored marker fields. Default and
explicit execution produce exactly one low-level selection decision with requested,
effective and basis fields. Fresh source is persisted, loadfile-loaded and executed
in the current host through direct/traced default/explicit routes. Compiled
descriptors remain unchanged. Unknown generated selection and zero-rule execution
project exact typed JSON identities and appropriate failure trace. A stale plan
combined with an unknown selector fails first at plan row-count validation.
The full route consumer passes 106 assertions per host. The temporary directory
is rooted under managed TMPDIR and its exact owned path is cleaned after the run.

# Cursor admission and descriptor

Cursor admission declares exactly 15 unique roles and executes each once: default,
AND, ordinary normalized, loaded, descriptor v1, emitted v2, generated direct,
generated trace, mixed parent/child, recursion, ordered landmarks, anchored choice,
static option removal, primary command and portable diagnostics.

AND rejects leading input and default seeks. A seeking child retains its policy
under an AND parent; recursive entries derive again. Normalized AST and descriptors
omit retired outer options. Generated plans retain only label/family; stale v1
identity rejects with the portable code. Generated direct/traced roles execute and
retain entry/decision/exit source identity. The emitted role, however, inspects
source contract/format/entry markers and absence of serialized overrides: it does
not load or execute that emitted text. The separate root-route suite above supplies
its own actual emitted execution, with its own fixture scope.

Static checks retain the CLI removal branch and low-level matcher functions while
rejecting high-level override state. Dynamic snake/camel options produce exact
typed removal fields; primary success, retired flag and help outputs are checked.
Reconstructed invalid edges/ownership sets preserve portable code, stage and field
names. The observed union includes all eight contract diagnostics. The complete
admission passes 119 assertions per host; no new interpreter route is introduced.

The descriptor consumer reads both current neutral contracts. All 36 family rows
check direct versus normalized projection, exact root/rule metadata, cursor/entry/
slot contracts, authored identity and absent obsolete fields. Valid edge rows
check exact semantic field sets, ownership, ordered target, default action index
zero versus blind null, block/fluent data and omission of non-semantic source_form.
Invalid reconstructed rows retain typed validation and exact fields.

Its loaded fixture is one explicit AND source. The test checks logical source ID
descriptor.spec, replaces it with inline in the comparison object, and only then
compares descriptor bytes. It verifies consume behavior on leading versus contiguous
input. This is not raw loaded/inline equality or loaded execution across all 36
families. Direct/normalized coverage has the wider family inventory. The complete
consumer passes 912 assertions per host, consistent with the prior focused reading
result; dated 875-assertion admission evidence is not rewritten.

# Bounded execution prefix

Source through line322 is a complete executable prefix. Two assertions lock the
eight parent/child and two structural fixture ID sets. Each fixture executes live
and normalized: blind dispatch, action edges, explicit call and recursion derive
the child policy from its own family. AND over seeking children finds ordered
landmarks; OR over consuming children makes anchored choice.

One assertion locks the 36-row family count. Each row executes live and normalized
with an explicit Top selector. Seek results respect repetition collection; Optional
keeps one hit and repeated modes keep both x matches in the input prefix x. That
input has one x in prefix and one trailing x. Consume rows accept either null or
their existing below-minimum diagnostic for the mismatching leading input.
The exact total is 2 + 2*(8+2) + 1 + 2*36 = 95 assertions per host. Later loaded,
trace, generated and failure blocks are not executed by this selected prefix and
remain owned by child37. No partial open block is synthetically executed.

# Knowledge reconciliation and focused evidence

Retrieved [[lua-rule-local-cursor-execution]], [[lua-rule-local-cursor-descriptor]],
[[lua-rule-local-cursor-admission]], [[lua-rule-local-cursor-normalization]],
[[lua-rule-local-cursor-preflight]], [[rule-local-cursor-neutral-contract]] and
[[outward-compiled-descriptor-four-backend-contract]]. Root selection owners were
read in child35 and remain authoritative; no repeated runtime derivation was needed.

Existing `.2.1` owns precise qualification of the descriptor card's current 875
count against fresh 912 and its existing generated-v1/outer-option wording. It now
also owns loaded byte-identity wording in that card, the root-routes card and the
shared outward card's Lua projection, including the loaded fixture's narrower
scope. Cursor admission's remaining recurrence/public/root wording is superseded
by closed neutral cursor and root successors. The neutral cursor card's current
65-case primary sentence must be dated against the current 66-case manifest.

That manifest is measured as 32,247 bytes, SHA-256
`4c764e19c00a78d42da093571cda6d3610f5c3dabb8cfe4703b47dfab317de2c`, with exactly
66 cases; this is an inventory check, not execution of the primary matrix.
No old Knowledge card, production source or neutral contract changes here.

Fresh proof is 99+106+119+912+95 = 1,331 per host, 2,662 total. Cursor governance
passes 36 families,18 edges,8 parent/child cases,15 Lua roles,6 recurring routes,
30 public documents,28 current denials,74 migration files,8 complete/0 pending
and60 mutations. All ten native jobs and the neutral job exit0.

The root checker is not rerun. Its two inputs match child35's committed bytes:
contract24,468/SHA4bc09c317301db51fffaf78cb6eb5d53d5cb85cb5ed144d6fb75076d8d7784d9
and checker59,547/SHA378525a907b8075c93557cb7bcaf528a4d72f61885f8cb061ff59fc9929e6a3b.
That leaf's root7/0/54/public24/18 stays dated. Installed PUC5.5 and LuaJIT are
the exercised hosts; unavailable supported PUC5.4 remains unverified. All 33 repair
roots, nil-error and public-selector failures, startup gates and parked named
arguments remain open. No full CI, all-runtime recurring driver, PGEN/RGX build,
dependency change or push is claimed.

# Exact replay

```bash
bash tools/project_data_run.sh python3 - <<'LUA_ROOT_CURSOR_READING_36'
from pathlib import Path
p = Path('.linkedspec-data/scratch/lua136')
p.mkdir(parents=True, exist_ok=True)
(p / 'select-tests.py').write_text('from pathlib import Path\nimport json\n\np = Path(\'.linkedspec-data/scratch/lua136\')\nsource = Path(\'lua/test/rule_local_cursor_execution_test.lua\').read_text().splitlines(keepends=True)\nassert source[321] == \'end\\n\'\nassert source[323] == \'with_temp_directory(function(root)\\n\'\ncontract = json.loads(Path(\'capability_conformance/rule_local_cursor_contract.json\').read_text())\nexpected = 3 + 2 * (len(contract[\'parent_child_cases\']) + len(contract[\'structural_replacements\']) + len(contract[\'family_cases\']))\n(p/\'cursor-execution-prefix.lua\').write_text(\'\'.join(source[:322]) + \'\\nassert(#failures == 0, table.concat(failures, " | "))\\nassert(assertions == \' + str(expected) + \', "cursor execution prefix count drift: " .. assertions)\\nprint("cursor execution prefix: " .. assertions .. " assertions passed")\\n\')\nprint(\'Selected cursor execution lines 1-322:\',expected,\'assertions\')\n')
(p / 'verify.py').write_text("from pathlib import Path\nimport hashlib, json\n\np = Path('.linkedspec-data/scratch/lua136')\nfor row in json.loads((p/'scope.json').read_text()):\n    data = b''.join(Path(row['path']).read_bytes().splitlines(keepends=True)[row['start']-1:row['end']])\n    assert len(data) == row['bytes'] and hashlib.sha256(data).hexdigest() == row['sha256']\nfor row in json.loads((p/'reused-root-inputs.json').read_text()):\n    data = Path(row['path']).read_bytes()\n    assert len(data) == row['bytes'] and hashlib.sha256(data).hexdigest() == row['sha256']\nfor host in ['puc','luajit']:\n    for name,line in {\n        'root-core': 'root-rule selection core: 99 assertions passed',\n        'root-routes': 'root-rule selection routes: 106 assertions passed',\n        'cursor-admission': 'rule-local cursor dual-ABI admission: 119 assertions passed',\n        'cursor-descriptor': 'rule-local cursor descriptor: 912 assertions passed',\n        'cursor-execution': 'cursor execution prefix: 95 assertions passed',\n    }.items():\n        assert (p/f'{name}-{host}.log').read_text() == line+'\\n'\nlog=(p/'neutral-cursor.log').read_text()\nfor marker in ['36 family spellings','18 edge cases','8 parent/child cases','15 Lua admission roles','6 recurring runtime legs','30 public documents','28 forbidden current claims','74 migration files','8 complete / 0 pending','60 drift mutations']:\n    assert marker in log,marker\nmanifest=Path('cli_conformance/manifest.json').read_bytes()\nassert len(manifest)==32247 and hashlib.sha256(manifest).hexdigest()=='4c764e19c00a78d42da093571cda6d3610f5c3dabb8cfe4703b47dfab317de2c'\nassert len(json.loads(manifest)['cases'])==66\nprint('PASS: exact source ranges; root99 + routes106 + cursor119 + descriptor912 + execution-prefix95 per host, 2662 assertions; cursor8/0/60/public30/28; prior root-neutral inputs byte-identical')\n")
(p / 'scope.json').write_text('[\n  {\n    "path": "lua/test/root_rule_selection_core_test.lua",\n    "start": 205,\n    "end": 278,\n    "bytes": 3499,\n    "sha256": "6397ee3b7f666d23a322a3d9af5d6c52a2227ee5c37b04df889a4a76a0ec01f8"\n  },\n  {\n    "path": "lua/test/root_rule_selection_routes_test.lua",\n    "start": 1,\n    "end": 353,\n    "bytes": 15031,\n    "sha256": "3988b1da2e9433000c9d8e1ed77a0c8ddb96d6be7b5294f8c6af1cab0ce2ef7c"\n  },\n  {\n    "path": "lua/test/rule_local_cursor_contract_test.lua",\n    "start": 1,\n    "end": 486,\n    "bytes": 17331,\n    "sha256": "9915dbb149a130a80fd9cb48704755052b84e946b8136e1fcab4f41fd8b18696"\n  },\n  {\n    "path": "lua/test/rule_local_cursor_descriptor_test.lua",\n    "start": 1,\n    "end": 260,\n    "bytes": 11583,\n    "sha256": "4fb29b2446ae146639116615d2d09f26f8539f6aca7c63ed11ec0e20d62aee2e"\n  },\n  {\n    "path": "lua/test/rule_local_cursor_execution_test.lua",\n    "start": 1,\n    "end": 327,\n    "bytes": 7856,\n    "sha256": "de13b7f5826ab56f3d7fee7fc028d5700f4ccf35f548a7aef9242116efe70f38"\n  }\n]\n')
(p / 'reused-root-inputs.json').write_text('[\n  {\n    "path": "capability_conformance/root_rule_selection_contract.json",\n    "bytes": 24468,\n    "sha256": "4bc09c317301db51fffaf78cb6eb5d53d5cb85cb5ed144d6fb75076d8d7784d9"\n  },\n  {\n    "path": "tools/check_root_rule_selection_contract.py",\n    "bytes": 59547,\n    "sha256": "378525a907b8075c93557cb7bcaf528a4d72f61885f8cb061ff59fc9929e6a3b"\n  }\n]\n')
(p / 'verify-history.py').write_text("from pathlib import Path\nimport hashlib,json,subprocess\n\nbase='5e716ab1efe0dae02d2d121dd1c227cc8395a61f'\nmanifest='docs/history/changes/manifest.jsonl'\nprior_raw=subprocess.check_output(['git','show',base+':'+manifest]).splitlines(keepends=True)\nrows=Path(manifest).read_bytes().splitlines(keepends=True)\nentries=[json.loads(row) for row in rows[1:]]\nentry=next(row for row in entries if row['segment_id']=='4977')\nassert entry['source_commit']==base and entry['source_blob']=='ab4dcaf6f388093b30d1183e9825b651d0ff7fd7'\nassert (entry['source_start_line'],entry['source_end_line'])==(244,459)\ndata=Path(entry['target_path']).read_bytes()\nassert entry['target_path']=='docs/history/changes/segment-4977-65e8f007697e.md'\nassert len(data)==entry['byte_count']==13109 and len(data.splitlines())==entry['line_count']==216\nassert hashlib.sha256(data).hexdigest()==entry['sha256']=='65e8f007697e26885118d213257a695012e39fd07323f9dae4834fb904fe6d6e'\nsource=subprocess.check_output(['git','show',base+':CHANGES.md'])\nassert data==b''.join(source.splitlines(keepends=True)[243:459])\nassert b''.join(source.splitlines(keepends=True)[:243])+data==source\nassert rows[-len(prior_raw[1:]):]==prior_raw[1:]\nprior_history=b''.join(Path(json.loads(row)['target_path']).read_bytes() for row in prior_raw[1:])\nquery=subprocess.check_output(['perl','tools/read_document_history.pl','--surface','change_history','--all'])\nassert query.endswith(data+prior_history)\nassert subprocess.check_output(['perl','tools/read_document_history.pl','--surface','change_history','--segment','4977'])==data\nprint('PASS: change-history segment4977 exact216-line/13109-byte clean-HEAD suffix; all prior manifest rows and full historical suffix intact')\n")
LUA_ROOT_CURSOR_READING_36
bash tools/run_python_project_data.sh .linkedspec-data/scratch/lua136/select-tests.py
for host in puc luajit; do
  bash tools/run_lua_project_data.sh "$host" lua/test/root_rule_selection_core_test.lua > ".linkedspec-data/scratch/lua136/root-core-$host.log" 2>&1 || exit
  bash tools/run_lua_project_data.sh "$host" lua/test/root_rule_selection_routes_test.lua > ".linkedspec-data/scratch/lua136/root-routes-$host.log" 2>&1 || exit
  bash tools/run_lua_project_data.sh "$host" lua/test/rule_local_cursor_contract_test.lua > ".linkedspec-data/scratch/lua136/cursor-admission-$host.log" 2>&1 || exit
  bash tools/run_lua_project_data.sh "$host" lua/test/rule_local_cursor_descriptor_test.lua > ".linkedspec-data/scratch/lua136/cursor-descriptor-$host.log" 2>&1 || exit
  bash tools/run_lua_project_data.sh "$host" .linkedspec-data/scratch/lua136/cursor-execution-prefix.lua > ".linkedspec-data/scratch/lua136/cursor-execution-$host.log" 2>&1 || exit
done
bash tools/run_python_project_data.sh tools/check_rule_local_cursor_contract.py > .linkedspec-data/scratch/lua136/neutral-cursor.log 2>&1 || exit
bash tools/run_python_project_data.sh .linkedspec-data/scratch/lua136/verify.py
bash tools/run_python_project_data.sh .linkedspec-data/scratch/lua136/verify-history.py
```

Related: [[lua-recursive-repeated-root-consumer-reading]],
[[lua-startup-reading-coverage]], [[rule-local-cursor-public-no-drift]].

# Mandatory history rollover and preservation

The first history check passed its 34 mutation controls but rejected the new
466-line Changes root at the mandatory 90% threshold. This leaf recorded rollover
ownership before invoking the existing complete-record tool. No capacity limit
changes. The tool archives only clean activation-HEAD CHANGES.md lines 244–459:
216 lines /13,109 bytes, source blob ab4dcaf6f388093b30d1183e9825b651d0ff7fd7,
as `docs/history/changes/segment-4977-65e8f007697e.md`, SHA-256
`65e8f007697e26885118d213257a695012e39fd07323f9dae4834fb904fe6d6e`.

The tool retains 250 lines /21,811 bytes. Git whitespace validation identifies its
new blank EOF line; the hot root is normalized to one terminal LF, leaving 249
lines /21,810 bytes. Root plus one boundary LF plus new segment reconstructs the
entire pre-rollover root exactly, including the new unarchived leaf entry.
Historical --all output is exactly new segment
followed by unchanged old historical output: 49,347 lines /3,562,294 bytes,
SHA-2563edaedcda5ad81e47fd81d60796ea3f9ba531d740bff415f827047bc56f1a476.
Combined hot-root, one boundary LF and historical bytes retain SHA-256
4fb7491faae83699c2732762221698ef335502bef33d1c576b2d4b404e1674df.
The query returns manifest segments only; include the hot root and retained record separator when verifying
complete current-plus-archived history. Auxiliary reading of the complete history
reader establishes that command boundary without advancing supporting-code credit.

The manifest grows by one segment record and its count field only: 35 lines
/19,919 bytes. The governed collection is 36 files including the root, within
ADR0118's 38-file/37-manifest-line/21,071-manifest-byte allowance. Earlier manifest
rows and segment bytes are exact; the embedded history verifier checks provenance
and retrieval without replaying the mutation. Shared history now passes 34 controls
across three surfaces and 67 segments.

The independent Git audit preserves 1,410 source/old-Knowledge/decision/history/
policy files exactly; the history manifest has only the intended new generation.
Of 2,582 prior task nodes, 2,579 stay exact and only this leaf, existing .2.1 and
startup .3.6 change. No task is added. All 90 Known headings, the parked authoring
tree and original chronology survive. All five embedded payloads match scratch
bytes; the exact 99-file/51-group/149-range source inventory reconstructs unchanged.

Memory passes at 60 lines. Knowledge passes at 1,122 facts /8,973 questions.
Current Notes is 396 lines /33,227 bytes. The book builds successfully; its
10,138,104-byte search-index warning remains owned by startup .41.9. The normal
nine-doctrine hooks remain the commit admission boundary.
