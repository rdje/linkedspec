---
id: lua-semantic-admission-source-alias-consumer-reading
title: Lua semantic admission and source aliases retain exact fixture and current-host carrier boundaries
answers:
  - "what exact Lua source did startup reading child 47 cover"
  - "what does the complete Lua semantic admission consumer prove"
  - "does Lua semantic admission run a fresh emitted child and all twenty query hashes"
  - "what do the remaining Lua static targets retain under an identity ceiling"
  - "which source-boundary alias carriers execute in the Lua 638 assertion suite"
  - "does Lua reading child 47 execute staged enrichment behavior tests"
  - "what focused and neutral evidence passed in Lua reading child 47"
date: 2026-09-13
status: exact reading and focused proof complete; all prior repair owners and runtime limitations remain open
tags: [lua, semantic-introspection, admission, source-location, aliases, staged-parsing, startup]
evidence: "LUA-STARTUP-READING.1.47 activates from 93b9ed32b2e4d1d64ddcbe0b6982f71c719d9ccc. Ten complete windows read four ranges, 1500 fragments /58749 bytes. Eight native jobs pass: remaining-static 122/admission 408/aliases 638 per host, 2336 assertions plus two staged helper loads. Semantic 6/20/128 and typed 14/0/231 neutral checks pass. No production change or new repair node."
reverify: "Run LUA_SEMANTIC_ALIAS_READING_47 and the managed commands below. The staged selection copies only complete helpers 1-222; no later staged behavior or full 890 count is claimed. Existing PUC nil-error failures and .2.34 fixture coverage remain open."
---

# Exact source coverage

| Repository source | Inclusive lines | Fragments / bytes | Raw SHA-256 |
| --- | --- | --- | --- |
| lua/test/semantic_index_static_remaining_test.lua | 74–337 | 264 /12332 | a7a34a4a8272fa5654e7a9fcd7b9fd0e2c756d7f8aa676c0d82b9b17236cc52a |
| lua/test/semantic_introspection_lua_admission_test.lua | 1–659 | 659 /26575 | 9754ccd41d83f0986b497413b8a7fc983d44b9cfb1f19502ba89d1afde66b31a |
| lua/test/source_boundary_compatibility_aliases_test.lua | 1–337 | 337 /11657 | 8ffec08f008c11d8cac520b3dbc7eaf8344e8eb873b5ee298ff124e7cf1a6f21 |
| lua/test/staged_ast_enrichment_contract_test.lua | 1–240 | 240 /8185 | ee6f9941183ceb21be6ab3d4a1d30e18b7d4c9ff2d9f95b3361855d74cd21b7a |

Complete credited windows are remaining-static 74–205,206–337; admission 1–170,
171–340,341–500,501–659; source aliases 1–165,166–337; staged enrichment 1–120,
121–240. Ordered range SHA-256 is
`ebf2c096652b76b88e286946976aeb3e61f90a1be006d0ef23d48225f3dbdecd`.
All 99 Lua sources remain baseline-identical to
`baeb984e36a94a15951cd23d4c52def5064cdaca`. Reading reaches 47/51 groups,
66,040 fragments /2,522,437 bytes and 90 complete files. Remaining-static,
semantic admission and source-alias consumers are fully read. Staged reading
ends inside enrichment_options; complete helpers 1–222 alone are loaded.

# Remaining static targets

The full 122-assertion consumer deep-equals privacy text and identity projections
at 4 records /3 relations with two private source references each. Identity ceiling
retains exact private spans/excerpts and UTF-8 source while advertising no public
content digest. Redaction belongs to the later public query boundary; this private
test seam is not an identity-ceiling public source leak.

Runtime-static expectation removes execution/event records and every relation
incident to them. Its exact 7/8 model remains has_execution=false, contains no
observed_as or redundant self dispatch, and retains two selected slots plus exact
repetition 2..2. Failed construction preserves the native bare_edge_target_undefined
diagnostic at normalize_edges with rule/target fields. Private projection alone
normalizes it to unknown_rule_reference at compile with portable ids, source
excerpt/span and 6/4 topology. Repeated E members keep separate occurrence ids,
order, shapes, excerpts and lines.

All four target materializations remain plain, detached and free of the selected
host/path fields. Parse-failure fallback is 3/2; missing-entry fallback is 4/2, each
with its exact diagnostic. Public empty/protected indexes omit direct test graph
accessors and the obsolete root semantic_query name; this does not imply the
index query API is absent. Forbidden imports/execution/environment tokens are
source scans. Earlier diagnostic-copy, source-correlation and conditional-entry
explanation defects remain outside these passing fixture claims.

# One twelve-role semantic consumer

The complete admission suite passes 408 assertions per installed host. Its ordered
twelve-role list and dispatch counters require each role exactly once. Both Lua
admission rows must be complete and contain the identical source path, canonical
driver and role list; the Lua rollout row and canonical inventory must also agree.
Running this existing consumer does not create a new formal admission or certify
the unavailable declared PUC 5.4 runtime/header pair.

Source normalization compares Lua's byte string with a decoded-equivalent string
copy, not distinct byte-array and string input types. Compiled graph/calls/privacy
and failed snapshots use their public authority/state/query views. The direct
runtime creates canonical events and a separately derived observed index. Loaded
and public-JSON-reconstructed engines preserve those events and the twentieth
runtime response digest.

Generated-plan public helpers, freshly loaded emitted direct/traced modules and
native/generated traced helpers all compare values, events and observed-query
digests. An isolated fresh child independently loads the emitted file, executes
direct/traced calls and derives both digests. Parent assertions check child status 0,
empty stderr, exact results/events/digests and nonempty trace. Temporary work stays
under managed TMPDIR and cleanup commands succeed; these wrappers have no separate
post-cleanup absence assertion. Source aliases below use current-host loadfile,
not this isolated-child route.

All 20 neutral requests compare immutable input, typed/raw-neutral JSON, status,
canonical record/relation ids, diagnostic codes, completeness and full response
hashes. Privacy none/text modes retain null/redaction paths, Unicode text and digest
shape. Page and budget fixtures keep exact completion and diagnostics; contract/
operation errors and explanation steps agree. Repeated queries leave static
snapshots unchanged, and edits to detached responses/capabilities cannot alter
fresh queries. Source scans and response-text checks deny selected host/private
tokens. They are not universal dynamic noninterference measurements.

This consumer does not exercise thrown-nil callbacks or the six mislabeled
missing-field events. Its pass does not close .2.2 or .2.34. Existing raw-false
.2.15, shared explanation-budget startup .82 and source-correlation owners also
remain open despite all 20 governed fixture hashes passing.

# Seven source-boundary aliases

The alias consumer passes 638 assertions per host. It locks the exact neutral
seven-name mapping, all 92 canonical helper rows and the current 250 common names.
Aliases remain outside that common inventory. Both zero- and one-argument authored
forms resolve to one contract retaining source name, canonical name, arity and
canonicalization flag; this is contract resolution, not runtime arity admission.

The mappings are capture_from_rule_start→capture_slice,
capture_len_from_rule_start/capture_slice_length→capture_slice_len,
capture_rest_length→capture_rest_len, capture_slice_here→start_capture_slice,
entry_named_map→entry_map and match_named_map→match_map.

Normal and reversed fixtures compare alias and canonical spellings on native,
public-AST reconstruction, filesystem-loaded, generated-plan and emitted-module
routes. The emitted module is loaded and run inside the current host, with exact
metadata identity. Unicode input has a four-scalar prefix before the named ab
match; normal captures retain that prefix, lengths 4/4 and remaining 6. Starting the
anonymous boundary after matching makes the earlier span unavailable: capture
values/lengths are null and remaining length 0. Both named maps retain name=ab.
All four fixtures run on all five carriers, with exact alias/canonical equality.

An unrelated invented helper still produces the exact attributed runtime error,
diagnostic owner/stage/detail and rule/top-rule fields. The wrappers' result helper
returns only these array-valued fixtures; no all-value or false-result guarantee
is inferred. Existing private finite-coordinate and typed-source diagnostic
limitations remain separately owned.

# Staged prefix and Knowledge reconciliation

The staged prefix defines valid authored marker/production fixtures, read-only
helpers, v1 function-body job construction, v2 marker/sidecar/provenance expectations,
typed error helpers and neutral required-diagnostic-field checks. It then begins
an options constructor that continues in child 48. No helper expectations or staged
behavior fixture is called by the selected prefix. Loading exact 1–222 succeeds
with zero original assertions; historical full 890 evidence stays with its prior
owner and does not become fresh proof here.

Read [[lua-semantic-introspection-admission]],
[[lua-typed-source-compatibility-alias-gap]],
[[lua-staged-ast-enrichment-current-depth-authority]],
[[semantic-introspection-recurring-gate]] and [[typed-source-location-recurring-gate]].
Reuse the completely read static projection plan and existing staged .2.1/.2.25
qualifications. Existing .2.1 now owns the admission card's current 6/9/98 and
next-recurring wording, and the alias card's current 246 inventory and pending
typed-source RED pointers. Date historical boundaries and preserve their exact
408/638 fixture evidence; current successors are complete. No old card is edited.

Fresh semantic neutral proof is six fixtures, 20 response hashes, 128 mutations,
rollout 9/9 and native admission 6/6. Fresh typed-source proof is 14/0/231, including
92+7+2 vocabulary, 33 diagnostics, six recursive-observation public documents and
eight program-wide public documents with their unchanged guards. Neither checker
reruns every backend consumer or closes previously measured gaps in its oracles.

All eight native jobs and both neutral checkers finish successfully. Native
assertions total (122+408+638)×2=2,336, plus two helper loads with no original
behavior assertions. No full CI, all-toolchain recurring gate, PGEN/RGX build,
primary matrix or push runs. All 34 repair roots, known PUC5.5 nil failures,
native-error exclusions and parked named arguments remain unchanged.

# Exact replay

```bash
bash tools/project_data_run.sh python3 - <<'LUA_SEMANTIC_ALIAS_READING_47'
from pathlib import Path
p=Path('.linkedspec-data/scratch/lua147')
p.mkdir(parents=True, exist_ok=True)
(p / 'select-tests.py').write_text('from pathlib import Path\n\np=Path(\'.linkedspec-data/scratch/lua147\')\nlines=Path(\'lua/test/staged_ast_enrichment_contract_test.lua\').read_text().splitlines(keepends=True)\nassert lines[221].strip()==\'end\' and lines[223].startswith(\'local function enrichment_options\')\n(p/\'staged-helper-prefix.lua\').write_text(\'\'.join(lines[:222])+\'\\nassert(assertions == 0 and #failures == 0)\\nprint("staged enrichment helper prefix: loads; no original behavior assertions executed")\\n\')\nprint(\'Selected unchanged complete staged helper prefix1-222; partial options function and all behavior fixtures remain outside proof\')\n')
(p / 'verify.py').write_text("from pathlib import Path\nimport hashlib,json\n\np=Path('.linkedspec-data/scratch/lua147')\nfor row in json.loads((p/'scope.json').read_text()):\n    data=b''.join(Path(row['path']).read_bytes().splitlines(keepends=True)[row['start']-1:row['end']])\n    assert len(data)==row['bytes'] and hashlib.sha256(data).hexdigest()==row['sha256']\nfor host in ['puc','luajit']:\n    for name,expected in {\n        'remaining':'semantic static remaining assertions: 122\\n',\n        'admission':'ok - composed Lua semantic admission (408 assertions)\\n',\n        'aliases':'Lua source-boundary compatibility aliases: 638 assertions passed\\n',\n        'staged-prefix':'staged enrichment helper prefix: loads; no original behavior assertions executed\\n',\n    }.items():\n        assert (p/f'{name}-{host}.log').read_text()==expected,(name,host)\n        assert (p/f'{name}-{host}.exit').read_text()=='0\\n',(name,host)\nassert (p/'neutral-semantic.log').read_text()=='semantic introspection contract: 6 fixture groups, 20 exact queries, 128 rejected mutations, rollout 9 complete / 0 pending, admission 6 complete / 0 pending\\n'\nassert (p/'neutral-typed.log').read_text()=='typed-source-location-contract: OK (3 sources; 7 positions; 6 direct spans; 3 derived texts; 8+8+33 state transitions; 6 recursive observations; 4 structural cases; 92 helper projections + 7 aliases + 2 internal ids; 33 diagnostics; recursive-observation public 6 documents/6 forbidden/10 surface guards; program-wide public 8 documents/8 forbidden/6 safety/10 surface guards; 14 complete / 0 pending rollout; 231 drift mutations)\\n'\nprint('PASS: four exact source ranges; remaining122/admission408/aliases638 per host (2336 assertions), two staged helper loads without original behavior assertions, semantic6/20/128 rollout9/9 admission6/6 and typed14/0/231; all previous failures and repair owners remain open')\n")
(p / 'scope.json').write_text('[\n  {\n    "path": "lua/test/semantic_index_static_remaining_test.lua",\n    "start": 74,\n    "end": 337,\n    "bytes": 12332,\n    "sha256": "a7a34a4a8272fa5654e7a9fcd7b9fd0e2c756d7f8aa676c0d82b9b17236cc52a"\n  },\n  {\n    "path": "lua/test/semantic_introspection_lua_admission_test.lua",\n    "start": 1,\n    "end": 659,\n    "bytes": 26575,\n    "sha256": "9754ccd41d83f0986b497413b8a7fc983d44b9cfb1f19502ba89d1afde66b31a"\n  },\n  {\n    "path": "lua/test/source_boundary_compatibility_aliases_test.lua",\n    "start": 1,\n    "end": 337,\n    "bytes": 11657,\n    "sha256": "8ffec08f008c11d8cac520b3dbc7eaf8344e8eb873b5ee298ff124e7cf1a6f21"\n  },\n  {\n    "path": "lua/test/staged_ast_enrichment_contract_test.lua",\n    "start": 1,\n    "end": 240,\n    "bytes": 8185,\n    "sha256": "ee6f9941183ceb21be6ab3d4a1d30e18b7d4c9ff2d9f95b3361855d74cd21b7a"\n  }\n]\n')
LUA_SEMANTIC_ALIAS_READING_47
bash tools/run_python_project_data.sh .linkedspec-data/scratch/lua147/select-tests.py
for host in puc luajit; do
  for item in remaining admission aliases staged-prefix; do
    case "$item" in
      remaining) script=lua/test/semantic_index_static_remaining_test.lua ;;
      admission) script=lua/test/semantic_introspection_lua_admission_test.lua ;;
      aliases) script=lua/test/source_boundary_compatibility_aliases_test.lua ;;
      staged-prefix) script=.linkedspec-data/scratch/lua147/staged-helper-prefix.lua ;;
    esac
    status=0
    bash tools/run_lua_project_data.sh "$host" "$script" > ".linkedspec-data/scratch/lua147/$item-$host.log" 2>&1 || status=$?
    printf '%s\n' "$status" > ".linkedspec-data/scratch/lua147/$item-$host.exit"
  done
done
bash tools/run_python_project_data.sh tools/check_semantic_introspection_contract.py > .linkedspec-data/scratch/lua147/neutral-semantic.log 2>&1 || exit
bash tools/run_python_project_data.sh tools/check_typed_source_location_contract.py > .linkedspec-data/scratch/lua147/neutral-typed.log 2>&1 || exit
bash tools/run_python_project_data.sh .linkedspec-data/scratch/lua147/verify.py
```

Related: [[lua-semantic-foundation-observation-fixture-reading]],
[[lua-startup-reading-coverage]], [[lua-semantic-static-projection-plan]].

# Preservation and focused completion

Independent preservation retains 1,424 prior source/Knowledge/decision/history/
policy files and 2,582 of 2,585 prior task nodes byte-exactly. Only this reading
leaf, existing .2.1 guidance and startup .3.6 change; no node is added. All ninety-one
Known book headings and the parked authoring tree remain exact. Three embedded
payloads match scratch, and independent reconstruction verifies every owned range
and all 47 completed groups with unchanged inventory/range/group digests.

History validation passes 34 mutation controls, three surfaces and 68 segments;
both pressure checks pass at Changes 326 and Notes 256 lines. Memory remains 60 lines
with correct activation and handoff semantics. Knowledge regeneration reports
1,133 cards /9,051 question keys. The book renders with its existing large-search-
index warning at 10,178,753 bytes, still owned by startup .41.9. Whitespace checks
pass. Normal doctrine and activation hooks remain required at landing; no full CI
or push is claimed here.
