---
id: startup-public-teaching-checker-blind-spots
title: "Startup reading found concrete public teaching gaps behind passing checkers"
answers:
  - "which startup tasks own contradictory book prose missed by public checkers"
  - "why do public no-drift checks pass stale logical and mutation paragraphs"
  - "why does the book introduction still call parse_job unavailable"
  - "why do old semantic rollout counts pass the public checker"
  - "which repair owns complete mdBook alignment after full startup reading"
  - "why does TOOLBOX still say Perl rejects standalone lifecycle blocks"
  - "which task corrects stale bare-block debugging instructions"
  - "why do stale cursor staged semantic and gap paragraphs survive public checks"
  - "does document input need to look like a spec source"
  - "are definedness helpers condition only in Perl return contexts"
  - "does exit_now terminate the Perl host process"
  - "does cat treat a null argument as empty text"
date: 2026-09-06
status: dated diagnostic evidence; repair state belongs to the owning task-tree
tags: ["startup-reading","book","contracts","verification"]
evidence: "SESSION-STARTUP-READING.31 preserves the recorded Toolbox/source controls at reading baseline baeb984e36a94a15951cd23d4c52def5064cdaca. The owning task is SESSION-STARTUP-READING.28. No implementation repair or whole-project signoff is claimed."
reverify:
  - "git diff baeb984e36a94a15951cd23d4c52def5064cdaca -- tools/check_logical_helper_contract.py tools/check_mutation_public_surface.py tools/check_staged_ast_enrichment_contract.py tools/check_semantic_introspection_contract.py tools/check_diagnostic_output_contract.py"
  - "bash tools/run_python_project_data.sh tools/check_logical_helper_contract.py"
  - "bash tools/run_python_project_data.sh tools/check_mutation_public_surface.py"
  - "bash tools/run_python_project_data.sh tools/check_staged_ast_enrichment_contract.py"
  - "bash tools/run_python_project_data.sh tools/check_semantic_introspection_contract.py"
  - "bash tools/run_python_project_data.sh tools/check_diagnostic_output_contract.py"
---

# Startup reading found concrete public teaching gaps behind passing checkers

This is the September 6 intake observation at the stated baseline. Current repair state and acceptance belong to
[SESSION-STARTUP-READING.28](docs/tasks/SESSION-STARTUP-READING.md), rather than a duplicated completion counter.

Five public checkers passed while their governed teaching retained specific false current claims. The intake records each actual paragraph, the checker function or scope that accepts it, and independent authority or runtime controls.

- `.28.1`: stale string-zero/empty-container truthiness and pending rollout text; exact denial coverage misses it.
- `.28.2`: stale remaining-backends hash-selector interpretation; whitespace is already normalized, but the actual denial is missing.
- `.28.3`: the introduction calls portable parse_job unavailable; that page is absent from the staged public-authoring reader set.
- `.28.4`: an included semantic page says the ledgers are now 3/9 and 2/6; the actual checker accepts it despite current 9/9 and 6/6 authority.
- `.28.5`: returned and nested definedness work on Perl despite condition-only catalog entries. True/false results were 1/empty string; no portable encoding promise follows. cat with null or an array returns null; the empty-string twin produces concatenated text.
- `.28.6`: exit_now raises a typed parse-control object while the host can continue; the catalog's process-exit sentence passes diagnostic public checks.

Each child requires the real bad paragraph and controlled variants, honest historical scope, appropriate executable examples, and the necessary checker repair. Passing finite checks is recorded as their measured scope, not evidence that these paragraphs are correct.

Sources: `tools/check_logical_helper_contract.py`, `tools/check_mutation_public_surface.py`, `tools/check_staged_ast_enrichment_contract.py`, `tools/check_semantic_introspection_contract.py`, `tools/check_diagnostic_output_contract.py`.

## Complete-book reading extends the measured inventory

`SESSION-STARTUP-READING.3.2.42` preserves complete physical reading of all 50 tracked book files
at the same baseline: 1,956,582 bytes, including configuration and SUMMARY. Formal alignment remains
`.4`-owned; `.41.1`–`.41.8` own the additional bounded repair work. No repair, rendered review,
or whole-codebase completion is implied.

| Repair owner | Actual current prose that needs repair | Why existing public checks miss it |
| --- | --- | --- |
| `.41.1` | Design rationale 23–27 and pipeline 111–124/236–241/258–263 call general staged dispatch future; handoff 693–696 explicitly says current parse_job is unsupported. | The staged reader's six paths omit design rationale and pipeline. Handoff is included, but its sentence is outside the exact stale-denial strings. |
| `.41.2` | Regex 133–142 describes caller-selected parse modes; blind-call introduction and generated-handlers 53–63 retain pending migration/v1-adapter claims. Runtime semantics 619–622 calls named selectors future. | The cursor contract governs the regex page only through two link-title markers; blind-call/generated-handler pages are omitted. Current cursor and gap checks pass. |
| `.41.3` | Descriptor introspection 96–99 calls native semantic APIs future despite current six-runtime admission. | The semantic reader requires selected markers and denials that do not cover this paragraph. |
| `.41.4` | Descriptor introspection 327–330 calls Dart gap admission pending; diagnostics 163–165 calls Lua dormant. Capture guide 919–921 presents old 4/5/57 status as current. | The exact eight-document gap reader omits descriptor introspection and diagnostics, and its required markers do not validate the whole current-status narrative. |
| `.41.5` | Get API 454–455 and pipeline 490 describe incorrect mode-only return/processing behavior. Pipeline 636–637 places leading-trivia skipping before every match/handler; runtime appendix 10.3 imposes spec-source requirements on document input. | These boundaries require the public/direct and combined-option controls below; unrelated admission success cannot establish the prose. |
| `.41.7` | Local CI recommends a full gate for every behavior change and describes the default as independent of backend toolchains. Handoff/local CI repeat 17 capabilities/85 states as current. | Current ADR 0073 uses focused ordinary commits; the observed canonical run executes mandatory native admission and reports 20 capabilities/100 states. Existing `.29` and policy `.5` retain authority for actual gate changes. |

Line references identify the unchanged reading baseline, not shifting edited lines. Explicitly dated v1,
rollout, and count observations must remain historical evidence. Current all-six staged admission does not
authorize ambient provider loading or import composition; current typed-source admission does not imply
that the excluded public typed-span API exists.

The checker mechanism was read directly: staged fixed reader paths/marker and denial loops
(`tools/check_staged_ast_enrichment_contract.py` 418–425/1557–1567), cursor public reader/check loops
(`tools/check_rule_local_cursor_contract.py` 1131–1154), semantic public checks
(`tools/check_semantic_introspection_contract.py` 445–491), and gap public checks
(`tools/check_inter_match_gap_capture_contract.py` 1090–1120). Their exact neutral/public inventories
pass in the frozen `.3.2.41` canonical candidate while these unchanged paragraphs remain present.
That is finite proof of a claim-coverage gap, not evidence that all unexamined assertions are false.

Four public/direct input controls compile `Top:: I { return(cursor_pos()) }` in the legal multiline
form. Empty input, ordinary `not a spec` text, and a leading ordinary space return zero without errors.
Input `\n# lead\nx` returns eight publicly and zero when the same descriptor's Top handler is invoked
directly from cursor zero. All direct controls return zero with no error. The wrapper's Runtime 115–154
skip loop and Compiler's scalar-reference guard explain these observations. They reverify the boundary
already recorded in [[ds-vhistory-leading-newline-oracle-boundary]].

Nine public compiler controls and eight isolated factory controls separately establish a combined-mode
false diagnostic, owned by `.42` and recorded in [[perl-get-mode-result-validation-precedence-drift]].
That runtime bug is distinct from correcting the book's mode-only description.
A separate forward static-projection control finds explicit inline I record loss, owned by .43 in
[[perl-semantic-inline-header-lifecycle-loss]]; stale admission counts do not establish that runtime defect.

Remaining lifecycle/helper/representation assertions under `.41.6` are assessment candidates only.
The Rust compatibility execute wrapper was checked against its documented direct-value route and is not
classified as an output defect. No unmeasured cross-runtime behavior is inferred from Perl controls.

Reverify the added current checkers through repository-managed Python:

```sh
bash tools/run_python_project_data.sh tools/check_rule_local_cursor_contract.py
bash tools/run_python_project_data.sh tools/check_inter_match_gap_capture_contract.py
```

The exact input-boundary control is:

```sh
bash tools/project_data_run.sh env PERL5LIB= perl -Iperl -MLinkedSpec -MJSON::PP - <<'PERL'
use strict;use warnings;
LinkedSpec::configure_trace(level=>'none');
my $j=JSON::PP->new->canonical;my $spec="Top::\n I { return(cursor_pos()) }\n";my(%ctx,%dctx);
my $parser=LinkedSpec::Get(\$spec,runtime_ctx_ref=>\%ctx);die 'compile parser' unless ref($parser) eq 'CODE' && !$ctx{last_error};
my $descriptor=LinkedSpec::Get(\$spec,return_descriptor=>1,runtime_ctx_ref=>\%dctx);die 'compile descriptor' unless ref($descriptor) eq 'HASH' && !$dctx{last_error};
for my $case (['empty','',0],['ordinary','not a spec',0],['leading_blank_comment',"\n# lead\nx",8],['leading_space',' x',0]) {
 my($name,$text,$expected)=@$case;my $copy=$text;
 my $public=$parser->(\$text);die "public $name" unless defined($public)&&$public==$expected&&!$ctx{last_error};
 pos($copy)=0;my $direct=$descriptor->{spec}{Top}{handler}->($descriptor,\$copy);
 die "direct $name" unless defined($direct)&&$direct==0&&!$dctx{last_error};
 print $j->encode({case=>$name,public_cursor=>$public,direct_handler_cursor=>$direct,public_error=>$ctx{last_error},direct_error=>$dctx{last_error}}),"\n";
}
PERL
```

## Toolbox lifecycle guidance omitted from the current reader

`SESSION-STARTUP-READING.3.2.44` checks TOOLBOX section 1 against the current
[[standalone-lifecycle-block-audit]] authority and the four .3.2.42 public inline/multiline explicit/bare
controls, which all execute return(7). TOOLBOX still says current Perl must reject a rule-item-leading
brace and describes normalization as future. This is stale guidance, not an unresolved behavior choice.

The standalone checker passes 9 placements / 4 duplicate forms / 6 ownership cases / 3 malformed twins /
6 runtime routes / 15 public documents / 7 stale-claim denials / 14 mutations. Its public reader at
`tools/check_standalone_lifecycle_block_contract.py` 67–80 reads only contract-listed paths; the exact
15-document list omits TOOLBOX. Thus this passing public proof cannot validate that instruction.
Startup .41.6 now explicitly owns its correction and actual-claim/path coverage alongside related
lifecycle teaching. Other assessment candidates there remain unverified until their own controls.
No TOOLBOX, checker, contract, or public-book edit is part of this reading checkpoint.

Reverify with `bash tools/run_python_project_data.sh tools/check_standalone_lifecycle_block_contract.py`
and the exact public lifecycle matrix in [[perl-semantic-inline-header-lifecycle-loss]].
