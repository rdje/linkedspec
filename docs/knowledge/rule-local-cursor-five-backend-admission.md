---
id: rule-local-cursor-five-backend-admission
title: "Rule-local cursor parity has one recurring six-runtime admission gate"
answers:
  - "is recurring five backend rule local cursor admission complete"
  - "how do I run the five backend cursor gate"
  - "which runtimes does the recurring cursor gate execute"
  - "which primary cases does the recurring cursor gate select"
  - "what canonical CI switch runs cursor parity"
  - "what is the current rule local cursor rollout"
  - "how many cursor migration files are governed"
  - "how many cursor drift mutations are rejected"
date: 2026-07-19
status: recurring five-backend admission complete; rollout 7 complete / 1 pending
tags: [cursor, rule-family, recurring-gate, backend-parity, generated-source, primary-cli, FUTURE-PARITY-BACKLOG]
evidence: "FUTURE-PARITY-BACKLOG.9.1.8 adds `tools/check_rule_local_cursor_five_backend.sh`. One invocation runs the neutral checker, the 14-role Perl consumer, exact 15-role Rust/Dart/Julia consumers, the same exact 15-role Lua consumer on PUC Lua and LuaJIT, five shared primary cases across five commands and default/POSIX environments, and generated-source/capability/language-coverage ledgers. `LINKEDSPEC_RUN_CURSOR_MATRIX=1` exposes the all-toolchain gate from canonical local CI while ordinary CI syntax-checks and requires the driver. The neutral checker locks six runtime commands, exact role lists, case identities, support checks, canonical registration, and seven recurring-gate mutations. Governance is 72 migration files, 7 complete / 1 pending, and 56 rejected mutations. No compiler, runtime, descriptor, or generated-plan behavior changes."
evidence_update_2026_07_19_runtime_proof: "The composed driver passes Perl 288, Rust and Dart exact admission consumers, Julia 104, PUC Lua 119, LuaJIT 119, the selected 5x2x5 primary projection, generated-source v1 support at 80/0/0, capability 80/0/0, and language coverage 246/105+1/122."
evidence_update_2026_07_19_signoff: "Knowledge Map 635/4,670, mdBook, memory, all four doctrines, canonical primary 65/65 in both environments, and Phase 0 1,031/1,031 in 631 seconds pass. Exact cleanup leaves no Rust target, Dart tool cache, Julia depot, mdBook build, Python cache, or temporary LinkedSpec harness output."
evidence_update_2026_07_20_julia_duplicate_slot: "Julia duplicate-slot admission adds one parse-mode-aware proof file to the cursor migration inventory under its existing Julia owner. Current governance is 73 files / 7 complete + 1 pending / 56 mutations; the recurring runtime topology and behavior remain unchanged."
reverify: "bash tools/check_rule_local_cursor_five_backend.sh"
---

# Recurring rule-local cursor admission

The final backend implementation already existed before this leaf. The recurring gate adds no cursor resolver,
execution route, descriptor variant, or generated-source format. It composes the admitted projections and makes
an omission or asymmetric runtime leg fail mechanically.

The six runtime legs are Perl, Rust, Dart, Julia, PUC Lua, and LuaJIT. The selected primary projection is exactly
`help`, `usage_removed_parse_mode`, `success_default_rule_seeks`, `success_and_rule_consumes`, and
`trace_stdout_medium`, run across all five commands with `POSIXLY_CORRECT` unset and set.

`recurring_five_backend_gate` is complete. `public_no_drift` remains pending under
`FUTURE-PARITY-BACKLOG.9.1.9`, after the separately owned identical dependency-regex identity decision
`.9.1.8.1`. Therefore the current cursor ledger is 7 complete / 1 pending, not program closure.

Related: [[rule-local-cursor-neutral-contract]], [[rule-local-cursor-and-bare-edge-contract]], and
[[root-rule-selection-five-backend-admission]].
