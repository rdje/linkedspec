---
id: lua-stdio-progressive-recognition-consumer-reading
title: Lua stdio progressive storage and transaction consumers retain their exact private boundaries
answers:
  - "what exact Lua source did startup reading child 34 cover"
  - "did Lua reading child 34 execute pre emission stdio cancellation"
  - "which Lua progressive and recognition carriers passed during startup reading"
  - "what does the Lua project storage oracle verify on both runtimes"
  - "does the Lua storage rejection probe create an external output directory"
  - "which recursive observation prefix proves five lowering assertions"
  - "which progressive transaction and storage guidance remains owned for qualification"
date: 2026-09-13
status: exact scoped reading and focused consumers pass; recursive runtime suffix and previous repairs remain open
tags: [lua, stdio, progressive-parsing, recognition, storage, observation, startup, evidence]
evidence: "LUA-STARTUP-READING.1.34 activates from 3c7503a02e0c4560eef95dd0952c4fb0f76035f3. Ten complete windows read 1500 fragments /59616 bytes. Both installed hosts pass stdio247, progressive178, recognition246 and recursive-lowering5: 1352 assertions. The separate storage oracle passes19 owners and three native modules per ABI with exact cleanup. Current neutral progressive and recognition proof passes; existing .2.1 owns precise guidance qualification. No production change."
reverify: "Run LUA_STDIO_RECOGNITION_READING_34 and the managed commands below; recursive-observation proof is limited to lowering."
---

# Exact reading

| Repository source | Inclusive lines | Fragments / bytes | Raw SHA-256 |
| --- | --- | --- | --- |
| lua/test/mcp_server_lua_stdio_test.lua | 194–468 | 275 /13402 | 04d7028d26aafec9b1ebfd3fd41061c62e3787bd61e96599f98698ed2deed956 |
| lua/test/progressive_span_dispatch_contract_test.lua | 1–464 | 464 /19030 | 10ad5e29d40a8a2d486c8842850cf1528aba9797bd4f16d3652cd5db9eef926b |
| lua/test/project_data_storage_test.lua | 1–48 | 48 /1868 | 25737d69fc2e0ff197e148d89a0e51547cbbe69c9607f9970328050e0a29f1c9 |
| lua/test/recognition_transaction_contract_test.lua | 1–571 | 571 /20493 | 463f5d0a74e6490da0b39f1142199770033381257304bc36a5e021bdec77607c |
| lua/test/recursive_observation_contract_test.lua | 1–142 | 142 /4823 | e78e666d3de04e6420e4403ba518821f0d47ab1fbe858edf0b8b355efefa6d27 |

Stdio windows are 194–335 and336–468; progressive windows are 1–175, 176–340
and341–464; storage1–48 is one window; recognition windows are 1–195, 196–390
and391–571; recursive1–142 is one window. All ten source outputs were complete.
Ordered range SHA-256 is
`4aaa093775ad1c374ee3383eaa7fed860790bcab924804476f630f69ebb549ee`.
All 99 Lua sources remain baseline-identical to
`baeb984e36a94a15951cd23d4c52def5064cdaca`. Reading reaches 34/51 groups,
46,651 fragments /1,742,358 bytes and 66 complete files. Recursive observation
remains partial, ending inside the first invalid-form fixture string.

Auxiliary proof selection reads all 196 lines of
`tools/test_lua_project_data_storage.sh` in two complete windows, 1–150 and151–196.
This does not advance the later supporting-code inventory. A combined Knowledge
read was truncated; both affected cards were then reread separately in full before
their conclusions were used. No scoped source reading was truncated.

# Complete stdio proof

The suffix exercises duplicate decoded keys, surrogate/NaN/overflow/empty-frame
rejection, depth64 lexical admission followed by root rejection, exact one-MiB
frames with CRLF, integer-only request IDs and safe integer endpoints, 128-byte
UTF-8 string IDs, and unconstrained fractional metadata. Token-kind metadata keeps
fraction/exponent values invalid in integer-only query fields on both runtimes.
Malformed and overlong frames drain to LF and recover; a complete final EOF frame
is accepted. Mixed discovery/list/capabilities/query input emits exact canonical LF
frames and releases handles and active requests at EOF.

The actual before_wire_emit callback now executes: it cancels the prepared request,
proves no bytes are emitted and verifies request release. Late cancellation cannot
retract successfully flushed output. Clean EOF avoids unnecessary flushes; injected
read/write/flush/log failures retain fixed diagnostics and release state without
closing caller streams. Invalid stream/log aliasing, authorization and options
reject before any input read. Source checks retain narrow module dependencies.
The complete consumer passes 247 assertions per installed host, including the
private pre-emission case distinguished from marker-only composition in child33.

# Progressive carriers and recognition transactions

The progressive consumer checks the current complete nine-row neutral ledger,
counts and exact ordinary/canonical path registration. Staged parse-job dispatch
remains a separate registry. One exact literal-parser/literal-rule/bare-span
assignment becomes the dedicated progressive node, with no residual generic call,
serialized fingerprint or seed. Eight malformed/static-effect forms reject before
execution, including recognition through rule or user-function closure.

A private host registry supplies the bounded callback, fingerprint, allowed top
rule, capabilities and ceilings. The seed copies decoded input, starts fresh
execution state and retains narrowing authority. Missing seed and a live recognition
token reject with their intended classifications. Native/reconstructed/generated
and independently loaded emitted source return the same detached child result;
the native and reconstructed parent cursor remains unchanged. Reusing a native
engine starts fresh execution. Emitted source contains no callback, registry,
fingerprint, source snapshot or cancellation object; a live seed enters only at
execution. Emitted code loads in the current host, not a fresh child process.
The carrier passes 178 per host. The separate dormant 273-case authority and the
earlier nested-step limitation under startup .37.1 are not rerun or closed here.

The recognition consumer checks the complete ledger, four dedicated nodes and
separate cursor-stack behavior. Invocation IDs and mark generations increase,
same-label child marks start empty, parent marks and detached snapshots stay
independent, and expired frame access diagnoses correctly. Positive tokens preserve
match presence separately from false/zero/empty/null payloads. Commit/rollback
restore the specified cursor, boundary and marks. Escape attempts, missing or
repeated attempts, wrong invocation/source, reused or non-token terminals, nested
checkpoints and unfinished unwind produce exact typed diagnostic fields.
Discard restores the snapshot. Six effect graphs and eight progress fixtures
retain the neutral policy. Native, reconstructed, generated and loaded emitted
execution all preserve successful false payloads. The complete consumer passes
246 assertions per host; it does not add any private authority to the facade.

# Storage and recursive prefix

The 48-line storage consumer requires its output root below routed TMPDIR, performs
a real native parse returning ssd, writes generated-v2 source and checks its marker,
then writes and rereads exact trace bytes. The existing storage oracle executes
that same source on both ABIs. It inventories 19 allocation owners, verifies managed
run/device relationships and routed temporary use, builds each three-module native
set in a directory containing a space, rejects symlinks/device drift and checks
generated-source and trace devices. The modules are PCRE2, filesystem and MCP system.

The external rejection probe reads filesystem-device metadata for existing OS
locations solely to select an alternate volume. It passes a nonexistent destination
to the already guarded builder, requires failure before creation, verifies absence
and the exact boundary diagnostic, and checks that no old Lua-owned directory remains
in that selected root. No project data is written off-volume. Necessary compiler,
SDK, header/library and installed-runtime reads retain the documented toolchain
exception. The oracle deletes only its exact repository-owned probe/native roots
and verifies their absence. Both runtime smoke checks and the final19/3 marker pass.

The recursive prefix defines observation capture, expected detached record shape
and the authored false-payload/Unicode-child fixture, then checks dedicated lowering:
one node, bare target, static child, no eager Child call and one serialized node.
The selector copies source through line138, closes that test block and checks all
five assertions. It excludes the later invalid fixtures, runtime observations,
bind-before-error paths and emitted execution. Prior full43 evidence stays dated.

# Knowledge and repair reconciliation

Retrieved [[lua-progressive-span-dispatch-recomposition]],
[[lua-progressive-span-dispatch-admission]], [[lua-progressive-span-dispatch-carriers]],
[[lua-recognition-transaction-admission]], [[lua-recognition-transaction-integration]],
[[lua-project-data-ssd-storage]] and [[lua-recursive-observation-admission]].
The MCP owners retrieved in child33 remain authoritative without re-derivation.

Existing `.2.1` now owns the progressive admission/carrier cards' pending recurrence
and public metadata, superseded by the complete recomposition successor; the
transaction integration card's current243 sentence, superseded by current246;
and the storage card's two-module wording, superseded by its actual three-module
oracle. Preserve dated implementation counts and private interfaces. No old card,
source or neutral contract is edited here. Recursive runtime/card reconciliation
retains its later complete-consumer owner.

Fresh proof is 247+178+246+5=676 per installed host, 1,352 assertions total, plus
the two storage runtime checks and the full storage oracle. Progressive governance
passes nine complete rows, zero pending, 116 contract mutations and public6/12/10/60.
Recognition passes138 ActionIR rows,250 calls,58 mutations,9/9 rollout, public3/26/45,
guide1/14/18 and Lua private-authority22/integration19/admission22 mutations.
All selected processes finish successfully. All thirty-three repair roots, prior
PUC5.5 nil-error and public-selector failures, pending PUC5.4 proof, startup gates
and parked named arguments remain unchanged. No full CI, PGEN/RGX build, push,
dependency change or exhaustive recursive runtime proof is claimed.

# Exact replay

```bash
bash tools/project_data_run.sh python3 - <<'LUA_STDIO_RECOGNITION_READING_34'
from pathlib import Path
p = Path('.linkedspec-data/scratch/lua134')
p.mkdir(parents=True, exist_ok=True)
(p / 'select-tests.py').write_text('from pathlib import Path\nsource = Path("lua/test/recursive_observation_contract_test.lua").read_text().splitlines(keepends=True)\nassert source[136].strip() == \'check_equal(serialized_count, 1, "serialized observation node is exact once")\'\nassert source[138] == "  local fixtures = {\\n"\nPath(".linkedspec-data/scratch/lua134/recursive-prefix.lua").write_text("".join(source[:138]) + \'end\\nassert(#failures == 0, table.concat(failures, " | "))\\nassert(assertions == 5, "recursive prefix count drift: " .. assertions)\\nprint("recursive lowering prefix: " .. assertions .. " assertions passed")\\n\')\n')
(p / 'verify.py').write_text("from pathlib import Path\nimport hashlib, json\n\np = Path('.linkedspec-data/scratch/lua134')\nfor row in json.loads((p / 'scope.json').read_text()):\n    data = b''.join(Path(row['path']).read_bytes().splitlines(keepends=True)[row['start'] - 1:row['end']])\n    assert len(data) == row['bytes']\n    assert hashlib.sha256(data).hexdigest() == row['sha256']\nfor host, runtime in [('puc', 'puc-lua'), ('luajit', 'luajit')]:\n    assert (p / f'stdio-{host}.log').read_text() == '[lua-mcp-stdio] PASS: 247 framing, lexical, canonical, cancellation, lifecycle, and authority assertions\\n'\n    assert (p / f'progressive-{host}.log').read_text() == f'Lua progressive span-dispatch contract: 178 assertions passed on {runtime}\\n'\n    assert (p / f'recognition-{host}.log').read_text() == f'Lua admitted recognition-transaction contract: 246 assertions passed on {runtime}\\n'\n    assert (p / f'recursive-{host}.log').read_text() == 'recursive lowering prefix: 5 assertions passed\\n'\nassert (p / 'storage.log').read_text() == (\n    '[lua-project-data-runtime-test] PASS: generated source and trace use routed storage\\n' * 2\n    + '[lua-project-data-test] PASS: 19 owners, three dual-ABI native modules, generated output, and traces stay on repository storage\\n'\n)\nprogressive = (p / 'neutral-progressive.log').read_text()\nfor value in ['rollout 9/9 complete, 0 pending', '116 contract mutations', 'public 6 documents/12 forbidden/10 outward/60 mutations']:\n    assert value in progressive, value\nrecognition = (p / 'neutral-recognition.log').read_text()\nfor value in ['138 ActionIR rows = 134 current + 4 dedicated', '250 call rows', '58 rejected mutations', 'rollout 9/9 complete', 'public sequence 3 documents/26 forbidden/45 mutations', 'capability guide 1 document/14 forbidden/18 mutations', 'Lua private authority 22 mutations', 'Lua private integration 19 mutations', 'Lua admission 22 mutations']:\n    assert value in recognition, value\nprint('PASS: exact source ranges; stdio247 + progressive178 + recognition246 + recursive-lowering5 per host, 1352 assertions; dual-ABI storage19/3 and current neutral progressive/recognition proof')\n")
(p / 'scope.json').write_text('[\n  {\n    "path": "lua/test/mcp_server_lua_stdio_test.lua",\n    "start": 194,\n    "end": 468,\n    "bytes": 13402,\n    "sha256": "04d7028d26aafec9b1ebfd3fd41061c62e3787bd61e96599f98698ed2deed956"\n  },\n  {\n    "path": "lua/test/progressive_span_dispatch_contract_test.lua",\n    "start": 1,\n    "end": 464,\n    "bytes": 19030,\n    "sha256": "10ad5e29d40a8a2d486c8842850cf1528aba9797bd4f16d3652cd5db9eef926b"\n  },\n  {\n    "path": "lua/test/project_data_storage_test.lua",\n    "start": 1,\n    "end": 48,\n    "bytes": 1868,\n    "sha256": "25737d69fc2e0ff197e148d89a0e51547cbbe69c9607f9970328050e0a29f1c9"\n  },\n  {\n    "path": "lua/test/recognition_transaction_contract_test.lua",\n    "start": 1,\n    "end": 571,\n    "bytes": 20493,\n    "sha256": "463f5d0a74e6490da0b39f1142199770033381257304bc36a5e021bdec77607c"\n  },\n  {\n    "path": "lua/test/recursive_observation_contract_test.lua",\n    "start": 1,\n    "end": 142,\n    "bytes": 4823,\n    "sha256": "e78e666d3de04e6420e4403ba518821f0d47ab1fbe858edf0b8b355efefa6d27"\n  }\n]\n')
LUA_STDIO_RECOGNITION_READING_34
bash tools/run_python_project_data.sh .linkedspec-data/scratch/lua134/select-tests.py
for host in puc luajit; do
  bash tools/run_lua_project_data.sh "$host" lua/test/mcp_server_lua_stdio_test.lua > ".linkedspec-data/scratch/lua134/stdio-$host.log" 2>&1 || exit
  bash tools/run_lua_project_data.sh "$host" lua/test/progressive_span_dispatch_contract_test.lua > ".linkedspec-data/scratch/lua134/progressive-$host.log" 2>&1 || exit
  bash tools/run_lua_project_data.sh "$host" lua/test/recognition_transaction_contract_test.lua > ".linkedspec-data/scratch/lua134/recognition-$host.log" 2>&1 || exit
  bash tools/run_lua_project_data.sh "$host" .linkedspec-data/scratch/lua134/recursive-prefix.lua > ".linkedspec-data/scratch/lua134/recursive-$host.log" 2>&1 || exit
done
bash tools/test_lua_project_data_storage.sh > .linkedspec-data/scratch/lua134/storage.log 2>&1
bash tools/run_python_project_data.sh tools/check_progressive_span_dispatch_contract.py > .linkedspec-data/scratch/lua134/neutral-progressive.log 2>&1
bash tools/run_python_project_data.sh tools/check_recognition_transaction_contract.py > .linkedspec-data/scratch/lua134/neutral-recognition.log 2>&1
bash tools/run_python_project_data.sh .linkedspec-data/scratch/lua134/verify.py
```

Related evidence: [[lua-mcp-consumer-reading]], [[lua-startup-reading-coverage]].

# Candidate verification

Independent preservation passes for 1,409 source, prior Knowledge, decision,
immutable-history and policy files. Of 2,582 old task nodes, 2,579 are byte-identical;
only this reading leaf, existing guidance repair .2.1 and startup .3.6 change.
No repair node is added. All 90 book limitation headings, the parked authoring tree,
history preambles/suffixes and live-history pointer remain intact. All three embedded
replay payloads match their executed files. The independent full inventory/range
replay passes at 34 completed groups.

Memory remains 60 lines and phase/handoff checks pass. Shared history passes
34 mutation controls and three surfaces/66 segments. Changes is 452 lines /
33,599 bytes (within cap, pressure warning); notes is 382 lines /31,695 bytes.
Knowledge generation produces 1,120 cards and 8,959 question keys. The book renders
successfully; its existing 10,132,371-byte search-index warning remains owned by
startup .41.9. Whitespace checks pass; normal per-leaf hooks govern landing.
