---
id: root-rule-selection-five-backend-admission
title: "Root-rule selection closes through one recurring six-runtime and public no-drift gate"
answers:
  - "is root rule selection parity complete"
  - "what is the final root rule selection rollout"
  - "how do I run the five backend root selection gate"
  - "which backends and Lua runtimes does the root selection gate execute"
  - "which primary cases does the recurring root selection gate execute"
  - "does root selection governance require ROADMAP.md and ROADMAP_V2.md"
  - "how many root selection public documents are checked"
  - "how many root selection drift mutations are rejected"
  - "what canonical CI switch runs root selection parity"
date: 2026-07-19
status: final recurring/public admission complete; rollout 7 complete / 0 pending
tags: [root-rule, top-rule, entry-selection, recurring-gate, public-no-drift, backend-parity, roadmap, FUTURE-PARITY-BACKLOG]
evidence: "FUTURE-PARITY-BACKLOG.9.1.1.2.6 adds `tools/check_root_rule_selection_five_backend.sh`. One invocation runs the neutral checker, Perl core and routes, the exact 15-role Rust/Dart/Julia consumers, the same exact 15-role Lua source on PUC Lua and LuaJIT, six shared primary cases across five commands and default/POSIX environments, and generated-source/capability/language-coverage ledgers. `LINKEDSPEC_RUN_ROOT_RULE_MATRIX=1` exposes that all-toolchain gate from canonical local CI while ordinary CI syntax-checks and requires the driver. The executable public contract requires 25 current documents including ROADMAP.md and ROADMAP_V2.md, rejects 19 stale current claims, and raises total drift mutations from 44 to 54. No backend resolver or generated-plan shape changes."
evidence_update_2026_07_19_signoff: "The full recurring driver passes all six runtime legs, the selected 5x2x6 primary projection, and support ledgers. Canonical CI passes the complete 65-case Perl reference suite in default and POSIX environments plus Phase 0 1,031/1,031 in 642 seconds. Cleanup removes only reproducible generated outputs and preserves tracked issue evidence."
evidence_update_2026_08_30_current_projection: "README-STABILITY-POLICY.1 removed root README from the changing root-status surface and its two denials, taking the live inventory from the dated 25/19 closeout to 24/17. FUTURE-PARITY-BACKLOG.23.2 binds the formal grammar's current all-six-runtime markerless-selection claim and rejects its obsolete Lua-topology warning, yielding current 24 documents / 18 denials while rollout 7/0 and 54 mutations remain exact."
reverify: "bash tools/check_root_rule_selection_five_backend.sh"
---

# Final root-rule selection admission

Root-selection semantics remain explicit selector first, first authored `Rule::` second, and first authored
ordinary `Rule:` when no marker exists. The final leaf does not introduce another resolver. It composes every
already-admitted route and makes the public state executable.

The recurring gate treats PUC Lua and LuaJIT as distinct runtime legs over one shared consumer source. Its primary
projection selects exactly the first-marker, markerless, explicit, unknown-selector, default-request-trace, and
escaped explicit request-trace cases from the shared 65-case manifest.

Both roadmap projections are now required current-state inputs. A green root checker therefore means semantic,
topology, recurring-driver, public-doc, and rollout state agree at 7 complete / 0 pending.

Related: [[root-rule-selection-precedence]], [[root-rule-rollout-roadmap-projection]], and
[[lua-root-rule-selection-admission]].
