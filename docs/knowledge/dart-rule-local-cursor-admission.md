---
id: dart-rule-local-cursor-admission
title: Dart is admitted against the complete rule-local cursor contract
answers:
  - "is Dart admitted for rule local cursor behavior"
  - "where is the Dart rule local cursor admission consumer"
  - "how many Dart cursor admission roles exist"
  - "what does the Dart cursor admission consumer prove"
  - "how is Dart cursor admission registered in local CI"
  - "what is the cursor rollout after Dart admission"
  - "how many rule local cursor migration files exist after Dart admission"
  - "how many rule local cursor mutations run after Dart admission"
date: 2026-07-18
status: fully verified and canonically admitted
tags: [dart, cursor, contract, admission, topology, diagnostics, FUTURE-PARITY-BACKLOG]
evidence: "FUTURE-PARITY-BACKLOG.9.1.5.6 adds dart/test/rule_local_cursor_contract_test.dart as one contract-declared 15-role consumer. It executes native default/AND, normalized JSON, loaded spec, descriptor v1, emitted/generated v2 direct/trace, mixed parent-child, recursion, both structural replacements, static option removal, primary retirement, and all eight portable diagnostics. The neutral checker requires one marker per role, the complete Dart test command, canonical tracked input, and optional backend registration; five new mutations cover the Dart topology and rollout. The registered Dart driver passes 271 package tests, primary 65x2, and corpus 105/105. Neutral governance reaches 67 files, 4 complete / 4 pending, and 39 mutations."
evidence_update_2026_07_18_signoff: "Knowledge Map passes at 613 facts / 4,432 question keys. Memory, task metadata, all four doctrines, mdBook, JSON, shell, and whitespace pass. Canonical local CI repeats Perl root consumers 7+5, Perl cursor admission 288, reference primary 65x2, and Phase 0 1,031/1,031 in 613 seconds before exit 0."
reverify: "(cd dart && dart test test/rule_local_cursor_contract_test.dart); bash tools/run_python_project_data.sh tools/check_rule_local_cursor_contract.py; bash tools/run_dart_local.sh"
---

The Dart cursor rollout is admitted by one omission-sensitive consumer at
`dart/test/rule_local_cursor_contract_test.dart`. Its role list is declared in
`capability_conformance/rule_local_cursor_contract.json`; the consumer maps
exactly that set and executes every declared role once.

The 15 roles compose the previously independent mechanism leaves without
duplicating their exhaustive fixture matrices. They prove normal default and
AND families, ordinary normalized state, file loading, descriptor v1,
generated-source v2 emission and direct/traced execution, mixed parent/child
ownership, recursion, both structural replacements, removal of caller-global
options, the primary command, and the complete portable diagnostic set.

`tools/check_rule_local_cursor_contract.py` locks the consumer path, each exact
function marker, `tools/run_dart_local.sh`'s complete test command, the tracked
input in `tools/run_ci_local.sh`, and that driver's optional canonical
registration. Removing a role, consumer, driver, canonical registration, or
Dart rollout status is one of the 39 rejected mutations.

Only `dart_backend` advances. Neutral, Perl, Rust, and Dart are complete;
Julia, dual-ABI Lua, recurring five-backend admission, and public no-drift
remain dependency-ordered.

Related: [[rule-local-cursor-neutral-contract]],
[[dart-rule-local-cursor-execution]],
[[dart-rule-local-cursor-descriptor]],
[[dart-generated-source-v2-rule-local-cursor]], and
[[dart-global-cursor-option-removal]].
