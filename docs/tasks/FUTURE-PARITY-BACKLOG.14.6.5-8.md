- ID: `FUTURE-PARITY-BACKLOG.14.6.5`
  Status: `done; closed` (2026-08-25; `.14.6.5.0-.4` are signoff-complete and shared Lua `.14.6.6` is next); Goal: Split, implement, privately admit, and independently recompose Julia parity across native, reconstructed, generated-plan, and independently loaded emitted-module carriers; Depends on: `.14.6.4`; Children: `.14.6.5.0-.14.6.5.4`.
- ID: `FUTURE-PARITY-BACKLOG.14.6.5.0`
  Status: `done; focused-signoff-complete` (2026-08-24; task-tree-first from exact clean `c56a36fd0315b754d5310c9fd56641aae902fafc`; no push); Goal: Establish one exact dormant final-path Julia RED and freeze disjoint authority/core, carrier-integration, canonical-admission, and independent-recomposition leaves without production behavior; Depends on: `.14.6.4.3`.
  Acceptance: retrieve ADR `0080`, the executable neutral contract, committed Dart/Rust precedents, and Julia typed-source/staged-registry/generated-v2/recognition authorities before code archaeology; prove the unrelated staged registry rejects `expr-v1`, the reserved assignment remains a generic unsupported call, and native, reconstructed, generated-plan, and independently loaded emitted-module carriers converge on the same missing dedicated-node boundary; keep the consumer outside ordinary and canonical discovery; freeze exact `.1-.4` ownership; synchronize task/index, Knowledge, book, bounded live histories, roadmaps, and memory without moving production, rollout, generated format, outward surfaces, typed recurrence, or canonical topology.
  Verification tier: `focused`.
  Focused checks: exact dormant RED with all pre-boundary groups GREEN and only the dedicated-node assertion RED; ordinary/canonical absence; staged-registry and four-carrier probes; progressive, typed-source, recognition, generated-source, capability, and language direct dependents; task/index, Knowledge, mdBook, histories, README/memory, nine doctrines, no-production-diff, exact diff.
  Canonical trigger: `none` — only an unrouted test and durable plan move; production, executable governance, CI topology, admission, generated format, dependency/toolchain, storage/path/doctrine infrastructure, and public surfaces remain unchanged.
  Ownership: `.0` owns only the dormant consumer and frozen plan; `.1` owns private immutable authority/core; `.2` owns the dedicated node and four dormant carriers; `.3` alone owns ordinary/canonical discovery, Julia rollout promotion, and parent behavioral closure; `.4` independently recomposes the admitted topology and continuity surfaces without behavior.
  Checklist: [x] clean activation/task ownership [x] ADR/Knowledge/Julia-authority retrieval [x] exact final-path RED [x] dormancy [x] four-carrier audit [x] `.1-.4` frozen ownership [x] durable synchronization [x] focused signoff [x] atomic commit/brief/clean handoff.
  Evidence: exact dormant consumer passes 55 assertions and fails only one combined generic-call/missing-dedicated-node assertion; the staged function-body registry rejects `expr-v1` at resolve, and native, `SpecFile`-JSON reconstructed, generated-plan, and independently included emitted-module routes preserve the same unsupported `dispatch_span` helper boundary. Ordinary complete Julia tests pass while `julia/test/runtests.jl` and canonical CI omit the consumer. Progressive 4/9/103, typed source 11/3/152, recognition 138/250/58, generated-source 80/0/0, capability 80/0/0, and language 250/126 direct dependents pass. Seven Julia production blobs, generated format, CI topology, rollout, typed recurrence, and outward surfaces are unchanged. ADR `0080`'s stale pre-Dart 3/9/95 prose is corrected to executable 4/9/103 truth. Knowledge, mdBook, histories, task/index, README/memory, nine doctrines, no-production-diff, and exact diff pass. Commit: `FUTURE-PARITY-BACKLOG.14.6.5.0 - freeze Julia progressive dispatch RED`.
- ID: `FUTURE-PARITY-BACKLOG.14.6.5.1`
  Status: `done; focused-signoff-complete` (2026-08-24; task-tree-first from exact clean Julia-RED commit `94e37e83adda097f63c3bb7d6aa9cdaca533fa2b`; no push); Goal: Add the private immutable Julia registry, invocation, same-source rebased view, narrowing authority, shared cancellation/budgets, chain limits, detachment, and exact diagnostics while preserving the final-path missing-node RED; Depends on: `.14.6.5.0`.
  Acceptance: retrieve ADR `0080`, committed Julia RED, the existing Julia `SourceLocation` and recognition authorities, the neutral progressive matrix/26 diagnostics, and committed Rust/Dart authority precedents before implementation; add one unexported ActionIR-independent Julia authority module and one dormant focused consumer covering immutable already-compiled entries, same-source Unicode-scalar view/rebasing, narrowing grants and ceilings, shared identity-bearing cancellation/deadline/steps, decreasing spans/depth/calls, callback containment and expiry, and deeply detached node-bounded results; preserve the exact 55-pass/one-RED final-path consumer and change no syntax, ActionIR, interpreter/generated carrier, generated format, rollout, ordinary/canonical route, or outward surface.
  Verification tier: `focused`.
  Focused checks: exact dormant authority consumer; original final-path 55-pass/one-RED and ordinary/canonical absence; complete ordinary Julia; progressive, typed-source, recognition, generated-source, capability, and language direct dependents; Knowledge, mdBook, bounded histories, task/index, README/memory, nine doctrines, no-carrier/no-rollout boundary, and exact diff.
  Canonical trigger: `none` — one private unexported authority module and its dormant consumer move without syntax, ActionIR, execution carriers, generated format, admission/rollout, dependency/toolchain, storage/path/doctrine infrastructure, or public behavior.
  Checklist: [x] clean activation/task ownership [x] ADR/Knowledge/neutral retrieval [x] private immutable authority [x] complete dormant focused consumer [x] exact RED/no-carrier boundary [x] durable synchronization [x] focused signoff [x] atomic commit/brief/clean handoff.
  Evidence: `BoundedChildParseAuthority` is included immediately after `SourceLocation` but remains unexported, ActionIR-independent, and absent from interpreter/generated carriers. It owns immutable validated entries containing already-compiled callbacks, copied decoded-source invocations, same-source Unicode-scalar views and typed global rebasing, capability/policy intersections and ceiling minima, exact cancellation-token identity/deadline/shared steps, contained decreasing repeats plus depth/call limits, callback/request expiry, UTF-8-bounded child failures, and deeply copied node-bounded plain results rejecting cycles/live/nonfinite/nonplain values. The dormant focused consumer passes 210 assertions across all neutral 8-view, 6-authority, 6-cancellation, 8-chain, 4-execution cases and all 26 diagnostic contexts plus nesting, rebasing, expiry, input isolation, containment, and detachment adversaries. The original final path remains exact at 55-pass/one-RED; ordinary/canonical discovery and seven carrier files are unchanged. Complete ordinary Julia passes; progressive 4/9/103, typed 11/3/152, recognition 138/250/58, generated/capability 80/0/0, language 250/126, Knowledge 860/7,265, mdBook, bounded histories, task/index, memory/README, all nine doctrines, no-carrier boundary, and exact diff pass. Commit: `FUTURE-PARITY-BACKLOG.14.6.5.1 - implement Julia progressive dispatch authority`.
- ID: `FUTURE-PARITY-BACKLOG.14.6.5.2`
  Status: `done; canonical-signoff-complete` (2026-08-24; task-tree-first from exact clean Julia-authority commit `bb34a85e6fe6ee43408721141309261c9e83bd00`; no push); Goal: Add the dedicated Julia node and native, reconstructed, generated-plan, and independently loaded emitted-module carriers through fresh opaque execution authority while retaining dormancy and rollout RED; Depends on: `.14.6.5.1`.
  Acceptance: retrieve ADR `0080`, committed Julia RED/private authority, exact Rust/Dart carrier precedents, and Julia ActionIR/parser/contracts/compiled/generated-v2/interpreter/recognition authorities before implementation; replace only the reserved exact literal-id/literal-top/bare-span assignment with one exclusive dedicated node, reject malformed/direct generic residual/static transaction forms, attach native, `SpecFile`-JSON reconstructed, generated-plan, and independently included emitted-module execution through a fresh opaque host authority per run, and preserve shared cancellation/budget plus live transaction defense; keep callbacks, registry, source snapshots, cancellation, and mutable authority absent from serialized/generated data; make the exact final-path consumer GREEN but dormant while changing no generated format, ordinary/canonical route, rollout 4/9/103, typed recurrence, public, or outward surface; if the mandatory bounded-history rollover crosses its finite resulting-tree control, own and review only the exact one-member/one-manifest-line capacity increment through a new indexed ADR, route update, Knowledge fact, and canonical proof without weakening any other pressure control.
  Verification tier: `canonical` (upgraded from focused after mandatory history rollover crossed finite infrastructure capacity).
  Focused checks: exact dormant carrier consumer plus separate authority consumer; malformed/static/live transaction and fresh-authority proofs; four-carrier equality and generated/emitted authority absence; complete ordinary Julia; progressive, typed-source, recognition, generated-source, capability, and language direct dependents; Knowledge, mdBook, bounded histories, task/index, README/memory, nine doctrines, no-admission/no-rollout boundary, exact finite history-capacity review, and exact diff.
  Canonical trigger: `mandatory` — the required change-history rollover publishes one immutable segment and therefore changes resulting-tree routing capacity; stage the exact candidate and run receipt-bound canonical CI before commit.
  Checklist: [x] clean activation/task ownership [x] ADR/Knowledge/precedent retrieval [x] dedicated node/static boundaries [x] four fresh-authority carriers [x] exact dormant GREEN [x] durable synchronization [x] focused behavior signoff [x] mandatory rollover generation [x] exact ADR/route capacity review [x] canonical receipt [x] atomic commit/brief/clean handoff.
  Acceptance Checklist:
  - [x] **REPRODUCE / ISSUE** — `rg -n` plus the dormant consumer reproduce the 55-pass/one-RED generic-call boundary; the mandatory rollover reproduces the exact 20-file/19-manifest-line doctrine denial.
  - [x] **ROOT CAUSE (WHY + WHERE)** — tool-backed root cause: Julia lacked an exclusive node/carrier route, invocation teardown could replace a primary error, and `doctrine/readme_stability/routes.jsonl` retained the twentieth-member cap.
  - [x] **FIX** — add the logical-only node, static closure, fresh opaque seed, four carriers, primary-error-preserving teardown, governed dormant inventory, segment `4992`, and ADR `0086`'s exact 21/20 controls.
  - [x] **ADDRESSED (verified)** — checks pass: carrier 62/62, authority 210/210, complete ordinary Julia, `mdbook build`, Knowledge 862/7,279, bounded history, and staged README 20/62/32.
  - [x] **NO REGRESSION** — `git diff --check` plus progressive 4/9/103, typed 11/3/152, recognition 138/250/58, generated/capability 80/0/0, and language 250/126 pass with format/discovery/outward guards exact.
  - [x] **LOCKSTEP** — native/reconstructed/generated-plan/independently included emitted-module results, neutral governance, task/index, roadmaps, live docs, book, Knowledge, ADR index, and route registry agree.
  Evidence: one exclusive `ActionProgressiveDispatchSpanExpr` owns only target/literal parser id/literal top rule/bare span binding; five malformed operand boundaries, residual generic calls, recognition-reachable effect graphs, and external compiled reconstruction reject before execution. `ProgressiveExecutionSeed` starts fresh invocation budget/call state on every top-level native or generated run while retaining shared narrowing cancellation/deadline/steps. Native, `SpecFile`-JSON reconstructed, generated-plan, and independently included emitted-module routes return the same detached result at unchanged parent cursor; serialized/generated data contain no callback, registry, fingerprint, source snapshot, cancellation token, or mutable authority. Live transaction defense preserves the primary progressive denial while the existing recognition authority restores and invalidates unfinished tokens during current or parent-frame teardown. Dormant carrier 62/62 and authority 210/210 pass; ordinary/canonical discovery remain absent. Complete ordinary Julia passes. Progressive stays 4/9/103 while governance records 9 dormant Julia carrier paths plus one Lua guard/5 paths; typed 11/3/152, recognition 138/250/58, generated/capability 80/0/0, and language 250/126 pass. The admitted Perl 127/127, Dart 7/7, and exact cfg-enabled Rust 1/1 consumers agree on the neutral inventory; Rust independently compiles and runs the emitted module and finishes the exact test in 1,427.16 seconds. Knowledge, rendered mdBook, bounded histories, task/index, memory, nine doctrines, no-admission/no-rollout boundary, exact diff, and receipt-bound canonical CI pass.
  Capacity authority: ADR `0086` and `change-history-twenty-first-member-capacity` own only immutable segment `4992`, route capacity 20→21 files, and manifest capacity 19→20 lines. Root/segment/manifest bytes, per-member and aggregate lines/bytes, owner, lifecycle, verifier, patterns, query/recovery behavior, and ADR `0069` storage authority remain unchanged.
  Canonical attempt one: all nine doctrines pass; the neutral checker then passes and the exact Perl admission rejects only its stale pre-Julia inventory snapshot. Exhaustive source search identifies the same snapshot in Rust and Dart, so all three admitted consumers now assert 9 dormant Julia carrier paths plus one Lua guard/5 paths in lockstep.
  Canonical completion: the corrected Perl 127/127, Dart 7/7, and cfg-enabled Rust 1/1 consumers pass before the exact staged candidate clears receipt-bound local CI. Commit: `FUTURE-PARITY-BACKLOG.14.6.5.2 - implement dormant Julia progressive carriers`.
- ID: `FUTURE-PARITY-BACKLOG.14.6.5.3`
  Status: `done; canonical-signoff-complete` (2026-08-24; task-tree-first from exact clean Julia-carrier commit `35a2b56c95a49270f39ace43d436ffd1330296d6`; no push); Goal: Admit the unchanged exact Julia consumer once through ordinary and canonical proof, promote only Julia rollout, and close the behavioral parent without typed-recurring or outward movement; Depends on: `.14.6.5.2`.
  Acceptance: retrieve ADR `0080`, committed Julia authority/carrier facts and exact dormant consumers, Perl/Rust/Dart admission precedents, neutral checker/current boundary, canonical driver/tracked-input topology, direct dependents, Knowledge, and book before implementation; preserve the carrier consumer's behavioral bytes, seven groups, 62 assertions, authority/node/four routes, generated format, production code, and separate dormant 210-assertion authority consumer while changing only its admission-owned header/snapshot/discovery assertions, moving the same file into ordinary discovery, and registering exactly one canonical requirement/marker/invocation; promote only Julia rollout and admission truth, close the behavioral `.14.6.5` parent, retain the Lua guard and every typed/recurring/public/outward boundary, and land only after exact staged receipt-bound canonical proof.
  Verification tier: `canonical`.
  Focused checks: unchanged exact Julia carrier consumer GREEN through its admitted path and no dormant duplicate; complete ordinary Julia discovery and separately runnable dormant authority consumer; neutral progressive plus admitted Perl/Rust/Dart consumers; typed-source, recognition, generated-source, capability, and language direct dependents; canonical-driver exact-count probes; generated/emitted authority-absence and no-production-diff guards; Knowledge, mdBook, bounded histories, task/index, README/memory, nine doctrines, and exact staged diff.
  Canonical trigger: `the canonical CI trust boundary and executable Julia rollout/admission truth move together; exact staged receipt-bound local CI is required before landing and closing the Julia behavioral parent`.
  Checklist: [x] clean activation/task ownership [x] ADR/Knowledge/carrier/admission retrieval [x] exact stable-identity Julia consumer move [x] exact ordinary/canonical registration [x] Julia-only neutral rollout/current-boundary promotion [x] direct-dependent/live-doc synchronization [x] focused proof [x] staged canonical receipt [x] atomic commit/brief/clean handoff.
  Activation evidence: exact clean `35a2b56c95a49270f39ace43d436ffd1330296d6`, matching committed canonical receipt, zero-byte brief, no rendered-book residue, no background job, and `.14.6.5.2`'s 62/62 carrier plus 210/210 authority evidence establish the owned base. `.3` may change only test discovery, canonical routing, Julia rollout/admission governance, behavioral-parent status, and their durable projections; Julia production, generated format, typed recurrence, public inventory, facade/schema/MCP/CLI/README, and outward surfaces remain unchanged.
  Evidence: Git blob `0794a062` moves from the dormant carrier path to `julia/test/progressive_span_dispatch_contract_test.jl`; only admission-owned header/snapshot/discovery assertions change, so the same seven groups remain 62/62. Ordinary Julia includes the path once; canonical CI has one exact tracked requirement/marker/invocation; no dormant duplicate remains. The separate authority consumer stays dormant at 210/210. Neutral truth advances only Julia to 5/9/106 with 9 Rust + 8 Dart + 9 Julia carrier paths, one Lua guard/5 paths, ten outward guards, and 26 diagnostics. Complete ordinary Julia, Perl 128/128, Dart 7/7, cfg Rust 1/1, typed 11/3/152, recognition 138/250/58, generated/capability 80/0/0, language 250/126, Knowledge 863/7,285, sole-facing mdBook, bounded histories, task/index, README/memory, nine doctrines, no-production-diff, exact staged diff, and receipt-bound canonical CI pass. Production/generated format, typed recurrence, public inventory, facade/schema/MCP/CLI/README, and outward surfaces do not move. Commit: `FUTURE-PARITY-BACKLOG.14.6.5.3 - admit private Julia progressive dispatch`.
- ID: `FUTURE-PARITY-BACKLOG.14.6.5.4`
  Status: `done; canonical-signoff-complete` (2026-08-25; task-tree-first from exact clean Julia-admission commit `7823f6fc7a7bd80e327921d8e0317fe343660375`; no push); Goal: Independently recompose the admitted Julia task topology, generated/emitted carrier invariants, neutral governance, bounded live docs, and clean resume pointer without changing behavior; Depends on: `.14.6.5.3`.
  Acceptance: retrieve ADR `0080`, committed Julia admission/authority/carrier facts, the executable neutral contract, and the prior unchanged-owner recomposition precedent before code archaeology; rerun the committed 62-assertion admitted carrier and separate 210-assertion dormant authority consumers unchanged; independently prove native, reconstructed, generated-plan, and included emitted-module equality plus absence of serialized callbacks, registries, source snapshots, cancellation, fingerprints, and mutable authority; preserve exact rollout 5/9/106, ordinary/canonical discovery, generated format, production, typed recurrence, Lua guard, public inventory, and every outward boundary; close `.14.6.5`, synchronize bounded continuity and sole-facing book truth, and hand off `.14.6.6` only after exact staged canonical proof and a clean commit.
  Verification tier: `canonical`.
  Focused checks: committed Julia carrier 62/62 and authority 210/210 consumers without source/test changes; complete ordinary Julia; progressive neutral 5/9/106 plus admitted Perl/Rust/Dart consumers; generated/emitted authority-absence and exact no-production/no-test/no-contract/no-CI-topology diff; typed-source, recognition, generated-source, capability, and language direct dependents; Knowledge, mdBook, bounded histories, task/index, README/memory, nine doctrines, and exact staged diff.
  Canonical trigger: `designated independent recomposition and parent closeout` — exact staged receipt-bound local CI must prove the committed Julia owners unchanged before `.14.6.5` closes and shared Lua `.14.6.6` becomes the frontier.
  Checklist: [x] clean activation/task ownership [x] ADR/Knowledge/precedent retrieval [x] unchanged Julia owner recomposition [x] generated/emitted authority-absence proof [x] neutral/direct-dependent no-movement proof [x] parent/live-doc/book closure [x] focused proof [x] staged canonical receipt [x] atomic commit/brief/clean handoff.
  Activation evidence: exact clean `7823f6fc7a7bd80e327921d8e0317fe343660375`, matching promoted canonical receipt, zero-byte brief, no unstaged or untracked path, and no background job establish the owned base. This leaf may change only task/frontier status and their bounded continuity, roadmap, Knowledge, ADR, and sole-facing book projections; production, tests, fixtures, executable contracts, canonical topology, generated format, rollout, typed recurrence, facade/schema/MCP/CLI/README, and outward surfaces remain unchanged.
  Evidence: the committed Julia carrier passes 62/62 and independently proves native, `SpecFile`-JSON reconstructed, generated-plan, and independently included emitted-module equality plus absence of serialized callbacks, registries, fingerprints, source snapshots, cancellation, and mutable authority. The separate dormant authority remains 210/210; complete ordinary Julia and the unchanged Perl 128/128, cfg-enabled Rust 1/1, and Dart 7/7 consumers pass. Neutral governance remains exactly 5/9/106; typed source 11/3/152, recognition 138/250/58, generated/capability 80/0/0, and language 250/126 pass without production, test, fixture, executable-contract, generated-format, or CI-topology changes. Independent review finds that the earlier Perl/Rust/Dart admission cards retained stale current Julia-pending prose because the neutral checker governs executable/outward surfaces while the Knowledge Map gate proves derived-index freshness rather than cross-card semantic agreement; bounded current projections are corrected while dated evidence remains unchanged. ADR `0080`, Knowledge, sole-facing mdBook, bounded histories, task/index, README/memory, nine doctrines, exact staged diff, and receipt-bound canonical CI pass. Julia parent `.14.6.5` closes unchanged and hands off shared Lua `.14.6.6`. Commit: `FUTURE-PARITY-BACKLOG.14.6.5.4 - close Julia progressive dispatch`.
- ID: `FUTURE-PARITY-BACKLOG.14.6.6`
  Status: `done; canonical-signoff-complete` (2026-08-25; `.0` committed at `84178bd82f238338e6119fa3e9ad2b6d82170fae`; `.1` committed at `6cbf8fdf9435dbadbf1d4c7ca4fbab89ba83811b`; `.2` committed at `4f6173d0dfad9065807bb45b7b15c618e86b38cc`; `.3` committed at `e5c66c4b6e42af900ef14f8857ee135dd985965c`; `.4` independently recomposed every committed owner unchanged); Goal: Split one Lua-5.1-compatible implementation and admit it independently on PUC Lua and LuaJIT across all four carriers; Depends on: `.14.6.5`; Children: `.14.6.6.0-.14.6.6.4`.
- ID: `FUTURE-PARITY-BACKLOG.14.6.6.0`
  Status: `done; focused-signoff-complete` (2026-08-25; task-tree-first from exact clean `e25791e549865ff6624e9e026c3c64060a9b30e6`; no push); Goal: Establish one exact shared Lua-5.1 dormant final-path RED on PUC Lua and LuaJIT and freeze disjoint authority, carrier, admission, and recomposition ownership without production behavior.
  Acceptance: retrieve ADR `0080`, the executable neutral contract, admitted Perl/Rust/Dart/Julia precedents, and Lua typed-source/staged-registry/generated-v2/recognition authorities before code archaeology; prove the unrelated staged registry rejects `expr-v1`, the reserved assignment remains a generic unsupported call, and native, normalized-JSON reconstructed, generated-plan, and independently loaded emitted-module carriers converge on the same missing dedicated-node boundary on both ABIs; keep the consumer outside ordinary and canonical discovery; freeze exact `.1-.4` ownership; synchronize task/index, Knowledge, book, bounded live histories, roadmaps, and memory without moving production, rollout, generated format, typed recurrence, or outward surfaces.
  Verification tier: `focused`.
  Focused checks: exact dormant RED on PUC Lua and LuaJIT with all pre-boundary groups GREEN and only the dedicated-node assertion RED; ordinary/canonical absence; staged-registry and four-carrier probes; complete ordinary Lua; progressive, typed-source, recognition, generated-source, capability, and language direct dependents; task/index, Knowledge, mdBook, histories, README/memory, nine doctrines, no-production-diff, and exact diff.
  Canonical trigger: `none` — only an unrouted shared test and durable plan move; production, executable governance, CI topology, admission, generated format, dependency/toolchain, storage/path/doctrine infrastructure, and public surfaces remain unchanged.
  Ownership: `.0` owns only the dormant dual-ABI consumer and frozen plan; `.1` owns one private shared Lua-5.1 immutable authority/core; `.2` owns the dedicated node and four dormant carriers on both ABIs; `.3` alone owns ordinary/canonical dual-ABI discovery, Lua rollout promotion, and behavioral-parent closure; `.4` independently recomposes the admitted topology and continuity surfaces without behavior.
  Checklist: [x] clean activation/task ownership [x] ADR/Knowledge/Lua-authority retrieval [x] exact dual-ABI final-path RED [x] dormancy [x] four-carrier audit [x] `.1-.4` frozen ownership [x] durable synchronization [x] focused signoff [x] atomic commit/brief/clean handoff.
  Evidence: the same Lua-5.1-compatible dormant consumer runs 86 assertions on PUC Lua and LuaJIT; 85 pass and only `Lua progressive RED: missing exclusive progressive_dispatch_span node` fails. The unrelated staged registry rejects `expr-v1` at resolve. The reserved assignment remains one `assign_scalar` over a generic `dispatch_span` call with one `unknown_helper` contract diagnostic and no dedicated node. Native and normalized-JSON reconstructed carriers return the same typed runtime error; generated-plan and independently loaded emitted-module carriers wrap the same unsupported-helper boundary. The emitted module preserves identity while hex-embedding normalized state and contains neither generic/dedicated spelling nor registry fingerprint. Ordinary/canonical discovery stays absent; all five production guard paths remain token-free. Complete Lua passes 178/178 on both ABIs, CLI 66/66 in both option environments, corpus 105, and storage 19/3. Progressive 5/9/106, typed 11/3/152, recognition 138/250/58, generated/capability 80/0/0, and language 250/126 pass. Synchronization finds that both roadmap compact rows retained pre-Julia-admission 4/9/103 truth because admission/recomposition updated only their lead projections; both current rows and one causal Knowledge fact are corrected without changing historical evidence. Knowledge, rendered mdBook, bounded histories, task/index, README/memory, all nine doctrines, no-production/no-discovery diff, and exact diff pass. Commit: `FUTURE-PARITY-BACKLOG.14.6.6.0 - freeze Lua progressive dispatch RED`.
- ID: `FUTURE-PARITY-BACKLOG.14.6.6.1`
  Status: `done; focused-signoff-complete` (2026-08-25; task-tree-first from exact clean Lua-RED commit `84178bd82f238338e6119fa3e9ad2b6d82170fae`; no push); Goal: Add one private ActionIR-independent Lua-5.1 immutable registry/invocation/source-view authority shared unchanged by PUC Lua and LuaJIT while preserving the final-path missing-node RED; Depends on: `.14.6.6.0`.
  Acceptance: reuse the existing private typed SourceAuthority and recognition-transaction precedents; own immutable already-compiled entries, copied decoded-source invocations, Unicode-scalar same-source rebasing, narrowing grants/ceilings, identity-bearing cancellation/deadline/shared steps, decreasing-span/depth/call limits, callback containment/expiry, detached bounded results, and all 26 neutral diagnostics in one unexported module; preserve syntax, ActionIR, interpreter/emitter carriers, generated format, rollout, discovery, and outward surfaces.
  Verification tier: `focused`.
  Focused checks: exact dormant authority consumer on PUC Lua and LuaJIT; unchanged 85-pass/one-RED final path and ordinary/canonical absence; complete ordinary Lua; progressive, typed-source, recognition, generated-source, capability, and language direct dependents; Knowledge, rendered mdBook, bounded histories, task/index, README/memory, nine doctrines, no-carrier/no-rollout boundary, and exact diff.
  Canonical trigger: `none` — one private unexported authority module and its dormant dual-ABI consumer move without syntax, ActionIR, execution carriers, generated format, admission/rollout, dependency/toolchain, storage/path/doctrine infrastructure, or public behavior.
  Checklist: [x] clean activation/task ownership [x] ADR/Knowledge/neutral retrieval [x] private immutable authority [x] complete dormant dual-ABI proof [x] exact RED/no-carrier boundary [x] durable synchronization [x] focused signoff [x] atomic commit/brief/clean handoff.
  Evidence: `lua/src/linkedspec/bounded_child_parse_authority.lua` is one direct Lua-5.1-compatible module absent from the package facade, ActionIR, interpreter, generated carrier, ordinary discovery, and canonical CI. Module-private weak-key state backs opaque immutable ceilings, entries, registries, chain frames, effective authority, callback-scoped views/requests, invocations, cancellation, and typed errors. Trusted entries contain already-compiled callbacks only; fresh invocations copy decoded sources into the existing private typed `source_location` authority. Unicode-scalar local positions/spans/diagnostics rebase to the original source identity; capability/policy intersections and every ceiling narrow; cancellation identity, absolute deadline, remaining steps, decreasing global-span chains, depth, and total calls remain shared. Callback views and retained nested requests expire after success/failure; child results are deeply copied, finite, JSON-shaped, live-field-free, cycle-free, and node-bounded; diagnostics are UTF-8-byte-bounded. The same dormant consumer passes 273/273 assertions on PUC Lua and LuaJIT across every neutral 8-view/6-authority/6-cancellation/8-chain/4-execution row, all 26 diagnostic contexts, nested rebasing/expiry, input/result isolation, cycle/nonfinite/live-field/node bounds, UTF-8 truncation, privacy, dormancy, and five no-carrier guards. The separate final path remains exact at 85-pass/one-RED on both ABIs. Complete Lua passes 178/178 per ABI, CLI 66/66 in both option environments, corpus 105, and storage 19/3. Progressive 5/9/106, typed 11/3/152, recognition 138/250/58, generated/capability 80/0/0, and language 250/126 pass without syntax, ActionIR, carrier, format, rollout, canonical, typed-recurring, or outward movement. Knowledge, rendered mdBook, bounded histories, task/index, README/memory, nine doctrines, no-carrier/no-rollout boundary, and exact diff pass. Commit: `FUTURE-PARITY-BACKLOG.14.6.6.1 - implement Lua progressive dispatch authority`.
- ID: `FUTURE-PARITY-BACKLOG.14.6.6.2`
  Status: `done; focused-signoff-complete` (2026-08-25; task-tree-first from exact clean Lua-authority commit `6cbf8fdf9435dbadbf1d4c7ca4fbab89ba83811b`; no push); Goal: Add the exclusive dedicated Lua node and native, normalized-JSON reconstructed, generated-plan, and independently loaded emitted-module carriers through fresh opaque execution authority on both ABIs while retaining dormancy; Depends on: `.14.6.6.1`.
  Acceptance: replace only the reserved literal-id/literal-top/bare-span assignment; reject malformed operands, residual generic calls, static recognition-effect reachability, and live tokens; use one Lua-5.1-compatible implementation on both ABIs; preserve parent cursor, shared cancellation/budget, detached result equality, generated-v2 format and plan, and absence of callbacks, registries, fingerprints, source snapshots, cancellation, or mutable authority from serialized/emitted data; make the exact final consumer GREEN but undiscovered.
  Verification tier: `focused`.
  Focused checks: exact dormant carrier consumer GREEN on PUC Lua and LuaJIT plus separate 273-assertion authority consumer; malformed/static/live transaction and fresh-authority proofs; four-carrier equality and generated/emitted authority absence; complete ordinary Lua; progressive, typed-source, recognition, generated-source, capability, and language direct dependents; Knowledge, rendered mdBook, bounded histories, task/index, README/memory, nine doctrines, no-admission/no-rollout boundary, and exact diff.
  Canonical trigger: `none` unless an independently necessary infrastructure boundary moves — private dormant carriers only; no ordinary/canonical route, rollout, public behavior, dependency/toolchain, storage/path/doctrine infrastructure, or generated-format version changes.
  Checklist: [x] clean activation/task ownership [x] ADR/Knowledge/carrier precedent retrieval [x] exclusive node/static boundaries [x] four fresh-authority carriers [x] exact dormant dual-ABI GREEN [x] durable synchronization [x] focused signoff [x] atomic commit/brief/clean handoff.
  Evidence: only the exact whole-statement literal-id/literal-top/bare-span assignment lowers to one `progressive_dispatch_span` node carrying target/parser/top/span; five malformed operand cases and residual generic calls reject before execution. Compile-time rule/function closure rejects `recognize_once` targets reaching `parser_registry_or_staged_dispatch`; runtime live-token defense restores and invalidates the unfinished token while preserving the primary progressive denial. Reusable opaque execution seeds copy decoded sources and start fresh budget/call state per parse while retaining shared cancellation/deadline/steps. Native, normalized `SpecFile`-JSON reconstructed, generated-plan, and independently loaded emitted-module routes return the same detached payload at unchanged parent cursor. The same dormant Lua-5.1 consumer passes 178/178 on PUC Lua and LuaJIT; authority remains 273/273. Normalized/emitted artifacts contain no callback, registry entry, fingerprint, decoded input snapshot, cancellation token, or mutable state. Complete Lua passes 178/178 per ABI, CLI 66/66 in both environments, corpus 105, and storage 19/3. Neutral governance remains rollout 5/9/106 with 9 dormant Lua carrier paths and zero backend guards; Perl 128/128, cfg-enabled Rust 1/1, Dart 7/7, and Julia 62/62 agree. Typed 11/3/152, recognition 138/250/58, generated/capability 80/0/0, and language 250/126 pass. Knowledge, rendered mdBook, bounded histories, task/index, README/memory, nine doctrines, exact diff, and no-admission/no-rollout/generated-format/outward guards pass. Commit: `FUTURE-PARITY-BACKLOG.14.6.6.2 - implement dormant Lua progressive carriers`.
- ID: `FUTURE-PARITY-BACKLOG.14.6.6.3`
  Status: `done; canonical-signoff-complete; committed e5c66c4b6e42af900ef14f8857ee135dd985965c` (2026-08-25; task-tree-first from exact clean Lua-carrier commit `4f6173d0dfad9065807bb45b7b15c618e86b38cc`; no push); Goal: Admit the unchanged shared Lua carrier consumer exactly once per ABI through ordinary and canonical proof, promote only the two Lua runtime rows, and close the behavioral parent without typed-recurring or outward movement; Depends on: `.14.6.6.2`.
  Acceptance: preserve consumer behavioral bytes, shared authority/node/four carriers, production, generated format, and separate authority proof; remove dormancy, register exact PUC Lua and LuaJIT ordinary/canonical routes once each, promote only Lua rollout/admission truth, retain typed recurrence and every facade/schema/MCP/CLI/README/public boundary, and land only after exact staged receipt-bound canonical CI.
  Verification tier: `canonical`.
  Focused checks: exact admitted carrier consumer on PUC Lua and LuaJIT plus unchanged separate authority consumer; consumer-byte preservation apart from admission-owned discovery assertions; ordinary dual-ABI discovery exactly once; progressive, typed-source, recognition, generated-source, capability, language, and unchanged Perl/Rust/Dart/Julia direct dependents; Knowledge, rendered mdBook, bounded histories, task/index, README/memory, nine doctrines, exact rollout/discovery/no-format/no-outward boundaries, and exact diff.
  Canonical trigger: `required` — the canonical CI trust boundary and executable dual-ABI Lua rollout/admission truth move together; exact staged candidate must pass receipt-bound `tools/run_ci_local.sh` before commit.
  Checklist: [x] clean activation/task ownership [x] precedent/Knowledge/neutral retrieval [x] byte-preserving ordinary move [x] exact dual-ABI ordinary/canonical routes [x] rollout-only governance promotion [x] focused/direct-dependent proof [x] durable synchronization [x] staged canonical receipt [x] atomic commit/brief/clean handoff.
  Evidence: activated task-tree-first from exact clean Lua-carrier commit `4f6173d0dfad9065807bb45b7b15c618e86b38cc`. Git preserves the `b755c29e` consumer identity across the dormant-to-ordinary move; only the admission header, neutral inventory, and discovery assertions change, retaining exact 178/178 behavior on PUC Lua and LuaJIT. `tools/run_lua_local.sh` contains the tracked consumer path exactly twice, once beside `$LUA_CMD` and once beside `$LUAJIT_CMD`; canonical CI requires the path once and contains one exact marker/invocation pair per ABI. Neutral governance advances only rollout rows six and seven to complete, renames the 9-path Lua inventory from dormant to admitted, retains zero backend guards/10 outward guards/26 diagnostics, and adds the expected two-marker/two-invocation/two-rollout mutation guards for 7/9/112. The separate authority remains dormant and passes 273/273 per ABI. Complete ordinary Lua passes both ABIs; Perl 129/129, cfg-enabled Rust 1/1 through the repository-local offline wrapper, Dart 7/7, and Julia 62/62 agree. Progressive 7/9/112, typed 11/3/152, recognition 138/250/58, generated/capability 80/0/0, language 250/126, Knowledge, rendered mdBook, bounded histories, task/index, README/memory, nine doctrines, exact topology/diff, and staged receipt-bound canonical CI pass. Production, private authority, generated-v2 format, typed recurrence, public inventory, facade/schema/MCP/CLI/README, and outward surfaces do not move. Commit: `FUTURE-PARITY-BACKLOG.14.6.6.3 - admit private Lua progressive dispatch`.
- ID: `FUTURE-PARITY-BACKLOG.14.6.6.4`
  Status: `done; canonical-signoff-complete` (2026-08-25; task-tree-first from exact clean Lua-admission commit `e5c66c4b6e42af900ef14f8857ee135dd985965c`; no push); Goal: Independently recompose the admitted shared Lua topology, both ABI executions, generated/emitted carrier invariants, neutral governance, bounded live docs, and clean resume pointer without changing behavior; Depends on: `.14.6.6.3`.
  Acceptance: rerun the committed shared carrier and separate authority consumers unchanged on PUC Lua and LuaJIT; prove four-carrier equality and generated/emitted authority absence; preserve exact rollout, discovery, generated format, production, typed recurrence, public inventory, and outward boundaries; close `.14.6.6`, synchronize continuity and sole-facing book truth, and hand off `.14.6.7` only after staged canonical proof and a clean commit.
  Verification tier: `canonical`.
  Focused checks: committed Lua carrier 178/178 and authority 273/273 consumers on PUC Lua and LuaJIT without source/test changes; complete ordinary Lua; progressive neutral 7/9/112 plus admitted Perl/Rust/Dart/Julia consumers; four-carrier equality, generated/emitted authority absence, and exact no-production/no-test/no-contract/no-CI-topology diff; typed-source, recognition, generated-source, capability, and language direct dependents; Knowledge, mdBook, bounded histories, task/index, README/memory, nine doctrines, and exact staged diff.
  Canonical trigger: `designated independent recomposition and parent closeout` — exact staged receipt-bound local CI must prove the committed shared Lua owners unchanged before `.14.6.6` closes and typed recurring proof `.14.6.7` becomes the frontier.
  Checklist: [x] clean activation/task ownership [x] ADR/Knowledge/precedent retrieval [x] unchanged dual-ABI Lua owner recomposition [x] four-carrier/generated authority-absence proof [x] neutral/direct-dependent no-movement proof [x] parent/live-doc/book closure [x] focused proof [x] staged canonical receipt [x] atomic commit/brief/clean handoff.
  Activation evidence: exact clean `e5c66c4b6e42af900ef14f8857ee135dd985965c`, matching promoted canonical receipt, zero-byte brief, no unstaged or untracked path, and no background job establish the owned base. This leaf may change only task/frontier status and their bounded continuity, roadmap, Knowledge, ADR, and sole-facing book projections; production, tests, fixtures, executable contracts, canonical topology, generated format, rollout, typed recurrence, facade/schema/MCP/CLI/README, and outward surfaces remain unchanged.
  Evidence: the committed shared carrier passes 178/178 on PUC Lua and LuaJIT and independently proves native, normalized-`SpecFile`-JSON reconstructed, generated-plan, and independently loaded emitted-module equality plus absence of serialized callbacks, registries, fingerprints, decoded source snapshots, cancellation, and mutable authority. The separate dormant authority remains 273/273 per ABI; complete ordinary Lua, Perl 129/129, cfg-enabled Rust 1/1, Dart 7/7, and Julia 62/62 pass. Neutral governance remains exactly 7/9/112; typed source 11/3/152, recognition 138/250/58, generated/capability 80/0/0, and language 250/126 pass. Git comparison with `e5c66c4b6e42af900ef14f8857ee135dd985965c` proves no production, test, fixture, executable-contract, generated-format, or CI-topology change; ordinary discovery remains one invocation per ABI, canonical CI retains one tracked path plus one exact route per ABI, and authority discovery remains absent. Independent review finds bounded current Lua-pending prose in the earlier Perl/Rust/Dart/Julia admission cards and Julia closeout card because executable neutral governance and derived-index freshness do not infer cross-card semantic agreement; current projections are corrected while dated evidence remains unchanged. ADR `0080`, Knowledge, sole-facing mdBook, roadmaps, bounded histories, task/index, README/memory, nine doctrines, exact staged diff, and receipt-bound canonical CI pass. Shared Lua parent `.14.6.6` closes unchanged at 7/9/112 and hands off typed recurring proof `.14.6.7`. Commit: `FUTURE-PARITY-BACKLOG.14.6.6.4 - close shared Lua progressive dispatch`.
- ID: `FUTURE-PARITY-BACKLOG.14.6.7`
  Status: `done; canonical-signoff-complete` (2026-08-25; task-tree-first from exact clean `872ea8ba4410b24c174c8c280a893592c8eb911e`; no push); Goal: Bind one exact five-source/six-runtime recurring proof and promote only typed `progressive_span_dispatch`; Depends on: `.14.6.6`.
  Acceptance: retrieve ADR `0080`, the executable progressive contract/checker, committed Perl/Rust/Dart/Julia/shared-Lua admissions, and the typed-source plus recursive-observation recurring precedents before implementation; add one repository-routed fail-fast driver that executes the progressive neutral checker, exact Perl, cfg-enabled Rust, Dart, Julia, PUC Lua, and LuaJIT consumers in that order, then typed-source, generated-source, capability, and language ledgers; bind the five immutable backend source groups to six runtime routes because one shared Lua source runs once per ABI; require, path-audit, syntax-check, and expose that driver only through one explicit canonical opt-in; promote only the typed `progressive_span_dispatch` row to complete for all six runtimes and add reason-checked topology/storage/rollout mutations; preserve the progressive behavioral rollout, all committed consumers and runtime code, generated format, public inventory, facade/schema/semantic/MCP/CLI/README boundaries, staged `.14.7`, combined `.14.8`, and public-no-drift `.14.6.8`; if required bounded engineering-notes rollover crosses its finite resulting-tree control, own and review only the exact one-member/one-manifest-line capacity increment through a new indexed ADR, route update, and Knowledge fact without weakening any other pressure control; synchronize current capability/Knowledge/book/roadmap/live projections and land only after exact staged receipt-bound canonical CI.
  Verification tier: `canonical`.
  Focused checks: exact new six-runtime progressive driver and each constituent neutral/runtime/support route; typed-source checker rollout/topology/mutation proof; progressive neutral 7/9/112 and unchanged admitted consumer counts; canonical-driver tracked/path/syntax/switch multiplicity; project-data workflow routing; generated-source, capability, language, typed-source, recognition, and complete ordinary backend direct dependents; exact no-production/no-consumer/no-generated-format/no-public-surface diff; Knowledge, rendered mdBook, bounded histories, task/index, README/memory, nine doctrines, and exact staged diff.
  Canonical trigger: `the typed recurring rollout, executable conformance topology, project-data routing registry, and canonical CI opt-in move together; exact staged receipt-bound local CI is required before landing and handing off public no-drift .14.6.8`.
  Ownership: `.14.6.7` owns only the recurring driver, its project-data/canonical registration, typed `progressive_span_dispatch` completion, reason-checked mutations, synchronized current projections, and any mechanically required one-member engineering-notes capacity increment; `.14.6.8` alone owns progressive public projection/no-drift, `.14.7` owns staged span dispatch, and `.14.8` retains combined program-wide public no-drift.
  Checklist: [x] clean activation/task ownership [x] ADR/Knowledge/recurring-precedent retrieval [x] exact five-source/six-route driver [x] project-data and canonical opt-in registration [x] typed progressive-only rollout promotion [x] reason-checked topology/storage/rollout mutations [x] direct-dependent/no-movement proof [x] exact mandatory history-capacity review [x] durable synchronization [x] focused signoff [x] staged canonical receipt [x] atomic commit/brief/clean handoff.
  Activation evidence: `git status --short` is empty at exact clean `872ea8ba4410b24c174c8c280a893592c8eb911e`; `git_message_brief.txt` is zero bytes; `.14.6.6.4` has a promoted matching canonical receipt; no background work remains. The committed progressive ledger is 7/9/112, typed source is 11/3/152, all six private runtime rows are admitted, and no runtime, consumer, generated format, public, or outward movement is authorized by this leaf.
  Evidence: `tools/check_progressive_span_dispatch_six_runtime.sh` is one executable repository-routed fail-fast driver over neutral, Perl 129, cfg-enabled Rust 1/1, Dart 7, Julia 62, PUC Lua 178, LuaJIT 178, then typed-source, generated-source, capability, and language support ledgers. Five immutable backend source groups bind to six ordered routes because shared Lua runs once per ABI. Canonical CI contains exactly two tracked requirements, one switch, and one invocation; project-data routing inventories the driver and its intentional missing-Julia failure case. Twelve reason-checked topology/storage/rollout mutations plus two stale-guide denials promote only typed `progressive_span_dispatch`, advancing typed governance 11/3/152 to 12/2/170 while behavioral governance remains 7/9/112. The progressive current-boundary assertion now requires typed completion over all six runtimes without changing its 112-mutation behavioral ledger. Git blame roots the stale capability-guide 2/9/pending paragraph in Perl admission `78bc66e1`; later runtime admissions omitted that current projection, so the checker now rejects both exact stale claims and Knowledge records the defect without rewriting dated history. Mandatory engineering-notes rollover publishes immutable segment `4991` and leaves the hot root at 250/512 lines; ADR `0087` advances only finite collection/manifest-line controls 16→17 and 15→16 at exact total 23,604 lines / 2,528,467 bytes, preserving every byte, aggregate, owner, lifecycle, verifier, and storage control. Git comparison with `872ea8ba4410b24c174c8c280a893592c8eb911e` proves no runtime, consumer, fixture, generated-format, facade/schema/semantic/MCP/CLI/README outward, or public behavior movement. Knowledge, capability guide, ADRs `0080`/`0087`, roadmaps, architecture, Toolbox, sole-facing mdBook, bounded histories, task/index, memory/live status, all nine doctrines, exact staged diff, and receipt-bound canonical CI pass. Commit: `FUTURE-PARITY-BACKLOG.14.6.7 - bind progressive recurring proof`.
- ID: `FUTURE-PARITY-BACKLOG.14.6.8`
  Status: `done; canonical-signoff-complete` (2026-08-25; task-tree-first from exact clean recurring-proof commit `b7c613be6ced8827271056950a85b77933733f9f`; no push); Goal: Correct the missed behavioral recurring projection, then close progressive public projection/no-drift without a public facade/schema/CLI/README admission or consuming combined `.14.8`; Depends on: `.14.6.7`.
  Acceptance: retrieve ADR `0080`, the committed progressive neutral/recurring contracts and consumers, typed recurring truth, the recursive-observation and recognition-transaction public-closeout precedents, Knowledge, and all current public projections before implementation; prove the completed `.14.6.7` owner plus its committed canonical driver conflict with the still-pending progressive behavioral `recurring` row; correct only that stale row to the committed driver path, promote `public_no_drift` through exact governed current-document paths, require current private-intrinsic/no-public-API projections, reject stale milestone claims and private-token exposure through reason-checked omission/injection mutations, update only the five immutable consumer groups' exact governance snapshots required by that contract promotion, and close `.14.6` at 9/9 without changing any parser, compiler, runtime, consumer behavior/fixture, generated format, public call inventory, facade, descriptor/result schema, semantic/MCP surface, CLI, README behavior, typed 12/2/170 truth, staged `.14.7`, or combined `.14.8`; synchronize ADR, Knowledge, capability guide, roadmaps, architecture, Toolbox, sole-facing mdBook, bounded histories, task/index, memory, and live status.
  Verification tier: `canonical`.
  Focused checks: checker-first RED for the completed-owner/pending-recurring mismatch; exact progressive neutral public/rollout/mutation GREEN plus the five-source/six-runtime driver with snapshot-only consumer diffs; typed-source, recognition, generated-source, capability, language, README/path/storage, and ordinary backend direct dependents; exact public-document markers/stale denials/outward guards; rendered mdBook; Knowledge, bounded histories, task/index, memory, nine doctrines, no-production/no-consumer-behavior/no-fixture/no-facade/schema/semantic/MCP/CLI/README-behavior diff, and exact staged diff.
  Canonical trigger: `the executable progressive rollout and public no-drift contract become final, and governed public projections move; exact staged receipt-bound local CI with the progressive opt-in is required before parent closure`.
  Ownership: `.14.6.8` owns the corrective behavioral-recurring promotion made necessary by `.14.6.7`'s completed driver but stale pending row, the independent progressive public projection/no-drift contract, exact current documentation, parent closure, and durable root-cause record; `.14.7` retains staged span dispatch and `.14.8` retains combined program-wide public no-drift.
  Checklist: [x] clean activation/task ownership [x] ADR/Knowledge/recurring/public-precedent retrieval [x] exact current public/outward inventory [x] checker-first governance RED [x] recurring-row correction [x] public projection/no-drift promotion [x] reason-checked omission/injection mutations [x] no-runtime/no-public-API proof [x] durable synchronization [x] focused signoff [x] staged canonical receipt [x] atomic commit/brief/clean handoff.
  Activation evidence: `git status --short` is empty at exact clean `b7c613be6ced8827271056950a85b77933733f9f`; `git_message_brief.txt` is zero bytes; the committed canonical receipt matches HEAD; no background work remains. `.14.6.7` is done and its exact driver passed neutral, Perl 129, cfg-enabled Rust 1, Dart 7, Julia 62, PUC Lua 178, LuaJIT 178, and four support ledgers, but its owned behavioral recurring row remains pending with no path. The mismatch is a bounded governance defect: the typed-only recurring scope copied an aggregate typed precedent even though the progressive contract independently defines the recurring row. No product behavior is missing. First composed closeout run makes neutral 9/9/116 plus public 6/12/10/60 GREEN, then Perl fails exactly four stale governance snapshots before any runtime assertion; all five source groups therefore require snapshot-only lockstep updates under this leaf.
  Evidence: checker-first execution fails exactly with `ContractError: completed recurring owner remains pending or is not bound to its committed driver`. The corrected neutral contract binds `recurring` to `tools/check_progressive_span_dispatch_six_runtime.sh`, completes only `public_no_drift`, and closes 9/9/116. Independent public governance requires six exact current documents, rejects twelve stale claims across ten guarded outward paths, and passes 60 reason-checked marker-deletion/stale-claim/private-token injection mutations. The exact recurring driver then passes neutral; Perl 129; cfg-enabled Rust 1/1 in 141.05 seconds; Dart 7; Julia 62; PUC Lua 178; LuaJIT 178; typed 12/2/170; generated-source 80/0/0; capability 80/0/0; and language 250/126. Git shows the five consumer source groups change only contract status/count/rollout snapshots: no fixture or behavioral assertion moves. Comparison with `b7c613be6ced8827271056950a85b77933733f9f` contains no parser, compiler, runtime, carrier, generated-format, public-call inventory, facade/schema/semantic/MCP/CLI/root-README behavior, or typed-contract change. ADR `0080`, Knowledge 873/7,355, capability guidance, roadmaps, architecture, Toolbox, sole-facing mdBook 79 files/15,208 KiB, bounded histories, task/index, memory/live status, README routing 20/62/32, and all nine doctrines pass. Exact staged receipt-bound canonical CI with `LINKEDSPEC_RUN_PROGRESSIVE_SPAN_MATRIX=1` passes before the atomic commit; parent `.14.6` closes and staged `.14.7` becomes the clean frontier. Commit: `FUTURE-PARITY-BACKLOG.14.6.8 - close progressive public no-drift`.
- ID: `FUTURE-PARITY-BACKLOG.14.7`
  Status: `active` (2026-08-25; audit `.0` and diagnostic `.1` are committed; neutral `.2` is canonical-signoff-complete from exact clean `54c65df8`; Perl dormant-boundary `.3.0` follows the atomic neutral commit; no push); Goal: Specify and implement staged AST enrichment where later loaded specs parse selected exact spans returned by an earlier AST level and stitch typed results through deterministic parse jobs; Children: audit/split `.0`, diagnostic prerequisite `.1`, neutral contract `.2`, backend parents `.3-.7`, recurrence `.8`, public authoring/closeout `.9`, and independent recomposition `.10`.
- ID: `FUTURE-PARITY-BACKLOG.14.7.0`
  Status: `done; focused-signoff-complete` (2026-08-25; task-tree-first from exact clean `fbcdedb6c79a20f232b5e77ee24a6e0cd57a7111`; no push); Goal: Audit the committed narrow function-body staged topology against the accepted general staged-AST architecture and freeze a dependency-complete, non-duplicative implementation plan before behavior.
  Depends on: completed progressive `.14.6`, accepted ADRs `0012`, `0014`-`0017`, typed source-location ADR `0056`, closed prototype tree `STAGED-LINKED-PARSING`, and five-backend/six-runtime function-body staged registries.
  Acceptance: retrieve the canonical staged architecture, annotation, registry/queue, language-neutrality, function-AST, source-location, Knowledge, mdBook, and existing backend authorities before code archaeology; use LinkedSpec's own descriptor/source/registry/runtime probes first; inventory exact current function-body payload/job/registry/queue/stitch/diagnostic behavior and all five backend/six runtime routes; distinguish already-current behavior from missing general authored `parse_job(...)`, selected exact-span metadata, result/failure policies, multiple parser families, deterministic recursive queues, cycle/depth/capability/resolution behavior, generated carriers, public and outward surfaces; audit relevant past changes and contradictions; freeze exact neutral, per-backend, recurrence, public-closeout, and independent-recomposition leaves without changing parser, compiler, runtime, fixture, generated format, registry behavior, public syntax/API/schema/semantic/MCP/CLI/README behavior, capability census, or progressive/typed truth; synchronize only the durable plan, Knowledge conclusions, roadmaps, architecture, Toolbox, sole-facing mdBook, bounded histories, task/index, memory, and live status.
  Verification tier: `focused`.
  Focused checks: exact committed function-body staged consumers and registry probes across Perl, Rust, Dart, Julia, PUC Lua, and LuaJIT; source/span/job/stitch/diagnostic inventories; generated/capability/language/semantic direct dependents; Knowledge, rendered mdBook, bounded histories, task/index, README/memory, nine doctrines, exact no-production/no-test/no-fixture/no-contract/no-format/no-CI-topology/no-outward diff.
  Canonical trigger: `none` — this leaf is a behavior-free authority audit and task split; executable governance, runtime behavior, dependencies/toolchains, storage/path/doctrine infrastructure, CI topology, and public surfaces remain unchanged.
  Ownership: `.14.7.0` owns only the read-only audit, durable causal/current-state facts, and dependency split. Follow-up leaves alone may own neutral authoring/dispatch contracts, backend behavior, admissions, recurrence, public no-drift, or recomposition. Combined program-wide `.14.8` remains separate.
  Checklist: [x] clean activation/task ownership [x] ADR/Knowledge/prototype retrieval [x] Toolbox-led five-backend/six-runtime current audit [x] exact implemented-versus-future inventory [x] contradiction/past-change audit [x] dependency-complete child split [x] durable synchronization [x] focused signoff [x] atomic commit/brief/clean handoff.
  Activation evidence: `git status --short` is empty at exact clean progressive-closeout commit `fbcdedb6c79a20f232b5e77ee24a6e0cd57a7111`; `git_message_brief.txt` is zero bytes; the promoted canonical receipt matches HEAD; no rendered book or background result remains. Retrieved Knowledge and ADRs establish that the current project already dispatches the narrow `body_parse_job` / `actionir-body.spec` / `action_block` family through deterministic registry adapters on Perl, Rust, Dart, Julia, PUC Lua, and LuaJIT. Toolbox-first descriptor/runtime probes prove the spec-returned payload/job survives normalization, dispatch, stitching, descriptor projection, and runtime output. The raw registry stable-sorts one-depth jobs and carries all declared policy strings, but current function-specific validators alone require `replace_field` / `body_ast` / `fail`; alternate result/failure policy behavior, multiple parser families, typed direct/derived source provenance, registered authority/provider resolution, recursive queues, cycle/depth/call bounds, general generated carriers, and public `parse_job(...)` remain future. Compile-phase wrong-top diagnostics preserve the original job on Julia/shared Lua but replace its id/path/payload/span/failure context with placeholders on Perl/Rust/Dart; `.1` owns that current narrow-path defect before feature expansion. The prototype decisions' old absolute “not implemented” clauses are historical baselines and require current-status annotations rather than reclassification of the accepted architecture.
  Signoff evidence: exact source comparison proves the current policy and diagnostic boundaries across all five registry owners. Direct Perl success plus wrong-top probe passes and reproduces only placeholder job context; exact Rust integration test passes 1/1; Dart staged-registry file passes 5/5; direct Julia, PUC Lua, and LuaJIT success/wrong-top probes pass with original job context. Neutral direct dependents pass unchanged: capability 80/0/0 with two exclusions/24 mutations, generated source 80/0/0, language 250/126, semantic 6/20/128 at 9/9 and 6/6, typed source 12/2/170, and progressive 9/9/116 plus public 6/12/10/60. The task partition checker reports 594 stable IDs and unchanged eight semantic/one history parts; the new member remains 221 lines / 73,822 bytes under unchanged pressure ceilings. ADR current notes, Knowledge, capability guidance, roadmaps, architecture, Toolbox, sole-facing book, bounded histories, task/index, memory/live status, README/memory, and all nine doctrines agree. Exact diff contains no production, test, fixture, executable contract, generated format, CI topology, capability manifest, facade/schema/semantic/MCP/CLI/README, or runtime behavior change. Commit: `FUTURE-PARITY-BACKLOG.14.7.0 - audit general staged AST boundary`.
- ID: `FUTURE-PARITY-BACKLOG.14.7.1`
  Status: `done; focused-signoff-complete` (2026-08-25; task-tree-first from exact clean audit commit `e50965b03a3e3b927516d9ddeb2a6a4c95a0fc37`; no push); Goal: Repair the current narrow staged registry's compile-phase diagnostic context parity before adding general dispatch behavior.
  Depends on: audit `.14.7.0` and the committed five-backend function-body registry.
  Acceptance: add RED assertions that a wrong `top_rule` reaches `phase=compile` with the original `job_id`, parent AST path, parser/resolved identities, top rule, payload kind, source span, and failure policy; pass the real normalized job through Perl, Rust, and Dart compile boundaries instead of synthesizing placeholders; preserve Julia/shared-Lua normalized-job propagation while adding their currently omitted `payload_kind` field; prove the unchanged success/resolve/execute/stitch paths on all five source backends and both Lua ABIs; change no annotation spelling, accepted parser identity, policy semantics, recursive queue, generated format, rollout, capability, public/outward surface, or docs beyond exact defect truth.
  Verification tier: `focused`.
  Focused checks: exact RED/GREEN wrong-top compile-context assertions plus unchanged staged registry success/resolve/stitch behavior on Perl, Rust, Dart, Julia, PUC Lua, and LuaJIT; backend-local focused suites; typed/progressive/generated/capability/language direct dependents; exact API/format/rollout/outward no-drift; Knowledge, rendered mdBook, bounded histories, task/index, README/memory, nine doctrines, and staged diff.
  Canonical trigger: `none` — this is one bounded diagnostic-context repair with no admission, public contract, executable governance/CI topology, generated format, dependency/toolchain, storage/path/doctrine infrastructure, or outward behavior movement.
  Ownership: `.14.7.1` owns only normalized-job propagation into Perl/Rust/Dart compile diagnostics, the missing Julia/shared-Lua `payload_kind` diagnostic field, exact cross-backend regression assertions, and synchronized current defect truth. Neutral general behavior remains exclusively `.2+`.
  Checklist: [x] clean activation/task ownership [x] audit/ADR/Knowledge retrieval [x] exact five-source/six-runtime RED assertions [x] minimal compile-context repair [x] unchanged success/resolve/stitch proof [x] direct-dependent/no-drift proof [x] durable synchronization [x] focused signoff [x] atomic commit/brief/clean handoff.
  Activation evidence: `git status --short` is empty at exact clean `e50965b03a3e3b927516d9ddeb2a6a4c95a0fc37`; `git_message_brief.txt` is zero bytes; no rendered book, incremental Rust cache, or background result remains. Audit `.0` and [[general-staged-ast-current-boundary]] root-cause the defect: Perl compile creates a two-field synthetic hash; Rust and Dart create explicit placeholder jobs; Julia/shared Lua already receive the original normalized job but omit its `payload_kind` from their diagnostic formatters. The current success path, resolve context, one built-in identity, and policy semantics are GREEN and out of scope.
  Signoff evidence: test-first wrong-top assertions reproduce exact placeholder id/path/payload/span/failure plus unresolved identity on Perl, the explicit placeholder boundary on Rust/Dart, and the missing payload field on Julia/PUC Lua/LuaJIT. The minimal repair passes the normalized job through Perl/Rust/Dart compile and adds only `payload_kind` to the Julia/shared-Lua formatter. GREEN proof is Perl phase 0 1,031/1,031; Rust exact integration 1/1; Dart staged registry 6/6; complete Julia with staged registry 39/39; and PUC Lua/LuaJIT 178/178 each. Direct dependents remain semantic 6 groups/20 queries/128 mutations plus six admissions and 5x2x3 CLI; typed 12/2/170; progressive 9/9/116; generated 80/0/0; capability 80/0/0; and language 250/126. Exact staged diff changes only five registry sources plus their exact tests; parser identity, success/resolve/execute/stitch behavior, policies, queue depth, generated format, capability/rollout, facade/schema/semantic/MCP/CLI/README, and public/outward behavior do not move. ADR current truth, Knowledge, roadmaps, architecture, Toolbox, sole-facing mdBook 79 files/15,232 KiB, bounded histories, task/index, memory/live status, README/memory, and all nine doctrines agree. Canonical CI is not triggered by this bounded focused-tier diagnostic repair. Commit: `FUTURE-PARITY-BACKLOG.14.7.1 - preserve staged compile diagnostic context`.
- ID: `FUTURE-PARITY-BACKLOG.14.7.2`
  Status: `done; canonical-signoff-complete` (2026-08-25; task-tree-first from exact clean diagnostic-parity commit `54c65df82800080a0cedf6b159f49b06aceb01af`; no push); Goal: Ratify and encode the exact backend-neutral general staged-AST enrichment contract before backend behavior.
  Depends on: diagnostic prerequisite `.1`, ADRs `0012`, `0014`-`0016`, and typed span authority ADR `0056`.
  Acceptance: checker-first executable schema and mutation oracle must define authored `parse_job(text_expr, options)`, a dedicated neutral ActionIR marker/sidecar boundary, direct same-source span versus ordered derived provenance, deterministic ids and queue order, caller-pre-registered parser authority, deterministic alias/declaring-spec/search-root/provider resolution performed before authored execution, cache/version/capability keys, all four result policies, all three failure policies, stage-chain isolation, recursive enqueue/stitch order, strict decreasing/cycle/depth/call/cancellation/resource ceilings, detached results, complete portable diagnostics, compatibility with the existing function-body v1 sidecar, five dormant backend consumers/six runtime routes, generated/reconstructed/emitted carrier requirements, rollout/public/outward guards, and exact ownership. It must explicitly reuse the typed source algebra and progressive bounded-authority principles without reusing progressive `dispatch_span` syntax or permitting filesystem/path authority from authored code. Planned verification is canonical because this adds the executable conformance/topology authority that later admissions consume.
  Verification tier: `canonical`.
  Focused checks: checker-first schema RED/GREEN; complete reason-checked mutation oracle; exact neutral inventories for authoring, provenance, resolution, cache, policies, recursive scheduling, bounds, detachment, diagnostics, compatibility, carriers, dormant consumers, rollout, public/outward guards, and downstream ownership; generated/capability/language/semantic/typed/progressive direct dependents; Knowledge, rendered mdBook, bounded histories, task/index, README/memory, nine doctrines, exact staged diff, and receipt-bound canonical CI.
  Canonical trigger: `mandatory` — this leaf publishes the executable conformance and topology authority consumed by every later backend admission.
  Ownership: `.14.7.2` owns only the backend-neutral executable contract, its checker/mutation proof, dormant route/carrier/consumer inventories, and synchronized contract truth. It changes no backend parser/compiler/runtime behavior, admission, rollout, generated format, or public/outward surface; `.3-.10` retain those owners.
  Checklist: [x] clean activation/task ownership [x] ADR/Knowledge/precedent retrieval [x] checker-first schema RED [x] complete neutral schema/oracle [x] mutation and ownership closure [x] direct-dependent/no-drift proof [x] durable synchronization [x] staged canonical receipt [x] atomic commit/brief/clean handoff.
  Activation evidence: `git status --short` is empty at exact clean `54c65df82800080a0cedf6b159f49b06aceb01af`; `git_message_brief.txt` is zero bytes; `docs/linkedspec-book/book` is absent; no background result remains. Diagnostic prerequisite `.1` is committed and the general staged behavior boundary remains unchanged. Required ADRs `0012`, `0014`-`0016`, `0056`, and `0080`; staged, typed-source, progressive, and current-boundary Knowledge cards; the mdBook; task ownership; and executable neutral precedents were retrieved before design.
  Implementation evidence: checker-first execution initially fails exactly because `capability_conformance/staged_ast_enrichment_contract.json` is absent. The first schema then fails an intentionally strengthened expected-count boundary because authored authority rejection and sibling/runtime isolation were omitted; both become executable cases before GREEN. The final repository-routed oracle selects artifact contract v1 with a future `staged_parse_job_v2` marker/sidecar record and reports 4 immutable registry entries; 2 sources; 8 provenance, 3 deterministic-id, 8 resolution, 6 authority, 10 cache, 4 queue, 3 isolation, 4 result-policy, 3 failure-policy, 10 chain, and 5 detachment cases; 4 carrier requirements; 5 dormant consumers over 6 routes; 10 outward guards; 37 diagnostics; 9 rollout legs; 35 exact owners; and 72 reason-checked mutations. Typed ordering compares field/source strings by Unicode scalar value and nonnegative indices numerically, with the queue case proving index 2 precedes 10. ADR `0088` is the canonical architecture decision and [[general-staged-ast-enrichment-neutral-contract]] is the retrieval card.
  Signoff evidence: the new checker plus semantic 6/20/128 at rollout 9/9 and admission 6/6, typed source 12/2/170, progressive 9/9/116 plus public 6/12/10/60, recognition 138/250/58, generated/capability 80/0/0, and language 250/126 pass. Knowledge generation/check reports 875 facts / 7,381 question keys. The sole-facing mdBook builds 79 files / 15,344 KiB; rendered HTML contains the new chapter at compiler position 28, previous/next navigation, both policy tables, and the artifact-v1/job-v2 distinction, then generated output is removed. The first canonical composition catches the new chapter at the adjacent exact aggregate-selector public inventory: discovery is 60 rather than the stale 59. The checker and its three canonical Knowledge owners now lock 60 public files / 25 classified history references / zero current examples, and the focused composed proof passes without weakening discovery or retirement. The corrected canonical composition next catches the new checker as a 32nd governed Python entrypoint; the project-data oracle and its canonical Knowledge owner now freeze 32 entrypoints with the same three Python temporary and 14 shell allocator owners. Exact source and checker guards keep five future consumers absent, all rollout except neutral pending, and all ten outward paths free of future tokens. Bounded histories remain within reviewed controls, task/index and memory are exact, and all nine doctrines pass. Exact fully staged receipt-bound canonical CI passes on the twice-corrected candidate before the atomic commit. Atomic handoff commit: `ab8802c94f3a0b65fc4d1e44f9a960e3c5b0c69c` (`FUTURE-PARITY-BACKLOG.14.7.2 - add neutral staged AST enrichment authority`); the brief is cleared and the tree is clean.
- ID: `FUTURE-PARITY-BACKLOG.14.7.3`
  Status: `done; closed` (2026-08-26; `.14.7.3.0-.4` are signoff-complete through the intended canonical Perl-admission commit; Rust `.14.7.4.0` is next; no push); Goal: Implement and admit the neutral staged-AST contract on Perl through independently committable leaves.
  Children: dormant RED `.0`; dedicated annotation/typed provenance `.1`; pre-registered resolution/cache plus policy stitching `.2`; recursive queue/bounds/diagnostics `.3`; reconstructed/generated carriers and Perl admission `.4`.
  Depends on: neutral contract `.2`. Every child consumes the same dormant final-path consumer; only `.4` may register it, promote Perl rollout, or change public-current projections.
- ID: `FUTURE-PARITY-BACKLOG.14.7.3.0`
  Status: `done; canonical-signoff-complete` (2026-08-25; task-tree-first from exact clean neutral-contract commit `ab8802c94f3a0b65fc4d1e44f9a960e3c5b0c69c`; no push); Goal: Freeze the exact Perl dormant general-staged RED and current function-body compatibility boundary without production behavior.
  Depends on: neutral executable contract `.14.7.2`, its ADR `0088`, and the committed Perl function-body v1 registry consumer.
  Acceptance: retrieve the neutral staged-AST contract/checker, current-boundary and Perl registry Knowledge, dormant-RED precedents, and exact Perl Toolbox probes before source archaeology; add one repository-routed Perl test consumer that remains absent from ordinary discovery, public registration, rollout, canonical topology, and generated carriers; advance only that predeclared consumer's neutral lifecycle from `pending_absent` to `dormant_red`, with the checker requiring its exact tracked path while proving zero ordinary/canonical registration; require the committed function-body v1 path to stay GREEN for normalized job identity, deterministic resolution, compile/execute/stitch, `replace_field` / `body_ast` / `fail`, and diagnostic context; freeze an exact general-v2 RED at the earliest missing dedicated `staged_parse_job_v2` annotation/typed-provenance boundary, with children `.1-.4` retaining that same final-path consumer and advancing its sole RED in their owned order; bind the child ownership sequence so `.1` alone adds the private node/provenance carrier, `.2` resolution/cache/policies, `.3` recursive queue/bounds/diagnostics, and `.4` fresh reconstructed/generated/emitted carriers plus admission; apply the mandatory complete-record change-history rollover and bound the live-status recent view to sixteen rows; if the new immutable segment crosses the finite resulting-tree controls, add one indexed ADR authorizing only the exact one-file/one-manifest-line increments while preserving every other route limit; otherwise change no production parser/compiler/runtime, existing test expectation, neutral semantic contract/counts/mutations, generated format, registry discovery, rollout/capability/public/outward surface, CLI, README, or canonical-CI topology; synchronize exact current/future truth in task/index, Knowledge, roadmap, architecture, Toolbox, live docs, and sole-facing mdBook.
  Verification tier: `canonical` (upgraded from focused after the mandatory change-history rollover crossed its finite resulting-tree capacity).
  Focused checks: exact new dormant Perl consumer proving function-body v1 GREEN and dedicated general-v2 boundary RED; existing Perl staged registry success/wrong-top compatibility; neutral staged checker inventories/mutations/guards plus exact Perl `dormant_red` lifecycle and zero route references; semantic, typed-source, progressive, generated-source, capability, and language direct dependents where the dormant test or lifecycle snapshot can affect discovery assertions; task partition/index, Knowledge, rendered mdBook, bounded histories including exact rollover reconstruction and finite route capacity, README/memory, nine doctrines, and exact no-production/no-registration/no-rollout/no-generated-format/no-public/no-canonical-topology diff.
  Canonical trigger: `mandatory` — the dormant behavior boundary remains focused, but its required complete-record history rollover publishes one immutable segment and changes finite resulting-tree file/manifest capacity; exact staged receipt-bound local CI is required before landing.
  Ownership: `.14.7.3.0` owns only the unregistered dormant Perl test consumer, its exact neutral `pending_absent` to `dormant_red` consumer/checker snapshot, v1 compatibility controls, exact first general-v2 RED, the same final path retained by `.1-.4`, synchronized boundary truth, mandatory complete-record rollover/live-status trim, and any mechanically required one-file/one-manifest-line finite history-capacity increment. It may not add or alter production behavior or any other neutral row. `.14.7.3.1-.4` retain the exact implementation/admission owners above; later backend parents `.4-.7`, recurrence `.8`, public closeout `.9`, and independent recomposition `.10` remain untouched.
  Checklist: [x] clean activation/task ownership [x] ADR/Knowledge/dormant-precedent retrieval [x] Toolbox-led Perl current-boundary probes [x] exact dormant v1-GREEN/general-v2-RED consumer [x] child ownership/final-path lock [x] direct-dependent/no-drift proof [x] mandatory history rollover/live-status bound [x] exact finite capacity ADR/route update [x] durable synchronization [x] focused signoff [x] staged canonical receipt [x] atomic commit/brief/clean handoff.
  Activation evidence: `git status --short --untracked-files=all` is empty at exact clean `ab8802c94f3a0b65fc4d1e44f9a960e3c5b0c69c`; `git_message_brief.txt` is zero bytes; the neutral-contract canonical receipt was promoted to that committed HEAD; `docs/linkedspec-book/book` is absent; no background result remains. `.14.7.2` is durably committed and all five backend consumers/six routes remain absent with only the neutral rollout leg complete.
  Implementation evidence: `t/staged_ast_enrichment_perl_contract.t` is repository-routed but absent from ordinary and canonical discovery. Its first 60 assertions freeze the full neutral inventories, only Perl's `dormant_red` lifecycle, the exact `.1-.4` ownership order, current function-body v1 resolve/load/compile/execute/stitch behavior, exact wrong-top diagnostic context, no implicit v1-to-v2 upgrade, and rejection of `expr-v1` at resolve without path loading. Assertion 61 is the sole intentional RED: current assignment lowering remains ordinary `ASSIGN` with one `LINKEDSPEC_UNSUPPORTED_ACTIONIR_HELPER:parse_job`, zero raw dependencies, and no `STAGED_PARSE_JOB_MARKER`. `tools/check_staged_ast_enrichment_contract.py` requires the exact tracked dormant path, restricts `dormant_red` to Perl, and proves zero references in `t/phase0_regression.t` and `tools/run_ci_local.sh`; all neutral counts, 72 mutations, rollout, routes, generated format, and outward guards remain unchanged. No production parser/compiler/runtime source changes.
  Continuity evidence: the mandatory complete-record rollover creates immutable `docs/history/changes/segment-4991-b1e841ec3afa.md` from activation commit `ab8802c94f3a0b65fc4d1e44f9a960e3c5b0c69c` and leaves the current `CHANGES.md` at 249/512 lines while the current live-status view is exactly sixteen rows. ADR `0089` authorizes only change-history resulting-tree capacity 21→22 files and manifest capacity 20→21 lines; root/member/aggregate line and byte limits, lifecycle, storage, owner, patterns, verifier, and query/recovery behavior are unchanged. The exact 22-file collection is 46,587/55,000 lines and 3,328,651/4,194,304 bytes.
  Signoff evidence: the dormant consumer reports exactly 60 GREEN assertions and one expected RED with `missing node=[STAGED_PARSE_JOB_MARKER]; unresolved helpers=[parse_job]; raw dependencies=0`. The neutral checker remains 4 registry entries, 8 provenance, 8 resolution/6 authority, 10 cache, 4 queue/3 isolation, 4 result, 3 failure, 10 chain, 5 detachment, 5 consumers/6 routes, 37 diagnostics, 9 rollout, 35 owners, and 72 mutations. Semantic 6/20/128 with rollout 9/9 and admission 6/6, typed source 12/2/170, progressive 9/9/116 plus public guards, generated and capability 80/0/0, and language 250/126 direct dependents pass. Knowledge generation/check reports 877 facts / 7,398 question keys. The sole-facing mdBook builds 80 files / 15,364 KiB; rendered HTML contains the dormant-boundary section, exact RED, project-status snapshot, and adjacent navigation, then generated output is removed. Task/index, bounded histories, README/memory, all nine doctrines, no-production/no-registration/no-rollout/no-generated-format/no-public/no-canonical-topology guards, and exact diff pass. The exact fully staged candidate clears receipt-bound canonical CI before the atomic commit.
  Atomic handoff evidence: commit `4a982f21a2980c1235da772d3396749f7aaed516` (`FUTURE-PARITY-BACKLOG.14.7.3.0 - freeze Perl staged AST enrichment RED`) contains the promoted canonical receipt; `git_message_brief.txt` is cleared to zero bytes, the generated mdBook output is absent, and the tree is clean with no background result remaining.
- ID: `FUTURE-PARITY-BACKLOG.14.7.3.1`
  Status: `done; focused-signoff-complete` (2026-08-26; task-tree-first from exact clean dormant-boundary commit `4a982f21a2980c1235da772d3396749f7aaed516`; no push); Goal: Add Perl's private dedicated parse-job annotation node and typed direct/derived source-provenance carrier while retaining the final-path RED.
  Depends on: neutral contract `.14.7.2`, ADR `0088`, the exact dormant final-path consumer committed by `.14.7.3.0`, the typed source-location algebra, and the current Perl ActionIR/scanner/rewriter authorities.
  Acceptance: retrieve the active leaf, ADR `0088`, staged current-boundary/neutral/dormant and typed-source Knowledge cards, and exact Perl Toolbox probes before source archaeology; introduce one private and exclusive `STAGED_PARSE_JOB_MARKER` / `staged_parse_job_v2` ActionIR representation only for the neutral contract's allowed `parse_job(text_expr, literal_options)` annotation form; preserve the marker as typed data rather than a generic helper callback or host-language invocation; attach typed source provenance using the existing source algebra, with direct spans for authored text and ordered derived segments for composed text; reject copied-text smuggling, reversed/out-of-bounds spans, empty derived segments, dynamic/malformed options, residual generic `parse_job` helper forms, and every non-annotation call shape; keep current function-body v1 behavior and diagnostics unchanged; retain the exact same dormant, unrouted final-path consumer and advance its sole expected RED to `.2`'s missing caller-pre-registered resolution/cache and result/failure policy authority; change no resolution/load/compile/cache/stitch behavior, recursive queue/resource policy, reconstructed/generated/emitted carrier, admission/discovery/rollout/capability/public/outward projection, generated format, CLI, README, canonical topology, or other backend; synchronize exact private-current/future truth in task/index, Knowledge, roadmap, architecture, Toolbox, live docs, and sole-facing mdBook.
  Verification tier: `focused` — this leaf changes only private Perl annotation lowering/provenance and its deliberately unrouted consumer; it neither admits a route nor changes public, infrastructure, storage, generated-format, or canonical topology.
  Focused checks: exact dormant Perl consumer proving marker/typed-provenance/current-v1 groups GREEN and only the next resolution/cache/policy boundary RED; existing Perl ActionIR/scanner/rewriter and staged registry compatibility; neutral staged checker inventories/mutations/guards with unchanged Perl `dormant_red` lifecycle and zero ordinary/canonical route references; typed-source, semantic, progressive, recognition, generated-source, capability, and language direct dependents where private lowering can affect shared assertions; task partition/index, Knowledge, rendered mdBook, bounded histories, README/memory, nine doctrines, and exact no-resolution/no-queue/no-carrier/no-admission/no-rollout/no-generated-format/no-public/no-canonical-topology diff.
  Canonical trigger: `none` — run receipt-bound canonical CI only if the implemented slice crosses a designated admission, milestone, public, infrastructure, storage, history-capacity, or final-push boundary.
  Ownership: `.14.7.3.1` owns only Perl's private dedicated annotation node, literal option normalization at that annotation boundary, typed direct/derived source provenance and its validation, the unchanged dormant consumer's RED advancement to the `.2` boundary, and synchronized current/future documentation. `.2` retains all resolution/cache/result/failure policy; `.3` all recursive scheduling/bounds/diagnostic rebasing; `.4` all fresh carriers/admission; other backends and combined recomposition remain untouched.
  Checklist: [x] clean activation/task ownership [x] ADR/Knowledge/Toolbox retrieval [x] exact private representation design [x] dedicated lowering/provenance implementation [x] malformed/dynamic/smuggling rejection [x] unchanged v1/next-boundary dormant oracle [x] direct-dependent/no-drift proof [x] durable synchronization [x] focused signoff [x] atomic commit/brief/clean handoff.
  Activation evidence: `git status --short --untracked-files=all` is empty at exact clean `4a982f21a2980c1235da772d3396749f7aaed516`; `git_message_brief.txt` is zero bytes; the `.14.7.3.0` canonical receipt is promoted to that committed HEAD; `docs/linkedspec-book/book` is absent; no background result remains. The same dormant final-path consumer is tracked and unrouted, with exactly its 61st assertion RED at the marker/provenance boundary before this leaf.
  Implementation evidence: `ActionIR::StagedParseJob` owns one exact exclusive assignment annotation, strict literal options, typed direct or flattened ordered-derived text plans, and private lowering; `StagedParseJob` owns the opaque marker, detached declaration sidecar, SourceLocation materialization/validation, and typed private failures; `StagedParseJobPolicy` rejects invalid dedicated events before execution. `LinkedRE` privately preserves full-match/defined-capture offsets for immediate typed conversion, the compiler runs the new declaration policy before progressive policy, and recognition effect closure rejects `STAGED_PARSE_JOB_MARKER` in uncommitted attempts. The same dormant consumer now has 120 GREEN assertions covering every neutral provenance case, live Unicode-scalar direct/derived spans, detachment, strict malformed/dynamic/copied-text rejection, residual generic closure, transaction denial, and unchanged function-body v1; its sole RED is `.2`'s missing pre-registered resolution/cache/result/failure authority. The language ledger's independent reverse check initially exposed `parse_job` as unclassified public inventory; explicit private-intrinsic classification restores unchanged 250/126 truth without adding any backend/public call.
  Signoff evidence: the dormant consumer reports 120 GREEN assertions and fails only assertion 121 with `missing authority=[pre_registered_resolution,immutable_cache,result_failure_policies]`; syntax checks pass for all three new owners, `LinkedRE`, `Compiler`, and the consumer. Eleven focused ActionIR/generated/typed/recognition/progressive files pass 249 tests, and broad Phase 0 passes 1,031/1,031 in 830 seconds. Neutral staged remains 4 registry entries, 8 provenance, 8 resolution/6 authority, 10 cache, 4 queue/3 isolation, 4 result/3 failure, 10 chain, 5 detachment, 5 consumers/6 routes, 37 diagnostics, 9 rollout, 35 owners, and 72 mutations. Semantic 6/20/128, typed 12/2/170, progressive 9/9/116 plus public guards, recognition 138/250/58, generated/capability 80/0/0, and language 250/126 pass. Knowledge generation/check reports 878 facts / 7,410 question keys. The mdBook builds 80 files / 15,380 KiB; rendered HTML contains the private boundary, exact RED, project status, and backend handoff, then generated output is removed. Task partitions remain 8 semantic/1 history/594 IDs; both histories, memory, README routing, all nine doctrines, exact no-route/no-neutral/no-registry/no-carrier/no-format/no-outward diff, and `git diff --check` pass. Focused tier remains correct: no admission, milestone, public, infrastructure, storage, history-capacity, generated-format, or canonical-topology trigger is crossed.
  Atomic handoff evidence: commit subject `FUTURE-PARITY-BACKLOG.14.7.3.1 - add Perl staged annotation provenance`; the committed candidate contains the synchronized task/index, Knowledge, roadmaps, architecture, Toolbox, live docs, and sole-facing mdBook; `git_message_brief.txt` is cleared after commit and `.14.7.3.2` is the next task-tree-first action.
- ID: `FUTURE-PARITY-BACKLOG.14.7.3.2`
  Status: `done; focused-signoff-complete` (2026-08-26; task-tree-first from exact clean private-carrier commit `3a99202ece0cf0c942d38aa7d8789a5690e76df3`; no push); Goal: Add Perl's caller-pre-registered resolution/cache authority and all result/failure stitching policies while recursive scheduling remains RED.
  Depends on: neutral contract `.14.7.2`, ADR `0088`, committed private marker/provenance `.14.7.3.1`, and the current narrow v1 staged registry as compatibility-only evidence.
  Acceptance: retrieve the active leaf, ADR `0088`, neutral resolution/authority/cache/result/failure cases and diagnostics, private marker/provenance fact, current v1 boundary, progressive bounded-authority precedent, and exact Toolbox probes before source archaeology; add one private caller-prepared immutable registry containing normalized logical identities and already-compiled opaque callbacks only, with aliases/declaring-relative/search-root/provider candidate outcomes supplied and frozen before authored execution; perform pure post-AST selection with exact missing/ambiguous/alias-collision/top/capability/version/policy diagnostics and no filesystem/provider/import/environment/network/loading/compilation authority; normalize selected top before deterministic v2 job-id construction; add immutable cache identity over content/import digests, contract versions, selected top, and sorted effective capabilities, with exact hit/miss/invalidation/isolation behavior and no failed/partial-result poisoning; execute only the current complete depth in deterministic typed parent-path/provenance/job-id order, give siblings isolated runtime inputs, and apply all four result policies (`replace_marker`, `replace_field`, `sibling_field`, `append_child`) plus all three failure policies (`fail`, `keep_text`, `diagnostic_node`) with detached results and exact marker/target/collision/kind diagnostics; retain the same dormant final-path consumer and advance its sole RED to `.3`'s missing breadth-first recursive scheduling, decreasing-chain/cycle/depth/call/cancellation/resource bounds, and original-source diagnostic rebasing; keep function-body v1 unchanged; add no recursive rescan, carrier reconstruction/generation/emission, admission/discovery/rollout/capability/public/outward projection, generated-format, CLI, README, canonical topology, or other backend behavior; synchronize task/index, Knowledge, roadmap, architecture, Toolbox, live docs, and sole-facing mdBook.
  Verification tier: `focused` — this leaf changes only private Perl post-AST native authority/cache/policy behavior and the deliberately unrouted consumer; it does not admit a route or change public, infrastructure, storage, generated-format, or canonical topology.
  Focused checks: exact dormant Perl consumer proving committed marker/provenance/v1 groups plus all neutral resolution/authority/cache/result/failure cases GREEN and only recursive scheduling/bounds/diagnostic rebasing RED; immutable-registry/cache/adversarial callback and detached-stitch tests; existing Perl staged registry compatibility; neutral staged checker with unchanged `dormant_red` lifecycle and zero ordinary/canonical route references; typed-source, semantic, progressive, recognition, generated-source, capability, and language direct dependents; task partition/index, Knowledge, rendered mdBook, bounded histories, README/memory, nine doctrines, and exact no-recursive-queue/no-carrier/no-admission/no-rollout/no-generated-format/no-public/no-canonical-topology diff.
  Canonical trigger: `none` — run receipt-bound canonical CI only if implementation crosses an admission, milestone, public, infrastructure, storage, history-capacity, generated-format, or canonical-topology boundary.
  Ownership: `.14.7.3.2` owns only the private caller-prepared immutable registry/cache, pure resolution from its frozen candidate outcomes, deterministic v2 job identity, current-depth native execution, result/failure policies, and synchronized next-boundary truth. `.3` retains recursive scheduling, decreasing-chain/cycle/resource/cancellation limits, and source-rebased diagnostics; `.4` retains reconstructed/generated/emitted fresh-authority carriers and admission; public authoring remains `.14.7.9`.
  Checklist: [x] clean activation/task ownership [x] ADR/Knowledge/neutral/Toolbox retrieval [x] exact frozen authority/cache design [x] pure resolution/job-id implementation [x] current-depth native execution/cache [x] four result/three failure policies [x] adversarial authority/detachment rejection [x] unchanged v1/next-boundary dormant oracle [x] direct-dependent/no-drift proof [x] durable synchronization [x] focused signoff [x] atomic commit/brief/clean handoff.
  Activation evidence: `git status --short --untracked-files=all` is empty at exact clean `3a99202ece0cf0c942d38aa7d8789a5690e76df3`; `git_message_brief.txt` is zero bytes; `docs/linkedspec-book/book` is absent; no background result remains. `.14.7.3.1` is durably committed with 120 GREEN assertions and only the pre-registered resolution/cache/result/failure authority RED; neutral rollout, ordinary/canonical discovery, generated carriers/formats, and outward surfaces remain unchanged.
  Implementation evidence: unexported `LinkedSpec::StagedASTEnrichment` freezes caller-completed alias/relative/ordered-root/provider outcomes and exact logical entries whose only executable authority is an already-compiled callback. Pure resolution, default-top-before-v2-id, version/capability/policy/source-detail narrowing, exact cache identity, locked plan-only cache, deterministic current-depth typed ordering, fresh sibling runtime requests, all four result and three failure policies, detached node-bounded results, atomic fail, retained diagnostics, and exact marker/target/live-result denials are implemented. The same dormant consumer has 133 GREEN top-level checks and one RED for `.3` recurrence, chain/cancellation/resource bounds, and source-rebased diagnostics; a newly returned inert marker stays unscanned. Seven direct-dependent Perl files pass 213 tests. Neutral staged 72 mutations, semantic 6/20/128, typed 12/2/170, progressive 9/9/116, recognition 138/250/58, generated/capability 80/0/0, and language 250/126 remain exact. Knowledge is 879 facts / 7,424 question keys; the mdBook builds 80 files / 15,400 KiB with exact rendered current-depth/status/handoff text and no retained build output. V1, ordinary/canonical discovery, rollout, carriers, format, public/outward surfaces, and other backends remain unchanged.
  Signoff evidence: the exact dormant oracle reports `Failed 1/134 subtests` and only the expected recursive-authority marker; module/test syntax and whitespace pass. Task partitions remain 8 semantic/1 history/594 IDs; both bounded histories remain below rollover pressure; Memory and Knowledge checks pass; rendered mdBook output was inspected then removed; all nine doctrines pass. Exact Git guards prove README, neutral staged/generated/language artifacts, phase-0 and canonical routes, Perl `dormant_red`/pending rollout, existing production files, recursive API, carriers, formats, public/outward paths, and other backends do not move. Focused tier remains correct because this leaf adds one private unrouted module and expands its dormant consumer without admission, milestone, public, infrastructure, storage, history-capacity, format, or canonical-topology movement. Intended commit subject: `FUTURE-PARITY-BACKLOG.14.7.3.2 - add Perl staged current-depth authority`; brief is cleared after commit and `.14.7.3.3` is next.
- ID: `FUTURE-PARITY-BACKLOG.14.7.3.3`
  Status: `done; focused-signoff-complete` (2026-08-26; task-tree-first from exact clean current-depth-authority commit `26809fe85337efca55fd2970cc4de2d1ca353232`; no push); Goal: Complete Perl recursive queue scheduling, decreasing-chain/cycle/resource guards, shared cancellation/resource authority, and original-source diagnostic rebasing while discovery remains dormant.
  Depends on: neutral contract `.14.7.2`, ADR `0088`, committed private marker/provenance `.14.7.3.1`, committed current-depth resolution/cache/policy authority `.14.7.3.2`, ADR `0056` typed provenance, and progressive bounded-authority precedent as mechanism evidence only.
  Acceptance: retrieve the active leaf, ADRs `0056`/`0088`, neutral queue/isolation/chain/detachment/diagnostic cases, the Perl marker/current-depth facts, typed source cursor algebra, progressive cancellation/resource precedent, and exact Toolbox probes before source archaeology; extend the same private Perl authority with one breadth-first `enrich_recursively` entrypoint that validates/resolves the complete current depth before executing any child, stitches settled jobs in depth/typed-parent-path/typed-provenance/job-id order, and discovers newly stitched markers only for the next depth; preserve fresh sibling parser state and one immutable registry/cache while carrying one immutable stage-chain view per callback; compute payload digest over exact text and reject exact active parser/top/payload/provenance tuple repeats as `staged_cycle`; allow repeated parser/top lineage only when every direct or ordered-derived child segment is contained by an active segment and total Unicode-scalar extent strictly decreases, otherwise `staged_chain_non_decreasing`; share and monotonically spend caller-owned cancellation identity/callback, absolute deadline/clock, remaining steps, total calls, maximum depth/calls/result nodes/diagnostic bytes across every depth without reset or elevation, with exact dispatch-entry and child-safe-point diagnostics; map child-local positions/spans/diagnostics through the job's direct or ordered-derived provenance back to original source identities without inventing a contiguous derived span, truncate only through the exact diagnostic-byte policy, and retain detached scheduler diagnostics; preserve `.2`'s pure resolution/cache, current-depth ordering, all result/failure policies, atomic publication, detachment, exact stitch diagnostics, and failed-result cache isolation; retain the same dormant final-path consumer and advance its sole RED to `.4`'s missing native/reconstructed/generated-plan/independently loaded emitted fresh-authority carriers plus ordinary/canonical admission and Perl rollout; keep function-body v1 unchanged; add no carrier reconstruction/generation/emission, route/admission/rollout/capability/public/outward projection, generated-format, CLI, README, canonical topology, or other backend behavior; synchronize task/index, Knowledge, roadmap, architecture, Toolbox, live docs, and sole-facing mdBook.
  Verification tier: `focused` — this leaf completes only private dormant recursive Perl authority and its unrouted consumer; it does not admit a route or change public, infrastructure, storage, generated-format, or canonical topology.
  Focused checks: exact dormant Perl consumer proving committed marker/provenance/resolution/cache/policy groups plus all neutral queue/isolation/chain/resource/detachment/diagnostic cases GREEN and only fresh carriers/admission RED; adversarial breadth-first ordering, same-text/different-provenance identity, derived containment/decrease, cycle, depth/call/step/cancellation/deadline, result-node/diagnostic-byte, callback safe-point, and rebasing tests; existing v1 and `.2` compatibility; neutral staged checker with unchanged `dormant_red` lifecycle and zero ordinary/canonical route references; typed-source, semantic, progressive, recognition, generated-source, capability, and language direct dependents; task partition/index, Knowledge, rendered mdBook, bounded histories, README/memory, nine doctrines, and exact no-carrier/no-admission/no-rollout/no-generated-format/no-public/no-canonical-topology diff.
  Canonical trigger: `none` — run receipt-bound canonical CI only if implementation crosses an admission, milestone, public, infrastructure, storage, history-capacity, generated-format, or canonical-topology boundary.
  Ownership: `.14.7.3.3` owns only recursive breadth-first scheduling over the existing native opaque markers, active-chain decrease/cycle logic, shared cancellation/deadline/step/call/depth/result/diagnostic limits, source-rebased child diagnostics, and synchronized next-boundary truth. `.4` retains all reconstructed/generated/emitted fresh-authority carriers, ordinary/canonical discovery, Perl rollout promotion, and parent behavioral closure; public authoring remains `.14.7.9`.
  Checklist: [x] clean activation/task ownership [x] ADR/Knowledge/neutral/Toolbox retrieval [x] exact recursive authority design [x] breadth-first next-depth scheduler [x] decreasing chain/cycle guards [x] shared cancellation/resource limits [x] original-source diagnostic rebasing [x] adversarial detachment/atomicity [x] unchanged `.2`/v1/next-boundary oracle [x] direct-dependent/no-drift proof [x] durable synchronization [x] focused signoff [x] atomic commit/brief/clean handoff.
  Activation evidence: `git status --short --untracked-files=all` is empty at exact clean `26809fe85337efca55fd2970cc4de2d1ca353232`; `git_message_brief.txt` is zero bytes; `docs/linkedspec-book/book` is absent; no background result remains. `.14.7.3.2` is durably committed with 133 GREEN top-level checks and only recursive queue/bounds/rebased-diagnostic authority RED; neutral rollout, ordinary/canonical discovery, generated carriers/formats, and outward surfaces remain unchanged.
  Implementation evidence: private `enrich_recursively` reuses the frozen resolution/cache/policy engine while preserving one-depth `enrich_ast`. Every complete depth prepares before callbacks and settles breadth-first; exact parser/top/exact-text-digest/full-provenance tuples detect cycles, and same-parser/top recurrence requires contained segments plus smaller total scalar extent. One caller/entry-narrowed cancellation/deadline/step/call/depth/result-node/diagnostic-byte authority spans all depths. Ephemeral contexts implement safe points and direct/ordered-derived source projection, then expire. The same dormant oracle has 141 GREEN top-level checks and one `.4` fresh-carrier/admission/Perl-rollout RED. Function-body v1, neutral lifecycle/rollout, ordinary/canonical discovery, formats, public/outward surfaces, and other backends do not move.
  Signoff evidence: exact `Failed 1/142 subtests` plus the sole carriers/admission marker passes; module/test syntax and whitespace pass; seven adjacent Perl files pass 230 tests. Neutral staged 72 mutations, semantic 6/20/128, typed 12/2/170, progressive 9/9/116, recognition 138/250/58, generated/capability 80/0/0, and language 250/126 remain exact. Knowledge is 880 facts / 7,437 question keys; mdBook builds 80 files / 15,408 KiB with exact rendered status/example text and no retained build output. Task partitions remain 8 semantic/1 history/594 IDs; bounded histories remain below rollover pressure; Memory, README, Knowledge, and all nine doctrines pass. Exact Git guards prove no carrier/admission/rollout/format/public/canonical-topology/other-backend movement. Focused tier remains correct because the leaf changes only private unrouted Perl authority and its dormant consumer. Intended commit subject: `FUTURE-PARITY-BACKLOG.14.7.3.3 - add Perl staged recursive authority`; brief is cleared after commit and `.14.7.3.4` is next.
- ID: `FUTURE-PARITY-BACKLOG.14.7.3.4`
  Status: `active` (2026-08-26; task-tree-first from exact clean recursive-authority commit `adcae40513fb8954cc22cae3941bccb108b9a5d8`; no push); Goal: Prove Perl native, normalized reconstructed, generated-plan, and independently loaded emitted routes use fresh authority, then admit the unchanged consumer exactly once, promote only Perl rollout, and close the behavioral parent.
  Depends on: neutral contract `.14.7.2`, ADR `0088`, committed Perl marker/provenance `.14.7.3.1`, current-depth authority `.2`, recursive authority `.3`, existing Perl reconstructed/generated/emitted carrier precedents, and ADR `0073` admission verification.
  Acceptance: retrieve the active leaf, ADR `0088`, the committed recursive-authority fact/oracle, neutral carrier/backend/rollout/canonical rows, exact Perl normalized reconstruction/generated-plan/emitted-module mechanisms, ordinary phase-0 and canonical registration topology, and analogous committed fresh-authority admission precedents before source archaeology; preserve one logical `STAGED_PARSE_JOB_MARKER`/`staged_parse_job_v2` declaration plus exact provenance/job/cache inputs through native, normalized reconstructed, generated-plan, and independently loaded emitted-module carriers; construct a fresh caller-supplied immutable registry/cache and recursive cancellation/resource authority for every top-level execution so one run cannot reuse callbacks, queue, cache, cancellation, deadline, budget, source authority, or mutable state from another; keep callbacks, compiled parsers, registry/source authority, cancellation/deadline/budget state, mutable queues, filesystem paths, and host handles absent from normalized/generated/emitted data; make all four routes return equal detached AST/sidecar/diagnostic results and preserve function-body v1 plus transaction closure; make the same final-path consumer fully GREEN, remove its dormant-only expectation without duplicating or weakening its assertion body, add it exactly once to ordinary discovery and once to canonical CI with exact tracked-file/marker/invocation guards, promote only the Perl backend consumer and `perl` rollout truth, close `.14.7.3` behaviorally, and retain Rust/Dart/Julia/Lua/recurrence/public rows pending; change no public `parse_job(...)` inventory, capability/outward/facade/schema/semantic/MCP/CLI/README surface, generated format unrelated to the logical carrier, other backend behavior, or combined `.14.8`; synchronize task/index, neutral authority/checker, Knowledge, roadmap, architecture, Toolbox, live docs, and sole-facing mdBook.
  Verification tier: `canonical` — this leaf changes ordinary/canonical test topology, executable Perl rollout/admission truth, and closes the behavioral backend parent.
  Focused checks: unchanged exact Perl oracle GREEN through native/reconstructed/generated-plan/emitted fresh-authority routes; fresh-run isolation and serialized-authority absence; function-body v1 and transaction closure; neutral staged checker plus exact Perl-only backend/rollout/mutation movement; ordinary phase-0 and canonical tracked-file/marker/invocation multiplicity; typed-source, semantic, progressive, recognition, generated-source, capability, and language direct dependents; Knowledge, rendered mdBook, bounded histories, task/index, README/memory, nine doctrines, and exact no-public/no-outward/no-other-backend diff.
  Canonical trigger: `mandatory` — ordinary/canonical discovery, executable backend admission/rollout truth, and the Perl behavioral-parent boundary move together; stage the exact candidate and run receipt-bound `tools/run_ci_local.sh` before commit.
  Ownership: `.14.7.3.4` owns only four Perl fresh-authority carriers, exact ordinary/canonical registration, Perl consumer/rollout promotion, and `.14.7.3` behavioral closure. Rust `.14.7.4+`, recurrence `.8`, public authoring `.9`, and combined program-wide `.14.8` remain independently owned.
  Checklist: [x] clean activation/task ownership [x] ADR/Knowledge/neutral/Toolbox/precedent retrieval [x] exact four-carrier design [x] native/reconstructed/generated/emitted fresh authority [x] serialized authority absence and run isolation [x] unchanged v1/transaction behavior [x] exact consumer GREEN [x] ordinary/canonical single admission [x] Perl-only rollout/parent closure [x] direct-dependent/no-drift proof [x] durable synchronization [x] staged canonical receipt [x] atomic commit/brief/clean handoff.
  Activation evidence: exact `git status --short --untracked-files=all` is empty at `adcae40513fb8954cc22cae3941bccb108b9a5d8`; `git_message_brief.txt` is zero bytes; `docs/linkedspec-book/book` is absent; no background result remains. `.14.7.3.3` is durably committed with 141 GREEN top-level checks and only fresh carriers/admission/Perl rollout RED; neutral lifecycle/rollout, ordinary/canonical discovery, generated carriers/formats, public/outward surfaces, and other backends remain unchanged.
  Implementation evidence: private `LinkedSpec::StagedASTEnrichmentRuntime` validates one exact host-only invocation configuration, constructs a new `LinkedSpec::StagedASTEnrichment` registry copy/cache before authored execution, preserves identity-bearing cancellation/clock authority, derives live transaction state from the descriptor/input, and runs recursive enrichment after the complete parent AST returns inside the existing typed-error boundary. Live and emitted-v2 `Execute` wrappers use this seam; normalized descriptor reconstruction re-enters it explicitly. The oracle proves equal detached native, normalized-descriptor, validated generated-plan, and independently loaded emitted-package AST/sidecar/diagnostic/cache/resource results; four distinct snapshot aggregates, sixteen callback refs, four cancellation identities/callbacks, four clocks, and four independent zero-hit/one-miss caches prove run isolation. Normalized ActionIR, generated plans, and emitted source contain no callback, compiled parser, registry snapshot, source authority, cancellation/deadline/budget state, mutable queue, path, or host handle. Function-body v1, generated-source v2, and transaction closure remain exact.
  Focused signoff evidence: the unchanged final-path contract is GREEN at 143 top-level checks; its exact path occurs once in phase-0 and its canonical tracked-file/syntax/marker/invocation topology is 1/1/1/1. The staged neutral checker reports 4 registry entries, 8 provenance, 8 resolution/6 authority, 10 cache, 4 queue/3 isolation, 4 result/3 failure policies, 10 chain, 5 detachment, 37 diagnostics, 9 rollout legs, 35 owners, and 78 mutations with only neutral+Perl complete. ActionIR 23 and generated-source 6 pass; the adjacent generated/typed/progressive/recognition set passes 204 tests. Semantic 6/20/128, typed 12/2/170, progressive 9/9/116, recognition 138/250/58, generated/capability 80/0/0, and language 250/126 pass. Knowledge is 881 facts/7,448 question keys; mdBook renders 80 files/14.91 MiB with the exact 143-check/four-route/next-Rust truth and is cleaned afterward. Change/engineering hot shards are 330/407 lines; task partitions remain 8 semantic/1 immutable history/594 IDs; Memory, README routing, Knowledge, exact diff, and all nine doctrines pass. Exact guards prove README, ten outward paths, four later consumers, other backend source, generated format, public inventory, facade/schema/semantic/MCP/CLI, and combined `.14.8` do not move.
  Canonical completion and handoff evidence: the exact fully staged candidate clears receipt-bound `bash tools/run_ci_local.sh`; pre-commit revalidates the receipt and all fast doctrines. Intended atomic commit subject: `FUTURE-PARITY-BACKLOG.14.7.3.4 - admit Perl staged-AST enrichment`; the brief is cleared after commit, `.14.7.3` is closed, and Rust dormant RED `.14.7.4.0` is next from the clean handoff.
- ID: `FUTURE-PARITY-BACKLOG.14.7.4`
  Status: `done; closed` (2026-08-26; `.14.7.4.0-.4` are signoff-complete through the intended canonical Rust-carrier-admission commit; Dart `.14.7.5.0` is next; no push); Goal: Implement and admit the neutral staged-AST contract on Rust through independently committable leaves.
  Children: dormant RED `.0`; dedicated annotation/typed provenance `.1`; pre-registered resolution/cache plus policy stitching `.2`; recursive queue/bounds/diagnostics `.3`; reconstructed/generated carriers and Rust admission `.4`.
  Depends on: completed Perl parent `.3` as reference evidence, never as semantic authority. Every child consumes the neutral contract and same dormant Rust final path; only `.4` may register it or promote Rust.
- ID: `FUTURE-PARITY-BACKLOG.14.7.4.0`
  Status: `done; focused-signoff-complete` (2026-08-26; task-tree-first from exact clean Perl-admission commit `fc3e3652efa73a2f47e8fdcc8d597eae0f40e73b`; no push); Goal: Freeze Rust's exact dormant general-staged RED and current function-body compatibility boundary without production behavior.
  Depends on: neutral executable contract `.14.7.2`, ADR `0088`, committed Perl admission `.14.7.3.4` as reference evidence only, Rust's current function-body v1 registry, and the committed outer-cfg Rust dormant-consumer convention.
  Acceptance: retrieve the active stable leaf, ADR `0088`, neutral/current-boundary/Perl-dormant Knowledge, exact Rust staged-registry and generated-carrier Toolbox probes, and the committed Rust progressive dormant precedent before source archaeology; add exactly one repository-routed `rust/linkedspec-runtime/tests/staged_ast_enrichment_contract.rs` consumer behind a dedicated outer cfg so ordinary Cargo discovery compiles zero active tests and canonical CI neither requires nor invokes it; advance only Rust's predeclared backend-consumer lifecycle from `pending_absent` to `dormant_red`, with the independent checker requiring the exact tracked path while proving zero canonical references and unchanged Rust rollout; keep current function-body v1 GREEN for normalized job identity, deterministic queue order, built-in resolution/load/compile/execute/cache records, exact `replace_field` / `body_ast` / `fail` semantics, and wrong-top diagnostic context; freeze one exact general-v2 final-path RED at the earliest missing dedicated `STAGED_PARSE_JOB_MARKER` / typed-provenance boundary through Rust's native, normalized reconstructed, generated-plan, and independently compiled emitted-source observation surfaces without adding carrier behavior; bind `.1-.4` to the same consumer and sole RED in order: `.1` private marker/provenance, `.2` caller-frozen resolution/cache/result/failure policies, `.3` breadth-first recurrence/bounds/source-rebased diagnostics, and `.4` fresh four-route authority plus canonical admission; change no Rust production source, existing test expectation, neutral behavior/count other than Rust lifecycle and its checker mutation, generated format, ordinary/canonical topology, rollout/capability/public/outward surface, other backend, CLI, README, or combined `.14.8`; synchronize task/index, Knowledge, roadmaps, architecture, Toolbox, bounded live docs, memory, and sole-facing mdBook.
  Verification tier: `focused` — only one deliberately failing outer-cfg test, its neutral lifecycle/checker snapshot, and durable boundary truth move; there is no admission, milestone, public, infrastructure, storage, history-capacity, generated-format, or canonical-topology change.
  Focused checks: exact cfg-enabled dormant Rust consumer with every neutral/current-v1/carrier-observation pre-boundary group GREEN and only one dedicated-marker/provenance assertion RED; ordinary Cargo target reports zero active tests; neutral staged checker requires Rust `dormant_red`, rejects the added lifecycle mutation, and proves no canonical-driver reference; current Rust integration/trace staged-registry tests plus generated source and progressive carrier direct dependents; semantic, typed-source, recognition, capability, and language governance where discovery snapshots can drift; task partition/index, Knowledge, rendered mdBook, bounded histories, README/memory, all nine doctrines, exact no-production/no-registration/no-rollout/no-generated-format/no-public/no-other-backend diff, and `git diff --check`.
  Canonical trigger: `none` — run receipt-bound canonical CI only if the implemented slice unexpectedly crosses a designated admission, milestone, public, infrastructure, storage, finite-history-capacity, generated-format, or final-push boundary.
  Ownership: `.14.7.4.0` owns only the exact outer-cfg dormant Rust consumer, Rust's neutral `pending_absent` to `dormant_red` lifecycle plus its checker mutation, current-v1 compatibility controls, the first general-v2 RED, the same final path retained by `.1-.4`, and synchronized boundary truth. `.1` retains marker/provenance; `.2` resolution/cache/result/failure policies; `.3` recurrence/bounds/diagnostic rebasing; `.4` fresh carriers/admission/Rust rollout. Later backends `.5-.7`, recurrence `.8`, public closeout `.9`, recomposition `.10`, and combined `.14.8` remain untouched.
  Checklist: [x] clean activation/task ownership [x] ADR/Knowledge/Toolbox/dormant-precedent retrieval [x] exact Rust current-boundary probes [x] exact outer-cfg v1-GREEN/general-v2-RED consumer [x] Rust-only neutral lifecycle/checker movement [x] child ownership/final-path lock [x] direct-dependent/no-drift proof [x] durable synchronization [x] focused signoff [x] atomic commit/brief/clean handoff.
  Acceptance Checklist:
  - [x] **REPRODUCE / ISSUE** — exact `RUSTFLAGS='--cfg linkedspec_staged_ast_enrichment_red' bash tools/run_cargo_local.sh test --offline --manifest-path rust/Cargo.toml -p linkedspec-runtime --test staged_ast_enrichment_contract` completes one test with one final `LINKEDSPEC_STAGED_AST_ENRICHMENT_RED`, missing `STAGED_PARSE_JOB_MARKER`, and absent `staged_parse_job_v2`; ordinary discovery reports 0 tests.
  - [x] **ROOT CAUSE (WHY + WHERE)** — the consumer's compiler/serialized/generated probes show one `Expr::AssignScalar` containing generic `Expr::Call name=parse_job` and no dedicated node; `rust/linkedspec-runtime/src/engine.rs`'s unknown-helper default returns `Undef`/JSON null, while `staged_parser_registry.rs` denies `expr-v1` at resolve.
  - [x] **FIX** — this leaf adds only the outer-cfg consumer plus Rust absent-lifecycle to `dormant_red`, zero-canonical-reference enforcement, one checker mutation, and durable truth; `.1-.4` retain production behavior.
  - [x] **ADDRESSED (verified)** — the opt-in test reaches line 552 only after neutral, current-v1, registry-denial, native, reconstructed, generated-plan, and independently compiled emitted observations pass; the sole expected RED text is exact.
  - [x] **NO REGRESSION** — current v1 and staged trace pass 1/1 each, generated-source v2 passes 1/1, staged/progressive ordinary targets report 0 tests, semantic/typed/progressive/recognition/generated/capability/language governance and all nine doctrines pass, and exact no-production/no-route/no-rollout guards are empty.
  - [x] **LOCKSTEP** — task/index, 79-mutation neutral contract/checker, 882-fact/7,457-key Knowledge Map, roadmaps, architecture, Toolbox, bounded live docs, memory, rendered/cleaned mdBook, ADR, and capability README agree on dormant Rust and next owner `.1`.
  Activation evidence: `git status --short --untracked-files=all` is empty at exact clean `fc3e3652efa73a2f47e8fdcc8d597eae0f40e73b`; `git_message_brief.txt` is zero bytes; the `.14.7.3.4` canonical receipt is promoted to that committed HEAD; `docs/linkedspec-book/book` is absent; no background result remains. Neutral and Perl are complete at 78 mutations, the Perl consumer is 143/143 and admitted once, all four later consumer paths are absent, and Rust rollout/public/outward truth remains pending.
  Implementation evidence: `rust/linkedspec-runtime/tests/staged_ast_enrichment_contract.rs` is the sole new executable surface and is file-gated by `linkedspec_staged_ast_enrichment_red`. Ordinary Cargo reports zero tests. The opt-in target preserves the complete neutral inventory and current v1 two-job order, four phases, built-in provider/cache, ActionIR result, function-specific policies, and wrong-top context; the current registry rejects general `expr-v1`. The compiler retains one generic `parse_job` call and no dedicated marker/sidecar; native, JSON-reconstructed, generated-plan, and independently compiled emitted routes return null. Only the final assertion fails with the exact missing-marker/typed-provenance RED. The neutral checker requires Rust `dormant_red`, zero canonical references, three later absent consumers, unchanged Rust rollout, and 79 mutations. No Rust production source, existing expectation, generated format, public/outward path, or other backend moves.
  Focused signoff evidence: the cfg-enabled target completes all pre-boundary work and fails only its one final test at line 552 with exact `LINKEDSPEC_STAGED_AST_ENRICHMENT_RED`; ordinary discovery is 0 tests. Current v1 integration and staged trace are 1/1 each; generated-source-v2 neutral plan/trace is 1/1; ordinary progressive discovery is 0 tests. Neutral staged reports 4 registry entries, 8 provenance, 8 resolution/6 authority, 10 cache, 4 queue/3 isolation, 4 result/3 failure policies, 10 chain, 5 detachment, 37 diagnostics, 9 rollout legs, 35 owners, and 79 mutations. Semantic 6/20/128, typed 12/2/170, progressive 9/9/116, recognition 138/250/58, generated/capability 80/0/0, and language 250/126 pass. Knowledge is 882 facts/7,457 keys; mdBook renders and is cleaned; current views, task partitions 8/1/594, Memory, README routing, exact diff, and all nine doctrines pass. Exact guards prove README, Rust production and existing tests, ordinary/canonical routes, generated format, Perl/Dart/Julia/Lua, public/outward paths, and Rust rollout do not move. Intended commit: `FUTURE-PARITY-BACKLOG.14.7.4.0 - freeze Rust staged-AST dormant RED`; clear the brief after commit, verify clean handoff, then activate `.14.7.4.1` task-tree-first.
- ID: `FUTURE-PARITY-BACKLOG.14.7.4.1`
  Status: `done; focused-signoff-complete` (2026-08-26; task-tree-first from exact clean Rust dormant-RED commit `e37a8b776bf65ece91f7c9861033afb41259adea`; no push); Goal: Add Rust's private dedicated parse-job annotation node and typed direct/derived provenance carrier while retaining the same dormant final path.
  Depends on: committed dormant boundary `.14.7.4.0`, ADR `0088`, the executable neutral contract, ADR `0056` typed source algebra, Rust's committed exclusive progressive-node/static-transaction precedent, and Perl marker/provenance evidence as reference only.
  Acceptance: retrieve the active stable leaf, Rust dormant-root-cause fact, neutral authored/provenance/diagnostic rows, typed-source and recognition effect owners, exact expression parser/compiler/validation/serde/engine/source-emitter boundaries, and committed Rust progressive exclusive-node precedent before editing production; make exact assignment-form `name = parse_job(text_expr, literal_hash_options)` the sole private annotation spelling and lower it to one dedicated logical `STAGED_PARSE_JOB_MARKER` node rather than a generic call; validate all required/optional literal options, parser/top/result/failure/target identities, unknown/duplicate keys, exact assignment-only placement, residual generic forms, and uncommitted-recognition reachability before execution; attach an inert detached `staged_parse_job_v2` declaration carrying normalized options, exact materialized text, and only typed direct same-source or nonempty ordered-derived Unicode-scalar provenance; derive provenance from live Rust source/capture authority rather than accepting copied text, path, match, registry, parser, callback, scheduler, or host authority in the node or sidecar; reject reversed/out-of-range/source-mismatched/empty-derived/transformed/literal copied text and copied-text smuggling with the exact neutral diagnostic family; preserve the same dormant consumer path and every `.0` neutral/current-v1/four-carrier observation, advance its sole final RED to `.14.7.4.2`'s missing caller-frozen resolution/cache/result/failure authority, and keep ordinary discovery at zero tests plus canonical references at zero; change no current function-body v1 record/behavior, staged registry authority, recurrence, reconstructed/generated/emitted carrier behavior beyond preserving the new logical node/sidecar, generated format version, ordinary/canonical topology, Rust rollout, capability/public/outward surface, other backend, CLI, README, or combined `.14.8`; synchronize task/index, Knowledge, ADR implementation note, roadmaps, architecture, Toolbox, bounded live docs, memory, and mdBook.
  Verification tier: `focused` — only private Rust annotation/provenance production plus its still-dormant consumer and durable truth move; there is no admission, rollout, generated-format, public, infrastructure, storage, history-capacity, milestone, or canonical-topology change.
  Focused checks: same cfg-enabled consumer with all neutral/current-v1/static-validation/typed-provenance/four-carrier groups GREEN and only one `.2` authority assertion RED; ordinary zero-test and zero-canonical-reference proof; exact malformed/residual/transaction/provenance-smuggling denials; Rust expression/compiler/validation/engine/source-emitter unit and integration direct dependents; typed-source, recognition, staged-neutral, semantic, progressive, generated-source, capability, and language governance; task/index, Knowledge, rendered mdBook, bounded histories, README/memory, all nine doctrines, exact no-registry/no-recurrence/no-admission/no-rollout/no-format/no-public/no-other-backend diff, and `git diff --check`.
  Canonical trigger: `none` — run receipt-bound canonical CI only if the implemented slice unexpectedly crosses admission, rollout, generated-format, public, milestone, infrastructure, storage, finite-history-capacity, or canonical-topology boundaries.
  Ownership: `.14.7.4.1` owns only the exclusive private Rust marker, literal option normalization/static closure, typed direct/ordered-derived provenance construction and denials, same-consumer next RED, and synchronized truth. `.2` retains pre-registered resolution/cache plus result/failure policies; `.3` recurrence/bounds/rebasing; `.4` fresh four-route authority, admission, Rust rollout, and parent closure. Later backends, recurrence `.8`, public `.9`, recomposition `.10`, and combined `.14.8` remain untouched.
  Checklist: [x] clean activation/task ownership [x] ADR/Knowledge/neutral/typed/progressive retrieval [x] exact parser/compiler/runtime design [x] exclusive marker and static closure [x] typed direct/derived provenance and denials [x] same-consumer `.2` RED [x] unchanged v1/carriers/dormancy [x] direct-dependent/no-drift proof [x] durable synchronization [x] focused signoff [x] atomic commit/brief/clean handoff.
  Acceptance Checklist:
  - [x] **REPRODUCE / ISSUE** — the committed cfg-enabled consumer reproduces one generic `parse_job` call/null result and the exact missing-marker/typed-provenance RED while current v1 and ordinary zero-test discovery remain exact.
  - [x] **ROOT CAUSE (WHY + WHERE)** — compiler/descriptor/generated/runtime probes identify `rust/linkedspec-core/src/expr.rs` generic-call lowering, `rust/linkedspec-runtime/src/engine.rs`'s unknown-helper null, and the absence of live capture spans needed to build typed provenance; no serialized copied text was treated as authority.
  - [x] **FIX** — add only the private dedicated node, strict static annotation contract, inert typed-provenance declaration sidecar, live span retention/materialization, and same-consumer `.2` RED; retain later authority/carrier/admission ownership.
  - [x] **ADDRESSED (verified)** — the cfg-enabled consumer makes every marker/options/provenance/malformed/transaction/four-carrier group GREEN and reaches only the exact `.14.7.4.2` caller-frozen-authority sentinel.
  - [x] **NO REGRESSION** — current v1, staged tracing, core/compiler, typed source, recognition, ordinary/canonical dormancy, governance, doctrines, and exact no-authority/no-rollout/no-format/no-public guards pass.
  - [x] **LOCKSTEP** — task/index, neutral lifecycle, Knowledge, ADR, roadmaps, architecture, Toolbox, live docs, memory, and mdBook agree on Rust private marker/provenance and next owner `.2`.
  Activation evidence: exact `git status --short --untracked-files=all` is empty at `e37a8b776bf65ece91f7c9861033afb41259adea`; `git_message_brief.txt` is zero bytes; `docs/linkedspec-book/book` is absent; all prior Rust/Cargo/book/doctrine jobs are consumed. `.14.7.4.0` is durably committed with ordinary 0 tests, one exact final missing-marker/provenance RED after all pre-boundary observations, Rust `dormant_red`, 79 neutral mutations, zero canonical references, unchanged Rust rollout, and no production behavior.
  Implementation evidence: `Expr::StagedParseJobMarker` exclusively claims only exact scalar assignment to `parse_job(text_expr, hash(...))`; required and optional literal options normalize into typed data, while duplicate/unknown/dynamic options, invalid identities/policies/targets, non-assignment residual calls, and recognition-reachable declaration effects fail closed. Runtime match state now retains compact participating capture byte spans long enough to convert direct `entry_text`, `entry_group(N)`, `match_text`, or `match_group(N)` and nonempty recursive `cat(...)` compositions into Unicode-scalar direct or ordered-derived provenance. `staged_parse_job_v2` materializes exact text and only inert logical metadata; copied/transformed text, smuggled copied fields, reversed/out-of-range/source-mismatched spans, empty derived provenance, and authority-shaped keys reject. The plain detached marker owns no registry, parser callback, source snapshot, scheduler, cache, path, cancellation, deadline, budget, queue, or host authority. Native, normalized reconstruction, generated-plan, and independently compiled emitted routes preserve equal logical nodes and markers without changing generated format v2.
  Focused signoff evidence: the cfg-enabled consumer completes every neutral/current-v1/static-validation/provenance/four-carrier assertion and fails only its exact final `.14.7.4.2` sentinel after 152.25 seconds on the final reviewed run; ordinary discovery remains 0 tests and canonical CI has zero references. Current function-body-v1 and staged trace pass 1/1 each; core library 197/197, typed-source 4/4, and recognition transaction 12/12 pass. Neutral staged reports 4 registry entries, 8 provenance, 8 resolution/6 authority, 10 cache, 4 queue/3 isolation, 4 result/3 failure policies, 10 chain, 5 detachment, 37 diagnostics, 9 rollout legs, 35 owners, and 79 mutations. Typed 12/2/170, progressive 9/9/116 plus public 60, recognition 138/250/58, semantic 6/20/128, generated/capability 80/0/0, and language 250/126 pass. Focused verification also exposed two pre-existing defects against exact base `e37a8b776bf65ece91f7c9861033afb41259adea`: traced compilation omits the existing progressive static validator, and both interpreted/generated gap-aware child-entry trace routes omit required `child_dispatch` lifecycle events. Neither is caused by or changed in this staged slice; the clean post-commit pivot must create dedicated defect task-tree ownership before repair. Intended commit: `FUTURE-PARITY-BACKLOG.14.7.4.1 - add Rust staged annotation provenance`; clear the brief after commit, verify clean handoff, then task-tree-own and resolve those defects before resuming `.14.7.4.2`.
- ID: `FUTURE-PARITY-BACKLOG.14.7.4.2`
  Status: `done; focused-signoff-complete` (2026-08-26; task-tree-first from exact clean corrective-closeout commit `17177e40f223cba3b36f3a061cab0dab3ec4d520`; no push); Goal: Add Rust's caller-pre-registered resolution/cache authority and all result/failure stitching policies while recursive scheduling remains RED.
  Depends on: neutral contract `.14.7.2`, ADR `0088`, committed Rust dormant boundary `.14.7.4.0`, committed marker/provenance `.14.7.4.1`, ADR `0056` typed-source authority, and the current narrow function-body-v1 registry as compatibility-only evidence. Corrective `TRACE-OBSERVABILITY.5.1-.4` is complete and supplies the exact clean activation base; it is not semantic authority for this leaf.
  Acceptance: retrieve this stable leaf, ADRs `0056`/`0088`, neutral resolution/authority/cache/result/failure rows and diagnostics, Rust dormant/marker Knowledge, the committed Perl current-depth implementation as mechanism evidence only, current Rust function-body-v1 boundary, Rust typed-source/progressive immutable-authority precedents, and exact Toolbox probes before implementation archaeology; add one private Rust caller-prepared immutable authority containing normalized logical identities and already-compiled opaque parser callbacks only, with alias/declaring-relative/ordered-search-root/ordered-provider candidate outcomes supplied and frozen before authored execution; perform pure post-AST selection with exact missing/ambiguous/alias-collision/top/version/capability/policy/source-detail diagnostics and no filesystem/provider/import/environment/network/loading/compilation authority; select and normalize the top rule before deterministic v2 job-id construction; add immutable plan-cache identity over content/import digests, contract versions, selected top, and sorted effective capabilities, with exact hit/miss/invalidation/run-isolation behavior and no child-result, failure, or partial-work poisoning; validate and execute only the current complete marker depth in deterministic typed parent-path/provenance/job-id order, give siblings fresh cursor/mark/capture/variable parser contexts, and apply all four result policies (`replace_marker`, `replace_field`, `sibling_field`, `append_child`) plus all three failure policies (`fail`, `keep_text`, `diagnostic_node`) with detached node-bounded plain results, atomic fail publication, retained portable diagnostics, and exact marker/target/collision/collection-kind/stale-marker/live-result denials; preserve newly returned inert markers without rescanning them; retain the same outer-cfg dormant consumer and advance its sole RED to `.3`'s missing breadth-first recurrence, decreasing-chain/cycle/depth/call/cancellation/resource guards, and original-source diagnostic rebasing; keep function-body v1 unchanged; add no recursive scheduler, reconstructed/generated/emitted fresh-authority carrier seam, ordinary/canonical discovery, Rust rollout/admission, generated-format version, public/outward/facade/schema/semantic/MCP/CLI/README surface, other backend behavior, or combined `.14.8`; synchronize task/index, Knowledge, ADR implementation note, roadmaps, architecture, Toolbox, bounded live docs, memory, and sole-facing mdBook.
  Verification tier: `focused` — this leaf changes only private Rust one-depth authority/cache/policy behavior and its deliberately outer-cfg dormant consumer; it does not admit a route or change rollout, public, infrastructure, storage, generated-format, milestone, or canonical topology.
  Focused checks: same cfg-enabled Rust consumer with committed neutral/current-v1/static-validation/typed-provenance/four-logical-carrier groups plus all resolution/authority/cache/result/failure cases GREEN and only `.3` recurrence/bounds/rebased-diagnostic authority RED; ordinary zero-test and zero-canonical-reference proof; immutable registry/cache, adversarial callback isolation, deterministic ordering, detached stitching, atomic fail, retained diagnostic, and marker/target/live-result denial tests; existing function-body-v1 integration/trace compatibility; Rust core/compiler/runtime and typed-source/progressive/recognition direct dependents; staged-neutral, semantic, generated-source, capability, and language governance; task partition/index, Knowledge, rendered mdBook, bounded histories, README/memory, all nine doctrines, exact no-recursive-queue/no-carrier-seam/no-admission/no-rollout/no-format/no-public/no-other-backend diff, and `git diff --check`.
  Canonical trigger: `none` — run receipt-bound canonical CI only if implementation unexpectedly crosses admission, rollout, generated-format, public, milestone, infrastructure, storage, finite-history-capacity, or canonical-topology boundaries.
  Ownership: `.14.7.4.2` owns only Rust's private caller-prepared immutable registry/cache, pure resolution from frozen candidate outcomes, selected-top-before-v2 identity, current-depth execution/order/isolation, four result and three failure policies, exact detachment/stitch diagnostics, same-consumer `.3` RED, and synchronized current truth. `.3` retains recursive breadth-first scheduling, decreasing-chain/cycle/resource/cancellation limits, and original-source diagnostic rebasing. `.4` retains reconstructed/generated/emitted fresh top-level authority, ordinary/canonical admission, Rust rollout promotion, and parent closure. Later backends `.5-.7`, recurrence `.8`, public authoring `.9`, recomposition `.10`, and combined `.14.8` remain untouched.
  Checklist: [x] clean activation/task ownership [x] ADR/Knowledge/neutral/precedent retrieval [x] exact frozen authority/cache design [x] pure resolution/job-id implementation [x] current-depth execution/order/isolation [x] four result/three failure policies [x] adversarial authority/detachment rejection [x] unchanged v1/next-boundary dormant oracle [x] direct-dependent/no-drift proof [x] durable synchronization [x] focused signoff [x] atomic commit/brief/clean handoff.
  Acceptance Checklist:
  - [x] **REPRODUCE / ISSUE** — committed cfg-enabled consumer reaches only the exact `.14.7.4.2` caller-frozen authority sentinel after marker/options/provenance/four-logical-carrier groups pass; ordinary discovery remains zero tests and canonical CI has zero references.
  - [x] **ROOT CAUSE (WHY + WHERE)** — Knowledge plus exact consumer/source probes show `rust/linkedspec-runtime/src/staged_parse_job.rs` constructs only inert detached marker data, while `staged_parser_registry.rs` is the incompatible function-body-v1 adapter; Rust has no private general-v2 frozen registry/cache, pure resolver, current-depth scheduler, or policy stitcher.
  - [x] **FIX** — implement only the private frozen general-v2 authority/cache/current-depth policy engine and extend the same dormant consumer to the `.3` recurrence boundary.
  - [x] **ADDRESSED (verified)** — exact authority/cache/policy groups pass and the sole intentional RED names only `.3` recurrence/bounds/diagnostic rebasing.
  - [x] **NO REGRESSION** — v1, marker/provenance, ordinary/canonical dormancy, rollout/format/public boundaries, direct dependents, and exact diff guards pass.
  - [x] **LOCKSTEP** — task/index, neutral governance, Knowledge, ADR, roadmaps, architecture, Toolbox, live docs, memory, and mdBook agree on Rust current-depth authority and next owner `.3`.
  Activation evidence: exact `git status --short --untracked-files=all` is empty at `17177e40f223cba3b36f3a061cab0dab3ec4d520`; the committed canonical receipt matches that HEAD; `git_message_brief.txt` is zero bytes; `docs/linkedspec-book/book` is absent; no gate, doctrine, or Phase-0 process remains. `.14.7.4.1` is durably committed with marker/options/provenance/four-logical-carrier groups GREEN and only `.2` caller-frozen authority RED. Corrective trace `.5.1-.4` is closed; neutral staged truth is 79 mutations with Rust `dormant_red`, ordinary discovery zero, canonical references zero, Rust rollout pending, and no public/outward movement.
  Implementation evidence: private `rust/linkedspec-runtime/src/staged_ast_enrichment.rs` freezes caller-completed alias/declaring-relative/ordered-root/provider candidate outcomes and logical entries whose only executable value is an opaque already-compiled callback. Resolution performs pure selection and exact missing/ambiguity/collision denials with no loader, path, provider query, import enumeration, environment, network, compiler, or registry mutation. Top/version/capability/policy/source-detail/ceiling checks only narrow authority. Default top precedes the deterministic v2 digest; the exact eight-field cache identity normalizes sorted effective capabilities, and each registry instance caches only immutable prepared plans. Failures, results, and partial work never populate that cache. One complete current depth prepares every job and target before callback execution, sorts typed paths/provenance/job ids, gives siblings fresh cursor/mark/capture/variable state, catches child panics, detaches/node-bounds plain results, and applies all four result plus three failure policies to an unpublished AST copy. Portable diagnostics retain full job context; missing/colliding/wrong-kind/stale targets and live results reject. Newly returned inert markers remain unscanned. The module is private and ordinary-build dead until `.4` adds a production carrier, so `lib.rs` applies a cfg-conditional allowance registered by the runtime manifest; `.4` owns its removal.
  Focused signoff evidence: the final cfg-enabled consumer compiles every neutral/current-v1/marker/provenance/four-logical-carrier plus resolution/authority/identity/cache/queue/isolation/result/failure/detachment/atomicity/denial assertion and then fails only its exact `.14.7.4.3` sentinel after 166.37 seconds. Ordinary discovery reports 0 tests and `tools/run_ci_local.sh` has zero references. Cfg library check and focused no-dependency Clippy complete with no new staged warning; the wider Clippy test command is blocked only by pre-existing `tests/integration_test.rs` approximate-constant lint outside this leaf. Runtime integration passes 197/197 including current function-body v1, recognition transaction 12/12, trace controls 11/11 including staged dispatch, and typed source 4/4. Neutral staged remains 4 registry entries, 8 provenance, 8 resolution/6 authority, 10 cache, 4 queue/3 isolation, 4 result/3 failure policies, 10 chain, 5 detachment, 37 diagnostics, 9 rollout legs, 35 owners, and 79 mutations; typed 12/2/170, progressive 9/9/116 plus public 60, recognition 138/250/58, and semantic 6/20/128 pass. Knowledge is 885 facts/7,487 question keys; the mdBook builds 80 files/15,460 KiB and is removed afterward. Both bounded histories remain below rollover, task/index, Memory/README, all doctrines, exact no-recursion/no-carrier/no-admission/no-rollout/no-format/no-public/no-other-backend guards, and `git diff --check` pass. Intended commit: `FUTURE-PARITY-BACKLOG.14.7.4.2 - add Rust staged current-depth authority`; clear the brief, verify clean handoff, then activate `.14.7.4.3` task-tree-first.
- ID: `FUTURE-PARITY-BACKLOG.14.7.4.3`
  Status: `done; focused-signoff-complete` (2026-08-26; task-tree-first from exact clean current-depth-authority commit `e2e752f5cda17f81584168512d5ad6f0aaef5859`; no push); Goal: Complete Rust recursive queue scheduling, decreasing-chain/cycle/resource guards, shared cancellation/resource authority, and original-source diagnostic rebasing while discovery remains dormant.
  Depends on: neutral contract `.14.7.2`, ADR `0088`, committed Rust dormant boundary `.14.7.4.0`, marker/provenance `.1`, current-depth resolution/cache/policy authority `.2`, ADR `0056` typed provenance, and progressive bounded-authority plus committed Perl recursive authority as mechanism evidence only.
  Acceptance: retrieve this stable leaf, ADRs `0056`/`0088`, neutral queue/isolation/chain/detachment/diagnostic cases, Rust marker/current-depth facts, typed source cursor algebra, progressive cancellation/resource precedent, committed Perl recursive mechanism evidence, and exact Toolbox probes before implementation archaeology; extend the same private Rust authority with one breadth-first recursive entrypoint that validates/resolves the complete current depth before executing any child, stitches settled jobs in depth/typed-parent-path/typed-provenance/job-id order, and discovers newly stitched markers only for the next depth; preserve fresh sibling parser state and one immutable registry/plan cache while carrying one detached immutable stage-chain view per callback; compute payload digest over exact UTF-8 text and reject exact normalized parser/top/payload-digest/full-typed-provenance repeats as `staged_cycle`; allow repeated parser/top lineage only when every direct or ordered-derived child segment is contained by an active segment and total Unicode-scalar extent strictly decreases, otherwise reject as `staged_chain_non_decreasing`; share and monotonically spend caller-owned identity-bearing cancellation/callback, absolute deadline/caller clock, remaining steps, total calls, maximum depth/calls/result nodes/diagnostic bytes across every depth without reset or elevation, with exact dispatch-entry and callback-safe-point diagnostics plus callback-context expiry; map child-local positions, spans, and diagnostics through direct or ordered-derived job provenance back to original source identities without inventing a contiguous span for derived text, retain `concatenate_in_order` on cross-segment spans, truncate only through the exact diagnostic-byte policy, and retain detached scheduler diagnostics; preserve `.2` pure resolution/cache/current-depth behavior, all result/failure policies, atomic fail publication, detachment, stitch diagnostics, and failed-result cache isolation; retain the same outer-cfg dormant consumer and advance its sole RED to `.4` fresh native/reconstructed/generated-plan/independently loaded emitted authority carriers, ordinary/canonical admission, Rust rollout, production call seam, and dead-code-allowance removal; keep function-body v1 unchanged; add no carrier reconstruction/generation/emission, route/admission/rollout/capability/public/outward projection, generated-format, CLI, README, canonical topology, other backend behavior, or combined `.14.8`; synchronize task/index, Knowledge, ADR implementation note, roadmaps, architecture, Toolbox, live docs, memory, and sole-facing mdBook.
  Verification tier: `focused` — this leaf completes only private dormant recursive Rust authority and its outer-cfg consumer; it does not admit a route or change public, infrastructure, storage, generated-format, or canonical topology.
  Focused checks: exact cfg-enabled dormant Rust consumer proving committed marker/provenance/resolution/cache/policy groups plus all neutral queue/isolation/chain/resource/detachment/diagnostic cases GREEN and only fresh carriers/admission RED; adversarial breadth-first ordering, same-text/different-provenance identity, derived containment/decrease, cycle, depth/call/step/cancellation/deadline, result-node/diagnostic-byte, callback safe-point/expiry, and direct/ordered-derived rebasing tests; ordinary zero-test and canonical-zero-reference dormancy; existing function-body v1 and `.2` compatibility; neutral staged checker with unchanged Rust `dormant_red` lifecycle; Rust formatting/check/Clippy plus focused direct dependents; typed-source, semantic, progressive, recognition, generated-source, capability, and language ledgers; task partition/index, Knowledge, rendered mdBook, bounded histories, README/memory, nine doctrines, and exact no-carrier/no-admission/no-rollout/no-generated-format/no-public/no-canonical-topology/no-other-backend diff.
  Canonical trigger: `none` — run receipt-bound canonical CI only if implementation crosses an admission, milestone, public, infrastructure, storage, history-capacity, generated-format, or canonical-topology boundary.
  Ownership: `.14.7.4.3` owns only recursive breadth-first scheduling over the existing private native markers, exact-text/full-provenance active-chain decrease and cycle logic, shared cancellation/deadline/step/call/depth/result/diagnostic limits, original-source child projection and diagnostic rebasing, same-consumer `.4` RED, and synchronized current truth. `.4` retains reconstructed/generated/emitted fresh top-level authority, the first production caller and dead-code-allowance removal, ordinary/canonical discovery, Rust rollout promotion, and parent closure. Later backends `.5-.7`, recurrence `.8`, public authoring `.9`, recomposition `.10`, and combined `.14.8` remain untouched.
  Checklist: [x] clean activation/task ownership [x] ADR/Knowledge/neutral/Toolbox/precedent retrieval [x] exact recursive authority design [x] breadth-first next-depth scheduler [x] decreasing chain/cycle guards [x] shared cancellation/resource limits [x] original-source diagnostic rebasing [x] adversarial detachment/atomicity [x] unchanged `.2`/v1/next-boundary oracle [x] direct-dependent/no-drift proof [x] durable synchronization [x] focused signoff [x] atomic commit/brief/clean handoff.
  Acceptance Checklist:
  - [x] **REPRODUCE / ISSUE** — the committed cfg-enabled consumer makes every current-depth group GREEN and reaches only the exact `.14.7.4.3` recurrence/bounds/rebasing sentinel; ordinary discovery remains zero and canonical CI has zero references.
  - [x] **ROOT CAUSE (WHY + WHERE)** — the exact private authority and dormant consumer prove `enrich_current_depth` intentionally stops after one depth and lacks cross-depth lineage/resources/context projection; the narrow function-body-v1 adapter remains a separate incompatible owner.
  - [x] **FIX** — extend only the private dormant Rust authority and same consumer through recursive scheduling, monotone shared bounds, expiring safe points, and typed original-source rebasing.
  - [x] **ADDRESSED (verified)** — every owned recursive/chain/resource/rebasing group is GREEN and only `.4` fresh carriers/admission/rollout/production seam remains RED.
  - [x] **NO REGRESSION** — v1, `.2`, ordinary/canonical dormancy, rollout/format/public boundaries, direct dependents, and exact diff guards pass.
  - [x] **LOCKSTEP** — task/index, neutral governance, Knowledge, ADR, roadmaps, architecture, Toolbox, live docs, memory, and mdBook agree on Rust recursive authority and next owner `.4`.
  Activation evidence: exact `git status --short --untracked-files=all` is empty at `e2e752f5cda17f81584168512d5ad6f0aaef5859`; `git_message_brief.txt` is zero bytes; `docs/linkedspec-book/book` is absent; no background result remains. `.14.7.4.2` is durably committed with every current-depth resolution/cache/order/isolation/result/failure/detachment assertion GREEN and only recurrence/bounds/rebasing RED. Neutral staged truth remains 79 mutations with Rust `dormant_red`, ordinary discovery zero, canonical references zero, Rust rollout pending, and no public/outward movement.
  Implementation evidence: private `enrich_recursively` discovers initial markers, resolves and stitch-validates a complete typed-sorted depth before callbacks, executes through the existing immutable registry/plan cache, and discovers only successfully stitched returned markers for the next depth. Every plan records depth, exact UTF-8 payload SHA-256, full prior stage chain, and the exact normalized parser/selected-top/digest/full-provenance active tuple. Exact tuple repeats reject as `staged_cycle`; same-parser/top lineage rejects as `staged_chain_non_decreasing` unless each direct/ordered-derived segment is contained and total Unicode-scalar extent decreases. One invocation state spends cancellation identity/callback, caller clock/deadline, steps, calls, depth, cumulative result nodes, and diagnostic bytes without reset or elevation. Ephemeral callback contexts enforce entry and safe-point checks, expire after settlement, and project child-local positions/spans/diagnostics through direct or ordered-derived provenance. Cross-segment spans retain `derived_text`/`concatenate_in_order`; oversized diagnostics become `staged_diagnostic_truncated`. Failed paths remain unpublished and detached; current-depth behavior is preserved.
  Focused signoff evidence: the final cfg-enabled consumer passes every prior marker/provenance/resolution/cache/policy group plus all ten neutral chain rows, real two-depth breadth-first order, shared cache/resources/cancellation/deadline, exact cycle and non-decrease preflight rejection, depth/call/step/result/diagnostic ceilings, callback cancellation/deadline/safe-point/expiry, and direct/ordered-derived rebasing, then fails only the exact `.14.7.4.4` fresh-carrier/production-seam/admission/rollout sentinel after 146.33 seconds. Ordinary discovery reports 0 tests; canonical CI has no exact test-path reference. Cfg library check and focused no-dependency Clippy pass with no new staged warning; only documented pre-existing runtime/test warnings remain. Runtime integration passes 197/197 including function-body v1, recognition transactions 12/12, trace controls 11/11 including staged dispatch, and typed source 4/4. Neutral staged remains 4 registry entries, 8 provenance, 8 resolution/6 authority, 10 cache, 4 queue/3 isolation, 4 result/3 failure policies, 10 chain, 5 detachment, 37 diagnostics, 9 rollout legs, 35 owners, and 79 mutations. Typed 12/2/170, progressive 9/9/116 plus public 60, recognition 138/250/58, semantic 6/20/128, generated/capability 80/0/0, and language 250/126 pass. Knowledge, rendered mdBook, bounded histories, task/index, Memory/README, nine doctrines, exact no-carrier/no-admission/no-rollout/no-format/no-public/no-other-backend guards, and `git diff --check` pass. Intended commit: `FUTURE-PARITY-BACKLOG.14.7.4.3 - add Rust staged recursive authority`; clear the brief, verify clean handoff, then activate `.14.7.4.4` task-tree-first.
- ID: `FUTURE-PARITY-BACKLOG.14.7.4.4`
  Status: `done; canonical-signoff-complete` (2026-08-26; task-tree-first from exact clean recursive-authority commit `de6f5a6545ae8aeb66243b2596277793611b39fe`; no push); Goal: Recompose Rust native/reconstructed/generated/emitted routes with fresh authority and canonically admit the unchanged consumer.
  Depends on: neutral contract `.14.7.2`, ADR `0088`, committed Rust marker/provenance `.14.7.4.1`, current-depth authority `.2`, recursive authority `.3`, current native/reconstructed/generated-v2/emitted top-level execution seams, and committed Perl/Rust progressive fresh-authority carrier/admission precedents as mechanism evidence only.
  Acceptance: retrieve this stable leaf, ADR `0088`, executable neutral carrier/backend/rollout rows, committed Rust recursive-authority fact, Perl staged carrier admission, Rust progressive carrier/admission precedent, generated-source v2 and function-body-v1 compatibility facts, canonical-driver topology, and exact Toolbox probes before implementation archaeology; add one private host-only Rust staged-enrichment invocation seed/runtime seam that constructs a fresh `FrozenStagedRegistry`/plan cache and `StagedRecursiveAuthority` per top-level execution, executes the complete parent parse first, then invokes the committed `enrich_recursively` inside the existing typed runtime-error boundary; attach that same seam to native live execution, serialized/normalized reconstructed execution, validated generated-plan execution, and independently compiled emitted-source execution without serializing callbacks, compiled parsers, registry/source authority, cancellation identity/callback, clock/deadline/budgets, mutable queue/cache, filesystem path, or host handle; prove equal detached AST/sidecars/diagnostics/cache/resources across all four routes while snapshots, callbacks, cancellation identities, clocks, and caches are fresh and cross-result mutation cannot leak; preserve exact marker/provenance/resolution/cache/queue/chain/resource/rebasing semantics, static recognition rejection, and function-body v1/generated-source v2 formats; remove the temporary outer test cfg, cfg-only library exports, manifest check-cfg registration, and cfg-conditional dead-code allowance only after the first production caller makes the module live; make the same consumer fully GREEN, discover it exactly once in ordinary Cargo and exactly once in canonical CI, update neutral status/backend/rollout truth only for Rust, close backend parent `.14.7.4`, and hand off Dart `.14.7.5.0`; change no public `parse_job(...)` authoring, facade/schema/semantic/MCP/CLI/README/outward surface, generated format, later backend, six-runtime recurrence `.8`, public closeout `.9`, recomposition `.10`, or combined `.14.8`; synchronize task/index, Knowledge, ADR implementation note, roadmaps, architecture, Toolbox, live docs, memory, and sole-facing mdBook.
  Verification tier: `canonical` — this leaf creates the first Rust production carrier, removes dormancy, changes canonical CI topology, promotes Rust rollout, and closes the Rust behavioral parent.
  Focused checks: unchanged exact Rust consumer GREEN through ordinary discovery; four fresh-authority route equality and serialized/generated/emitted authority-absence probes; independent cache/snapshot/cancellation/clock identities and cross-result detachment; function-body v1 plus marker/current-depth/recursive compatibility; neutral staged checker with Rust complete/rollout-only movement; generated-source, typed-source, semantic, progressive, recognition, capability, and language ledgers; canonical-driver exact one-input/one-command/one-marker probes; Rust formatting/check/Clippy and direct dependents; task partition/index, Knowledge, rendered mdBook, bounded histories, README/memory, nine doctrines, exact no-public/no-generated-format/no-other-backend diff, and staged receipt-bound canonical CI.
  Canonical trigger: `mandatory` — the first production carrier, exact consumer admission, canonical-CI route, Rust rollout promotion, and backend-parent closure move together; land only after exact staged receipt-bound local CI.
  Ownership: `.14.7.4.4` owns only fresh Rust native/reconstructed/generated/emitted top-level authority, the private production invocation seam, removal of the now-obsolete dormant cfg/dead-code scaffolding, exact consumer admission, Rust-only neutral rollout, parent `.14.7.4` closure, and synchronized truth. Dart `.5`, Julia `.6`, shared Lua `.7`, recurrence `.8`, public authoring `.9`, recomposition `.10`, combined `.14.8`, generated-format changes, and outward APIs remain untouched.
  Checklist: [x] clean activation/task ownership [x] ADR/Knowledge/neutral/precedent retrieval [x] production seam design [x] four fresh-authority carriers [x] serialized-authority absence/detachment [x] dormant-scaffolding removal [x] ordinary/canonical admission [x] Rust rollout/parent closure [x] direct-dependent/no-drift proof [x] durable synchronization [x] canonical receipt [x] atomic commit/brief/clean handoff.
  Acceptance Checklist:
  - [x] **REPRODUCE / ISSUE** — the committed ordinary target discovers zero tests, while the exact cfg run passes every marker/authority/recurrence assertion and reaches only the `.4` carrier/admission sentinel.
  - [x] **ROOT CAUSE (WHY + WHERE)** — Toolbox and engine/source-emitter probes localize the missing seam: complete private authority exists, but no options-bearing top-level caller can inject opaque host state and invoke it post-parent; the test cfg and conditional dead-code allowance therefore remain necessary at the activation commit.
  - [x] **FIX** — add only the private production seed/seam, four fresh-authority carriers, exact admission/rollout movement, and obsolete dormant-scaffolding removal.
  - [x] **ADDRESSED (verified)** — the unchanged semantic consumer is fully GREEN through four fresh routes and exact ordinary/canonical discovery.
  - [x] **NO REGRESSION** — preserve v1/v2 formats, marker/authority semantics, later backends, public/outward surfaces, direct dependents, and exact diff guards.
  - [x] **LOCKSTEP** — task/index, neutral governance, Knowledge, ADR, roadmaps, architecture, Toolbox, live docs, memory, mdBook, and canonical topology agree on Rust admission and Dart `.14.7.5.0` next.
  Activation evidence: exact `git status --porcelain=v1 --untracked-files=all` is empty at `de6f5a6545ae8aeb66243b2596277793611b39fe`; `git_message_brief.txt` is zero bytes; `docs/linkedspec-book/book` is absent; no gate result remains. `.14.7.4.3` is durably committed with every recursive queue/chain/resource/safe-point/rebasing assertion GREEN and only fresh carriers/production/admission/rollout RED. Neutral staged truth remains 79 mutations with Rust `dormant_red`, ordinary discovery zero, exact test-path canonical references zero, Rust rollout pending, and no public/outward movement.
  Implementation evidence: opaque host-only `StagedAstEnrichmentSeed` stores caller-frozen logical snapshot/options plus already-compiled callbacks, cancellation, and clock recipes. `start()` constructs a new `FrozenStagedRegistry` with empty plan cache and a new `StagedRecursiveAuthority` for every top-level execution. Native and both generated accumulator/direct option-bearing contexts start the recipe before parent execution, preserve authored isolation, and call `enrich_recursively` only after the complete parent value through the existing diagnostic-returning boundary. Native, JSON-reconstructed, validated generated-plan, and independently compiled generated-source-v2 `execute_with_options` routes each run twice through one seed and return equal detached `{ast, sidecars, diagnostics, cache, resources}` records. Every run records one entry/zero hits/one miss, separate callback/cancellation/clock observation, and no cross-result mutation. Compiled JSON, plans, and emitted logical source contain no callback, parser, registry/source authority, cancellation/deadline/budget state, mutable queue/cache, path, or host handle. The first real caller removes the outer test cfg, cfg-only exports, manifest check-cfg registration, and conditional dead-code allowance; no-seed behavior remains unchanged.
  Focused signoff evidence: the ordinary Rust contract is GREEN at 1/1 after 206.76 seconds, including its independently compiled emitted child. The neutral checker reports 4 registry entries, 8 provenance, 8 resolution/6 authority, 10 cache, 4 queue/3 isolation, 4 result/3 failure policies, 10 chain, 5 detachment, 37 diagnostics, 9 rollout legs, 35 owners, and 84 mutations with neutral+Perl+Rust complete. Perl remains 143/143. Rust library tests pass 170/170; recognition 12/12, trace controls 11/11, and typed source 4/4 pass. Formatting, library check, and normal no-dependency Clippy pass; a diagnostic `-D warnings` probe confirms only the pre-existing 27 runtime lint baseline and none in `staged_ast_enrichment.rs`. Typed 12/2/170, progressive 9/9/116 plus public 60, recognition 138/250/58, semantic 6/20/128, generated/capability 80/0/0, and language 250/126 pass. Knowledge is 887 facts/7,511 question keys. The mdBook renders 80 files/16 MiB and its reproducible output is removed. Exact topology requires the Rust path and invocation once; root README, generated format v2, function-body v1, later consumers/source, public/outward surfaces, and combined `.14.8` do not move.
  Canonical completion and handoff evidence: Knowledge regeneration, bounded histories, memory/README, task/index, nine doctrines, exact diff/no-drift guards, and the fully staged receipt-bound `bash tools/run_ci_local.sh` candidate pass; pre-commit revalidates the receipt and fast doctrines. Intended atomic subject: `FUTURE-PARITY-BACKLOG.14.7.4.4 - admit Rust staged enrichment carriers`; clear the brief after commit, verify clean handoff, then activate Dart `.14.7.5.0` task-tree-first.
- ID: `FUTURE-PARITY-BACKLOG.14.7.5`
  Status: `done; closed` (2026-08-27; `.14.7.5.0-.4` are signoff-complete through the intended canonical Dart-carrier-admission commit; Julia `.14.7.6.0` is next; no push); Goal: Implement and admit the neutral staged-AST contract on Dart through independently committable leaves.
  Children: dormant RED `.0`; dedicated annotation/typed provenance `.1`; pre-registered resolution/cache plus policy stitching `.2`; recursive queue/bounds/diagnostics `.3`; reconstructed/generated carriers and Dart admission `.4`.
  Depends on: completed Rust parent `.4` as reference evidence, never as semantic authority. Every child consumes the neutral contract and the same dormant Dart final path; only `.4` may register it or promote Dart.
- ID: `FUTURE-PARITY-BACKLOG.14.7.5.0`
  Status: `done; canonical-signoff-complete` (2026-08-26; task-tree-first from exact clean Rust-admission commit `94d20becf2c8a17438a2509538a9878ecd96c9b9`; committed as `93b035c3f1417d81abbf2527ffe3280f66d33df8`; no push); Goal: Freeze Dart's exact dormant general-staged RED and current function-body compatibility boundary without production behavior.
  Depends on: neutral executable contract `.14.7.2`, ADR `0088`, committed Perl/Rust admissions as reference evidence only, Dart's current function-body-v1 registry, and the committed Dart progressive `test_dormant` convention.
  Acceptance: retrieve this stable leaf, ADR `0088`, neutral/current-boundary and admitted Perl/Rust Knowledge, exact Dart staged-registry/generated-source Toolbox probes, and the committed Dart progressive dormant/carrier precedents before production archaeology; add exactly one repository-routed `dart/test_dormant/staged_ast_enrichment_contract_test.dart` consumer while keeping the eventual admitted `dart/test/staged_ast_enrichment_contract_test.dart` path absent, ordinary Dart discovery free of the consumer, and canonical CI without its path, marker, or invocation; advance only Dart's predeclared backend-consumer lifecycle from `pending_absent` to `dormant_red`, with the independent checker requiring the exact dormant path while proving zero ordinary/canonical registration and unchanged Dart rollout; keep current function-body v1 GREEN for normalized job identity, deterministic resolution/compile/execute/cache records, `replace_field` / `body_ast` / `fail` semantics, and wrong-top diagnostic context; freeze one exact general-v2 final-path RED at the earliest missing dedicated `STAGED_PARSE_JOB_MARKER` / typed-provenance boundary across Dart native execution, `SpecFile`-JSON reconstruction, validated generated-plan execution, and independently analyzed/executed emitted-source observation surfaces without adding carrier behavior; bind `.1-.4` to this same consumer and sole RED in order: `.1` private marker/provenance, `.2` caller-frozen resolution/cache/result/failure policies, `.3` breadth-first recurrence/bounds/source-rebased diagnostics, and `.4` fresh four-route authority plus canonical admission; keep admitted Perl/Rust behavior unchanged while moving only their exact neutral current-projection expectations for the new status, mutation count, and Dart lifecycle; apply the mandatory complete-record change-history rollover and keep the live-status current view at sixteen rows; because its immutable segment crosses finite resulting-tree controls, add one indexed ADR authorizing only the exact one-file/one-manifest-line increments while preserving every other route limit; otherwise change no Dart production source, existing behavioral expectation, neutral behavior/count other than Dart lifecycle and its checker mutation, generated format, ordinary/canonical topology, rollout/capability/public/outward surface, other backend, CLI, README, or combined `.14.8`; synchronize task/index, Knowledge, roadmaps, architecture, Toolbox, bounded live docs, memory, and sole-facing mdBook.
  Verification tier: `canonical` (upgraded from focused after the mandatory change-history rollover crossed its finite resulting-tree capacity).
  Focused checks: exact repository-routed dormant Dart consumer with every neutral/current-v1/four-carrier-observation pre-boundary group GREEN and only one dedicated-marker/provenance assertion RED; fatal Dart analysis includes the dormant file while ordinary package discovery and canonical references remain zero; neutral staged checker requires Dart `dormant_red`, rejects the lifecycle mutation, and keeps rollout pending; admitted Perl/Rust consumers require the exact same current neutral snapshot without behavioral movement; current Dart staged-registry/function trace tests plus generated-source and progressive carrier direct dependents; semantic, typed-source, recognition, capability, and language governance where discovery snapshots can drift; task partition/index, Knowledge, rendered mdBook, bounded histories, README/memory, all nine doctrines, exact no-production/no-registration/no-rollout/no-generated-format/no-public/no-other-backend diff, and `git diff --check`.
  Canonical trigger: `mandatory` — the required rollover publishes one immutable segment and changes finite resulting-tree routing capacity; stage the exact candidate and run receipt-bound canonical CI before commit.
  Ownership: `.14.7.5.0` owns the exact dormant Dart consumer, Dart's neutral `pending_absent` to `dormant_red` lifecycle plus checker mutation, exact admitted Perl/Rust neutral current-projection expectations, current-v1 compatibility controls, the first general-v2 RED, the same final path retained by `.1-.4`, synchronized boundary truth, mandatory change-history segment `4990`, and only its exact 22→23 file / 21→22 manifest-line capacity decision. `.1` retains marker/provenance; `.2` resolution/cache/result/failure policies; `.3` recurrence/bounds/rebasing; `.4` fresh carriers/admission/Dart rollout. Julia `.6`, Lua `.7`, recurrence `.8`, public `.9`, recomposition `.10`, and combined `.14.8` remain untouched.
  Checklist: [x] clean activation/task ownership [x] ADR/Knowledge/Toolbox/dormant-precedent retrieval [x] exact Dart current-boundary probes [x] exact dormant v1-GREEN/general-v2-RED consumer [x] Dart-only neutral lifecycle/checker movement [x] admitted-consumer current-projection repair [x] child ownership/final-path lock [x] direct-dependent/no-drift proof [x] durable synchronization [x] focused behavior signoff [x] mandatory rollover/capacity decision [x] canonical receipt [x] atomic commit/brief/clean handoff.
  Acceptance Checklist:
  - [x] **REPRODUCE / ISSUE** — the explicit dormant consumer completes four pre-boundary tests and fails only one exact final missing-marker/typed-provenance assertion while ordinary discovery and canonical references stay zero.
  - [x] **ROOT CAUSE (WHY + WHERE)** — `action_parser.dart` lowers the assignment through generic `ActionAssignScalarExpr(ActionCallExpr)`; `_evaluateCall` emits structured `unknown_helper`; reconstruction/emission recompile that same logical call; the separate v1 registry denies `expr-v1` at resolve.
  - [x] **FIX** — added only the dormant consumer plus Dart lifecycle/checker mutation and synchronized boundary truth; `.1-.4` retain all production behavior.
  - [x] **ADDRESSED (verified)** — fatal analysis passes; exact opt-in output is four GREEN/one named RED; v1 compatibility, native/reconstructed/generated-plan/emitted observations, and dormant topology are exact.
  - [x] **NO REGRESSION** — ordinary Dart package is 416/416, neutral/progressive/generated/capability/language governance passes, and production/admission/rollout/format/public/other-backend diffs remain absent; mandatory history-capacity movement upgrades final proof to canonical.
  - [x] **LOCKSTEP** — task/index, neutral contract/checker, Knowledge, ADR, roadmaps, architecture, Toolbox, live docs, memory, and mdBook agree on dormant Dart at 85 mutations and `.1` next.
  Activation evidence: `git status --short --untracked-files=all` is empty at exact clean `94d20becf2c8a17438a2509538a9878ecd96c9b9`; `git_message_brief.txt` is zero bytes; the `.14.7.4.4` canonical receipt is promoted to that committed HEAD; `docs/linkedspec-book/book` is absent; no background result remains. Neutral, Perl, and Rust are complete at 84 mutations; the admitted Perl 143-assertion and Rust four-route consumers are GREEN; Dart/Julia/Lua/recurrence/public/outward truth remains pending.
  Implementation evidence: `dart/test_dormant/staged_ast_enrichment_contract_test.dart` freezes the full neutral inventory, exact five-consumer lifecycle, unchanged function-body-v1 job/cache/stitch and wrong-top context, and general `expr-v1` resolve denial. Compiled JSON contains exactly one `assign_scalar` target and one generic `call` named `parse_job`, with no marker or sidecar. Native and `SpecFile`-JSON-reconstructed execution emit `unknown_helper name="parse_job" rule_label="Top"`; validated generated-plan and independently analyzed/executed emitted source preserve that rejection through generated execution failure. The final assertion alone names missing `STAGED_PARSE_JOB_MARKER` / `staged_parse_job_v2`. The checker requires the exact dormant path, final-path absence, zero canonical references, Dart `dormant_red`, and 85 mutations while rollout stays pending. No file under `dart/lib/`, existing `dart/test/`, `tools/run_ci_local.sh`, public/outward surface, generated format, later backend, CLI, or README changes.
  Focused signoff evidence: fatal analysis passes; the exact dormant run reports 4 passed/1 intentional RED; ordinary Dart discovery passes 416/416 and therefore excludes `test_dormant/`; neutral staged governance passes 4 registry entries, 8 provenance, 8 resolution/6 authority, 10 cache, 4 queue/3 isolation, 4 result/3 failure policies, 10 chain, 5 detachment, 37 diagnostics, 9 rollout legs, 35 owners, and 85 mutations. Progressive remains 9/9/116 plus public 60; generated capability remains 80/0/0; language remains 250 current names / 105+1 corpus / 126 public Perl contracts; capability remains 80 pass/0 partial/0 gap. Knowledge regeneration reports 888 facts/7,520 question keys. Mandatory change-history rollover then crosses finite file-count/manifest-line controls, so canonical proof is now required. Intended atomic subject: `FUTURE-PARITY-BACKLOG.14.7.5.0 - freeze Dart staged-AST dormant RED`; clear the brief, verify clean handoff, then activate `.14.7.5.1` task-tree-first.
  Continuity evidence: mandatory complete-record rollover creates immutable `docs/history/changes/segment-4990-eefd32bcaf3a.md` from activation commit `94d20becf2c8a17438a2509538a9878ecd96c9b9` and leaves synchronized `CHANGES.md` at 243/512 lines while live status retains exactly sixteen recent rows. ADR `0091` authorizes only change-history resulting-tree capacity 22→23 files and manifest capacity 21→22 lines; root/member/aggregate line and byte limits, lifecycle, storage, owner, patterns, verifier, and query/recovery behavior remain unchanged. The exact 23-file collection is 46,806/55,000 lines and 3,348,383/4,194,304 bytes.
  Canonical-attempt evidence: the first exact staged candidate passes all nine doctrines and reaches the admitted Perl consumer, then fails only three neutral current-projection assertions: pre-Dart status, 84 mutations, and Dart `pending_absent`. Exact source scan proves the admitted Rust consumer repeats those same three stale values. Runtime, policy, carrier, format, rollout, and outward assertions do not fail. The same leaf therefore owns the minimal six-value projection repair plus updated assertion descriptions and a recurrence-prevention Knowledge fact before canonical restart. Focused repair proof is Perl 143/143 and Rust 1/1 including independently compiled emitted execution.
  Canonical completion and handoff evidence: the unchanged exact staged candidate passes receipt-bound local CI after the representative process-locality proof is rerun outside the outer managed sandbox; phase 0 is 1,032/1,032 and the staged receipt binds candidate `8b8cc7ee081da73506403ef148df4ed1e341a3da6596f31136ce83998f8fab43`. All nine pre-commit doctrines pass, post-commit activation resolves to first parent `94d20bec`, the receipt promotes to committed HEAD `93b035c3f1417d81abbf2527ffe3280f66d33df8`, the brief is zero bytes, and the clean handoff activates `.14.7.5.1` task-tree-first.
- ID: `FUTURE-PARITY-BACKLOG.14.7.5.1`
  Status: `done; focused-signoff-complete` (2026-08-26; task-tree-first from exact clean dormant-Dart commit `93b035c3f1417d81abbf2527ffe3280f66d33df8`; no push); Goal: Add Dart's private dedicated parse-job annotation node and typed direct/derived provenance carrier while retaining the same dormant final path.
  Depends on: committed dormant boundary `.14.7.5.0`, ADR `0088`, the executable neutral contract, ADR `0056` typed source algebra, Dart's committed progressive exclusive-node and source-location precedents, and Perl/Rust marker/provenance evidence as reference only.
  Acceptance: retrieve this stable leaf, the Dart dormant root-cause fact, neutral authored/provenance/diagnostic rows, typed-source and recognition-effect owners, exact Dart parser/AST/compiled-serialization/interpreter/generated-source and live source/match/capture boundaries, and the committed Dart progressive exclusive-node precedent before editing production; make exact assignment-form `name = parse_job(text_expr, literal_hash_options)` the sole private annotation spelling and lower it to one dedicated logical `STAGED_PARSE_JOB_MARKER` node rather than a generic call; validate all required and optional literal options, parser/top/result/failure/target identities, unknown/duplicate keys, exact assignment-only placement, residual generic forms, and recognition-reachable effects before execution; attach one inert detached `staged_parse_job_v2` declaration carrying normalized options, exact materialized text, and only typed direct same-source or nonempty ordered-derived Unicode-scalar provenance; derive provenance from live Dart source/match/capture authority rather than accepting copied text, path, match, registry, parser, callback, scheduler, or host authority in the node or sidecar; reject reversed/out-of-range/source-mismatched/empty-derived/transformed/literal copied text and copied-text smuggling with the exact neutral diagnostic family; preserve the same dormant consumer path and every `.0` neutral/current-v1/four-carrier-observation group, advance its sole final RED to `.14.7.5.2`'s missing caller-frozen resolution/cache/result/failure authority, and keep ordinary discovery plus canonical references at zero; change no current function-body-v1 record/behavior, staged registry authority, result/failure stitching, recurrence, reconstructed/generated/emitted execution authority beyond preserving logical node/sidecar data, generated format version, ordinary/canonical topology, Dart rollout, capability/public/outward surface, other backend, CLI, README, or combined `.14.8`; synchronize task/index, Knowledge, ADR implementation note, roadmaps, architecture, Toolbox, bounded live docs, memory, and sole-facing mdBook.
  Verification tier: `focused` — only private Dart annotation/provenance production, its still-dormant consumer, and durable truth move; there is no admission, rollout, generated-format, public, infrastructure, storage, history-capacity, milestone, or canonical-topology change.
  Focused checks: the same dormant Dart consumer with all neutral/current-v1/static-validation/typed-provenance/four-logical-route groups GREEN and only one `.2` authority assertion RED; ordinary package discovery and canonical references remain zero; exact malformed/residual/transaction/provenance-smuggling denials; Dart parser/compiled-spec/runtime/generated-source and source-location direct dependents; typed-source, recognition, staged-neutral, semantic, progressive, generated-source, capability, and language governance; task/index, Knowledge, rendered mdBook, bounded histories, README/memory, all nine doctrines, exact no-registry/no-policy/no-recurrence/no-admission/no-rollout/no-format/no-public/no-other-backend diff, and `git diff --check`.
  Canonical trigger: `none` — run receipt-bound canonical CI only if implementation unexpectedly crosses admission, rollout, generated-format, public, milestone, infrastructure, storage, finite-history-capacity, or canonical-topology boundaries.
  Ownership: `.14.7.5.1` owns only the exclusive private Dart marker, literal-option normalization/static closure, typed direct/ordered-derived provenance construction and denials, same-consumer next RED, and synchronized truth. `.2` retains caller-frozen resolution/cache plus result/failure policies; `.3` recurrence/bounds/rebasing; `.4` fresh four-route authority, admission, Dart rollout, and parent closure. Later backends, recurrence `.8`, public `.9`, recomposition `.10`, and combined `.14.8` remain untouched.
  Checklist: [x] clean activation/task ownership [x] ADR/Knowledge/neutral/typed/progressive retrieval [x] exact parser/compiler/runtime design [x] exclusive marker and static closure [x] typed direct/derived provenance and denials [x] same-consumer `.2` RED [x] unchanged v1/logical routes/dormancy [x] direct-dependent/no-drift proof [x] durable synchronization [x] focused signoff [x] atomic commit/brief/clean handoff.
  Acceptance Checklist:
  - [x] **REPRODUCE / ISSUE** — the committed consumer reproduced four GREEN groups and only the exact generic `parse_job` / missing-marker-and-provenance RED while current v1, ordinary discovery, and canonical references remained exact.
  - [x] **ROOT CAUSE (WHY + WHERE)** — Dart lowered the reserved assignment through `ActionAssignScalarExpr(ActionCallExpr)`, and `RuntimeRegexMatch` exposed group text but no authoritative capture boundaries; source/tool probes proved that copied text or `indexOf` recovery could not distinguish equal repeated captures such as `(a)(a)`.
  - [x] **FIX** — the exact assignment now lowers to one dedicated private node with strict literal options and residual/reachability closure; runtime construction emits an inert detached v2 declaration whose direct spans come from source authority and whose ordered-derived capture spans are proven lazily by zero-width live-regex instrumentation.
  - [x] **ADDRESSED (verified)** — seven groups pass and only the exact `.2` sentinel remains RED: `[pre_registered_resolution,immutable_cache,result_failure_policies]`; native, reconstructed, generated-plan, and independently emitted logical routes produce equal markers.
  - [x] **NO REGRESSION** — fatal analysis, 132 direct-dependent tests, all 416 ordinary Dart tests, neutral/typed/recognition/semantic/progressive/generated/capability/language governance, dormancy/topology/format/public/other-backend guards, and exact diff checks pass.
  - [x] **LOCKSTEP** — task/index, Knowledge, ADR, roadmaps, architecture, Toolbox, bounded live docs, memory, and rendered mdBook agree on the private Dart marker/provenance boundary and `.2` next.
  Activation evidence: exact `git status --short --untracked-files=all` is empty at `93b035c3f1417d81abbf2527ffe3280f66d33df8`; `git_message_brief.txt` is zero bytes; the `.14.7.5.0` canonical receipt matches committed HEAD; `docs/linkedspec-book/book` is absent; no background result remains. The committed dormant consumer completes four pre-boundary tests and fails only its missing-marker/typed-provenance assertion; Dart lifecycle is `dormant_red`, neutral governance is 85 mutations, ordinary/canonical references are zero, and production/rollout/generated-format/public/outward boundaries remain unchanged.
  Implementation evidence: `ActionStagedParseJobExpr` exclusively represents the reserved assignment, serializes only normalized logical annotation data, bypasses the ordinary helper registry, and is rejected if it survives as a generic call or appears in recognition-reachable effects. The private runtime constructor materializes exact detached `STAGED_PARSE_JOB_MARKER` / `staged_parse_job_v2` records. Direct entry/match text uses authoritative same-source Unicode-scalar spans; recursive `cat` composition carries nonempty ordered-derived records. `RuntimeRegexMatch` retains only participating original group identities and regex option bits, then proves a requested live capture's exact code-unit boundaries by structurally locating that group, injecting named zero-width boundary probes, rerunning at the original whole-match start, and requiring identical whole and selected-capture text. This distinguishes equal repeated captures without substring guessing and rejects numeric-backreference or otherwise unprovable patterns. No registry, callback, cache, policy, scheduler, path, or host authority enters the node or sidecar.
  Focused signoff evidence: the exact dormant consumer is 7 GREEN / 1 intentional `.2` RED and preserves all eight neutral provenance cases, Unicode direct spans, `(a)(a)` ordered-derived spans, sorted capabilities, current function-body-v1 behavior, and four equal logical routes. Fatal analysis passes; 132 parser/compiler/runtime/emitter/source-location dependents and all 416 ordinary Dart tests pass. Neutral staged governance remains 4 registry, 8 provenance, 8 resolution/6 authority, 10 cache, 4 queue/3 isolation, 4 result/3 failure, 10 chain, 5 detachment, 37 diagnostics, 9 rollout, 35 owners, and 85 mutations. Typed source is 12/2/170; recognition 138/250/58; semantic 6/20/128; progressive 9/9/116 plus public 60; generated and capability are each 80/0/0; language is 250/105+1/126. Knowledge regenerates at 891 facts/7,538 question keys; mdBook renders successfully and its output is removed. README, public umbrella, current-v1 registry, generated format/version, capability contract, canonical topology, other backends, rollout/admission, recurrence/policies, and outward truth do not move. Intended atomic subject: `FUTURE-PARITY-BACKLOG.14.7.5.1 - implement Dart staged marker provenance`; clear the brief, verify clean handoff, then activate `.14.7.5.2` task-tree-first.
- ID: `FUTURE-PARITY-BACKLOG.14.7.5.2`
  Status: `done; focused-signoff-complete` (2026-08-26; task-tree-first from exact clean marker/provenance commit `16f9769a07e6d79f38fa95ff75e56e1ab8f8be43`; no push); Goal: Add Dart's caller-pre-registered resolution/cache authority and all result/failure stitching policies while recursive scheduling remains RED.
  Depends on: neutral contract `.14.7.2`, ADR `0088`, committed dormant boundary `.14.7.5.0`, committed marker/provenance `.14.7.5.1`, the Dart source/detachment/runtime authorities, and committed Perl/Rust current-depth authorities as mechanism evidence only; the current narrow function-body-v1 staged registry remains compatibility-only evidence.
  Acceptance: retrieve this stable leaf, ADR `0088`, neutral resolution/authority/cache/queue/isolation/result/failure/detachment cases and diagnostics, Dart marker/provenance/current-v1 facts, Dart plain-data/runtime/source owners, committed Perl/Rust `.2` mechanisms, and exact Toolbox probes before implementation archaeology; add one private caller-prepared immutable general-v2 registry containing normalized logical identities and already-compiled opaque callbacks only, with alias/declaring-relative/ordered-root/ordered-provider candidate outcomes supplied and frozen before authored execution; perform pure post-AST selection with exact missing/ambiguous/collision/top/version/capability/policy/source-detail/ceiling diagnostics and no filesystem/provider/import/environment/network/loading/compilation/registry-mutation authority; select/default top before deterministic `parse_job:v2:sha256` identity; add invocation-local immutable-plan caching over normalized spec/content/import/top/version/contract-version/effective-capability identity with exact hit/miss/invalidation/isolation behavior and no result/failure/partial-work poisoning; discover and validate one complete current marker depth, order it by typed parent path/provenance/job id, give every sibling a fresh cursor/mark/capture/variable/request context, and execute already-compiled callbacks only after every resolution and stitch target is valid; detach and node-bound plain child results before atomically applying `replace_marker`, `replace_field`, `sibling_field`, or `append_child`, and apply `fail`, `keep_text`, or `diagnostic_node` with exact marker/target/collision/kind/live/cyclic/overflow diagnostics over an unpublished AST copy; preserve nested returned markers as inert and advance the same dormant consumer's sole RED to `.3` breadth-first recurrence, decreasing-chain/cycle/depth/call/cancellation/resource bounds, callback safe points, and original-source diagnostic rebasing; keep function-body v1, `.1` marker/provenance, and logical native/reconstructed/generated/emitted marker equality unchanged; add no recursive rescan, top-level authority seed/carrier execution, admission/discovery/rollout/capability/public/outward projection, generated-format version, semantic/facade/schema/MCP/CLI/README surface, canonical topology, other backend behavior, or combined `.14.8`; synchronize task/index, Knowledge, ADR implementation note, roadmaps, architecture, Toolbox, bounded live docs, memory, and sole-facing mdBook.
  Verification tier: `focused` — this leaf changes only private Dart one-depth authority/cache/policy behavior and its deliberately unrouted consumer; it does not admit a route or change rollout, public, infrastructure, storage, generated format, milestone, or canonical topology.
  Focused checks: the same dormant Dart consumer with committed neutral/current-v1/static-validation/typed-provenance/four-logical-route groups plus every neutral resolution/authority/cache/queue/isolation/result/failure/detachment case GREEN and only `.3` recurrence/bounds/rebased-diagnostic authority RED; ordinary discovery and canonical references remain zero; immutable registry/cache, adversarial callback isolation, deterministic typed ordering, detached stitching, atomic fail, retained diagnostics, and marker/target/live-result denials; current function-body-v1 integration/trace compatibility; Dart parser/compiler/runtime/source and detachment direct dependents; staged-neutral, typed-source, recognition, semantic, progressive, generated-source, capability, and language governance; task/index, Knowledge, rendered mdBook, bounded histories, README/memory, all nine doctrines, exact no-recursive-queue/no-carrier-seam/no-admission/no-rollout/no-format/no-public/no-other-backend diff, and `git diff --check`.
  Canonical trigger: `none` — run receipt-bound canonical CI only if implementation unexpectedly crosses admission, rollout, generated format, public, milestone, infrastructure, storage, finite-history capacity, or canonical topology.
  Ownership: `.14.7.5.2` owns only Dart's private caller-frozen immutable registry/cache, pure selection from caller-completed outcomes, selected-top-before-v2 identity, current-depth order/isolation, four result and three failure policies, exact detachment/stitch diagnostics, the same-consumer `.3` RED, and synchronized truth. `.3` retains recursive breadth-first scheduling, decreasing-chain/cycle/resource/cancellation limits, callback safe points, and original-source diagnostic rebasing. `.4` retains fresh native/reconstructed/generated/emitted top-level authority, ordinary/canonical admission, Dart rollout promotion, and parent closure. Later backends `.6-.7`, recurrence `.8`, public authoring `.9`, recomposition `.10`, and combined `.14.8` remain untouched.
  Checklist: [x] clean activation/task ownership [x] ADR/Knowledge/neutral/precedent retrieval [x] exact frozen authority/cache design [x] pure resolution/job-id implementation [x] current-depth execution/order/isolation [x] four result/three failure policies [x] adversarial authority/detachment rejection [x] unchanged v1/marker/logical routes and `.3` RED [x] direct-dependent/no-drift proof [x] durable synchronization [x] focused signoff [x] atomic commit/brief/clean handoff.
  Acceptance Checklist:
  - [x] **REPRODUCE / ISSUE** — the exact committed consumer reports `+7 -1` and reaches only `.2`'s `[pre_registered_resolution,immutable_cache,result_failure_policies]` sentinel after all marker/options/provenance/four-logical-route groups pass while ordinary/canonical discovery remains zero.
  - [x] **ROOT CAUSE (WHY + WHERE)** — Knowledge, ADR `0088`, neutral JSON, `staged_parse_job.dart`, and runtime probes show Dart now constructs only inert declaration data; `staged_parser_registry.dart` remains the deliberately narrow function-body-v1 owner, and no private general-v2 frozen resolver/cache/current-depth scheduler/policy stitcher exists.
  - [x] **FIX** — one private module now owns the frozen general-v2 authority/cache/current-depth policy engine, and the same dormant consumer advances to the `.3` recurrence boundary.
  - [x] **ADDRESSED (verified)** — all five newly owned authority/cache/policy groups are GREEN at `+12 -1`; the sole intentional RED names only breadth-first recurrence, decreasing-chain/cycle/shared-resource authority, and source-rebased diagnostics.
  - [x] **NO REGRESSION** — v1, marker/provenance, four logical routes, ordinary/canonical dormancy, rollout/format/public boundaries, 102 direct dependents, 416 ordinary tests, governance, and exact diff guards pass unchanged.
  - [x] **LOCKSTEP** — task/index, neutral governance, Knowledge, ADR, roadmaps, architecture, Toolbox, bounded live docs, memory, and rendered mdBook agree on Dart current-depth authority and next owner `.3`.
  Activation evidence: exact `git status --porcelain=v1 --untracked-files=all` is empty at `16f9769a07e6d79f38fa95ff75e56e1ab8f8be43`; `git_message_brief.txt` is zero bytes; `docs/linkedspec-book/book` is absent; no background gate remains. `.14.7.5.1` is durably committed with seven GREEN groups and only `[pre_registered_resolution,immutable_cache,result_failure_policies]` RED. Neutral staged truth remains 85 mutations with Dart `dormant_red`, ordinary discovery and canonical references zero, Dart rollout pending, and no generated-format/public/outward movement.
  Reproduction and root-cause evidence: the exact repository-routed command returns `+7 -1` with only `LINKEDSPEC_STAGED_AST_ENRICHMENT_DART_RED: missing authority=[pre_registered_resolution,immutable_cache,result_failure_policies]`. ADR/Knowledge retrieval plus exact source search confirms `staged_parse_job.dart` stops at detached declaration construction, `staged_parser_registry.dart` accepts only the fixed v1 function-body contract, and Dart has no general-v2 current-depth authority. Committed Perl/Rust `.2` implementations are consulted only for host-language mechanism; the executable neutral artifact remains semantic authority.
  Implementation evidence: private `staged_ast_enrichment.dart` freezes caller-completed alias, declaring-relative, ordered-root, and provider candidate outcomes plus exact logical-name-to-opaque-callback bindings. Selection is pure and typed; runtime register/load are denials. Entry versions, tops, capabilities, policies, source detail, and numeric ceilings only narrow caller authority. Default top selection precedes deterministic job identity, and the exact normalized eight-field cache identity stores immutable plans only. One complete current depth is discovered, resolved, target-validated, and typed-path/provenance/job ordered before callbacks; siblings receive fresh cursor, mark, capture, variable, and request aggregates. Finite acyclic node-bounded plain results are detached into an unpublished AST copy through all four result and three failure policies, so atomic failure leaks no sibling work and returned markers remain inert. No recursive scheduler, carrier seed, ambient discovery/loading/compilation, public export, or admission seam is added.
  Focused signoff evidence: the exact dormant consumer advances from `+7 -1` to `+12 -1`; the sole failure is `LINKEDSPEC_STAGED_AST_ENRICHMENT_DART_RED` for `.3`'s `[breadth_first_recursion,decreasing_chain_cycle_guards,shared_resource_bounds,source_rebased_diagnostics]`. Fatal analysis, 102 parser/compiler/runtime/emitter/source/detachment direct dependents, and all 416 ordinary Dart tests pass. Neutral staged governance remains 4 registry, 8 provenance, 8 resolution/6 authority, 10 cache, 4 queue/3 isolation, 4 result/3 failure, 10 chain, 5 detachment, 37 diagnostics, 9 rollout, 35 owners, and 85 mutations. Typed source is 12/2/170; recognition 138/250/58; semantic 6/20/128; progressive 9/9/116 plus public 60; generated and capability remain 80/0/0; language remains 250/105+1/126. Knowledge regenerates at 892 facts/7,552 question keys; mdBook renders and its generated output is removed. Exact guards prove no ordinary/canonical registration, public umbrella, v1 registry, neutral contract, generated/other-backend, format, rollout, or outward movement. Intended atomic subject: `FUTURE-PARITY-BACKLOG.14.7.5.2 - add Dart staged current-depth authority`; clear the brief after commit, verify clean handoff, then activate `.14.7.5.3` task-tree-first.
- ID: `FUTURE-PARITY-BACKLOG.14.7.5.3`
  Status: `done; focused-signoff-complete` (2026-08-27; task-tree-first from exact clean current-depth-authority commit `7355445cdf9f10287f712f93553cfe4b5c169906`; atomic commit pending; no push); Goal: Complete Dart recursive queue scheduling, decreasing-chain/cycle/resource guards, shared cancellation/resource authority, callback safe points, and original-source diagnostic rebasing while discovery remains dormant.
  Depends on: neutral executable contract `.14.7.2`, ADR `0088`, committed Dart marker/provenance `.14.7.5.1`, committed current-depth authority `.14.7.5.2`, Dart typed source and runtime cancellation/resource owners, and committed Perl/Rust recursive authorities as host-language mechanism evidence only.
  Acceptance: retrieve this stable leaf, ADR `0088`, neutral queue/chain/bounds/detachment/diagnostic rows, Dart current-depth and typed-source/runtime facts, committed Perl/Rust `.3` facts and implementations, and exact Toolbox probes before production archaeology; extend only the private Dart general-v2 authority so markers stitched by one complete stage are collected after that stage settles, enqueued for the next depth, and processed breadth-first by depth then typed parent path/provenance/job id without rescanning an unfinished depth; retain one invocation-local frozen registry and plan cache across all depths while giving every job fresh sibling execution state; carry an immutable active chain of parser/top/payload-digest/provenance tuples, reject an exact tuple cycle, and require repeated parser/top on the same provenance lineage to be strictly contained with smaller total Unicode-scalar extent; share identity-bearing cancellation, monotonic deadline, total-call, step, stage-depth, result-node, and diagnostic-byte authority without reset or extension, charge dispatch entry and child safe points, and expire callback request/source authority after completion or failure; rebase child-local positions/spans/diagnostics through direct or ordered-derived provenance to original typed source segments without inventing one contiguous span for derived text, while bounding retained diagnostic UTF-8 bytes; correct the review-discovered one-depth preflight gap by rejecting duplicate non-append result-slot claims and any target that overwrites or contains another queued marker before the first callback; preserve all otherwise valid complete-depth preflight, atomic result/failure policies, detached node bounds, selected-top identities/cache behavior, current function-body v1, `.1` marker/provenance, and logical native/reconstructed/generated/emitted marker equality; advance the same dormant consumer's sole RED to `.4` fresh top-level native/reconstructed/generated/emitted authority, ordinary/canonical admission, Dart rollout promotion, and parent closure; add no top-level authority seed/carrier execution, public export, ordinary/canonical discovery, rollout/capability/public/outward projection, generated-format version, semantic/facade/schema/MCP/CLI/README surface, other backend behavior, or combined `.14.8`; synchronize task/index, Knowledge, ADR implementation note, roadmaps, architecture, Toolbox, bounded live docs, memory, and sole-facing mdBook.
  Verification tier: `focused` — this leaf changes only private dormant Dart recursive authority and the same explicitly unrouted consumer; it does not move admission, rollout, generated format, public behavior, infrastructure, storage, milestone, or canonical topology.
  Focused checks: the same dormant consumer with all prior 12 groups plus every neutral recursive queue/chain/cycle/resource/cancellation/safe-point/rebasing case GREEN and only `.4` fresh-carrier/admission/rollout authority RED; ordinary discovery and canonical references remain zero; two-depth breadth-first order, same-depth no-rescan, shared cache/resource identity, exact cycle/non-decrease preflight, depth/call/step/result/diagnostic ceilings, callback cancellation/deadline/safe-point/expiry, and direct/ordered-derived rebasing; fatal Dart analysis, current function-body-v1 integration/trace compatibility, parser/compiler/runtime/source/detachment direct dependents, and complete ordinary Dart; staged-neutral, typed-source, recognition, semantic, progressive, generated-source, capability, and language governance; task/index, Knowledge, rendered mdBook, bounded histories, README/memory, all nine doctrines, exact no-carrier/no-admission/no-rollout/no-format/no-public/no-other-backend diff, and `git diff --check`.
  Canonical trigger: `none` — run receipt-bound canonical CI only if implementation unexpectedly crosses admission, rollout, generated format, public, milestone, infrastructure, storage, finite-history capacity, or canonical topology.
  Ownership: `.14.7.5.3` owns only breadth-first recursive scheduling over the private Dart authority, immutable lineage checks, shared monotone cancellation/resource limits, expiring callback safe points, original-source rebasing, the same-consumer `.4` RED, and synchronized truth. `.4` retains fresh native/reconstructed/generated/emitted top-level authority, production seam, ordinary/canonical admission, Dart rollout promotion, and parent closure. Later backends `.6-.7`, recurrence `.8`, public authoring `.9`, recomposition `.10`, and combined `.14.8` remain untouched.
  Checklist: [x] clean activation/task ownership [x] ADR/Knowledge/neutral/precedent retrieval [x] exact `.3` RED reproduction [x] complete-depth target-conflict correction [x] recursive queue/lineage design [x] shared cancellation/resource authority [x] callback safe points/expiry [x] original-source diagnostic rebasing [x] same-consumer `.4` RED [x] direct-dependent/no-drift proof [x] durable synchronization [x] focused signoff [ ] atomic commit/brief/clean handoff.
  Acceptance Checklist:
  - [x] **REPRODUCE / ISSUE** — the exact repository-routed consumer is `+12 -1`; all current-depth groups pass and only `[breadth_first_recursion,decreasing_chain_cycle_guards,shared_resource_bounds,source_rebased_diagnostics]` remains RED.
  - [x] **ROOT CAUSE (WHY + WHERE)** — ADR/Knowledge, the neutral JSON, and complete module/test inspection prove `enrichStagedCurrentDepth` intentionally hard-codes `stage_depth: 1`, an empty `stage_chain`, and no rescan; `StagedRuntimeContext` has only fresh parser registers, while no invocation-wide lineage, cancellation/clock/counters, callback lifetime, or provenance projector exists.
  - [x] **FIX** — `enrichStagedRecursively` now processes complete breadth-first depths through one registry/cache and monotone invocation authority; strict tuple lineage, safe points/expiry, typed direct/derived rebasing, and complete-depth target reservation are covered by the same dormant consumer.
  - [x] **ADDRESSED (verified)** — every owned recursive group is GREEN at `+18 -1`; the sole intentional RED names only `.4` fresh carriers, production seam, ordinary/canonical admission, and Dart rollout.
  - [x] **NO REGRESSION** — all `.0-.2` groups, v1, four logical marker routes, 102 direct dependents, 416 ordinary tests, dormant topology, formats/public boundaries, governance, and exact diff guards remain GREEN/unchanged.
  - [x] **LOCKSTEP** — task/index, neutral governance, Knowledge, ADR, roadmaps, architecture, Toolbox, bounded live docs, memory, and mdBook agree on complete Dart recursive authority and next owner `.4`.
  Activation evidence: exact `git status --porcelain=v1 --untracked-files=all` is empty at `7355445cdf9f10287f712f93553cfe4b5c169906`; `git_message_brief.txt` is zero bytes; `docs/linkedspec-book/book` is absent; the `.14.7.5.2` focused commit passed all nine pre-commit doctrines and its post-commit activation check; no background result remains. The committed dormant consumer is `+12 -1` with only `[breadth_first_recursion,decreasing_chain_cycle_guards,shared_resource_bounds,source_rebased_diagnostics]` RED; Dart remains `dormant_red`, ordinary/canonical references remain zero, rollout remains pending, and generated-format/public/outward truth is unchanged.
  Retrieval and root-cause evidence: the stable leaf, all of ADR `0088`, the executable neutral queue/isolation/chain/diagnostic policy, current Dart one-depth fact, typed-source and Dart progressive authority facts, committed Perl/Rust recursive facts, and exact Toolbox route were read before production archaeology. The exact dormant command reproduces `+12 -1` and its sole sentinel. Complete `staged_ast_enrichment.dart` inspection shows `_preparePlan` fixes depth one and empty chain, `enrichStagedCurrentDepth` returns after one discovered marker set, returned markers are deliberately inert, and callback context owns no shared or source-aware authority. Existing Dart progressive authority supplies only host-language mechanism evidence for identity-bearing cancellation, monotone safe points, callback expiry, and typed rebasing; the neutral artifact remains semantic authority.
  Review-discovered defect evidence: `_validateStitchTarget` checked every prepared job only against the unchanged initial AST. Two individually valid `replace_field` jobs could therefore claim the same slot and silently overwrite the earlier result, while one target could overwrite an independently queued marker so the later failure occurred only after callbacks began. The neutral complete-stage validation rule requires both conflicts to reject before callback one. This active private-scheduler leaf owns the minimal cross-plan reservation/overlap defense and dormant zero-callback regressions before recursive signoff; no public or admitted boundary moves.
  Implementation evidence: `StagedRecursiveAuthority` carries one identity-bearing cancellation token/probe, caller clock/deadline, remaining steps, total calls, maximum depth, cumulative result-node count, and diagnostic-byte count without reset. `enrichStagedRecursively` completely prepares, authority-checks, target-reserves, and typed-sorts each depth before callback one; returned markers enter only the next depth. Frames bind normalized resolved parser, selected top, SHA-256 of exact UTF-8 payload, and full provenance. Exact repeats reject as `staged_cycle`; same-parser/top descendants require strict source-segment containment and smaller total Unicode-scalar extent. Fresh sibling contexts expose shared `safePoint` and direct/ordered-derived position, span, and diagnostic rebasing only during their callback lifetime. One shared depth validator also rejects duplicate non-append claims, mixed append/replace claims, non-append ancestor collisions, and targets containing another queued marker while preserving deterministic multiple appends to one list.
  Focused signoff evidence: the exact repository-routed dormant consumer advances from `+12 -1` to `+18 -1`; its sole nineteenth failure is `LINKEDSPEC_STAGED_AST_ENRICHMENT_DART_RED` for `[fresh_native_reconstructed_generated_emitted_authority,production_seam,ordinary_canonical_admission,dart_rollout]`. Fatal Dart analysis reports no issues. The exact parser/compiler/runtime/emitter/source/detachment dependent set passes 102/102 and ordinary Dart passes 416/416 after the target-reservation correction. Neutral staged governance remains 4 registry entries, 8 provenance, 8 resolution/6 authority, 10 cache, 4 queue/3 isolation, 4 result/3 failure policies, 10 chain, 5 detachment, 37 diagnostics, 9 rollout legs, 35 owners, and 85 mutations. Typed source remains 12/2/170; recognition 138/250/58; semantic 6/20/128; progressive 9/9/116 plus public 60; generated/capability 80/0/0; language 250/105+1/126. Knowledge regenerates at 893 facts/7,567 question keys; the mdBook renders and its generated output is removed. Exact topology/diff guards prove the final admitted test path, ordinary/canonical references, carriers, rollout, v1 registry, neutral artifact, generated format/version, public/outward surfaces, README, other backends, and combined `.14.8` do not move. Intended atomic subject: `FUTURE-PARITY-BACKLOG.14.7.5.3 - add Dart staged recursive authority`; clear the brief, verify clean handoff, then activate `.14.7.5.4` task-tree-first.
- ID: `FUTURE-PARITY-BACKLOG.14.7.5.4`
  Status: `done; canonical-signoff-complete` (2026-08-27; task-tree-first from exact clean recursive-authority commit `053178b81bc095810fcc4ef8f0c8a8f7463a2458`; no push); Goal: Recompose Dart native/reconstructed/generated/emitted routes with fresh authority and canonically admit the unchanged consumer.
  Depends on: committed Dart recursive authority `.14.7.5.3`, ADR `0088`, the executable neutral staged contract, Dart's admitted progressive four-carrier topology as host-language mechanism evidence, and committed Perl/Rust staged carrier/admission evidence as topology precedent only.
  Acceptance: retrieve this stable leaf, all of ADR `0088`, Dart marker/current-depth/recursive Knowledge cards, the admitted Dart progressive seed/carrier/admission fact and implementation, Perl/Rust staged carrier/admission facts, exact current dormant consumer, four Dart production carrier seams, neutral lifecycle/rollout checker, and canonical-driver topology before production archaeology; add one opaque host-only staged-enrichment recipe that owns caller-frozen logical registry/options plus already-compiled callbacks and cancellation/clock recipes but no mutable invocation state, and whose start operation creates a fresh registry/cache/recursive authority for every top-level execution; invoke the recipe only after the complete parent AST/value exists through native, `SpecFile`-JSON reconstructed, validated generated-plan, and independently analyzed/executed emitted-source routes; run each route twice and require equal detached AST/sidecar/diagnostic/cache/resource results with fresh callback, cancellation-token, clock, cache, lineage, and budget observation on every run; keep callback, parser implementation, registry/source snapshots, cancellation/deadline/budget authority, mutable queue/cache, host handles, and paths absent from compiled JSON, plans, and emitted logical source; preserve no-seed behavior, current function-body v1, exact general-v2 annotation/provenance/recursive semantics, failure atomicity, and primary diagnostic identity; move the same stable consumer from `dart/test_dormant/staged_ast_enrichment_contract_test.dart` to `dart/test/staged_ast_enrichment_contract_test.dart` without a dormant duplicate, remove only its intentional `.4` sentinel/admission-owned dormancy assertions, and keep all nineteen behavioral groups GREEN; require the admitted path exactly once in ordinary Dart discovery and exactly once by canonical CI through one tracked-path requirement, one exact log marker, and one repository-routed invocation; promote only Dart's predeclared staged backend lifecycle and rollout row, advance the neutral mutation inventory only through exact admission/topology guards, close parent `.14.7.5`, and leave Julia/Lua/recurrence/public/recomposition rows pending; change no generated format/version, capability state, typed staged recurrence, public authoring, outward/facade/schema/MCP/CLI/README surface, dependency/toolchain/storage/doctrine infrastructure, other backend behavior, or combined `.14.8`; synchronize task/index, neutral governance, Knowledge, ADR, roadmaps, architecture, Toolbox, bounded live docs, memory, and sole-facing mdBook.
  Verification tier: `canonical` — this leaf moves production execution, ordinary/canonical discovery, Dart rollout/admission truth, and closes a backend parent.
  Focused checks: the stable consumer at its admitted path with all nineteen groups GREEN and no dormant duplicate; fresh authority and twice-per-route equality across native/reconstructed/generated-plan/emitted execution; exact serialized/generated authority absence; complete ordinary Dart and fatal analysis; current function-body-v1, parser/compiler/runtime/emitter/source/detachment direct dependents; admitted Perl/Rust consumers after neutral lifecycle/mutation movement; staged-neutral, typed-source, recognition, semantic, progressive, generated-source, capability, and language governance; exact canonical path/log/invocation count; task/index, Knowledge, rendered mdBook, bounded histories, README/memory, all nine doctrines, exact no-format/no-public/no-other-backend diff, and `git diff --check`.
  Canonical trigger: `mandatory` — production carriers, ordinary discovery, canonical CI topology, executable Dart rollout/admission truth, and backend-parent closure move together; stage the exact candidate and run receipt-bound `bash tools/run_ci_local.sh` before landing.
  Ownership: `.14.7.5.4` owns only the fresh top-level Dart recipe, native/reconstructed/generated-plan/emitted production seams, stable consumer admission, exact ordinary/canonical registration, Dart lifecycle/rollout promotion, parent `.14.7.5` closure, and synchronized projections. Julia `.6`, Lua `.7`, staged recurrence `.8`, public authoring `.9`, recomposition `.10`, and combined `.14.8` remain untouched.
  Checklist: [x] clean activation/task ownership [x] ADR/Knowledge/precedent retrieval [x] exact `.4` RED reproduction [x] four production-seam archaeology [x] fresh opaque recipe [x] native/reconstructed/generated/emitted execution [x] stable consumer admission [x] neutral lifecycle/rollout/topology promotion [x] direct-dependent/no-drift proof [x] durable synchronization [x] focused signoff [x] staged canonical receipt [x] atomic commit/brief/clean handoff.
  Acceptance Checklist:
  - [x] **REPRODUCE / ISSUE** — exact repository-routed execution from clean `053178b8` is `+18 -1`; the final path and both canonical references are absent, the dormant path is present, and the sole sentinel names fresh four-route authority, production seam, admission, and Dart rollout.
  - [x] **ROOT CAUSE (WHY + WHERE)** — Toolbox/Knowledge-guided inspection shows `LinkedSpecRuntimeEngine` accepts only `boundedChildParseAuthority`, `_RuntimeExecutionContext` starts only that progressive seed, `_parse` returns the complete parent `result.value` directly, and `executeGeneratedParserV2` plus emitted `execute` forward only the progressive recipe. The dedicated staged marker therefore survives native/reconstructed/generated-plan/emitted logical carriers, but no route starts `FrozenStagedRegistry`/`StagedRecursiveAuthority` or calls `enrichStagedRecursively` after the parent result.
  - [x] **FIX** — `StagedAstEnrichmentSeed` now creates one fresh registry/cache/recursive state per top-level execution; engine, reconstructed, generated-plan, and emitted `execute` routes complete it only after the parent value, and the stable consumer plus exact Dart lifecycle/rollout/canonical topology are admitted.
  - [x] **ADDRESSED (verified)** — all nineteen groups pass through the admitted path, four routes execute twice with fresh isolated authority, and ordinary/canonical registration is exact once.
  - [x] **NO REGRESSION** — v1, marker/provenance/recursion, serialized-authority absence, complete Dart, admitted predecessors, governance, formats/public/other backends, and exact diff guards pass.
  - [x] **LOCKSTEP** — neutral lifecycle/rollout, parent/task/index, Knowledge, ADR, roadmaps, architecture, Toolbox, live docs, memory, mdBook, ordinary discovery, and canonical topology agree.
  Activation evidence: exact `git status --porcelain=v1 --untracked-files=all` is empty at `053178b81bc095810fcc4ef8f0c8a8f7463a2458`; `git_message_brief.txt` is zero bytes; `docs/linkedspec-book/book` is absent; `.14.7.5.3` passed all nine commit-hook doctrines and its post-commit activation check; no background result remains. The committed consumer is `+18 -1` with only `[fresh_native_reconstructed_generated_emitted_authority,production_seam,ordinary_canonical_admission,dart_rollout]` RED. Dart remains `dormant_red`, ordinary/canonical references remain zero, staged rollout remains pending, and generated-format/public/outward/other-backend truth is unchanged.
  Retrieval and root-cause evidence: the stable leaf; all 371 lines of ADR `0088`; the neutral and Dart marker/current-depth/recursive Knowledge cards; the admitted Dart progressive seed/carrier fact; admitted Perl/Rust staged carrier facts; and the exact Toolbox route were read before production archaeology. Exact probes locate the missing seam at `LinkedSpecRuntimeEngine` construction/context start/parent-value return and at `executeGeneratedParserV2` plus emitted `execute`; the existing progressive path proves Dart's opaque recipe/start/forwarding mechanism without supplying staged semantics. The canonical driver contains only admitted Perl/Rust staged paths and no Dart staged path, while the consumer itself proves equal authority-free logical markers across all four routes.
  Implementation evidence: private `StagedAstEnrichmentSeed` eagerly validates and deeply owns the caller-frozen logical snapshot/options plus opaque callback bindings, while an authority factory and detached outcome sink remain host-only. Every `start()` reconstructs `FrozenStagedRegistry` with an empty plan cache and a new `StagedRecursiveAuthority`; the one-use execution state rejects live recognition state and completes `enrichStagedRecursively` only after the parent result. `LinkedSpecRuntimeEngine`, JSON reconstruction through the same engine, `executeGeneratedParserV2`, and emitted-source `execute` forward only the opaque seed; no-seed behavior remains the raw inert marker. The stable consumer moves without a dormant duplicate, and canonical CI now requires/logs/invokes that exact path once. Neutral truth promotes only Dart to complete and adds five admission/rollout mutations, for 90 total.
  Focused behavior evidence: the admitted consumer is 19/19. Its carrier group runs native, normalized `SpecFile`-JSON reconstruction, validated generated-plan, and independently analyzed/executed emitted source twice through one seed. All eight runs return equal detached AST/sidecar/diagnostic/cache/resource records; each has one entry/zero hits/one miss/one call plus fresh cancellation-token, cancellation-callback, clock, callback, budget, lineage, and outcome observation, with no cross-run state reuse. Compiled JSON and emitted logical source contain no callback, parser implementation, registry/source snapshot, cancellation/deadline/budget state, mutable queue/cache, path, or host handle. Fatal analysis is clean; the exact direct-dependent set passes 102/102 and ordinary Dart passes 435/435. Neutral governance passes at 90 mutations; admitted Perl remains 143/143 and Rust's independently compiled emitted carrier remains 1/1 (298.01-second test execution).
  Durable synchronization evidence: neutral governance, ADR `0088`, architecture and roadmap projections, Toolbox routes, the sole-facing staged contract/project status/backend handoff pages, bounded live status/change/engineering notes, memory, and the current/historical Dart plus Perl/Rust carrier Knowledge cards now agree on 19/19, 435/435, 90 mutations, exact final-path admission, unchanged v1/v2/public boundaries, and Julia `.14.7.6.0` next. The Knowledge Map regenerates at 894 facts/7,577 question keys, mdBook renders successfully with repository-local output removed, bounded histories remain below rollover thresholds, memory is 59 lines, and `git diff --check` passes.
  Canonical completion and handoff evidence: the exact fully staged candidate clears receipt-bound `bash tools/run_ci_local.sh`; all nine doctrines, the complete required backend/governance matrices, both 66/66 primary-CLI option environments, and phase 0 at 1,032/1,032 pass. The staged receipt binds base `053178b81bc095810fcc4ef8f0c8a8f7463a2458` plus the final candidate; pre-commit revalidates that exact receipt and the fast doctrines. Intended atomic subject: `FUTURE-PARITY-BACKLOG.14.7.5.4 - admit Dart staged enrichment carriers`; clear the brief after commit, verify the promoted receipt and clean handoff, then activate Julia `.14.7.6.0` task-tree-first.
- ID: `FUTURE-PARITY-BACKLOG.14.7.6`
  Status: `pending`; Goal: Implement and admit the neutral staged-AST contract on Julia through the same RED/authority/policy/queue/carrier split `.0-.4` used by prior backends.
  Depends on: completed Dart parent `.5`; only `.4` may register the consumer or promote Julia.
- ID: `FUTURE-PARITY-BACKLOG.14.7.6.0`
  Status: `pending`; Goal: Freeze Julia's exact dormant general-staged RED and current function-body compatibility boundary.
- ID: `FUTURE-PARITY-BACKLOG.14.7.6.1`
  Status: `pending`; Goal: Add Julia's private dedicated parse-job annotation node and typed direct/derived provenance carrier.
- ID: `FUTURE-PARITY-BACKLOG.14.7.6.2`
  Status: `pending`; Goal: Add Julia's pre-registered resolution/cache authority and all result/failure stitching policies.
- ID: `FUTURE-PARITY-BACKLOG.14.7.6.3`
  Status: `pending`; Goal: Complete Julia recursive queue scheduling, decreasing-chain/cycle/resource guards, and source-rebased diagnostics.
- ID: `FUTURE-PARITY-BACKLOG.14.7.6.4`
  Status: `pending`; Goal: Recompose Julia native/reconstructed/generated/emitted routes with fresh authority and canonically admit the unchanged consumer.
- ID: `FUTURE-PARITY-BACKLOG.14.7.7`
  Status: `pending`; Goal: Implement and admit one Lua-5.1-compatible neutral staged-AST path independently on PUC Lua and LuaJIT through dormant RED `.0`, private annotation/provenance `.1`, resolution/policies `.2`, recursive queue/carriers `.3`, dual-ABI admission `.4`, and independent ABI recomposition `.5`.
  Depends on: completed Julia parent `.6`; one shared source must execute independently on both ABIs and only `.4` may register/promote it.
- ID: `FUTURE-PARITY-BACKLOG.14.7.7.0`
  Status: `pending`; Goal: Freeze one shared dormant general-staged RED and current function-body compatibility boundary on both Lua ABIs.
- ID: `FUTURE-PARITY-BACKLOG.14.7.7.1`
  Status: `pending`; Goal: Add one private Lua-5.1-compatible dedicated parse-job annotation node and typed direct/derived provenance authority.
- ID: `FUTURE-PARITY-BACKLOG.14.7.7.2`
  Status: `pending`; Goal: Add shared pre-registered resolution/cache authority plus all result/failure stitching policies on both ABIs.
- ID: `FUTURE-PARITY-BACKLOG.14.7.7.3`
  Status: `pending`; Goal: Complete shared recursive queues, bounds, source-rebased diagnostics, and fresh native/reconstructed/generated/emitted carriers while discovery remains dormant.
- ID: `FUTURE-PARITY-BACKLOG.14.7.7.4`
  Status: `pending`; Goal: Canonically admit the unchanged shared consumer exactly once on PUC Lua and once on LuaJIT.
- ID: `FUTURE-PARITY-BACKLOG.14.7.7.5`
  Status: `pending`; Goal: Independently recompose both admitted Lua ABI routes and close the shared backend parent without behavior change.
- ID: `FUTURE-PARITY-BACKLOG.14.7.8`
  Status: `pending`; Goal: Bind one exact neutral/five-source/six-runtime recurring driver and promote only typed `staged_dispatch` plus the corresponding current capability row.
  Depends on: all backend parents `.3-.7` closed. Acceptance: immutable source groups, ordered runtime routes, support ledgers, topology/status/storage mutations, a current staged-dispatch capability row, and exact current function-body compatibility; leave the general-authoring future exclusion plus public projection pending for `.9` and combined typed `public_no_drift` pending for `.14.8`. Planned verification is canonical admission/milestone proof.
- ID: `FUTURE-PARITY-BACKLOG.14.7.9`
  Status: `pending`; Goal: Admit and document the portable `parse_job(...)` authored surface, remove every stale future claim, close staged public projection/no-drift, and change no unrelated facade/schema/semantic/MCP/CLI/README surface.
  Depends on: recurring proof `.8`. Acceptance: exact grammar/helper inventory, examples, diagnostics, capability/exclusion truth, user-facing mdBook, backend handoff, public stale-claim/outward guards, and mutation proof; `parse_job` becomes current only through the neutral dedicated node and registered authority contract, never a generic host callback or runtime path loader. Planned verification is canonical public-boundary proof.
- ID: `FUTURE-PARITY-BACKLOG.14.7.10`
  Status: `pending`; Goal: Independently recompose the committed neutral, five-source/six-runtime, typed/capability, public-authoring, generated/carrier, diagnostic, and narrow-function compatibility owners without replacement behavior, close `.14.7`, and hand off combined program-wide `.14.8`.
  Depends on: public closeout `.9`; planned verification is canonical milestone/recomposition proof.
- ID: `FUTURE-PARITY-BACKLOG.14.8`
  Status: `pending`; Goal: Recompose six-runtime admissions and recurring governance, close examples/tooling/mdBook/Knowledge Map alignment, reject stale-helper and unsafe-cursor drift, and complete public no-drift for the full authoring model.
<!-- Source ranges and their immutable migration digest are recorded in docs/tasks/FUTURE-PARITY-BACKLOG.index.jsonl. -->
