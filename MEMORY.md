# MEMORY — resume pointer (memory layer A; overwrite-only, keep ≤ ~60 lines)
LinkedSpec is a progressive-extraction parser DSL. This is the bounded layer-A pointer to *now*, not history.
## How to resume
- Read `README.md`, `MEMORY_ARCHITECTURE.md`, and `SESSION_BOOTSTRAP.md`.
- Track under `docs/tasks/`/`docs/TASK_TREE.md`; follow `COMMIT.md`; use `KNOWLEDGE_MAP.md`/`TOOLBOX.md` first; require an owning leaf and run `scripts/check_memory_architecture.sh` before commit.
## Current state (OVERWRITE this block each update — do not append)
- activation_commit: `4c8ab7ee` — clean atomic-237 base for focused independently emitted Dart proof leaf
  `INTER-MATCH-GAP-CAPTURE.4.4`; no push.
- latest_completed_leaf: `INTER-MATCH-GAP-CAPTURE.4.4 - prove Dart emitted gap execution`; signoff-complete for
  intended atomic 238/300 from clean `4c8ab7ee`, without admission or outward-surface movement.
- active_work_unit: none after intended atomic 238; Dart gap state now crosses authored, private native,
  normalized reconstruction, compatible descriptor, generated-plan, and independently emitted paths behind the
  `.4.5` consumer skip.
- next_action: commit/brief-clear/prove clean atomic 238, then activate Dart primary/admission/parent-closeout leaf
  `INTER-MATCH-GAP-CAPTURE.4.5` task-tree-first with canonical verification.
- in_flight_uncommitted: none after intended atomic 238; no background job.
- blockers: none. Emitted proof is 1/1, explicit consumer 4/4, direct dependents 100/100, complete Dart is 402 plus
  one skip/storage 23/47/CLI 66x2/corpus 105, and all gap/recognition/duplicate/typed matrices pass. Book is
  79/14,828, Knowledge 837/7,053, bounded histories and all nine doctrines pass. No canonical CI runs; `.4.5`
  retains primary/admission, promotion, parent closeout, and canonical proof.
- session_refresh_cadence: proactively recommend `/clear` after at most three completed commits, sooner after a
  large/debug-heavy slice or context compaction, and only at a committed clean handoff boundary.
- historical_handoff_guard: repeated-action next owner `FUTURE-PARITY-BACKLOG.10.1` remains required until
  `FUTURE-PARITY-BACKLOG.22` moves that immutable closeout fact out of bounded current memory.
- current_typed_source: neutral/public structure plus all six internal runtimes are admitted at 9/5/114; no
  public API is admitted and combined program-wide `.14.8` remains future. Recognition transactions are current
  on all six runtimes at 137/246/58; recurrence and public no-drift are current at 9/9.
- recurring_rollout_fact: the original contract combines recurring and public no-drift in one `.14.8` row.
  Detailed plan/history assign `.14.2.6` driver/topology only; do not add a fifteenth row or promote `.14.8`.
- backend_audit_fact: Perl/Rust/Dart/Julia/PUC-Lua/LuaJIT typed-source authorities and private transactions are
  admitted without facade exports; exact carriers/registers/ABIs live in their Knowledge cards.
- current_storage_contract: ADR `0053` keeps all project data on the repository filesystem; use routed wrappers,
  never OS temp/home caches. The complete Dart oracle now enumerates 23 temp owners and 47 locked packages.
- current_path_contract: ADR `0052` and `REPO-ROOT-PATHS` require durable root-relative paths and runtime-derived
  absolute roots; moved/outside-CWD execution is canonical.
- current_semantic_governance: capability 80/0/0; semantic six groups / 20 responses / 128 mutations, rollout 9/9,
  native admission 6/6; MCP rollout complete/141.
- current_readme_contract: README stays 106/5,197 under hard 128/6,144 caps; route changeable detail to its
  registry-owned reader/overflow destinations.
- current_verification_contract: ADR `0073` makes focused proof the ordinary per-leaf default and canonical proof
  mandatory at designated admission/milestone/public/infrastructure and final clean push boundaries.
- documentation_debt_owner: ADR `0066` composition-closes all stores; bounded live/history/task/Knowledge owners
  and their rollover/index gates remain current.
- current_transaction_contract: exact checkpoint/recognize-once/commit/rollback syntax keeps match and payload
  separate, linear tokens, invocation-framed marks, cursor-only v1 progress, and recurrence/public no-drift 9/9.
- current_future_direction: gap stays 3/6/56 with private Perl and Rust admitted. Dart `.4.1-.4.4` now prove
  authored/static/compiled metadata, private native execution, reconstruction, descriptors, generated plans, and
  independent emitted execution behind the `.4.5` skip. `.4.5` next owns Dart primary/admission only; Julia,
  both Lua ABIs, and public rows remain pending; `.14.5.1` waits for `.7`.
- latest_bootstrap_read: 2026-08-15 — roadmap/codebase/mdBook and continuity/tooling review complete; emitted Dart
  `.4.4` is signoff-complete from clean atomic 237 at `4c8ab7ee`; `.4.5` follows after clean atomic 238.
