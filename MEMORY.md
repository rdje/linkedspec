# MEMORY — resume pointer (memory layer A; overwrite-only, keep ≤ ~60 lines)

LinkedSpec is a progressive-extraction parser DSL. This file is layer A of
`MEMORY_ARCHITECTURE.md`: the bounded pointer to *now*, not a history log.

## How to resume
- Read `README.md`, `MEMORY_ARCHITECTURE.md`, and `SESSION_BOOTSTRAP.md`.
- Work is tracked under `docs/tasks/` (index: `docs/TASK_TREE.md`); commit per `COMMIT.md`.
- Check `KNOWLEDGE_MAP.md` before re-deriving facts; use `TOOLBOX.md` first for diagnosis.
- No change without an owning task-tree leaf; run `scripts/check_memory_architecture.sh` before commit.

## Current state (OVERWRITE this block each update — do not append)
- latest_completed_leaf: `FUTURE-PARITY-BACKLOG.9.1.1.2.3.1` — Dart marker-optional validation, one ordered
  compiled resolver, portable failures, strict/descriptor identity, package 266, primary 65x2, corpus 105,
  canonical signoff, and cleanup land at clean commit `241a34f0`.
- latest_commit: `241a34f0` — `FUTURE-PARITY-BACKLOG.9.1.1.2.3.1 - implement Dart root resolution`
  (ahead: 216; push at threshold 300).
- prepared_commit: `FUTURE-PARITY-BACKLOG.9.1.1.2.3.2 - converge Dart root routes`.
- active_work_unit: Dart composed routes `.9.1.1.2.3.2` are complete, fully verified, safely cleaned, and staged
  for the prepared commit; no topology-admission `.3.3` work has started.
- next_action: run final staged doctrine/whitespace checks, commit `.3.2`, clear the brief, prove clean, then
  activate Dart topology admission `.3.3` task-tree-first.
- current_proof: Loaded and normalized-JSON state preserve ordered authored identity and use the single resolver.
  Generated direct/traced and fresh emitted direct/traced execution apply explicit > first marker > first rule;
  low trace records requested/effective/basis. Loader and generated failures preserve portable zero/unknown
  stage/code/entry fields, while stale contract/plan validation remains first. Generated v2/format 2 identity,
  minimal ordered label/family rows, descriptors, and existing APIs remain unchanged. Focused route/owner proof
  passes 86+14; complete Dart format 59/0, analyzer, package 269, primary 65x2, and corpus 105 pass. Root 29,
  mdBook, KM 606/4,360, all four doctrines, canonical Perl root 7+5, cursor 288, reference primary 65x2, and Phase
  0 1,031/1,031 in 628 seconds pass. Safe cleanup removes the generated book/cache. Admission `.3` remains
  pending, so rollout stays 3/7. No background job runs.
- latest_bootstrap_read: 2026-07-18 — full roadmap, repository codebase, mdBook, active task, Knowledge Map,
  Toolbox, ADR `0046`, neutral/Perl root precedent, exact Rust owners/tests/routes, and gates reviewed.
- pivot_guard: never pivot while dirty; finish, verify, document, commit, and clean the current leaf first.
- push_policy: do not push mid-PNT unless explicitly instructed or the documented 300-commit threshold is reached.
- environment: always use `perl -Iperl`; clear `PERL5LIB` for phase0. Full phase0 needs a 20-minute timeout; allow
  at least 30 minutes for the complete canonical gate when the machine is under concurrent build load.
  Julia offline verification may use a writable depot stacked before the installed read-only package depot.
- deferred: root selection implementation `.9.1.1.2.3-.6`; generated parser+stimuli `.8.1`; cursor `.9.1.5.6-.9`;
  inter-match gap/named-slot contract `.1-.7` only
  after cursor completion and activation; semantic/MCP `.10.1`; inspector `.13.1`; authoring `.14`/`.15`;
  parenthesis-free conditions; lexical codeblock capture only if justified.
- blockers: none. in_flight_uncommitted: completed, verified Dart route `.3.2` is staged for its prepared commit;
  no implementation or verification remains. Parked ignored work is untouched.
