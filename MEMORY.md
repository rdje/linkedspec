# MEMORY — resume pointer (memory layer A; overwrite-only, keep ≤ ~60 lines)
LinkedSpec is a progressive-extraction parser DSL. This is the bounded layer-A pointer to *now*, not history.
## How to resume
- Read `README.md`, `MEMORY_ARCHITECTURE.md`, and `SESSION_BOOTSTRAP.md`.
- Track under `docs/tasks/`/`docs/TASK_TREE.md`; follow `COMMIT.md`; use `KNOWLEDGE_MAP.md`/`TOOLBOX.md` first;
  require an owning leaf and run `scripts/check_memory_architecture.sh` before commit.
## Current state (OVERWRITE this block each update — do not append)
- activation_commit: `e6a75830` — clean immutable Lua core HEAD from which projection `.14.2.5.2` activated
  task-tree-first (166/300; no push).
- latest_completed_leaf: Lua typed helper projection `.14.2.5.2` is signoff-complete and lands as atomic commit
  167/300 from activation commit `e6a75830`; no push.
- active_work_unit: none after this atomic landing; admission `.14.2.5.3` is the next pending task-tree frontier.
- next_action: verify commit 167/300, clear/verify the brief and clean handoff, then activate admission `.14.2.5.3`
  task-tree-first from the new clean HEAD.
- in_flight_uncommitted: none in the intended post-landing handoff; no background job, generated book, or managed-
  run residue remains.
- blockers: none. Core 133/133 and projection 240/240 pass per ABI; aliases remain 638/638 per ABI; complete Lua
  passes package 177/177 per ABI, MCP 83,166 bytes, CLI 66x2, corpus 105/105, storage 18/3, and exact marker.
  Neutral remains 7/7/41 and language remains 246/105+1/122. Book passes 79 files / 14,188 KiB and Knowledge is
  exact at 789/6,504; all seven doctrines and definitive canonical containment/relocation, CLI 66x2, RAM 65%, and
  Phase 0 1,031/1,031 in 662 seconds pass.
- current_typed_source: neutral/public composition plus Perl, Rust, Dart, and Julia internal runtimes are admitted
  at 7 complete / 7 pending with 41 mutations; PUC-Lua/LuaJIT and public authored values/transactions remain future.
- rust_audit_fact: all 92 canonical names and seven aliases project through one per-input immutable authority while
  retaining byte registers and exact external values across native/reconstructed/generated-plan carriers; the
  exact ordinary four-test consumer is canonically registered and `rust_runtime` is complete.
- dart_audit_fact: one immutable authority/value core and all 92+7 projections pass the admitted ordinary/canonical
  4/4 consumer across native/reconstructed/generated/emitted carriers while retaining UTF-16 registers and results.
- julia_audit_fact: private non-exported `SourceLocation` owns copied text and immutable scalar/code-unit/coordinate
  tables plus detached values/errors; all 92+7 helpers route through one per-context authority while retaining
  native code-unit registers/results. The exact 127-assertion consumer is now ordinary and canonically admitted.
- lua_audit_fact: each runtime input now owns one copied private authority; all exact 92+7 helper routes construct
  typed positions/spans for validation, coordinates, slicing, materialization, marks, capture boundaries, and cursor
  control while retaining zero-based UTF-8-byte registers and results. Projection passes 240/240 on both ABIs;
  ordinary/canonical admission remains exclusively `.14.2.5.3`.
- current_storage_contract: ADR `0053` keeps project outputs, caches, logs, fixtures, and scratch on the repository
  filesystem; use repository-routed wrappers and never default project data to OS temp/home caches.
- current_path_contract: ADR `0052` and `REPO-ROOT-PATHS` require durable root-relative paths and runtime-derived
  absolute roots; moved/outside-CWD execution is canonical.
- current_semantic_governance: capability 80/0/0; semantic six groups / 20 responses / 128 mutations, rollout 9/9,
  native admission 6/6; MCP rollout complete/141.
- preserved_contract_handoff: repeated-action closure next_owner remains `FUTURE-PARITY-BACKLOG.10.1`.
- current_future_direction: ADR `0056` continues through `.14.2-.8`; rendered mdBook readability remains queued
  and nonurgent; no pivot while dirty.
- latest_bootstrap_read: 2026-08-07 — README, memory architecture, resume/commit/bootstrap owners, roadmap/task,
  ADR `0056`, typed-source rollout/dormant/runtime/register/alias/carrier Knowledge, admitted projection precedents,
  frozen consumer/neutral catalogs, private Lua implementation, and sole-facing status claims refreshed.
- pivot_guard: finish/land/clear/prove `.14.2.5.2` clean before activating Lua admission `.14.2.5.3`.
- push_policy: hard lock at 300 new local commits; Lua projection `.14.2.5.2` is intended 167/300; no push.
- environment: use project-data wrappers; `perl -Iperl`; canonical nested macOS containment may require approved
  execution and up to 30 minutes.
