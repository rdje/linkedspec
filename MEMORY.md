# MEMORY — resume pointer (memory layer A; overwrite-only, keep ≤ ~60 lines)

LinkedSpec is a progressive-extraction parser DSL. This file is layer A of
`MEMORY_ARCHITECTURE.md`: the bounded pointer to *now*, not a history log.

## How to resume
- Read `README.md`, `MEMORY_ARCHITECTURE.md`, and `SESSION_BOOTSTRAP.md`.
- Work is tracked under `docs/tasks/` (index: `docs/TASK_TREE.md`); commit per `COMMIT.md`.
- Check `KNOWLEDGE_MAP.md` before re-deriving facts; use `TOOLBOX.md` first for diagnosis.
- No change without an owning task-tree leaf; run `scripts/check_memory_architecture.sh` before commit.

## Current state (OVERWRITE this block each update — do not append)
- latest_completed_leaf: `FUTURE-PARITY-BACKLOG.9.1.1.2.3.2` — Dart loaded/normalized/generated/emitted/trace/
  diagnostic routes, package 269, primary 65x2, corpus 105, canonical signoff, and cleanup land at clean commit
  `5be5fc55`.
- latest_commit: `5be5fc55` — `FUTURE-PARITY-BACKLOG.9.1.1.2.3.2 - converge Dart root routes`
  (ahead: 217; push at threshold 300).
- prepared_commit: `FUTURE-PARITY-BACKLOG.9.1.1.2.3.3 - admit Dart root selection`.
- active_work_unit: Final Dart topology/reference admission `.9.1.1.2.3.3` has implementation, complete Dart
  proof, 34-mutation governance, Dart-only 4/7 promotion, and public/book/KM lockstep; canonical closeout remains.
- next_action: regenerate/stage final proof docs, clean safe generated artifacts, commit `.3.3`, clear the brief,
  prove clean, then activate Julia `.4` task-tree-first.
- current_proof: One exact ordered 15-role Dart consumer executes neutral selection/failure/strict plus native,
  loaded, reconstructed, generated/emitted direct/traced, descriptor, diagnostic, runtime trace, primary, and
  request-trace roles once and only once. Governance locks source inventory, drivers, six primary ids, rollout,
  and 34 mutations. Focused admission/core/routes/emitter passes 1+14; complete Dart format 60/0, analyzer, package
  270, primary 65x2, and corpus 105 pass. Root checker reports 4 complete / 3 pending; mdBook, KM 607/4,371, all
  four doctrines, canonical Perl root 7+5, cursor 288, reference primary 65x2, and Phase 0 1,031/1,031 in 656
  seconds pass. Cleanup and commit remain. No background job runs.
- latest_bootstrap_read: 2026-07-18 — full roadmap, repository codebase, mdBook, active task, Knowledge Map,
  Toolbox, ADR `0046`, neutral/Perl root precedent, exact Rust owners/tests/routes, and gates reviewed.
- pivot_guard: never pivot while dirty; finish, verify, document, commit, and clean the current leaf first.
- push_policy: do not push mid-PNT unless explicitly instructed or the documented 300-commit threshold is reached.
- environment: always use `perl -Iperl`; clear `PERL5LIB` for phase0. Full phase0 needs a 20-minute timeout; allow
  at least 30 minutes for the complete canonical gate when the machine is under concurrent build load.
  Julia offline verification may use a writable depot stacked before the installed read-only package depot.
- deferred: root selection implementation `.9.1.1.2.4-.6`; generated parser+stimuli `.8.1`; cursor `.9.1.5.6-.9`;
  inter-match gap/named-slot contract `.1-.7` only
  after cursor completion and activation; semantic/MCP `.10.1`; inspector `.13.1`; authoring `.14`/`.15`;
  parenthesis-free conditions; lexical codeblock capture only if justified.
- blockers: none. in_flight_uncommitted: `.3.3` admission contract/test/governance/public/KM/task/live changes are
  complete through Dart-local proof but not yet canonically verified or committed. No background job runs. Parked
  ignored work is untouched.
