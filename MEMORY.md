# MEMORY — resume pointer (memory layer A; overwrite-only, keep ≤ ~60 lines)
LinkedSpec is a progressive-extraction parser DSL. This is the bounded layer-A pointer to *now*, not history.
## How to resume
- Read `README.md`, `MEMORY_ARCHITECTURE.md`, and `SESSION_BOOTSTRAP.md`.
- Track under `docs/tasks/`/`docs/TASK_TREE.md`; follow `COMMIT.md`; use `KNOWLEDGE_MAP.md`/`TOOLBOX.md` first;
  require an owning leaf and run `scripts/check_memory_architecture.sh` before commit.
## Current state (OVERWRITE this block each update — do not append)
- activation_commit: `58f4bb85` — clean dormant Dart RED HEAD from which immutable core `.14.2.3.1` activated
  task-tree-first (153/300; no push).
- latest_completed_leaf: `.14.2.3.1` lands as intended 154/300 with subject
  `FUTURE-PARITY-BACKLOG.14.2.3.1 - implement Dart source location core`.
- active_work_unit: none after immutable Dart core `.14.2.3.1`; the next leaf is not yet activated.
- next_action: prove the `.14.2.3.1` landing clean, then activate projection `.14.2.3.2` task-tree-first and only
  then route the exact 92+7 helpers through typed values with unchanged public results and carrier behavior.
- in_flight_uncommitted: none at intended handoff; no background job remains after the complete Dart, mdBook,
  doctrine, definitive canonical, and final focused gates.
- blockers: none. Core selection passes 2/2; full dormant execution passes 3/4 and fails only at the `.2` projection
  catalog; fatal analysis and ordinary 379 pass. Complete Dart format 98/0, storage 20/47, CLI 66x2, corpus 105/105,
  language coverage 246/105/122, neutral typed 5/9/39, mdBook 79/14,152 KiB with separate rendered blocks, and
  Knowledge Map 786/6,427 pass. All seven doctrines and definitive canonical CI pass capability 80/0/0, composed
  semantic/MCP admissions, containment/relocation, CLI 66x2, RAM 61%, and Phase 0 1,031/1,031 in 686 seconds.
- current_typed_source: neutral/public composition plus Perl and Rust internal runtimes are admitted at 5/9 with
  39 mutations; Dart/Julia/PUC-Lua/LuaJIT remain pending.
- rust_audit_fact: all 92 canonical names and seven aliases project through one per-input immutable authority while
  retaining byte registers and exact external values across native/reconstructed/generated-plan carriers; the
  exact ordinary four-test consumer is canonically registered and `rust_runtime` is complete.
- dart_audit_fact: one immutable authority/value core now passes exact 3/7/6/3 and four-error proof; all 92 canonical
  names and seven aliases retain existing routes, and the dormant full consumer fails only at absent `.2` projection.
- current_storage_contract: ADR `0053` keeps project outputs, caches, logs, fixtures, and scratch on the repository
  filesystem; use repository-routed wrappers and never default project data to OS temp/home caches.
- current_path_contract: ADR `0052` and `REPO-ROOT-PATHS` require durable root-relative paths and runtime-derived
  absolute roots; moved/outside-CWD execution is canonical.
- current_semantic_governance: capability 80/0/0; semantic six groups / 20 responses / 128 mutations, rollout 9/9,
  native admission 6/6; MCP rollout complete/141.
- preserved_contract_handoff: repeated-action closure next_owner remains `FUTURE-PARITY-BACKLOG.10.1`.
- current_future_direction: ADR `0056` continues through `.14.2-.8`; rendered mdBook readability remains queued
  and nonurgent; no pivot while dirty.
- latest_bootstrap_read: 2026-08-07 — README, memory architecture, resume/commit/bootstrap owners, roadmap/task,
  ADR `0056`, relevant typed-source/Dart runtime Knowledge cards, Toolbox, live authorities, and book claims refreshed.
- pivot_guard: prove `.14.2.3.1` clean before activating projection `.14.2.3.2` task-tree-first.
- push_policy: hard lock at 300 new local commits; immutable Dart core `.14.2.3.1` is 154/300; no push.
- environment: use project-data wrappers; `perl -Iperl`; canonical nested macOS containment may require approved
  execution and up to 30 minutes.
