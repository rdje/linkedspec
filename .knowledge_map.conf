# LinkedSpec integration for the portable Knowledge Map bundle.
# The bundle resolves this repository-root-relative initializer before generating or checking the map.
: "${KM_ENV_INITIALIZER:=tools/project_data_env.sh}"
